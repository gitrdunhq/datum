# Gap Ledger — MVP → Aspirational

The ADRs are the **north star** (intent); the code is an **MVP**. This ledger is the delta — what's
stubbed today vs the ADR target — so iteration is directed, not vibes. Each row has an ADR, the
current MVP state, the aspirational target, and the acceptance demo that proves the climb.

> Rule: we iterate the MVP **up** toward the ADRs. We do **not** downgrade ADRs to match stubs. When a
> climb lands, update the row (and the ADR's "as-built" note if its decision genuinely evolved).

| # | Area | ADR | MVP today | Aspirational target | Acceptance demo | Priority |
|---|------|-----|-----------|---------------------|-----------------|----------|
| G1 | ContextCrane wiring | 0030 | ✅ **done** — graph injects the crane; triage/lane_plan/synthesis build **all** prompts (initial **and** retries) via `crane.assemble` (per-phase budget). Real hoisting arrives with G2. | every model prompt via the crane; real hoisted context (G2) | retries route through `crane.assemble` ✔ (counting-crane test); oversized → `ContextBudgetExceededError` ✔ | **P0 ✅** |
| G2 | Context adapters | 0004 | `adapters.py` return canned strings | real Serena/TokenSave (code) + Context7/Headroom (NL) | a lane's packet contains real hoisted symbols + compressed docs | P1 |
| G3 | Token counter | 0030 | ✅ **real tokenizer behind the port** — `build_tiktoken_counter` (`data/tokenizers.py`, `[tokenizer]` extra) is the default via `build_token_counter`, degrading to the `len//4` heuristic when tiktoken is absent (offline CI) or `DATUM_TOKENIZER=heuristic`; the crane is wired with it. | MLX-native tokenizer adapter for exact on-device counts. | crane uses the injected counter ✔; tiktoken path covered (skipped offline); heuristic fallback ✔ | P2 |
| G4 | Execution host | 0011/0012 | `LocalHost` (patch); Docker/Tart shells | hardened `X86DockerHost`: egress allowlist, rlimits, guaranteed teardown | apply+test a diff in a container; teardown leaves nothing; egress blocked | P1 |
| G5 | eedom gate | 0006 | 🟡 **adapter honors eedom's real CLI** — invokes `eedom evaluate … --output-json`, maps his published `ReviewDecision` schema (#389) faithfully (verdict/severity 1:1; category mapped); fail-open. Running the real binary needs the container (Mac). | real containerized `eedom evaluate` on a code/dep diff, version-pinned | mapping ✔ (fake runner = his real JSON); live binary run pending hardware | P1 |
| G6 | Data plane | 0005/0013/0031/0032 | 🟢 **pluggable + centralized adapters built** — SQLite ledger (default) behind `RunLedger`; `InMemoryCheckpointer` (default) behind `CheckpointStore`; **`PostgresLedger` (psycopg, lazy)** and **`RedisCheckpointer` (redis-py, Valkey-compatible)** added behind the same ports, URL-dispatched in `build_ledger`/`build_checkpointer` (`[database]` extra). Per-run DB branch + live-server runs remain. | local + centralized adapters behind both ports; per-run DB branch | Postgres unit + opt-in real-server conformance (`DATUM_TEST_POSTGRES_URL`) ✔; Redis conformance via `fakeredis` ✔; resume roundtrip + metering ✔; per-run DB branch + live servers pending | P2 |
| G7 | Verifier rigor | 0007/0010/0017 | 🟡 **deterministic RED-before-GREEN gate** (`evaluate_tdd_gate`, typed `LaneVerification`/`GateResult`, Ordering property) + style gate. REFLECT scoring + SKEPTIC adversarial bug-hunt are LLM (need live model). | RED-before-GREEN enforced ✔; planted bug caught by SKEPTIC (live model) | RED-before-GREEN ✔; SKEPTIC/REFLECT pending live oMLX | P2 |
| G8 | Compounding | 0020 | 🟢 **loop closed (deterministic)** — CLOSEOUT node reads the ledger trace → `lessons_from_trace` → `harvest` → auto-binds via a `RuleBinder` into the writable learned-rules dir; `build_rule_registry` reads **packaged + learned**, so the crane lifts learned rules next run. `record_fire`/`prune_unfired` for anti-bloat. | richer lesson derivation from a *real* multi-run ledger; propose-and-gate human UX; eedom-policy binding (container); SKEPTIC/REFLECT lessons (live model). | harvest/bind/prune ✔; CLOSEOUT auto-binds a regression from a retried failure + surfaces rejects for gating ✔; learned rule read back by the multi-root (crane) registry ✔ | P3 |
| G9 | GitHub projection | 0023 | 🟡 **mapping + port built** — `project_dag(epic, lanes, waves)` → `ProjectionPlan` (epic + sub-issue per lane, `wave:`/`status:` labels), deterministic; `IssueProjector` port + `FakeIssueProjector` (records + synthetic refs). | GitHub-MCP adapter (create epic + sub-issues, apply labels, sync status) behind the same port; wire into a `datumax run`. | mapping + fake publish ✔; live GitHub mirroring pending the MCP adapter | P3 |
| G10 | Live oMLX | 0003 | mock-tested; transports exist | smoke run against the user's oMLX endpoint | `datumax run` completes a real tic-tac-toe lane on hardware | P1 |
| G11 | Port the remaining seams | 0032 | 🟡 **eedom → `ReviewGate` (plugin registry) + status → `StatusSource` ported**; eedom I/O moved core→data; returns typed `ReviewDecision` (fail-open). GitHub projection still concrete/unbuilt. | each external dep behind a port + factory + conformance suite | ReviewGate/StatusSource conformance ✔; GitHub port remains. (eedom-JSON "enhancement" not needed — he already publishes ReviewDecision via #389; adapter now consumes it, see G5.) | P2 |
| G12 | Persona registry for Roles + Skills | 0033/0004/0020/0003 | ✅ **done (ADR-0033):** `Role`/`Skill` shapes + `PersonaRegistry` port + `FilePersonaRegistry` (markdown+frontmatter, deterministic, registered in `PERSONA_REGISTRIES`) + `build_persona_registry` factory. Crane owns the registry and `compose_system(role_id, scope_tags)` fills the old `BASE_PERSONA & SKILL_PERSONA_HERE` seam; `prompts/*.md` migrated to packaged `personas/roles/*.md`; triage/lane_plan/synthesis source their prompt from the registry via the crane. The GitNexus suite + bug-hunt (7 skills) were imported from the datum skill library into `personas/skills/*.md`, tagged by **purpose** (`planning` vs `troubleshooting`). The crane lifts skills **only when needed**: PLANNER lifts `planning` skills (prefix); a failed synthesis attempt lifts `troubleshooting` skills into the **variable suffix slot** on retry (`crane.lift_skills`, prefix-cache-safe); routine lanes lift none. BASE_PERSONA prepended to every system prompt; 4 distilled domain Skills (swift/aws/web/research) imported; raw corpus parked in non-packaged `persona-sources/`. **Semantic (RAG) selection built:** `SemanticPersonaRegistry` adds embedding `match_skills` (MiniLM), default when `[semantic]` installed, degrading to deterministic keyword match otherwise (ADR-0034); `crane.compose_system(query=...)` folds the query-matched domain skill into the prefix. | thread lane/ticket text as the `query` from planner/executor; hotfix-route troubleshooting from attempt 1; LLM tie-break tier; harvested rules delivered as Skills (ADR-0020). | planning task lifts planning skills ✔; failed attempt lifts troubleshooting into the suffix (prefix stays clean) ✔; routine lane lifts none ✔ | P3 |

| G13 | Subagent harness for playbook skills | 0035/0033/0017 | 🟡 **seam built** — `Skill.delivery: inline\|subagent`; `gitnexus-bug-hunt` + `agentic-research-workflow` tagged `subagent`; crane **never inlines** them (`_render_skills` filters); `Worker` port + `WORKERS` registry + `FakeWorker` encode `run(playbook, inputs, schema) -> WorkerResult`. | live worker: GitNexus-MCP + oMLX tool loop, canary-first spawning (BASE_PERSONA §2). | playbooks never inlined ✔; Worker port conformance ✔; live worker returns findings JSON that feed the gate — pending hardware | P3 |

| G14 | Held-out rule promotion | 0036/0020/0013 | ⬜ **design (ADR-0036)** — today `harvest`/`closeout` auto-binds a rule on the *origin run's* evidence; `record_fire`/`prune_unfired` are a **usage** signal, not a **value** signal (a rule can fire and still overfit the run that birthed it). | two-signal promotion: a harvested rule sits in **probation** and is promoted only on **held-out** (independent-run) deterministic value margin — lowers REPEATED_FAILURE/EEDOM_REJECT for matching `scope_tags` across ≥N runs, never raises rejects; demote on regression (zero-LLM). | a rule that helps only its origin run is **not** promoted; one that cuts repeat-failure across held-out runs **is**; a reverted rule never silently re-binds. Needs the multi-run ledger (pairs with G6/G8). | P3 |
| G15 | Mechanistic insight narratives | 0037/0020/0034 | ⬜ **design (ADR-0037)** — `lessons_from_trace` derives lessons deterministically (robust but shallow: *what*, not *why*). | optional, **non-authoritative** LLM "why" narrative attached to a `Lesson`/rule (cognition-tier, flagged model-made), **supplementing** the deterministic evidence — never gates, off in deterministic/offline mode. | narrative attaches to a deterministic lesson; harvest/promotion/binding **identical with and without it**; absent when no model. | P3 |

**Sequencing intuition:** G1 (crane wiring) is the unlock — it gives one seam to then drop real
adapters (G2) and a real tokenizer (G3) behind. G4/G5/G10 make execution+review real. G6 makes state
durable. G7 deepens quality. G8/G9 are the compounding/human-surface layers.

**Compounding rigor (G14/G15, design):** prior art from **Arbor** (RUC-NLPIR) — separate the signal a
rule is *optimized* on from the signal that *keeps* it (held-out promotion, G14), and let an optional
LLM narrate the *why* on top of the deterministic lesson without ever entering the trust path (G15).

**Enforcement (landed):** the climbs are guarded — `mypy --strict` is clean, a pre-commit hook (ruff +
mypy) runs locally, and PR CI (`.github/workflows/datum-ax-ci.yml`, SHA-pinned) runs the full gate via
`scripts/ci.sh` (ruff format-check + ruff check + mypy + pytest) — so a regression can't land silently.
