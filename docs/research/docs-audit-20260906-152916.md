# Old Bones — audit of the datum documentation tree

**run_id:** 20260906-152916 · **auditor:** Opus agent, report-only (no doc edited) · **branch at audit time:** `datum/integration-lanes`

**Scope.** Every file under `docs/` except `docs/epics/` and `docs/research/`. Root `README.md`, `SKILL.md`, `AGENTS.md`, `CLAUDE.md` read for comparison only.
**Ground truth.** `docs/FLOW.md` (2026-09-06), `datum/` (Python CLI), `skills/src/` (TypeScript workflows).
**Caveat on dates.** All `git log` dates below were taken with the working tree on branch `datum/integration-lanes`, not `main`. That branch carries an in-flight epic whose SPEC adds a new FLOW.md section on integration lanes; FLOW.md's last commit here is `bb2512a2 docs(flow): close the blocked-on-own-test gap (#440)`.

**What FLOW.md and the code actually say** (the yardstick used throughout):

- `skills/src/shared/models.ts:35` — `PHASES: readonly Phase[] = ['refine', 'plan', 'properties', 'act', 'validate', 'review', 'closeout']`. **Seven phases. No Discovery, no Classify, no Triage-as-phase, no Architect, no PR, no PR Comments.**
- State is `.datum/pipeline-state.json` plus per-epic mirrors under `.datum/epics/<slug>/`. There is no `.datum/state.json` on disk.
- Config is `.datum/config.json` (a `.datum/config.toml` also exists for the local-LLM side).
- `datum/commit_queue.py`, `datum/dedupe.py`, `datum/schemas/` do not exist. FLOW.md §5: "**Dead producers with no consumer** (#394) — closed in 9b6ffcb/1840733: … `commit_queue.py`, `dedupe.py` … were deleted with their tests."

---

## Record by file

### `docs/FLOW.md`

- **Last commit:** 2026-09-06
- **Purpose (as stated):** "This document describes the flow datum *wants*: a TDD delivery pipeline where an LLM may propose work but never certifies it…"
- **Verdict:** CURRENT — and the only authoritative document in the tree.
- **Evidence checked:** phase list against `PHASES`; `.datum/pipeline-state.json` against the on-disk `.datum/`; `Lane.kind` `'structural' | 'behavioral'`; the deleted dead producers; the `datum-route` deletion and its replacement tripwire `tests/test_skill_bundles_have_consumers.py`; the runtime-contract claims against `skills/src/shared/utf8.test.ts` / `agent-types-ordering.test.ts`. No wrong claim found.
- **Action:** keep. It is the document every other file in this report should be reconciled against or deleted in favour of.

---

### `docs/DATUM.md` — **linked from README.md as "Full Skill Reference"**

- **Last commit:** 2026-09-05 (`1a5eabd0 docs: list lane_plan_digest.py in the module tree`)
- **Purpose (as stated):** "**Status:** Revision 5 (LEAN + Axiomatic Integration). **Scope:** One epic. Build a portable Agent Skill that automates the full Refine → Plan → Properties → Act → Validate → Review → PR Comments → Closeout workflow."
- **Verdict:** DEAD. This is the original epic brainstorm/design spec for datum, not a reference for datum. Its own §9 is titled "**Open questions for PLAN.md**" and §8 "Success criteria" — the artefacts of a document written *before* the system existed. README presents it as the reference manual for the system that exists.
- **Evidence — wrong claims a reader is told are current:**
  - §3.4 / §4.3: "State file (`.datum/state.json`) for phase tracking, RUN_ID, model log, per-lane progress, replay" with a 60-line schema (`"in_flight_cap": 7`, `"gemba_verdicts": []`, `"wait_times"`). No such file. FLOW.md: "State lives in `.datum/pipeline-state.json` (written only after `datum pipeline-state-save` verifies the phase against git)". `ls .datum/` returns `pipeline-state.json`, not `state.json`.
  - §4.4: "Config file at repo root (`.datum/config.toml`) for model tier overrides, gate policy, and skill version pinning" followed by a 90-line TOML block (`[gates]`, `[yolo].hard_stops`, `[pipeline] max_concurrent = 7`, `[tools] claude_code = { subagent = "Task" }`). FLOW.md Preflight names `.datum/config.json` and the `RepoConfig` type in `shared/types.ts`. None of those TOML tables is read by any phase.
  - §4.6.2 / §7.8: "GREEN agents must not see test source code. This is enforced by `test_signal.py` which redacts test runner output". `test_signal.py` is not in the tree. FLOW.md's GREEN reads the lane spec file and runs the suite, and the closed gap "A GREEN blocked on the lane's own test needed an operator to steer the lane" turns on GREEN having *diagnosed a test fixture precondition exactly* — GREEN plainly reads tests. The enforcement that exists is `green_edited_tests` (GREEN may not *write* tests), which is a different invariant from blindness.
  - §4.7.5: "The commit queue is a small Python process spawned at pipeline start … Lane agents communicate with it via a Unix socket … `.datum/runs/<RUN_ID>/commit-queue.sock`". Deleted as a dead producer in #394. FLOW.md's Act merges via `datum worktrees merge` — per-lane temporary squash commits folded into one batch commit.
  - §4.6.4 / §7.10: the `test_ratchet.py` pre-commit hook as the enforcement for REFACTOR. FLOW.md's REFACTOR enforcement is the post-stage batch, `strayCleanSteps`, and `refactor_skipped`.
  - §4.9.8: "ACT done → Validate green → Review done → **PR opened → PR Comments triage → PR Comments fixes → PR merged** → Closeout starts" and "Closeout never runs against an unmerged PR." There is no PR phase. Closeout runs on the merged epic branch; FLOW.md Closeout is "tag `epic/<branch>/<runId>` → archive → `datum housekeep-epic <branch>`".
  - §5.5: the `scripts/lane-tools/manifest.toml` sandbox with `ulimit`, network namespaces and `lane_tool_sandbox_violation` as a yolo hard stop. No such runtime exists; `scripts/lane-tools/` is a leftover directory.
  - §4.1: the package layout is `SKILL.md / references/ / scripts/ / assets/`. The real layout is `datum/` (Python package) + `skills/src/*.ts` compiled to `skills/*.js`. The one line that *is* current — `lane_plan_digest.py` — was added by the most recent commit, which is why the file reads as maintained.
- **Action:** delete, and repoint README line 87 at `docs/FLOW.md`. **Constraint:** `tests/test_datum_hardening.py:179` asserts `(ROOT / "docs/DATUM.md").exists()` — that assertion must be changed in the same commit or the deletion fails the suite. If the design history is worth keeping, move it to `docs/archive/DATUM-design-rev5.md` with a one-line header saying it is the 2026-era design brainstorm and that FLOW.md supersedes it.

---

### `docs/datum-workflow.md` — **linked from README.md as "Workflow Diagram"**

- **Last commit:** 2026-06-11
- **Purpose (as stated):** "# DATUM Workflow / ## Pipeline Overview" — two mermaid flowcharts plus a phase summary table.
- **Verdict:** DEAD. Every structural element of both diagrams describes machinery that was removed or never built.
- **Evidence:**
  - The main flowchart routes `Act --> Validate --> Review --> CreatePR --> MergeGate`, with `PRComments --> MergeGate` and `Merged --> Closeout`. No PR or merge-gate phase exists; `PHASES` ends at `closeout`.
  - `Properties --> Architect --> Act` and the table row "| **Architect** | Properties | ADRs + C4 diagrams | Blocks if significant decisions lack ADRs |". There is no architect phase or gate. `datum gate` has no `architect` verdict.
  - `StartQueue["⚙️ Start Commit Queue"]` and `StartDrift["🔍 Start Spec Drift Detector"]` as the first two nodes of Act. Both deleted (#394); no spec-drift sidecar exists.
  - `Adversarial["🟡 Adversarial Agent — Try to Break It"]` runs *after* REFACTOR. In the real lane (FLOW.md §3) the **skeptic panel is three lenses that run after GREEN and before REFACTOR**, and a BROKEN verdict triggers one verified GREEN retry then `skeptic_broken`.
  - `SecSidecar["🛡️ datum-security Sidecar — STRIDE + Secrets"]`. No such sidecar; Security is one of the four *Review* lenses (`skills/src/datum-review.ts:42` neighbourhood).
  - Failure handling is drawn as `Classify --> ENV/REASONING/HARD_STOP --> RetryLadder --> "3x exhausted"`. FLOW.md's model is per-stage named failures and a lane demotion state machine; `diagnose_failure.py`'s ENVIRONMENTAL/REASONING ladder is not what the lane runner does.
  - Table: "| **Properties** | … | Skippable in yolo (skipped for Patch tier) |". Directly contradicted by FLOW.md §5: "**Properties has no halt** — closed in e4f2e2f: gate failures halt in yolo too, Properties included."
  - Table: "| **Triage** | TASKS.md | `.datum/routing.json` | **Always required** |". `routing.json` is real but FLOW.md §5 records it is now "written and gated, never committed, and a test forbids any phase commit batch from adding a `.datum` path" — the table implies a committed artefact.
  - Table: "| **PR + Merge** | Review-passing code | Merged PR | **Always required** |" — the pipeline never opens a PR.
- **Action:** delete, and repoint README line 88 at `docs/diagrams/README.md`, which indexes the four rendered FLOW.md diagrams and is current. A reader following README's "mermaid flowcharts of the pipeline" today gets a picture of a pipeline that has not existed for three months.

---

### `docs/datum-pipeline-overview.md`

- **Last commit:** 2026-07-07
- **Purpose (as stated):** "datum takes a ticket and turns it into tested, reviewed, merged code — fully automated, with hard structural gates at every phase."
- **Verdict:** STALE. The best of the narrative docs — the Plan/Act/lane material is accurate — with three wrong claims. Not an orphan: `docs/epics/datum/consumer-first-build-order/WALKTHROUGH.md:93` links it.
- **Evidence:**
  - Line 11, the pipeline diagram: `REFINE ----> PLAN ----> ACT ----> VALIDATE ----> REVIEW ----> CLOSEOUT` — **Properties is missing entirely**, although the file describes a system whose PROPERTIES.md drives the skeptic panel and Review. FLOW.md: seven phases, Properties between Plan and Act.
  - Line 56: "**Closeout** — Merges to target branch, closes GitHub issues, tags the run, generates docs." Closeout does not merge. FLOW.md Closeout: "collect batch … synthesize CURRENT_STATE / CHANGELOG / RETRO / follow-ups … → tag `epic/<branch>/<runId>` → archive → `datum housekeep-epic <branch>`". The merge that happens is the *lane* squash-merge inside Act.
  - Line 52: "**Review** — … Non-critical findings get filed as GitHub issues for follow-up." Review writes REVIEW-REPORT.md and halts on high/critical; follow-up filing is Closeout's `datum closeout-file-followups`, and FLOW.md is explicit that "the filer opens issues only at `--min-severity` high and reports the medium/low count retained in the manifest" — i.e. the *reverse* of "non-critical get filed".
  - Line 67: "**Self-healing** | If the pipeline crashes unexpectedly, it auto-files a deduplicated GitHub issue before halting." `dedupe.py` was deleted in #394; `datum bugfile` still exists but is operator-invoked, not a crash hook.
- **Action:** update — four edits: add Properties to the line-11 diagram and give it a section; rewrite line 56's Closeout sentence to tag/archive/housekeep; correct line 52 to say Closeout files high-severity follow-ups; delete or requalify the "Self-healing" row. Worth keeping: it is the only short, readable "what is datum" doc and its Plan/Act/lane content is right.

---

### `docs/LANDSCAPE.md`

- **Last commit:** 2026-09-05
- **Purpose (as stated):** "# Landscape / ## Tech Stack / ## File Tree" — generated by `datum landscape` (`datum/cli.py:958`: `out_path = Path("docs/LANDSCAPE.md")`).
- **Verdict:** STALE (a generated snapshot of a tree that has since moved), with a rendering defect.
- **Evidence:**
  - The File Tree section is emitted with indentation but **no directory names**, so 400 lines read as a flat list of orphan basenames: eight consecutive `SKILL.md (83 LOC)` / `SKILL.md (89 LOC)` entries at lines 33-38, then bare `full.jsonl`, `prompt.txt`, `RETRO.md` repeated eight times. As a navigation aid it conveys nothing.
  - Line 25 lists a root `TASKS.md (35 LOC)`; there is no root `TASKS.md` today (`ls TASKS.md` → No such file). FLOW.md §5 records why it went: "a root artifact whose epic-scoped destination exists is kept in place as `KEPT_ROOT`" (bb673842, BUG T/U/V) — the stale root TASKS.md was itself a closeout defect.
  - It indexes the dead files audited here as though they were part of the codebase: `apply-fts-patch.mjs (56 LOC)`, `field-notes-bodyman-epic-ui1-20260520.md (442 LOC)`, `gitnexus-fts-fix.md (151 LOC)`, `DATUM.md (1227 LOC)`, `MEMORY.md (171 LOC)`.
  - `## GitNexus Enrichment` (line 517) contains only the empty marker pair `<!-- gitnexus:start --> <!-- gitnexus:end -->` — the enrichment described in `references/00-discovery.md:27` has never run.
  - Self-referential drift: line 264 records `LANDSCAPE.md (470 LOC)` while the file is now 520 lines.
- **Action:** keep the command, regenerate the file, and fix the tree renderer to print directory paths — or stop committing the output. It is a cache, not a document; nothing but `SKILL.md:23` (`/datum landscape   Generate docs/LANDSCAPE.md from filesystem analysis`) refers to it, and no phase reads it.

---

### `docs/CURRENT_STATE.md`

- **Last commit:** 2026-05-27
- **Purpose (as stated):** "# Current State / **Last updated:** May 2026 / **Epic:** DATUM V2 Modernization & Migration"
- **Verdict:** DUPLICATE of root `CURRENT_STATE.md` — and the stale half of the pair. **This is the sharpest contradiction in the tree: two files with the same title, both written as the authoritative statement of what shipped, disagreeing about the repository layout.**
- **Evidence:**
  - "**Repository Restructuring (`src/` layout)**: Safely migrated the orchestrator codebase from `datum/` into `src/datum/`." `ls src/datum` → No such file or directory. The package is at `datum/`, and root `CURRENT_STATE.md` (2026-07-07) cites `skills/src/shared/models.ts`, `datum/worktree_manager.py`, `datum/cli.py` — i.e. the layout this file says was migrated away from.
  - "**Layout**: DATUM source code lives in `src/datum/`. … Treat `src/datum/models/` as the single source of truth for contract data." Under "Architecture notes", framed as a standing instruction to future agents. It is wrong, and it is the kind of claim an agent acts on.
  - "**MCP Router Proxy Integration**: … Implemented `datum mcp sync`". No `mcp` subcommand exists in `datum/cli.py`.
  - "Deleted 22 raw `.schema.json` files". `assets/schemas/` still exists and `datum/models/*_schema.py` sits *beside* it; the purge described is partial at best.
  - "## Active work / Next Steps — V4 'Teach' Phase Implementation: Rework `datum-teach`…; Fleet Command…". No `datum-teach` in the tree; Fleet Command is shelved in `docs/HORIZON.md`.
  - **Which one the code reads:** `skills/src/datum-plan.ts:63` runs `if [ -f CURRENT_STATE.md ]; then head -80 CURRENT_STATE.md; …` from the repo root, and `skills/src/datum-closeout.ts:147` commits `'CURRENT_STATE.md'` at the root. `docs/CURRENT_STATE.md` has **no producer and no reader**. FLOW.md's Plan inputs row ("SPEC.md, CURRENT_STATE.md, prior defects…") means the root file.
- **Action:** delete. Root `CURRENT_STATE.md` is the one Closeout writes and Plan reads; a second file with the same name and a wrong layout claim is an active hazard, not redundancy. (`references/00-discovery.md:98` — "If CURRENT_STATE.md was updated in the last 7 days … skip Discovery" — is ambiguous between the two today; deleting the docs copy resolves it.)

---

### `docs/ROADMAP.md`

- **Last commit:** 2026-05-21
- **Purpose (as stated):** "# Strategic Roadmap / *'No leapfrogging. Every feature earns its place in sequence.'* / This roadmap is constantly optimized by the `triage` phase"
- **Verdict:** DEAD (and a DUPLICATE of root `ROADMAP.md`, which is itself stale at 2026-06-13 but at least populated).
- **Evidence:**
  - Every section is empty: "## Current Queue (Up Next) — *Triage will slot items here before they enter discovery.*"; "## Planned Initiatives — *(empty bullet)*"; "## Parking Lot / Blocked — *(empty bullet)*". Thirteen lines, zero content, unchanged in three and a half months.
  - "constantly optimized by the `triage` phase" — triage is not a phase (`PHASES` has seven; triage is a step inside Plan writing `.datum/routing.json`), and nothing in `skills/src/` writes to a roadmap.
  - The only roadmap datum touches is the root one: FLOW.md §5 records "Refine's ROADMAP append stays strict" (a4f50e93), and README line 89 links `ROADMAP.md` at the root.
- **Action:** delete. Keeping an empty second roadmap next to the real one only creates the question of which is which.

---

### `docs/TASKS.md`

- **Last commit:** 2026-05-21
- **Purpose (as stated):** "# Implementation Plan (TASKS.md)" — a two-task worked example (`task-001: First Task` / `task-002: Second Task`, files `src/a.py`, `src/b.py`).
- **Verdict:** DEAD. It is a template/fixture, not a document, sitting in the docs root under a filename the pipeline treats as a real artefact.
- **Evidence:**
  - The real TASKS.md is epic-scoped: FLOW.md's Plan handoff table, "`docs/epics/<b>/TASKS.md` | human-readable | Properties, `gate_plan`". Nothing consumes `docs/TASKS.md`.
  - `SKILL.md:7` makes `TASKS.md` a skill-activation trigger — "Also activates when `docs/epics/*/TICKET.md`, `docs/epics/*/SPEC.md`, or `TASKS.md` is present" — and `docs/datum-workflow.md:54` routes `DetectEntry -->|"TASKS.md found"| Act`. A stray TASKS.md with `src/a.py` in it is exactly the class of collision FLOW.md already had to close once (bb673842: "collided with a stale root TASKS.md").
  - Its contents are also out of date as a template: it has no `kind` field, though `Lane.kind` (`'structural' | 'behavioral'`) now "decides whether Act runs RED/GREEN at all".
- **Action:** delete, or move to `assets/templates/TASKS.md` where the other templates live. It should not be reachable by a filename the entry-point detector recognises.

---

### `docs/HORIZON.md`

- **Last commit:** 2026-05-27
- **Purpose (as stated):** "Ideas parked for future evaluation when the architecture dictates necessity."
- **Verdict:** STALE. The parking-lot function is legitimate and the four shelved ideas are still shelved; the framing has drifted.
- **Evidence:**
  - "## DATUM V3: Dual Pipeline Architecture — **Shelved:** Await V2 stabilization before assimilating the `datum-orchestrator` and `datum-ba` skill trees." The V2/V3/V4 version scheme appears nowhere else in the current tree; FLOW.md, SKILL.md and the code carry no version label. `datum-orchestrator` and `datum-ba` do not exist.
  - "## Dual Mode (Headless Runner)" is listed as shelved, but root `ROADMAP.md` lists "Headless orchestrator for datum-local variant" under **Planned**. Two docs, opposite dispositions of the same idea.
  - The "Artifacts removed" lines (`product_state.py`, `p1-triage.md`–`p4-handoff.md`, `product` CLI subcommand) are accurate and are the file's real value — a record of what was cut and why.
- **Action:** update — drop the "DATUM V2/V3/V4" version framing, and reconcile the Dual Mode entry against root `ROADMAP.md` (one of the two is wrong). Otherwise keep; a written record of deliberate non-decisions is worth its 24 lines.

---

### `docs/MEMORY.md`

- **Last commit:** 2026-06-23
- **Purpose (as stated):** "This file acts as the primary collective memory for the DATUM factory. It contains past feedback, rules, and engineering conventions."
- **Verdict:** DEAD + ORPHAN. It is an imported memory dump from a *different* project (Swift/macOS, "bodyman-epic-executor"), never reconciled with datum, and nothing links to or reads it.
- **Evidence:**
  - Its core rule contradicts the pipeline it claims to govern: "2. GREEN agent writes the implementation (sees ACs only, **NEVER sees the test**)" and "Use `green-no-read-tests.sh` hook (blocks Read + Bash on test files when source exists)". No such hook exists, and FLOW.md's GREEN reads the lane spec, runs the suite and reports `needs_write` naming specific test files — it plainly reads tests. The real invariant is `green_edited_tests` (may not *write* them).
  - "Max 6 concurrent agents … Never launch more than 6 Agent calls in one batch. If a batch has more tasks, split into waves of 6." datum packs lanes "into batches of ≤5" (FLOW.md §2 Act).
  - "REFACTOR agent verifies both files, runs tests, checks ACs" — a three-agent model with no reflect stage, no skeptic panel, no intake batch, no named failures.
  - The table "| 3-agent GREEN | Works in RED's worktree | **GREEN agent commits the implementation** |" versus FLOW.md's rule that "no phase script asks an agent to `git commit` except `datum-awake`" (e6c565a) — and lane stages commit under the runner's gates, not on their own authority.
  - Provenance is visible in the text: "The implementation has no degrees of **frcaliper**" (line 129) — "freedom" mangled by a global find/replace of a peer session's name — and Swift-specific entries (`defer inside if-let`, `XCTAssert`, `swift test`) in a Python/TypeScript repo. `SKILL.md Phase 3` and `bodyman-epic-executor` are cited as the authority for the whole model.
- **Action:** delete. Every rule in it is either wrong for datum or superseded by FLOW.md, and the one true thing (RED/GREEN/REFACTOR exists) is documented properly elsewhere. If any of it is still wanted it belongs in the operator's own memory, not in datum's docs tree where an agent reading "collective memory for the DATUM factory" will take it as normative.

---

### `docs/architecture/INVARIANTS.md`

- **Last commit:** 2026-05-27
- **Purpose (as stated):** "These invariants appear across multiple epics and are candidates for promotion to permanent, non-negotiable rules."
- **Verdict:** DEAD — the dead-producer class FLOW.md names in #394, with a broken output on top.
- **Evidence:**
  - Its producer `datum/analyze_properties.py` **has no caller**: a repo-wide search finds only its own docstring/argparse (`datum/analyze_properties.py:8,172`) and a graphify dump. No phase, script or CLI subcommand invokes it. `docs/epics/datum/integration-lanes/SPEC.md:141` reaches the same conclusion independently — "scan found only the cross-epic aggregator `datum/analyze_properties.py`, a different feature".
  - The file contradicts its own contract. The header promises invariants that "appear across multiple epics"; the producer's docstring says "appear in **2+** epics as candidates" with `--threshold 2`. **All 68 entries read "*Appears in 1 epic(s)*.*"** Nothing in the file meets the bar the file states for inclusion.
  - Entries 32–58 are short restatements of entries 1–31 ("## 5. Once `datum classify` is called … ALWAYS returns a JSON result within 2 seconds" / "## 34. classify always returns within 2s"), so roughly half the file is a de-duplication failure.
  - Entries 59–68 are the unfilled PROPERTIES.md template placeholders, scraped as if they were invariants: "## 59. [What must NEVER happen]", "## 61. [What must ALWAYS be true]", "## 66. [Latency/throughput/size bounds]".
  - Entry 29 is a prose exclusion note from one epic's PROPERTIES.md, promoted to numbered-invariant status: "## 29. *Exclusion note:* This epic modifies pipeline tooling, not user-facing systems…"
  - The invariants that did survive are about `datum classify` and `datum landscape` (epic-1 features), not about the pipeline FLOW.md describes. The real invariant record is FLOW.md §5 Properties: the fast-check/Hypothesis property tests in `skills/src/shared/verdicts.property.test.ts` and `tests/test_gate_properties.py`.
- **Action:** delete the file and either delete `datum/analyze_properties.py` with it (the #394 treatment — a producer with no consumer) or wire it to a phase and fix the threshold/dedup/placeholder bugs first. Publishing a file whose every row violates its own stated inclusion rule is worse than not publishing it.

---

### `docs/adr/000-template.md`

- **Last commit:** 2026-05-21
- **Purpose (as stated):** a MADR template — "* Status: [proposed | rejected | accepted | deprecated | … | superseded by …]".
- **Verdict:** ORPHAN. The template is fine; the process it serves does not run.
- **Evidence:** The only referrer is `references/03.5-architect.md` ("Check `docs/adr/` for an existing, current ADR that covers it… MUST write a new ADR using the MADR format to `docs/adr/`") and `references/02-plan.md:8`. **There is no architect phase** — `PHASES` is the seven in `models.ts`, and no workflow script dispatches an architect agent. `docs/adr/` contains the template and nothing else after four months, which is the expected outcome of a producer that never runs.
- **Action:** keep (30 lines, correct MADR, zero maintenance cost), but note in the closing section that `references/03.5-architect.md` describes a phase that does not exist. The template is not the problem; the reference doc pointing at it is.

---

### `docs/practice/README.md`

- **Last commit:** 2026-05-21
- **Purpose (as stated):** "# Practice Ledger / Record manual manager interventions and roadmap decisions here before formal tooling is built to automate them."
- **Verdict:** ORPHAN, benign. Three lines; the directory it heads is empty.
- **Evidence:** It has a producer — `datum/bootstrap/seed_state_docs.py:182` (`practice_dir = Path("docs/practice")`) creates it on bootstrap — but no reader and no entries in four months. The "formal tooling" it defers to arrived: FLOW.md's `datum review-accept` records operator decisions in `REVIEW-RESPONSE.md`, and Closeout writes RETRO.md.
- **Action:** keep or delete, low stakes. If deleted, remove the `seed_state_docs.py` line that recreates it, or the next `datum init` puts it back.

---

### `docs/diagrams/` — `README.md` + `flow-lane-sequence.png`, `flow-lane-states.png`, `flow-pipeline.png`, `flow-propose-verify.png`

- **Last commit:** 2026-09-05 (all five)
- **Purpose (as stated):** "Rendered from the Mermaid blocks in [docs/FLOW.md](../FLOW.md) (`mmdc`, 2× scale)."
- **Verdict:** CURRENT.
- **Evidence checked:** The README's four-row table maps each PNG to the right FLOW.md section (propose/verify shape, §1 pipeline, §3 one lane, §3 lane states) and the legend matches FLOW.md's own ("Legend for every diagram below: **purple rounded boxes are LLM agents** … teal … are deterministic"). The PNGs are one commit behind FLOW.md, so I checked whether that gap matters: the only FLOW.md commit since is `bb2512a2`, a **one-line insertion** into the §5 gap list (`1 file changed, 1 insertion(+)`) touching no ` ```mermaid ` block. The renders are therefore in sync with the diagram sources. The re-render command in the README is complete and runnable.
- **Action:** keep. This is the correct pattern for the rest of the tree: a short index that names its source of truth and the command to regenerate.

---

### `docs/field-notes-bodyman-epic-ui1-20260520.md`

- **Last commit:** 2026-05-21
- **Purpose (as stated):** "# DATUM Field Notes — THE RECORD Epic UI-1 (2026-05-20) / **Project:** THE RECORD (macOS meeting transcription, 1688+ tests, Swift 6.2)"
- **Verdict:** DEAD as guidance, valuable as history — the ancestor of FLOW.md's §5 gap ledger, written before that ledger existed.
- **Evidence:**
  - Its headline recommendation **shipped**: "Add `task_complexity: structural | behavioral` to task schema… Reserve RED-GREEN-REFACTOR for `behavioral` tasks only", validated in the task-005 trial append ("Single-agent structural flow produces equivalent correctness at ~3× lower overhead"). FLOW.md now: "`Lane.kind` (`'structural' | 'behavioral'`, from tasks.json `kind`) decides whether Act runs RED/GREEN at all — a structural lane goes straight to REFACTOR." Landed, under a different field name.
  - Every remaining "Fix needed in skill" targets code that no longer exists: Fix 1 rewrites `Path("assets/")` in `gate.py`, `contracts.py`, `scripts/bootstrap/install_hooks.py`; Fix 3 fixes `scripts/skeleton_creator.py`'s heading parser; Fix 4 adds `--mode xcuitest-headless` to `test_signal.py`; Fix 5 has `datum init` create an `assets` symlink. `test_signal.py` and `skeleton_creator.py` are not in the tree, and `datum init` now materialises `.datum/skills`, `.claude/agents/datum-*.md` and `.datum/hooks`.
  - "### 3. Commit queue and sidecar processes were not started — DATUM Act requires: `commit_queue.py` (Unix socket process for serialized commits), `spec_drift_detector.py` (sidecar…)". Both deleted (#394). A reader takes this as a known operational gap rather than as removed machinery.
  - It is about a Swift/XCUITest repo, invoked from `~/.claude/skills/rpa/` — a datum ancestor under a different name.
- **Action:** move to `docs/archive/` (or delete) with a header line recording that the structural/behavioral recommendation shipped as `Lane.kind` and the rest targets deleted code. It is the only document in the tree written from live observation and it earned its conclusion; it should not sit in `docs/` looking like an open defect list.

---

### `docs/gitnexus-fts-fix.md`

- **Last commit:** 2026-05-21
- **Purpose (as stated):** "# GitNexus FTS Fix — Read-Only Connection Blocks Extension Load / **Affects:** gitnexus 1.6.4, 1.6.5, 1.6.6-rc.34 (confirmed)"
- **Verdict:** DEAD (also ORPHAN — nothing links it; the verdict that drives the action is DEAD).
- **Evidence:** It is a monkeypatch write-up for a **third-party dependency's** compiled output — "**File:** `dist/core/lbug/pool-adapter.js` — `doInitLbug()`, around line 524" — pinned to gitnexus 1.6.x, four months old, with instructions to hand-edit an npx cache and "restart the MCP server (restart Claude Code or kill the gitnexus mcp process on port 3282)". Its own verification step names a foreign repo: "`gitnexus_query({ query: "delete remove file", repo: "bodyGuy" })`". It documents no datum behaviour, no datum command, and no datum file. Line numbers in a vendored `dist/` bundle are unmaintainable by construction.
- **Action:** delete. If the patch still matters it belongs in an upstream issue on gitnexus (the doc already lists six: #1090, #1191, #1255, #1287, #1403, #1449), not in datum's docs tree.

---

### `docs/apply-fts-patch.mjs`

- **Last commit:** 2026-05-21
- **Purpose (as stated):** no header; it is the executable form of the doc above.
- **Verdict:** DEAD (also ORPHAN).
- **Evidence:**
  - Line 3 hardcodes one machine's npx cache hash: ``const target = `${process.env.HOME}/.npm/_npx/32f98f05d98eef45/node_modules/gitnexus/dist/core/lbug/pool-adapter.js`;`` — it cannot run anywhere else, including on this repo's CI.
  - It asserts an exact match count against a vendored bundle and aborts otherwise (`if (count !== 2) { console.error(...); process.exit(1) }`), so it is guaranteed to fail on any gitnexus newer than 1.6.6-rc.34.
  - It is the only `.mjs` in a repo whose CLAUDE.md mandates TypeScript source and whose `skills/` build pipeline is `skills/src/*.ts → scripts/build-workflows.sh → skills/*.js`. An executable script in `docs/` is in the wrong tree regardless of its content.
- **Action:** delete, together with `docs/gitnexus-fts-fix.md`. They are one artefact in two files.

---

## Closing

### Ranked deletions and merges

| # | Action | File | Why it ranks here |
|---|---|---|---|
| 1 | **Delete + repoint README:87** | `docs/DATUM.md` | README's single "Full Skill Reference" pointer. 1227 lines of a system that was never built, presented as the manual. Blocked on `tests/test_datum_hardening.py:179` — change that assertion in the same commit. |
| 2 | **Delete + repoint README:88** | `docs/datum-workflow.md` | README's other pointer. Diagrams a PR phase, an Architect gate, a commit queue and a spec-drift sidecar, none of which exist. |
| 3 | **Delete** | `docs/CURRENT_STATE.md` | Duplicate of the root file Closeout writes and Plan reads, and it instructs agents that source lives in `src/datum/`. |
| 4 | **Delete** | `docs/MEMORY.md` | Another project's memory, contradicting datum's own GREEN contract, filed as "collective memory for the DATUM factory". |
| 5 | **Delete (+ its producer)** | `docs/architecture/INVARIANTS.md` | Dead producer with no caller; every row violates the file's own stated inclusion threshold. |
| 6 | **Delete** | `docs/gitnexus-fts-fix.md` + `docs/apply-fts-patch.mjs` | Third-party monkeypatch pinned to one machine's npx cache hash and a foreign repo name. |
| 7 | **Delete** | `docs/ROADMAP.md` | Empty duplicate of the root roadmap. |
| 8 | **Delete or move to `assets/templates/`** | `docs/TASKS.md` | A fixture under a filename the entry-point detector treats as a real artefact. |
| 9 | **Move to `docs/archive/`** | `docs/field-notes-bodyman-epic-ui1-20260520.md` | Historically valuable, operationally misleading; its main recommendation shipped as `Lane.kind`. |
| 10 | **Update (4 edits)** | `docs/datum-pipeline-overview.md` | Worth saving: add Properties, fix Closeout, fix follow-up filing, drop "self-healing". |
| 11 | **Update (framing)** | `docs/HORIZON.md` | Drop the V2/V3/V4 scheme; reconcile Dual Mode against root ROADMAP.md. |
| 12 | **Regenerate / fix renderer** | `docs/LANDSCAPE.md` | A cache with a broken tree renderer, not a document. |

Net: nine files gone, three fixed. `docs/FLOW.md`, `docs/diagrams/`, `docs/adr/000-template.md` and `docs/practice/README.md` survive untouched.

### The three most misleading claims a new reader meets first

A new reader starts at `README.md` and follows its two documentation links. Both land on dead files, which is why all three of these are reachable in two clicks.

1. **"`docs/DATUM.md` — Full Skill Reference — phases, gates, config, reference docs"** (`README.md:87`). The linked file is the pre-implementation design brainstorm, section 9 of which is literally "Open questions for PLAN.md". A reader who trusts the label configures `.datum/config.toml` with `[gates]` and `[yolo].hard_stops` tables that nothing reads, and looks for `.datum/state.json`, which does not exist.
2. **"GREEN agents must not see test source code. This is enforced by `test_signal.py`"** (`docs/DATUM.md` §7.8, restated in §4.6.2 and §5.4). Presented as a release-blocking invariant with a canary self-test. The file does not exist, and the actual invariant is narrower and different — GREEN may *read* tests but not *write* them (`green_edited_tests`, FLOW.md §5). This is the single claim most likely to send someone hunting for a redaction bug in a redactor that was never built. `docs/MEMORY.md` repeats it independently as a "daily driver" rule, which makes it look corroborated.
3. **"| **Properties** | … | Skippable in yolo |"** (`docs/datum-workflow.md:171`), directly contradicted by FLOW.md §5: "**Properties has no halt** — closed in e4f2e2f: gate failures halt in yolo too, Properties included." Same file's flowchart also shows `Act → Validate → Review → CreatePR → MergeGate → Merged → Closeout`, teaching a reader a pipeline shape with a PR phase that does not exist and without the unconditional Act halt that is the pipeline's central rule.

### Contradictions between docs that both claim authority

- **`docs/CURRENT_STATE.md` vs root `CURRENT_STATE.md`.** Both are titled "Current State" and both are written as the authoritative post-epic statement. The docs copy (2026-05-27) says under "Architecture notes": "**Layout**: DATUM source code lives in `src/datum/`." The root copy (2026-07-07) cites `skills/src/shared/models.ts`, `skills/src/shared/utils.ts`, `datum/cli.py`. Only the root one has a producer (`datum-closeout.ts:147`) and a consumer (`datum-plan.ts:63`, which reads `head -80 CURRENT_STATE.md` from the repo root and feeds it to the decomposer). The docs copy is unreachable by the pipeline but reachable by a human or an agent browsing `docs/`, and `references/00-discovery.md:98` refers to "CURRENT_STATE.md" without disambiguating. A Plan agent that reads the wrong one plans against `src/datum/`.
- **`docs/DATUM.md` vs `docs/FLOW.md` on the determinism boundary.** DATUM.md §7.1 and §7.10 assert the same principle FLOW.md does ("Constraints that rely on LLM compliance only are not constraints; they are aspirations") but locate enforcement in a hook layer, a subprocess wrapper, a commit queue and a lane-tools sandbox — none of which exist. FLOW.md locates it in `datum-cli` batch steps and exit codes. Two documents, same philosophy, incompatible mechanisms, no cross-reference between them.
- **`docs/HORIZON.md` vs root `ROADMAP.md` on the headless runner.** HORIZON shelves "Dual Mode (Headless Runner)"; root ROADMAP lists "Headless orchestrator for datum-local variant" under **Planned**.
- **`docs/architecture/INVARIANTS.md` vs itself.** Header: invariants that "appear across multiple epics"; producer default `--threshold 2`; all 68 rows: "*Appears in 1 epic(s)*".

### Weighting by inbound links

- **`README.md` links exactly two docs-tree files** — `docs/DATUM.md` and `docs/datum-workflow.md` — and both are DEAD. Its other three links (`CURRENT_STATE.md`, `CHANGELOG.md`, `ROADMAP.md`) resolve to root files, which is correct, and which is what makes the `docs/` duplicates of two of them dead weight. **Every README pointer into `docs/` is currently wrong.** Fixing those two lines is the highest-value change in this audit.
- **`SKILL.md` links no docs-tree file for content.** It mentions `docs/LANDSCAPE.md` once as a command output (`:23`), and otherwise refers only to `docs/epics/<branch>/` artefacts (TICKET, SPEC, REVIEW-REPORT, REVIEW-RESPONSE). SKILL.md is self-contained and consistent with FLOW.md — notably its review-accept section (`:159`) matches FLOW.md's closures on content keys, file+line fallback and the three-iteration hard stop. It carries no stale-doc debt.
- **`AGENTS.md` / `CLAUDE.md` link none of the audited files.** CLAUDE.md defers wholly to AGENTS.md and adds the generated-JS warning; both are consistent with the code.
- **`references/` links `docs/adr/`** from `03.5-architect.md` and `02-plan.md`, and `docs/LANDSCAPE.md` from `00-discovery.md`. `references/03.5-architect.md` and `references/00-discovery.md` describe an Architect phase and a Discovery phase that are not in `PHASES` — the `references/` tree is outside this audit's scope but is the next place to look, and it is the reason `docs/adr/000-template.md` is an orphan.
- **Nothing at all links** `docs/CURRENT_STATE.md`, `docs/ROADMAP.md`, `docs/TASKS.md`, `docs/HORIZON.md`, `docs/MEMORY.md`, `docs/field-notes-*.md`, `docs/gitnexus-fts-fix.md`, `docs/apply-fts-patch.mjs`. `docs/datum-pipeline-overview.md`'s only inbound link is from an archived epic walkthrough.
