# Review Report

**Findings:** 18 unique (10 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion |
|---|---|---|---|---|---|
| SEC-001 | high | datum/cli.py | 40 | DATUM_PROJECT_DIR environment variable is used unvalidated in os.chdir(). Any process that can set this env var (including parent shell, CI pipelines, or a compromised environment) can redirect datum's working directory to an arbitrary path including paths outside the repo (e.g. /etc, /usr). This is an environment-variable injection enabling path traversal — datum then reads and writes files (config.json, .datum/ state, lane-plan.json) in the attacker-controlled directory. | Validate that os.path.realpath(project_dir) starts with a known safe prefix (e.g. the user's home directory or a configured allowed base), or refuse to chdir to absolute paths that escape a configurable workspace root. At minimum, resolve and log the effective path before trusting it. |
| SEC-002 | medium | datum/cli.py | 205 | Branch name from ensure_feature_branch() is interpolated directly into a filesystem path via Python str.format() without sanitization: `config.get('epic_dir_pattern', 'docs/epics/{branch}').format(branch=branch)`. A branch name containing path traversal sequences (e.g. `../../etc`) would cause epic_dir to resolve outside the repo. `mkdir(parents=True, exist_ok=True)` then creates directories at the attacker-controlled path, and TICKET.md is written there. | After computing epic_dir, assert that Path(epic_dir).resolve() is a subdirectory of the project root before calling mkdir(). Alternatively, sanitize branch to match /^[a-zA-Z0-9._/-]+$/ and reject any value containing '..'. |
| SEC-003 | medium | datum/cli.py | 144 | In _install_workflows(), the code removes an existing symlink and replaces it with a new one pointing into the package directory. There is a TOCTOU race between link.unlink() and link.symlink_to(js): a concurrent process could insert a symlink to a sensitive file (e.g. ~/.ssh/authorized_keys) in ~/.claude/workflows/ between the unlink and the new symlink creation. Since the installed JS files are executed by Claude Code, a race-won symlink replacement could cause arbitrary code execution. | Use an atomic rename pattern: write to a unique temp path then os.replace() into the final destination. This eliminates the window between unlink and symlink_to. |
| SEC-004 | low | datum/detect.py | 158 | The fallback return value from _detect_test_command is `f"echo 'no test command for {lang}/{framework}'"`. The lang and framework values are derived from file-system content (pyproject.toml, package.json, directory names). If lang or framework contained a single-quote character, the resulting shell string `echo '...'...'` would break shell quoting when consumed by a caller that passes this string to a shell. The current callers use shlex.split() before subprocess.run(), which mitigates exploitation, but the quoting defect makes the contract fragile. | Return a safe sentinel instead: `None` or an empty list `[]` rather than a shell string. Callers already handle None (tdd_driver.py:111). This eliminates the quoting surface entirely. |
| PERF-001 | high | skills/src/datum-tdd-act-lane.ts | 447 | O(n×m) dependency filtering using Array.includes() instead of Set. For each task (n), checking if failed deps exist (m) triggers full array scan. With 5+ tasks × 3+ failed deps, becomes O(n×m) instead of O(n+m). | Convert priorFailures array to Set at line 444: const priorFailuresSet = new Set(priorFailures). Then use priorFailuresSet.has(d) instead of priorFailures.includes(d) on line 447. |
| PERF-002 | high | skills/src/datum-tdd-act-lane.ts | 457 | O(n×m) dependency filtering using Array.includes() in intra-batch dependency check. For each task, checking membership in batchLaneIds array via .includes() is O(n) per call. With multiple batches and many deps, accumulates O(n×m) cost. | Convert batchLaneIds to Set: const batchLaneIdsSet = new Set(batchLaneIds) at line 445. Use batchLaneIdsSet.has(d) instead of batchLaneIds.includes(d) on both lines 447 and 457. |
| PERF-003 | medium | skills/src/datum-tdd-act-lane.ts | 75 | File ownership check uses nested .some() with .endsWith() for both forbidden and allowed file lists. With many files changed (n) and many forbidden/allowed files (m), becomes O(n×m). Also bidirectional endsWith check (f.endsWith(fb) || fb.endsWith(f)) is redundant. | Pre-compute forbidden/allowed file sets normalized once: const forbiddenSet = new Set(forbiddenFiles.map(f => f.toLowerCase())), const changedSet = new Set(changed.map(f => f.toLowerCase())). Use Set intersection/membership checks instead of nested .some() with .endsWith(). |
| PERF-004 | low | skills/src/datum-go.ts | 131 | Array allocation via .slice() in batch loop creates new array copy for each batch. With 100+ tasks and MAX_BATCH=5, creates 20+ new arrays. Minor overhead but unnecessary allocations can fragment memory. | Use array view indices instead: store {start, end} pairs and access allLaneIds.slice(batch.start, batch.end) only when needed. Or use generator to lazily create batches rather than materializing all upfront. |
| CORR-001 | critical | datum/detect.py | 37 | Kotlin repos using `build.gradle.kts` will always be detected as Java, never Kotlin. The `markers` dict iteration order (Python 3.7+ insertion order) hits `java` before `kotlin`. Since `build.gradle.kts` appears in java's file list, `_detect_language` returns `'java'` immediately and never reaches the `kotlin` entry. The kotlin guard that mirrors the typescript/javascript deduplication is absent. | Remove `build.gradle.kts` from the `java` marker list and add a priority check: after matching `java` via `pom.xml` or `build.gradle`, check whether `build.gradle.kts` also exists and return `'kotlin'` instead — mirroring the typescript-over-javascript guard at line 46. |
| CORR-002 | high | datum/detect.py | 72 | Variable `lang` is reused as a loop variable inside the fallback `os.walk` block (line 72: `lang = ext_to_lang[ext]`), silently shadowing the outer `lang` variable that was the return value from the marker-check pass. Because the marker loop calls `return lang` on first match, the shadow only affects the fallback path — but within that path, `lang` ends up holding the last-seen extension's language string rather than being used solely as a dict key accumulator. This is harmless today but masks a latent bug if a caller references `lang` after the loop without going through `max(ext_count)`. | Rename the loop variable to `ext_lang` on line 72: `ext_lang = ext_to_lang[ext]; ext_count[ext_lang] = ext_count.get(ext_lang, 0) + 1` to avoid shadowing. |
| CORR-003 | high | datum/detect.py | 153 | The uv-vs-pip fallback check at line 153 is logically inverted for the common case. It checks `not uv.lock exists AND not pyproject.toml exists` to fall back to `python -m pytest`. But a project with only `requirements.txt` and no `pyproject.toml` would reach this branch (since `requirements.txt` triggers `python` detection) — correctly falling back. However, a project with `pyproject.toml` but WITHOUT `uv.lock` (pip-based pyproject project) would get `uv run pytest` even though uv is not configured. The condition should be: if `uv.lock` does NOT exist, use `python -m pytest`; the presence of `pyproject.toml` alone is not sufficient to conclude uv is in use. | Change the condition to `if not (root / 'uv.lock').exists(): return 'python -m pytest -x -q'` — only default to `uv run pytest` when `uv.lock` is present. |
| CORR-004 | high | datum/cli.py | 185 | `json.loads(config_path.read_text())` at line 185 will raise `json.JSONDecodeError` if `.datum/config.json` exists but contains malformed JSON (e.g. partially written by a previous interrupted `datum init`). There is no try/except around this call, causing `datum init` to crash with an unhandled exception rather than a user-friendly error message. | Wrap the `json.loads` call in a try/except block: on `json.JSONDecodeError`, print a warning such as `[yellow]Existing config is malformed — overwriting with detected values[/yellow]` and fall through to write the freshly detected config. |
| CORR-005 | high | datum/cli.py | 140 | `link.unlink()` at line 140 will raise `IsADirectoryError` on Python < 3.12 (and on some platforms in general) if `link` is a directory rather than a file or symlink. The outer condition (`link.is_symlink() or link.exists()`) is true for directories, but `Path.unlink()` does not remove directories. If a user has a directory named `datum-go.js` in `~/.claude/workflows/`, the install loop crashes. | After the `is_symlink()` check fails (i.e. it's a regular file or directory), use `shutil.rmtree(link)` if `link.is_dir()`, otherwise `link.unlink()`. Or add an explicit `if link.is_dir(): shutil.rmtree(str(link))` branch before `link.symlink_to(js)`. |
| CORR-006 | high | skills/src/datum-go.ts | 106 | The lane plan path is now `docs/epics/${epicBranch}/lane-plan.json` (changed from a hardcoded `.datum/lane-plan.json`). However, `epicBranch` is derived from a prior `agent()` call parsing branch info. If that agent call returns null or a malformed string, `epicBranch` would be undefined/empty, producing a path like `docs/epics//lane-plan.json` or `docs/epics/undefined/lane-plan.json`. There is no null-guard on `epicBranch` before it is interpolated into the path. | Add a null/empty check on `epicBranch` after the branch-detection agent call. If empty, fall back to `.datum/lane-plan.json` directly rather than silently constructing a bogus path. |
| CORR-007 | medium | datum/detect.py | 108 | `_detect_ts_test_framework` checks `'jest' in content` on the raw text of `package.json`. This substring match will match package names like `jest-circus`, `babel-jest`, `ts-jest`, `@jest/types` etc., even in projects that use Vitest with a jest-compatibility adapter. Since `'vitest' in content` is checked first this is partially mitigated — but a project using vitest with jest-compat that also has `@jest/globals` in devDependencies would correctly return `vitest`. The correctness risk is the converse: a project that has `'vitest'` mentioned only in a comment inside package.json would return `vitest` erroneously. | Parse `package.json` as JSON and inspect the `dependencies`, `devDependencies`, and `scripts` keys instead of doing raw substring matching on the full file text. This eliminates false positives from comments, string values, and peer dependency strings. |
| CORR-008 | medium | datum/detect.py | 96 | `_detect_python_test_framework` always returns `'pytest'` — the final `return 'pytest'` at line 105 is unconditional. The function has no branch that returns `'unittest'` or any other framework. This means a project with no pytest markers (no `pyproject.toml`, no `pytest.ini`, no `conftest.py`, no `tox.ini`) that uses `unittest` exclusively will still be detected as `pytest`, producing an incorrect `test_command` of `uv run pytest`. | Add detection for unittest: check for `import unittest` in existing test files, or check for `unittest.TestCase` usage. If no pytest markers are found and unittest usage is detected, return `'unittest'` and add a corresponding entry in `_detect_test_command` returning `'python -m pytest'` (pytest can run unittest tests) or `'python -m unittest discover'`. |
| CORR-009 | medium | skills/src/datum-go.ts | 93 | In the updated `datum-go.ts`, `READ_CONFIG_PROMPT` is called unconditionally (no `!a.testCommand` guard) at line 93 when entering the `act` phase. The `datum-tdd-act.ts` change added a guard (`!a.testCommand || !a.language`), but `datum-go.ts` always calls the agent to read config regardless of whether args already supply these values. This results in an unnecessary agent call and a token overhead on every `datum go` invocation. | Mirror the guard from `datum-tdd-act.ts`: only call the config-reading agent when `!a.testCommand` or `!a.language` is falsy; otherwise use the arg values directly. |
| CORR-010 | critical | skills/src/datum-tdd-act-lane.ts | 290 | The SPEC (REQ-1 through REQ-7, all three tasks in TASKS.md) requires ruff + mypy pre-checks to be inserted into the GREEN phase of `runLane` in `datum-tdd-act-lane.ts`. The diff contains NO changes to `skills/src/datum-tdd-act-lane.ts` and `skills/datum-tdd-act-lane.js` does not contain any ruff/mypy logic. The primary deliverable of this epic — the fail-fast validation gate — is entirely absent from the implementation. | Implement `runRuffCheck` and `runMypyCheck` helpers in `skills/src/datum-tdd-act-lane.ts` and wire them into `runLane` between the GREEN agent call (line 290) and the `green.tests_pass` gate (line 299) per the SPEC. Rebuild the generated JS with `bash scripts/build-workflows.sh`. |
