# Review Report

**Findings:** 5 unique (2 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion | Key |
|---|---|---|---|---|---|---|
| ARCH-001 | **high** | /Volumes/Extra/repos/gitrdunhq/datum/datum/path_utils.py | 96 | Function state_for_run() references .datum/state.json as a fallback, which no longer exists after the epic. Per docs/architecture/state-store.md, no module outside the documented exceptions may read/write .datum/state.json. Closeout collectors run before archive.py (references/08-closeout.md stage 1 vs stage 3), so the fallback to non-existent live state causes collect_brief_defects, collect_lane_tools, collect_platform to read empty state, losing telemetry per Requirement 9. | Remove the fallback entirely: return the archival path unconditionally with `return run_dir(run_id) / 'state.json'`. Collectors should only read from archived state after archive.py has run, or if reading live state is needed, the collectors must be reordered after archive or must call the canonical load_state() and archive it themselves before closeout. | 74c571ff |
| ARCH-002 | **medium** | /Volumes/Extra/repos/gitrdunhq/datum/datum/closeout/archive.py | 46 | Module directly deletes .datum/state.db via state_db.unlink(), bypassing the canonical datum.state accessor. No clear/archive operation is exposed by the canonical accessor, so this module reaches around the abstraction boundary with a bare filename reference. Violates the principle that datum/state.py is the sole accessor for state operations. | Add a clear_and_archive(run_id: str) function to datum/state.py that encapsulates both the archival snapshot and the deletion, keeping file-level operations inside the accessor. Then call it from archive.py, or document in archive.py with a code comment why this exception (like migrate.py and corpus_sql.py) is necessary. | a67df81a |
| CORR-001 | **high** | datum/migrate.py | 26 | R1: FAIL — Requirement 1's AC states `grep -rn "STATE_FILE\s*=" datum/ --include='*.py'` must return zero matches outside datum/state.py 'except in datum/rollback.py and datum/archive.py where STATE_FILE ... refers only to the archival-export path'. datum/migrate.py:26 defines `STATE_FILE = Path(".datum/state.json")` and is not in the exception list, so the AC's literal grep command fails (it returns a 3rd file). Requirement 3 separately sanctions migrate.py keeping a state.json-backed legacy importer (option b, documented at migrate.py:1-8), so the two ACs are in tension, but as written R1's AC is unmet. | Either add migrate.py to R1's documented-exception list (amend SPEC wording) or rename migrate.py's constant so it isn't literally `STATE_FILE =` (e.g. `LEGACY_STATE_FILE`), and note the intentional exception in a comment the same way rollback.py/archive.py do. | e5173729 |
| CORR-002 | **low** | skills/src/datum-tdd-act-lane.ts | 1 | scope-creep: this diff (branch datum/state-single-source-of-truth vs main) includes a large body of unrelated pipeline-infrastructure changes not tied to any SPEC.md requirement in docs/epics/datum/state-single-source-of-truth/SPEC.md — new agents/datum-structural.md, the batch-runner fixes (skills/src/shared/batch.ts), agent-type fallback (skills/src/shared/agent-types.ts), and datum-review.js prompt-template changes. These came from dogfooding commits (e.g. 30cf6b22 'fix(act): a structural lane runs a writing stage', 3aa3ab24 'fix(agents): an agent type the host has not registered falls back to the default agent') mixed into the same branch history as the state-accessor migration. | Confirm these commits are intentionally bundled (self-hosted pipeline fixes needed to execute this epic) rather than an accidental branch base drift; if intentional, note it in the epic's TASKS.md/PROPERTIES.md so reviewers don't mistake it for scope creep. | 9b3a2751 |
| CORR-003 | **info** | docs/epics/datum/state-single-source-of-truth/SPEC.md | 76 | Non-Functional Requirement 'No new production callers introduced ... beyond the 7+3 migrated modules and the 2 documented exceptions': the stated grep also should match datum/migrate.py by the same STATE_FILE pattern (see CORR-001), but the NFR's own expected-match list omits migrate.py too — the NFR text and R1's AC share the same unresolved gap, making this NFR UNVERIFIABLE as literally written until the SPEC reconciles R1/R3 over migrate.py's exception status. | Amend the NFR's expected match list to include datum/migrate.py as a third documented exception, consistent with Requirement 3's decision. | 7531b713 |
