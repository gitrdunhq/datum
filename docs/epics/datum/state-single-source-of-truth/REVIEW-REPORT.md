# Review Report

**Findings:** 5 unique (0 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion | Key |
|---|---|---|---|---|---|---|
| SEC-001 | **medium** | skills/src/shared/lane-steps.ts | 674 | structuralDeliverableSteps() (new in this diff) builds shell commands via `test -e ${q(o.wt)}/${q(f)}` and `git -C ${q(o.wt)} log ... -- ${scoped}`, where `q` (defined at lane-steps.ts:17) only escapes literal double-quote characters: `const q = (s) => "\"" + s.replace(/\"/g, '\\\"') + "\""`. Inside a double-quoted shell string, `$(...)`, backticks and `$VAR` are still expanded by the shell. `files` is `[...testFiles, ...implFiles]` (skills/src/datum-tdd-act-lane.ts:1662-1666), which ultimately comes from the task plan/spec content — text that can be influenced by an epic author or by LLM-generated planning output derived from user-supplied SPEC.md content. A declared deliverable filename containing `$(...)` or backticks will be executed as a shell command when this batch script runs via runBatch/agentFn, i.e., attacker/prompt-injection-controlled command execution in the CI/agent runner sandbox. | Validate each file path against a strict allow-list pattern (e.g. `^[\w./-]+$`, no leading '/', no '..') before building the batch step, or switch `q()` to a shell-safe single-quote escaper (`'` -> `'\''`) so `$()`/backticks are not expanded inside the quoted argument. | 18233b8c |
| SEC-002 | **low** | skills/src/shared/batch.ts | 111 | The batch wrapper switched from sourcing the generated step script (`. "$__f"`) to executing it as a child process (`bash "$__f"`). This is a documented behavioral fix (zsh vs bash), not a vulnerability by itself, but it does mean the script content (built from step commands, which can include agent- or spec-derived strings elsewhere in the same file) is now handed to a fresh `bash` invocation whose own quoting/escaping bugs upstream (see SEC-001) become full command execution outside the original shell's variable scope, slightly widening blast radius of any injected step content. | No action needed beyond fixing the underlying quoting issue in SEC-001; noting for awareness since the execution model changed in this diff. | ac97331e |
| CORR-001 | **low** | skills/src/shared/agents.ts | 1 | scope-creep: SPEC.md Out of Scope explicitly excludes 'Re-architecting how the TS Workflow pipeline (skills/src/datum-tdd-act*.ts) tracks lane execution' yet this diff touches skills/src/shared/{agents,batch,lane-steps,prompts,schemas,triage-classify,agent-types}.ts (and their generated skills/datum-*.js counterparts) plus new agents/datum-structural.md — none of these are tied to any of the 10 state-accessor requirements. | Split the TS workflow / agent-type / structural-reviewer changes into a separate PR/epic; they are unrelated to the state.json consolidation and were declared out of scope in SPEC.md. | 86578273 |
| CORR-002 | **low** | datum/worktree_manager.py | 142 | scope-creep: new `_rebase_reused_lane_branch` logic (rebasing a reused lane worktree branch onto base_sha, fixing #341 wf_a7f50762-9d3) is unrelated to any of the state-single-source-of-truth SPEC requirements (state.py/state.json/state.db consolidation). | Land the worktree-manager rebase fix as its own change with its own review, separate from the state-accessor epic. | 1ebfb375 |
| CORR-003 | **info** | docs/epics/datum/state-single-source-of-truth/SPEC.md | 0 | Requirement 10 (AC for tests/test_pipeline_scheduler.py / tests/test_rollback.py) worded as 'unchanged behavior before and after migration' but no pre-migration snapshot exists in the diff to compare against — verified only that the post-migration tests exist and pass against canonical accessor semantics (golden/expected values), not an actual A/B comparison against the old JSON-file code path since that code path no longer exists in the tree. | None needed for merge; noting as a spec-wording gap rather than an implementation defect since both named test files (tests/test_pipeline_scheduler.py, tests/test_rollback.py) do exist with the required assertions. | a4be8153 |
