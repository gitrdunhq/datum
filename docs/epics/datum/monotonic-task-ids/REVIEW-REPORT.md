# Review Report

**Findings:** 10 unique (6 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion | Key |
|---|---|---|---|---|---|---|
| PERF-001 | **high** | /Volumes/Extra/repos/gitrdunhq/datum/datum/task_ids.py | 50 | The next_task_number function calls `git show HEAD:{path}` for every single committed file in the repository (line 62-67), not just the task-related files specified in SPEC Requirement 2 AC. This violates PROPERTY(PERFORMANCE-002) which mandates 'exactly one `git show HEAD:<path>` per enumerated file' and PROPERTY(PERFORMANCE-001) which requires scanning 100 synthetic epics in under 2 seconds. For a typical repo with hundreds or thousands of files beyond the ~100 tasks.json and lane-plan.json files, this creates N+1 subprocess overhead that scales linearly with total committed file count rather than task file count. | Filter the file list to only include docs/epics/*/tasks.json and */lane-plan.json files (via regex matching, similar to the pattern used in find_task_id_collisions at datum/lane_plan.py:301) before entering the loop that calls git show. This aligns with SPEC Requirement 2 AC which explicitly states the counter should scan only these two file types and implements the PROPERTY(PERFORMANCE-002) requirement for linear cost in task files, not total repo files. | 5fc85718 |
| ARCH-001 | **high** | datum/id_pattern.py | 12 | SPEC Requirement 5 AC1 and NFR row mandate 'A single pattern definition lives in a shared location' with 'exactly one source definition', but five hand-maintained copies exist: (1-2) assets/schemas/task.schema.json lines 7 and 28 (id and depends_on fields, duplicated); (3) datum/id_pattern.py lines 12-17 (LANE_ID_PATTERN); (4-5) skills/src/shared/lane-id-pattern.ts lines 12-16 (exported string) and line 19 (regex literal). The TS file's comment at lines 4-11 admits copies must be kept aligned by hand, with only the exported string (copy 4) parity-tested, not the regex literal (copy 5). | Establish single source of truth: define LANE_ID_PATTERN once in a language-neutral format (e.g., a dedicated config file), then code-generate datum/id_pattern.py and skills/src/shared/lane-id-pattern.ts from it. Add build-time validation that all five consumers match. Replace hand-sync comments with autogeneration notices. | e992814e |
| ARCH-002 | **high** | assets/schemas/task.schema.json | 7 | assets/schemas/task.schema.json (designated by SPEC Req 5 AC1 as 'the shared location') is never imported or referenced by any Python or TypeScript code. The de-facto single source is datum/id_pattern.py instead, inverting the dependency direction the SPEC specified. The JSON schema remains a hand-maintained duplicate with no tooling to synchronize it against the Python/TS sources, violating Req 5's requirement that downstream modules consume FROM a single shared location. | Make assets/schemas/task.schema.json authoritative: add generated notices to Python/TS files, implement tooling that generates them from the JSON schema, and update CI to fail if any consumer deviates from the source. | 0e5e8b31 |
| ARCH-003 | **medium** | datum/lane_plan.py | 238 | Requirement 5 AC (line 39 of SPEC) names datum/lane_plan.py as a location where no literal task-id regex should remain. However, two extraction patterns exist: _TASK_ID_PREFIX_PATTERN (line 238) = [A-Z]{2,6}-\d+, and _RENUMBERED_ID_PATTERN (line 693) = [A-Z]+-\d+. The regex at line 693 is unbounded [A-Z]+ while the shared LANE_ID_PATTERN requires [A-Z]{2,6}. A 9-letter prefix would match line 693 but fail validation against the shared pattern, creating a silent skew in derive_integration_lanes and _renumbered_prefix_and_start. | Import LANE_ID_PATTERN from datum.id_pattern and use it to validate prefix matches. If extraction requires capture groups (which the shared pattern lacks), add a test asserting that all valid prefixes extracted by these local patterns also pass isLaneId(), ensuring consistency. | 1594082f |
| ARCH-004 | **medium** | datum/lane_plan.py | 874 | SPEC Requirement 2 AC specifies counter 'returns max(existing) + 1'. In main() line 874-875, a broad `except Exception: start = 1` silently downgrades the hard failure contract of next_task_number (which uses subprocess check=True) into a fallback to 1. This can manufacture the duplicate-id collision condition that Requirement 6's task_id_collision gate is designed to detect post-hoc. The parallel find_task_id_collisions function (line 279) takes the opposite stance: 'A git failure with a HEAD present raises.' | Replace broad exception handling with specific error handling: either re-raise git failures (to match find_task_id_collisions' contract), or log the failure and emit a structured error to the caller rather than silently defaulting. If a fallback is necessary, document why duplicate ids are acceptable in that case. | 86a5fb97 |
| CORR-001 | **high** | datum/task_ids.py | 50 | R-002: FAIL — the counter (`next_task_number`) scans EVERY committed file in the repo tree (`git ls-tree -r HEAD --name-only` with no path filter) and regex-searches raw file content for `<PREFIX>-(\d+)` substrings anywhere, rather than being scoped to the `id`/`depends_on` fields of `docs/epics/*/tasks.json` and `lane-plan.json` as Req-2's AC requires. This picks up unrelated prose (e.g. SPEC.md's own example text `DAT-142` inflates the DAT counter) and crashes outright on non-UTF-8 committed files. Reproduced live: `python3 -c "from datum.task_ids import next_task_number; next_task_number('.', 'DAT')"` run against this actual repo raises `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xef...` from inside `subprocess.run(text=True)` because the unscoped scan tries to git-show and decode a binary file as UTF-8 text. | Restrict the scan to paths matching `docs/epics/*/tasks.json` and `*/lane-plan.json`, parse them as JSON, and extract only the `id` and `depends_on` fields (as `find_task_id_collisions` already does), rather than grepping raw byte content of arbitrary committed files. | b2b5bb35 |
| CORR-002 | **high** | datum/lane_plan.py | 301 | R-006: FAIL — `find_task_id_collisions` (and its `_EPIC_TASKS_PATH = re.compile(r'^docs/epics/.+/tasks\.json$')` filter) only scans committed `tasks.json` files, never `lane-plan.json` files, even though Req-6's AC explicitly requires detecting a collision when 'a committed `tasks.json` or `lane-plan.json` already contains an identical `<PREFIX>-<n>` id'. `tests/test_lane_plan_task_id_collision.py` only ever seeds `docs/epics/*/tasks.json` fixtures (confirmed via `git grep -n 'lane-plan.json|tasks.json' tests/test_lane_plan_task_id_collision.py`), so a lane-plan.json-only collision is untested and undetected. | Extend `_EPIC_TASKS_PATH` (or add a parallel scan) to also match `**/lane-plan.json`, and extract lane ids from its `lanes`/`topological_order` structure, not just tasks.json's `tasks` array. | 8a2d3cf0 |
| CORR-003 | **high** | datum/id_pattern.py | 12 | R-005: FAIL — the AC requires 'a single pattern definition ... under assets/schemas (or equivalent single source of truth) ... consumed by both' the Python constr patterns and the TS regexes. Instead the identical hand-rolled regex literal is independently duplicated three times: `datum/id_pattern.py:12-17` (Python source of truth), `skills/src/shared/lane-id-pattern.ts:12-19` (separate TS literal, not generated from or importing the Python source), and `assets/schemas/task.schema.json` (a third copy of the same literal string, not referencing either). Consistency is enforced only after the fact by `tests/test_id_pattern_ts_parity.py`, which diffs the two string literals as text — that is drift detection, not a single consumed source. | Generate the TS literal and the JSON-schema pattern from the Python `LANE_ID_PATTERN` (or a shared JSON/YAML definition under assets/schemas) at build time, so there is exactly one place a maintainer edits the pattern. | 7f58cb88 |
| CORR-004 | **info** | docs/epics/datum/monotonic-task-ids/SPEC.md | 39 | R-005: UNVERIFIABLE (partial) — the AC names `skills/src/triage-classify.ts` as a file that must reference the shared pattern, but that file does not exist anywhere in the repo tree (`ls skills/src/triage-classify.ts` -> No such file or directory). The requirement's file list is stale relative to the actual codebase, so this specific sub-clause cannot be checked against real code. | Correct the file list in SPEC.md/TASKS.md or confirm the file was renamed/removed before this epic, so the AC references files that exist. | 18197fc6 |
| CORR-005 | **low** | skills/src/shared/batch.ts | 232 | scope-creep: this diff adds a `batch_truncated` (#539) detection/retry path in `skills/src/shared/batch.ts` and a matching retry branch in `skills/src/shared/agents.ts` (line ~269). Both are labeled with issue #539 in their comments and address batch-reply truncation handling — unrelated to any of the monotonic-task-ids SPEC's 8 requirements. | Land the #539 batch-truncation fix as its own commit/PR separate from the monotonic-task-ids epic so each is reviewable and revertible independently. | 6b490312 |
