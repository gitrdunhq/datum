# Review Report

**Findings:** 4 unique (3 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion | Key |
|---|---|---|---|---|---|---|
| ARCH-001 | **high** | /Volumes/Extra/repos/gitrdunhq/datum/datum/lane_plan.py | 242 | Hardcoded task id pattern duplicates shared source. Line 242 defines _TASK_ID_PREFIX_PATTERN as a literal regex that should reference the shared pattern from id_pattern.py or assets/schemas/task.schema.json. SPEC Requirement 5 AC explicitly mandates: 'No literal task-\d+-only or task-INT-\d+-only regex remains duplicated inline in datum/lane_plan.py after this change — each references the shared pattern definition instead.' | Replace the hardcoded pattern with an import from datum.id_pattern (e.g., use is_lane_id() function or LANE_ID_PATTERN constant) to maintain a single source of truth for the id pattern across all Python and TypeScript consumers. | 3e6f2a63 |
| ARCH-002 | **high** | /Volumes/Extra/repos/gitrdunhq/datum/datum/lane_plan.py | 673 | Hardcoded task id pattern _RENUMBERED_ID_PATTERN duplicates shared source. This pattern should not be defined inline but should reference the shared pattern definition. Same violation as ARCH-001 but at a different location. SPEC Requirement 5 AC mandates no hardcoded regex patterns in datum/lane_plan.py. | Replace the hardcoded _RENUMBERED_ID_PATTERN with an import and reuse of the shared pattern from id_pattern.py. The function _renumbered_prefix_and_start should validate against the shared LANE_ID_PATTERN instead of a duplicate pattern. | e3549aa0 |
| CORR-001 | **high** | assets/schemas/task.schema.json | 1 | R-005/Assumption-8: FAIL — SPEC.md's Assumption Audit #8 (status: decided, addendum 2026-09-07) explicitly states 'TASK is an ordinary four-letter prefix like any other; the pattern does not carve it out' and that the legacy TASK-001 fixture in tests/test_lane_plan_schema_int_ids.py should be 'updated by task-002 rather than encoded as an exclusion' — explicitly calling a lookaround-free carve-out a 'Rust-regex contortion' to be avoided. The shipped $defs.laneId.pattern does exactly that: it uses an explicit negative-alternation construction that matches every 4-letter prefix except the literal TASK (and every 5-letter prefix except DATUM), so TASK-001 fails re.fullmatch against the pattern (verified directly: re.fullmatch(pattern, 'TASK-001') is False). tests/test_lane_plan_schema_int_ids.py was left unmodified (confirmed via git diff — no changes to that file) and still asserts TASK-001 raises ValidationError (lines 86/104/122), and datum/task_renumber.py's task-002 RED Note in docs/epics/datum/monotonic-task-ids/TASKS.md directly countermands the SPEC decision by instructing 'tests/test_lane_plan_schema_int_ids.py must NOT be modified... must keep passing byte-identical' — the opposite of what Assumption 8 decided. | Either update SPEC.md's Assumption 8 to reflect the decision actually implemented (TASK is carved out, the old fixture kept as-is), or implement Assumption 8 as written: update tests/test_lane_plan_schema_int_ids.py to accept TASK-001 and simplify the pattern to a plain [A-Z]{2,6}-\d+ alternative without the TASK/DATUM literal exclusions. | 651c1da9 |
| CORR-002 | **info** | docs/epics/datum/monotonic-task-ids/SPEC.md | 133 | R-006: PASS but implementation location differs from spec's stated scan target — read: datum/lane_plan.py (find_task_id_collisions, _cli_task_id_collisions, --validate wiring at lines 251-330 and ~900-930) rather than datum/gate.py; the SPEC's Context section names datum/gate.py's _INT_LANE_PREFIX as the touch point for id-shape checks but the task_id_collision check itself lives entirely in lane_plan.py with no corresponding code in gate.py. Functionally the AC (structured task_id_collision code, colliding id, two conflicting paths, distinguishable exit) is met per tests/test_lane_plan_task_id_collision.py (all 37 relevant tests pass), so this is a documentation/wording note, not a functional failure. | No action required; note for spec-quality only. | fb7581dd |
