# Review Report

**Findings:** 6 unique (2 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion |
|---|---|---|---|---|---|
| ARCH-001 | high | skills/src/datum-go.ts | 167 | Agent executing shell commands (datum init, date) to bootstrap workflow. Agents are unreliable at shell command execution, making this orchestration pattern brittle. Also creates implicit circular dependency where workflow calls CLI command that affects branch state. | Export bootstrap logic from cli.py into a reusable module (e.g., datum.bootstrap.resolve_branch_and_runid) and call it directly from the workflow via a dedicated agent task, or embed the bootstrap logic in TypeScript to avoid shell execution. |
| ARCH-002 | high | datum/cli.py | 309 | Presentation logic (JSON vs text output) mixed with business logic via contextlib.redirect_stdout(). Multiple conditional branches for json_output throughout the function create cross-cutting concerns in the command handler, violating three-tier separation (DPS-100: Presentation never touches Logic directly). | Extract output formatting into separate presentation layer. Create a Result class with both JSON and text representations, compute it once in business logic, then format for output. Remove contextlib.redirect_stdout() and nested conditionals. |
| ARCH-003 | medium | skills/src/datum-go.ts | 173 | The 'adopted' field is extracted from CLI response (lanePlanPath, adopted are optional fields in type) but never used in the workflow. This suggests incomplete implementation or dead code being carried forward. | Either use the 'adopted' flag for conditional logic in the workflow (e.g., different handling for adopted branches), document why it's needed for future use, or remove it from the extraction if not needed. |
| ARCH-004 | medium | datum/cli.py | 377 | Business logic (adoption detection: determining if a branch was adopted vs newly created) lives directly in the CLI command handler. This couples the adoption decision to the CLI interface rather than keeping it in a business logic module. | Extract adoption detection logic into datum.state or datum.bootstrap module. Make it a pure function that takes branch_before, current_branch, and ticket_existed, returns adopted boolean. Reuse from both CLI and workflow if needed. |
| ARCH-005 | medium | datum/cli.py | 315 | Nested _fail function tightly couples error formatting logic with the command handler. The function duplicates JSON/text error output logic instead of centralizing error formatting, creating inconsistency across the CLI. | Create a centralized error handler (e.g., format_error(message, json_output) -> str or dict) in a utils module. Use it consistently across all CLI commands. Remove the nested function. |
| ARCH-006 | low | datum/cli.py | 290 | The --json flag adds complexity to init command, but JSON output mode swallows all downstream output (redirect_stdout). This makes debugging difficult if helpers fail, and signals that the function is doing too much (init should initialize, not orchestrate output modes). | Consider if --json mode truly belongs in init, or if it should be a separate init-json command. If kept, ensure errors still surface with full diagnostics regardless of output mode. |
