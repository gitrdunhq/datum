# Review Report

**Findings:** 12 unique (5 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion |
|---|---|---|---|---|---|
| SEC-001 | medium | skills/src/datum-tdd-act-lane.ts | 409 | Test file paths (`testFiles`) are interpolated unquoted into a shell command string that is handed to an agent to execute verbatim (`--files ${testFiles.join(' ')}`). File names originate from the lane plan / task file list, which is effectively agent/LLM-controlled content. A crafted or corrupted file path containing shell metacharacters (spaces, `;`, `$(...)`, backticks, quotes) would be interpreted by the shell when the agent runs the composed command, enabling command injection in the lane worktree. | Quote each path individually (e.g. build the argument list as `testFiles.map(f => `"${f}"`).join(' ')`) or, better, pass the file list via a temp file/heredoc the same way the regex patterns were just hardened in this diff, and have test-count-gate read paths from that file instead of the command line. |
| SEC-002 | low | datum/worktree_manager.py | 98 | The stale-worktree path used for automatic `git worktree remove` is extracted via regex from git's own stderr message. While git constrains this to a real registered worktree path, the value is not otherwise validated (e.g. confirmed to live under the repo's known worktree root) before being passed to `worktree remove`, so any path git reports here is removed without an allowlist check. | Before calling `git worktree remove`, verify `stale_path` resolves under the expected worktree base directory (e.g. `.worktrees/` or configured lanes root) and reject/log otherwise rather than removing unconditionally. |
| SEC-003 | low | datum/cli.py | 644 | `lane-plan-distribute` writes `lane-plan.json` into every `--target` directory supplied on the command line with no validation that the target path is confined to expected worktree directories; combined with `dest_dir.mkdir(parents=True, exist_ok=True)`, arbitrary paths (including outside the repo, e.g. via `../../`) will be created and written to if this command is ever invoked with attacker-influenced target strings. | Validate that each `target` resolves under a known worktrees root (e.g. `.worktrees/` or the repo root) before creating directories/writing files, and reject paths that escape it. |
| PERF-001 | medium | datum/cli.py | 668 | File read performed multiple times in loop — src.read_bytes() called for each target destination instead of once | Read the file once before the loop and reuse the bytes: `src_bytes = src.read_bytes()` then `dest.write_bytes(src_bytes)` inside loop |
| PERF-002 | low | skills/src/shared/agents.ts | 45 | Two-pass array partition using filter — lines array iterated twice (once for logLines, once for statusLines) | Combine into single pass: partition logLines and statusLines in one loop to reduce array traversals |
| ARCH-001 | high | datum/cli.py | 315 | Closure function _fail() captures json_output parameter from enclosing init() scope, creating hidden coupling and implicit state dependencies. The function behavior changes based on outer context, making it impossible to test independently. | Extract error handling into a separate ErrorHandler class or factory that takes output_mode as explicit parameter. Pass the handler instance to _fail() instead of capturing from closure. |
| ARCH-002 | high | datum/cli.py | 289 | init() function intermixes business logic with presentation logic via json_output parameter. Multiple conditional branches (lines 339, 348, 357, 371, 395, 398) scatter presentation concerns throughout the function, violating separation of concerns principle. | Extract presentation logic into separate OutputFormatter class with methods for each output mode. init() calls formatter methods instead of conditional branches. This separates business logic from I/O concerns. |
| ARCH-003 | medium | datum/cli.py | 309 | Using contextlib.redirect_stdout() to suppress output is fragile. Any downstream code writing to stderr, using logging, or alternative I/O paths will leak through. Creates implicit dependency on specific I/O implementation of downstream functions. | Inject a logging/output abstraction into downstream functions (ensure_feature_branch, seed_state_docs) instead of suppressing I/O at caller level. Use a no-op logger for json_output mode. |
| ARCH-004 | high | datum/skeleton_creator.py | 359 | Return type changed from str to str | None without updating callers. Lines 452, 468, 478 assign and use path value without None checks, risking template.format() receiving None and returning broken templates. | Either: (1) keep return type as str and return empty string for docs-only convention, or (2) update all callers to check for None before using path. Current callers at lines 468 (template.format) and 478 (dict return) must handle None explicitly. |
| ARCH-005 | high | datum/worktree_manager.py | 90 | Nested control flow has logic bug: line 90 checks result.returncode but result variable may not have been updated at line 85-89 if branch_exists is False. Line 85-89 assignment only happens inside if block, leaving result pointing to original failure. This causes incorrect error path execution. | Extract each recovery strategy (retry with existing branch, stale worktree recovery) into separate helper functions. Make control flow explicit: if first attempt fails, try each recovery path sequentially, checking result after each. Consider state machine or explicit error type matching instead of regex parsing. |
| CORR-001 | high | skills/src/shared/agents.ts | 38 | verifyCommitIndependently searches unbounded full repo history for the target commit message, causing false 'committed: true' matches from unrelated ancestor commits. commitPrefix is `red(taskId)`/`green(taskId)`/`refactor(taskId)` where taskId is a short reused label (T1, T2...). The lane worktree is branched from base_sha, which carries the ENTIRE ancestor history of the repo. If an earlier, unrelated epic also had a task named T1 and its RED-stage commit message was `red(T1): RED complete`, that ancestor commit now satisfies `logLines.find(l => l.includes(target))` for the CURRENT run's lane T1/RED check even though this run's agent produced no commit at all, masking a real agent failure as success. | Scope the log search to only commits unique to this lane/run, e.g. `git log <base_sha>..HEAD --format="%H %s"`, instead of unrestricted `git log`. |
| CORR-002 | medium | datum/skeleton_creator.py | 359 | New test_convention parameter on infer_test_path() (docs-only/directory) is never wired into build_skeleton(), so R3/AC2 is not satisfied end-to-end. build_skeleton() (skeleton_creator.py:441, called at lines ~551/644/673 — the only path the real pipeline uses) always invokes infer_test_path() with the default test_convention='flat', so a flat-extension test stub is still force-generated for docs-only tasks in production even though AC2 requires otherwise — the new behavior is only exercised when infer_test_path is called directly by unit tests, never through the actual generation path. | Thread a per-task test_convention (from task/lane-plan metadata or config) through build_skeleton() into infer_test_path(), and add an integration-level test that exercises build_skeleton()/run_preflight, not just infer_test_path in isolation. |
