# Review Report

**Findings:** 4 unique (1 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion |
|---|---|---|---|---|---|
| SEC-001 | medium | skills/src/datum-go.ts | 174 | epicBranch is now sourced from `datum init --json` CLI output and flows unsanitized into path templates (skeletonDir/epicDir at lines 181/184) and into per-lane shell command strings built in datum-tdd-act-lane.ts (e.g. line 62 `git -C "${wt}" diff --name-only HEAD~1 HEAD`, line 409 `bash scripts/test-count-gate --repo "${wt}" --files ${testFiles.join(' ')} ...`) that sub-agents are instructed to run verbatim via the Bash tool. Git branch names may legally contain shell metacharacters (backtick, $(), ;, |, &) since git only restricts a narrow character set (space, ~^:?*[\, control chars), so a crafted branch name can achieve command injection when processed by the pipeline. | Validate epicBranch (and any other branch/path-derived value used to build shell commands) against a strict allow-list regex (e.g. `^[A-Za-z0-9._/-]+$`) immediately after receiving it from `datum init --json`, before it is interpolated into any prompt/shell string; reject or reuse the existing epicSlug() sanitizer for filesystem paths. |
| SEC-002 | low | skills/src/shared/utils.ts | 319 | pathBoundaryMatch(), added to fix the raw-suffix-collision bug in verifyFileOwnership, still does not normalize/resolve paths before the endsWith comparison. `pathBoundaryMatch('../../../etc/passwd', 'etc/passwd')` returns true, so verifyFileOwnership(['../../../etc/passwd'], ['etc/passwd']) reports ok:true — a directory-traversal path can be treated as an authorized/owned file even though it escapes the lane's intended scope, undermining the boundary the rewrite was meant to enforce. | Normalize both operands (e.g. via path.normalize/path.resolve relative to the worktree root) and reject any changed path that resolves outside the worktree before applying pathBoundaryMatch, rather than doing a purely lexical endsWith comparison. |
| ARCH-001 | high | datum/cli.py | 309 | Presentation/Logic coupling: init() command uses contextlib.redirect_stdout() to suppress output from downstream functions (ensure_feature_branch, _install_workflows, seed_state_docs.main) when --json is enabled. This violates DPS-100 (Presentation/Logic separation). Downstream functions have no awareness of 'quiet mode' and cannot adapt their behavior accordingly. If they encounter errors and try to print diagnostics, those outputs are silently lost. | Pass an explicit output_mode or quiet parameter to ensure_feature_branch(), _install_workflows(), and seed_state_docs.main() so each function controls its own output behavior based on mode. Callers should not use context managers to suppress output from dependencies. Example: ensure_feature_branch(name, quiet=json_output). |
| ARCH-002 | medium | datum/skeleton_creator.py | 452 | Return type change without defensive programming: infer_test_path() now returns str | None (line 364), but the call at line 452 in build_skeleton() does not handle the None case. The path is directly used in template formatting at line 468 without validation. While the default test_convention='flat' prevents None in current callers, the API contract is violated and future callers passing test_convention='docs-only' would silently produce malformed output with 'None' in the path field. | Add explicit None-handling in build_skeleton(): either add test_convention parameter passthrough and raise an error if path is None, or add an assertion like 'assert path is not None, f"test path inference failed for {task_id}/{ac_id}"' before template formatting. Consider whether the function should skip generating a test entry entirely when test_convention='docs-only' rather than rendering a broken path. |
