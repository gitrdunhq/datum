# Review Report

**Findings:** 9 unique (5 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion |
|---|---|---|---|---|---|
| SEC-001 | high | skills/src/shared/utils.ts | 319 | verifyFileOwnership's pathBoundaryMatch treats any full-path suffix collision as a match (`a===b || a.endsWith('/'+b) || b.endsWith('/'+a)`). This was meant to fix a raw-substring bug but still lets a changed file like `malicious/src/foo.ts` satisfy an allow-list entry of `src/foo.ts` (and similarly can mis-match forbidden-file checks), because it compares raw string suffixes instead of path-segment-anchored paths. | Compare normalized, segment-anchored paths (e.g. split on '/' and compare segment arrays, or resolve relative to repo root) instead of string `endsWith('/'+...)`. Add a regression test asserting `malicious/src/foo.ts` does NOT match allowed `src/foo.ts`. |
| PERF-001 | high | datum/worktree_manager.py | 76 | Sequential subprocess calls in error recovery path of create_lane_worktree(). When initial 'git worktree add -b' fails, the code executes multiple git commands sequentially: rev-parse (line 77), worktree add retry (line 85), potentially worktree remove (line 101), worktree prune (line 113), and another worktree add (line 114). Called once per lane creation, this chain of subprocess calls (each ~100ms overhead) becomes a hot path bottleneck when creating 10+ lanes. | Restructure error handling to minimize subprocess calls: (1) Check if branch exists before the initial worktree add attempt using rev-parse in a single batch, or (2) parse git error messages more carefully to avoid the stale-worktree path when not needed, or (3) batch multiple git commands into a single script rather than sequential calls. |
| PERF-002 | high | datum/worktree_manager.py | 320 | N+1 subprocess pattern in housekeep_epic(). Line 314 calls 'git branch --merged' once, then lines 317-322 iterate through results and call 'git branch -d' individually for each matching branch. If deleting N merged branches with the epic prefix, this creates 1 + N subprocess calls instead of batching them. | Batch the branch deletion: collect all matching branch names, then call 'git branch -d' once with multiple arguments: `git branch -d name1 name2 name3...`. Alternatively, use `git branch --merged | grep '^  <prefix>' | xargs git branch -d` pattern. |
| PERF-003 | medium | datum/worktree_manager.py | 276 | Unnecessary sorting in cleanup_run_worktrees(). The call to 'sorted(run_dir.iterdir())' sorts directory entries (O(n log n)) before cleanup iteration. Since cleanup order is immaterial, this sorting is wasted work. | Remove the sorted() call: iterate directly with 'for lane_dir in run_dir.iterdir()'. If deterministic output is required (e.g., for testing), document that and consider sorting only for output, not iteration. |
| PERF-004 | low | datum/worktree_manager.py | 96 | Regex search on every error path. Line 96 calls 're.search(r"already used by worktree at...", result.stderr)' in the hot path error handler of create_lane_worktree(). While regex itself is fast, it's executed on every worktree creation failure. | Profile to confirm this is significant. If profiling shows overhead, consider using string methods (find/index) instead of regex for simple substring extraction, or cache the compiled pattern. |
| ARCH-001 | high | datum/cli.py | 309 | Output suppression via redirect_stdout creates hidden abstraction leak. The init() function suppresses stdout from downstream functions (ensure_feature_branch, _install_workflows, seed_state_docs.main) when --json mode is active. These functions are unaware their output is being suppressed, creating a fragile dependency that breaks if downstream code adds logging or side effects. | Inject a 'quiet_mode' or 'output_handler' parameter through the dependency chain so each downstream function explicitly respects the presentation mode. Alternatively, use a context variable or dependency injection container to thread output behavior through the call stack. |
| ARCH-002 | high | datum/worktree_manager.py | 96 | Tight coupling to git error message format via regex. create_lane_worktree() has complex error recovery logic (lines 72-122) that depends on parsing git stderr with regex to detect 'already used by worktree at'. This is brittle — git may change error message formatting across versions, breaking the recovery logic. | Extract error detection into a separate, testable function (e.g., _parse_worktree_conflict_error()) that isolates the regex pattern. Better yet, use git's structured output modes (--porcelain, JSON) to avoid string parsing where possible. Add explicit version guards or fallback error handling if relying on message format. |
| ARCH-003 | medium | datum/worktree_manager.py | 298 | Mixed concerns in housekeep_epic(). The function combines three independent operations: deleting merged branches, removing pipeline-state.json, and pruning stale worktrees. These are temporal coupling — they happen to execute together but have no logical relationship. Reusing one concern requires reusing all three. | Split into single-responsibility functions: delete_merged_lanes(epic_branch), remove_pipeline_state(), and prune_worktrees(). Allow callers to compose them. Define housekeep_epic() as an orchestrator calling these three if all are needed, but allow independent reuse. |
| ARCH-004 | medium | datum/worktree_manager.py | 42 | God function: create_lane_worktree() handles too many concerns — worktree creation, branch existence checking, stale worktree detection, recovery/cleanup (lines 66-123). The function has 6 git calls and complex conditional logic spanning 57 lines, violating the 500-line/file principle and making the function hard to test and maintain. | Extract helper functions: _ensure_lane_branch_exists(), _detect_stale_worktree_conflict(), _resolve_stale_worktree_lock(). This improves testability and allows each concern to be understood independently. The main function becomes a readable orchestration of these helpers. |
