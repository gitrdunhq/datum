# Review Report

**Findings:** 9 unique (1 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion | Key |
|---|---|---|---|---|---|---|
| PERF-001 | **medium** | datum/integration_invariants.py | 165 | derive_integration_lanes violates gate-performance NFR with O(n²) complexity. Lines 165-173 iterate all pairs of merge-frontier groups, calling _group_is_ancestor() twice per pair; lines 177-185 call sorted() on each topo-sort iteration (O(n² log n) total). SPEC.md §5 requires 'not more than a single additional linear pass over PROPERTIES.md/tasks.json per invocation'; this implementation is quadratic instead. | Refactor to build the successor graph in a single pass without pairwise comparisons. For topological sort, use Kahn's algorithm with a deque to track ready nodes (no repeated sorting). The current scale (typically K<20 merge-frontier groups per epic) makes the performance impact acceptable in practice, but the implementation violates the stated non-functional requirement. | a8d76789 |
| ARCH-001 | **high** | datum/gate.py | 1087 | gate_plan calls derive_integration_lanes to verify AC9.1 (INT lane depends_on matches invariant Covers union), but this violates the Gate performance NFR. The NFR mandates 'not more than a single additional linear pass over PROPERTIES.md/tasks.json per invocation', yet derive_integration_lanes performs multiple passes: grouping invariants (pass 1), computing task ancestors (pass 2), building successor graph (pass 3), and topological sort (pass 4). The gate re-derives full lane objects just to extract depends_on, when it could compute the union directly from invariant_rows with a single grouping pass. | Extract the grouping logic (compute groups by sorted(set(covers))) into a pure utility function. The gate can use this to recompute which invariants belong to each group, verify their union matches the lane's depends_on, and avoid the topological sort and ancestor computation. Alternatively, gate_plan could verify depends_on against invariant_rows directly: for each INT lane, find all rows whose grouped key (sorted unique covers) would produce that lane's id, compute their union, and compare. | be2382b4 |
| ARCH-002 | **medium** | datum/integration_invariants.py | 124 | The module _integration_test_path lazily imports detect_command_language from datum.lane_plan. While the reciprocal lazy import in lane_plan (line 657) avoids a hard circular dependency, this creates a subtle bidirectional dependency between a leaf parser module (integration_invariants, responsible for parsing and deriving invariants) and an orchestrator module (lane_plan, responsible for plan construction). SPEC §2 establishes that Plan calls the invariant reader, but does not authorize the reverse direction. Although lazy imports mitigate the risk, the logical inversion remains: a data-extraction module depends on a higher-level orchestrator. | Pass the detected language (or test_command interpretation) as a parameter to derive_integration_lanes, rather than having integration_invariants import from lane_plan. This makes the dependency direction explicit and unidirectional: lane_plan → integration_invariants for data extraction, never the reverse. Alternatively, extract detect_command_language into a shared utility module (e.g. datum/command_detect.py) that both modules can import without creating a circular dependency. | 9fa2522b |
| CORR-000 | **info** | docs/epics/datum/integration-lanes/SPEC.md | 0 | Methodology note: git merge-base HEAD main resolves to a commit far back (9d1b24a, ~511 commits behind HEAD), because this branch also carries an unrelated prior epic (hermetic-test-git-fixtures) and hundreds of self-hosted dogfooding commits. Diffing against that merge-base or against main...HEAD would pull in ~50k unrelated LOC. Instead I located the commit that introduced this epic's SPEC.md (1c9a932e, 'refine: write SPEC.md + QUESTIONS.md') and diffed from its parent (9e6c7aaf) to HEAD — the epic's own 79 commits / 122 files / ~9.3k LOC. All verdicts below are against that range. | None — informational. | 096dbaaa |
| CORR-001 | **medium** | datum/gate.py | 1334 | R1/AC1.2: FAIL (partial) — gate_properties only enforces the '>=2 Covers entries' floor for spec:-sourced rows (`if row["source"].startswith("spec:") and len(row["covers"]) < 2: fail(...)`); it never checks that a question:Q<N>-sourced row has at least 1 Covers entry. AC1.2 says 'the list has >=2 entries unless Source is question:Q<N>, in which case 1 entry is permitted' — implying a question row still needs >=1, not 0. A question row with an empty Covers cell (`| Q1 | ... | | question:Q1 |`) parses to `covers: []` (datum/integration_invariants.py:91, `[c.strip() for c in covers.split(",") if c.strip()]`) and passes gate_properties/gate_plan unchanged, then derive_integration_lanes groups it under key `()` and emits a task-INT-<n> lane with `depends_on: []` — an integration lane that depends on nothing and is never gated by AC9.1 in any meaningful way. | In gate_properties, also fail (e.g. `invariant_covers_insufficient: <id>`) when a question:-sourced row has zero Covers entries. | 0ec94b74 |
| CORR-002 | **low** | skills/src/datum-tdd-act-lane.ts | 1041 | scope-creep: the #440 'GREEN blocked on the lane's own test → bounded RED repair' feature (new branch around `ownTestTargets`/`red-repair` prompts, ~50 LOC) is unrelated to any R1-R10 requirement in this epic's SPEC.md; it is a separately-motivated fix (elonchesd epic-2 task-006) that landed on this same long-lived branch. | No action needed for this review; flag for its own review record if one doesn't already exist. | 5f32427e |
| CORR-003 | **low** | skills/src/shared/lane-steps.ts | 1 | scope-creep: the deterministic 'code tells' (unslop-code) scan wired into REFACTOR (datum/code_tells.py, codeTellSteps/parseTellScan/tellLines in lane-steps.ts, runRefactor's tells-gated skip logic in datum-tdd-act-lane.ts) is not tied to any requirement in this epic's SPEC.md. | No action needed for this review; confirm it has its own spec/review trail. | 86109d2a |
| CORR-004 | **low** | skills/src/shared/batch.ts | 1 | scope-creep: batch-runner robustness fixes bundled into this diff (prose-exit-126 handling as batch_script_failed, empty-code-fence-as-empty-reply retry, corrupt-script retry routed to the balanced model, ownership-diff 'since' param replacing HEAD~1) are infrastructure fixes unrelated to R1-R10. | No action needed for this review; these appear to already have their own commit-level review notes per FLOW.md's Closed section. | 55c6de3f |
| CORR-005 | **low** | docs/DATUM.md | 0 | scope-creep: large documentation deletions/rewrites (docs/DATUM.md, docs/MEMORY.md, docs/CURRENT_STATE.md, docs/datum-workflow.md, docs/architecture/INVARIANTS.md, datum/analyze_properties.py removal, datum/landscape.py rewrite) are part of a separate 'stale docs' cleanup effort, not required by this epic's SPEC.md. | No action needed for this review. | 84a00a81 |
