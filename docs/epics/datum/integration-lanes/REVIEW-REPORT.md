# Review Report

**Findings:** 6 unique (2 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion | Key |
|---|---|---|---|---|---|---|
| PERF-SUB-001 | **medium** | datum/gate.py | 100 | resolve_epic_dir spawns git subprocess without memoization | Add module-level memoization to resolve_epic_dir using functools.lru_cache or a simple _resolved_epic_dir module variable that caches the result across multiple calls within a single invocation. | a0f61e40 |
| ARCH-001 | **high** | datum/gate.py | 1054 | Validation of INT lane consistency is conditionally guarded by invariant_rows existence. If invariant_rows is empty but int_lane_ids is non-empty (orphaned INT lanes), the AC9.1 validation requiring INT lane depends_on to match invariant Covers union never executes. This allows inconsistent state: INT lanes in the plan without corresponding integration invariants in PROPERTIES.md. | Add an explicit check: if int_lane_ids is non-empty, then invariant_rows must also be non-empty. Either move the depends_on validation outside the `if invariant_rows:` guard, or add a separate validation block that fails when int_lane_ids is non-empty but invariant_rows is empty. This enforces AC9.1 consistently. | 966bb105 |
| CORR-R10-001 | **high** | docs/FLOW.md | 126 | R10: FAIL — AC10.1 requires docs/FLOW.md to gain a section describing integration lanes as RED-only (no GREEN in this slice), scheduled at merge frontiers, and with failure routing to covered tasks (not the INT lane). FLOW.md's integration-lane text (line 126) covers merge-frontier scheduling and lane construction, but never mentions RED-only/GREEN behavior or failure routing — a repo-wide grep for 'RED-only', 'no GREEN', 'covered task', or any integration-triage phrase in docs/FLOW.md returns zero hits. | Add a sentence to the FLOW.md integration-lanes section stating that in this slice an INT lane still runs the full RED/GREEN/skeptic/REFACTOR path (per SPEC.md §6 Out of Scope) and that RED-only execution plus failure-to-covered-task routing is deferred to slice 2, so the doc doesn't silently omit AC10.1's required content. | 0b50f502 |
| CORR-R4-001 | **low** | datum/lane_plan_digest.py | 56 | R4: FAIL (documented, deliberate) — AC4.1 literally requires that a pre-slice plan with no `kind` key on any lane digest every lane as `kind: "task"`. `build_digest`'s per-field copy only sets `kind` when already present on the source lane (no `entry.setdefault("kind", "task")` alongside the other setdefault calls), and `tests/test_lane_plan_digest.py:156` asserts the opposite behavior (`"kind" not in ...`). This is a genuine self-conflict inside SPEC.md itself: AC4.1's literal text collides with the Backward-Compatibility NFR (pre-slice plans must digest byte-identically) and with AC4.3/AC9.2 which already treat an absent kind as task everywhere else. REVIEW-RESPONSE.md already surfaced and accepted this exact conflict with that reasoning, so this is not a fresh implementation bug — it is AC4.1's wording that needs correcting, not the code. | Amend SPEC.md AC4.1 to say a pre-slice plan digests with `kind` simply absent (matching every other AC's absent-means-task convention), rather than literally synthesizing `kind: "task"`; no code change needed. | f8df2505 |
| CORR-R2-001 | **info** | docs/epics/datum/integration-lanes/SPEC.md | 33 | R2: UNVERIFIABLE — AC2.2 requires each question-sourced invariant's Invariant text to be 'a checkable, testable expectation (not a restatement of the question)'. This is a semantic-quality judgment about LLM-authored prose; gate_properties (datum/gate.py:1298-1320) only validates table shape, column count, and Source-pattern matching, not whether the text restates the question. No static check or test in the diff could settle this either way for arbitrary generated PROPERTIES.md content. | If mechanical enforcement is desired, add a heuristic gate check (e.g. reject near-duplicate text vs. the question); otherwise treat AC2.2 as reviewer-judgment-only and out of gate scope. | f2eac5c3 |
| CORR-SCOPE-001 | **low** | tests/test_worktree_manager.py | 1 | scope-creep: the diff against merge-base 9d1b24a touches 321 files / ~50k lines, far beyond SPEC.md's own classification metadata (estimated_files: 14, estimated_loc: 650, listing only gate.py, lane_plan.py, lane_spec_export.py, models/lane_plan_schema.py, skeleton_creator.py, the properties-derive prompt, docs/FLOW.md, SKILL.md). Large unrelated areas (tests/test_worktree_manager.py +1262, tests/test_lane_state_markers.py +719, vitest.config.ts, uv.lock, docs/research/*, docs/diagrams/*) are present in this diff. | No action required for this slice's authorship — the branch appears to have accumulated other epics'/commits' work before this slice landed. If a tighter review diff is wanted, rebase/cherry-pick the integration-lanes-specific commits onto a fresher main before requesting review. | fea4a6c4 |
