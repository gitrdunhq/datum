# Review Report

**Findings:** 12 unique (6 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion | Key |
|---|---|---|---|---|---|---|
| PERF-001 | **medium** | datum/integration_invariants.py | 153 | Nested loop computing merge-frontier group dependencies calls _group_is_ancestor redundantly: for each pair (a, b), both _group_is_ancestor(a, b) and _group_is_ancestor(b, a) are invoked separately. This is O(n² * 2) calls to _group_is_ancestor instead of O(n²/2) with a bidirectional check. | Optimize by computing only the upper triangle of pairs and handling both directions in a single call to _group_is_ancestor, reducing redundant calls by ~50%: use enumerate(keys) with keys[i+1:] to avoid checking both (a,b) and (b,a). | 844210f1 |
| PERF-002 | **medium** | datum/integration_invariants.py | 165 | Topological sort of merge-frontier groups uses sorted() inside the while loop (line 166), resulting in O(n² log n) complexity instead of O(n). The loop runs n times, and each iteration sorts up to n elements. | Replace the fixed-iteration topological sort with Kahn's algorithm using a deque: maintain a queue of nodes with in_degree 0, process them in order, and decrement successors' in_degree. This is O(n+m) where m is the number of edges. | 633d5736 |
| PERF-003 | **low** | datum/gate.py | 77 | Transitive closure computation uses a while loop with fixed-point iteration to compute all transitive dependencies. This is O(n³) in the worst case (outer loop O(n), for each node O(n), for each dependency O(n)) and requires up to n iterations to converge. | Replace with a proper transitive closure algorithm: Floyd-Warshall is O(n³) but with better constants, or use DFS-based approach which is O(n+m). Alternatively, precompute and cache transitive deps if gate_plan is called frequently on the same plan. | c50f4fc4 |
| ARCH-001 | **critical** | skills/src/shared/plan-steps.ts | 28 | Integration invariants feature is disconnected from lane planning. The lanePlanCommand function does not pass the --properties flag to datum lane-plan, so PROPERTIES.md is never read during lane plan generation. This prevents integration lanes from being created despite the full infrastructure being implemented. | Update lanePlanCommand to include --properties parameter: `datum lane-plan --input ... --output ... --md-output ... --properties ${q(`${epicDir}/PROPERTIES.md`)}`. This must match the requirement in lane_plan.py line 707 which expects the --properties argument. | 10fabbbd |
| ARCH-002 | **high** | datum/cli.py | 162 | The lane_plan_cmd CLI function does not expose a --properties option. While the underlying lane_plan.py module supports --properties (line 707), the CLI wrapper never passes this through, creating a disconnected API where the feature cannot be invoked from the command line. | Add a properties_file parameter to lane_plan_cmd with typer.Option and include it in the args list passed to lane_plan_main: add to args with `['--properties', properties_file]` when properties_file is provided. | d91a7fac |
| ARCH-003 | **high** | datum/gate.py | 19 | Module-level imports of integration_invariants (lines 20-27) at gate.py create tight coupling between the gate module and the integration invariants feature. While the functions are only used in gate_plan(), the imports happen at module load time, making gate.py depend on integration_invariants unconditionally. | Move the integration_invariants imports inside the gate_plan() function (around line 1039 where they're first used) to reduce coupling and make the feature optionally loadable. This follows the pattern already used in lane_plan.py line 657. | d3246c28 |
| ARCH-004 | **medium** | datum/lane_plan.py | 681 | Integration lanes are appended sequentially at the end of topological_order without interleaving with task lanes. While this is topologically valid (INT lanes depend on task lanes which come before them), it violates the semantic intent stated in SPEC.md AC3.2 that groups should be 'ordered by n starting at 1, assigned in topological order of the group's covered-task tuple'. This could obscure the merge-frontier scheduling semantics. | Merge INT lanes into topological_order at their proper positions using a merge sort or by rebuilding topological_order to interleave INT lanes based on their covered task dependencies, ensuring INT lanes follow the tasks they cover rather than being appended at the end. | 27e15174 |
| CORR-001 | **high** | datum/lane_plan_digest.py | 56 | R4: FAIL — AC4.1 requires that `datum lane-plan-digest`'s output include `kind` for every lane and that a pre-slice plan (no `kind` key on any lane) digest every lane as `kind: "task"`. `build_digest`'s per-field loop (`for field in _LANE_FIELDS: if field in lane and lane[field] is not None: entry[field] = lane[field]`) only copies `kind` when it is already present on the source lane; unlike `files`/`reads`/`depends_on` there is no `entry.setdefault("kind", "task")`. Verified empirically: `build_digest({'lanes': {'task-001': {'title': 't', 'files': ['a.py'], 'depends_on': []}}, 'topological_order': ['task-001'], 'total_lanes': 1}, 'sha')` produces a digest entry with no `kind` key at all, not `kind: "task"`. | Add `entry.setdefault("kind", "task")` alongside the other setdefault calls in build_digest. | cad4e34f |
| CORR-002 | **high** | datum/integration_invariants.py | 141 | R6: FAIL — AC6.2 requires INT-lane skeleton functions to be named from the invariant's `ID` column value (e.g. `II3` -> `test_ii3`), via `_extract_invariant_id` (datum/skeleton_creator.py:484-487) parsing a leading `ID:` token off the AC text. But `derive_integration_lanes` builds `acceptance_criteria` from `groups.setdefault(key, []).append(inv["invariant"])` — only the invariant's free text, never `inv["id"]`. `tests/test_integration_invariants_frontier.py:70` confirms this directly: `task1_lane["acceptance_criteria"] == ["Task 1 output is unique"]` for invariant id `INV-020`, with no id prefix. In the real pipeline `_extract_invariant_id(ac_text)` will therefore return None for every INT-lane AC and skeleton names fall back to the generic slugified name, not the invariant id — `tests/test_skeleton_integration_lane.py` only exercises the naming logic against hand-written AC strings that are manually prefixed with `"II1: ..."`, which is not what `derive_integration_lanes` actually produces, so the gap is untested end-to-end. | Thread the invariant id into the AC text (e.g. prefix `f"{inv['id']}: {inv['invariant']}"`) when building `groups[key]` in derive_integration_lanes, or pass the id through the lane structure separately and consume it explicitly in skeleton_creator instead of parsing it back out of free text. | dbb8e2fe |
| CORR-003 | **high** | SKILL.md | 173 | R10: FAIL — AC10.2 requires SKILL.md to document `kind: "integration"` as a lane kind and the new gate error/warning names `invariant_missing_for_question`, `invariant_covers_unknown_task`, `no_integration_invariants`, plus the reserved `integration_failed` name. None of these five strings appear anywhere in SKILL.md (git grep on HEAD returns zero hits for all of them, and for `kind:`/`"kind"` generally). SKILL.md's only integration-lanes-related edit is a one-line note that PROPERTIES.md now needs an `## Integration Invariants` table (line 173); the lane-kind and gate-name documentation landed in docs/FLOW.md instead (line 126), which satisfies AC10.1 but not the SKILL.md-specific AC10.2. | Add the `kind: "integration"` lane-kind description and the four named error/warning/reserved strings to SKILL.md, per AC10.2. | 262d4561 |
| CORR-004 | **info** | docs/epics/datum/integration-lanes/SPEC.md | 33 | R2: UNVERIFIABLE — AC2.2 requires each question-sourced invariant's text to state "the answer as a checkable, testable expectation (not a restatement of the question)". This is a semantic-quality judgment about LLM-authored prose with no static check enforcing it (gate_properties only validates table shape, column count, and source-pattern matching, not whether the Invariant text is a restatement vs. a testable predicate). No code or test in the diff could settle this either way. | If this needs to be enforced mechanically, add a heuristic gate check (e.g. reject invariant text that is a near-duplicate of the question text); otherwise treat AC2.2 as reviewer-judgment-only and drop it from automated gating expectations. | a2d2be0f |
| CORR-005 | **low** | tests/test_worktree_manager.py | 1 | scope-creep: git diff main...HEAD (merge-base 9d1b24a) touches 319 files and ~50k lines, far beyond the SPEC's own classification metadata (`estimated_files: 14`, `estimated_loc: 650`, listing only datum/gate.py, lane_plan.py, lane_spec_export.py, models/lane_plan_schema.py, skeleton_creator.py, the properties-derive prompt, docs/FLOW.md, SKILL.md). Most of the diff is unrelated to Integration Lanes (e.g. tests/test_worktree_manager.py +1262 lines, tests/test_pr_comment_monitor.py, vitest.config.ts, uv.lock, tests/test_lane_state_markers.py +719 lines) — this reflects the epic branch having diverged from main long before this slice and accumulating many other epics'/tasks' commits, not scope creep introduced by this specific slice's authors, but it means the recorded diff for this review is far larger than the SPEC anticipated. | No action needed on this slice's account; if future reviews want a tighter diff, rebase/cherry-pick the integration-lanes-specific commits onto a fresher main before review. | b79db064 |
