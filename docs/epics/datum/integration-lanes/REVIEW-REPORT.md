# Review Report

**Findings:** 11 unique (4 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion | Key |
|---|---|---|---|---|---|---|
| PERF-001 | **medium** | datum/gate.py | 1306 | Multiple linear passes over PROPERTIES.md content in gate_properties. The function calls `has_integration_invariants_section()` (line 1306) which scans for the heading, and then `parse_integration_invariants()` (line 1310) which independently scans and extracts the section. Both functions call `_heading_index()` separately, resulting in two linear passes over the same content when the section exists. This violates PERF-001: 'gate_properties adds at most one additional linear pass over PROPERTIES.md content'. | Refactor to combine the heading check and parsing into a single pass. Consider having `parse_integration_invariants()` return a sentinel value (e.g., exception with a specific type for missing section) that distinguishes 'section missing' from 'section present but malformed', eliminating the need for the separate `has_integration_invariants_section()` check. This would reduce from two O(n) passes to one O(n) pass through the content. | 36279f3e |
| PERF-002 | **medium** | datum/gate.py | 1037 | Multiple linear passes over PROPERTIES.md content in gate_plan. Similar to PERF-001, the gate_plan function calls `has_integration_invariants_section()` (line 1037) and then `parse_integration_invariants()` (line 1039), resulting in two independent scans when the section exists. This violates PERF-002: 'gate_plan adds at most one additional linear pass over PROPERTIES.md content'. | Apply the same refactoring as suggested for PERF-001 to consolidate the two passes into one. Refactor `parse_integration_invariants()` to provide a way to distinguish missing sections from malformed ones without requiring a separate pre-check. | 8d61cc71 |
| PERF-003 | **low** | datum/integration_invariants.py | 18 | The `_heading_index()` function (called by both `has_integration_invariants_section()` and `parse_integration_invariants()`) performs a redundant linear scan when both functions are called on the same content. Each call to `md_text.splitlines()` creates a new list copy, and each call to `_heading_index()` iterates through this list. For large PROPERTIES.md files, this repeated string splitting and iteration could be inefficient. While PERF-003 states files are 'read exactly once', this doesn't prevent inefficiency in parsing/scanning. | Consider caching the result of `splitlines()` or refactoring the integration_invariants module to accept pre-split lines as an argument, allowing callers to split once and reuse the result across multiple functions. Alternatively, combine `has_integration_invariants_section()` and `parse_integration_invariants()` logic to avoid calling `_heading_index()` twice on the same content. | d1a5df15 |
| ARCH-001 | **high** | datum/integration_invariants.py | 119 | Language detection logic is duplicated between `datum/lane_plan.py` (via `detect_command_language` and `LANE_LANGUAGE_TEST_COMMANDS`) and `datum/integration_invariants.py` (via `_is_pytest_command`). The new implementation is weaker: when `global_test_command` is None or not set, `build_lane_plan` passes empty string to `derive_integration_lanes`, causing `_is_pytest_command("")` to return False and producing `.ts` file paths even in Python repositories. This violates AC3.3 which specifies only pytest (.py) and TS/JS runner (.ts) branches, with no fallback to an unspecified language. | Remove `_is_pytest_command` and instead: (1) extract the test_command language detection into a shared helper in `datum/detect.py` or similar, (2) use that helper in both `lane_plan.py` and `integration_invariants.py`, (3) align the default when test_command is None/empty (currently lane_plan defaults to empty string while gate_plan defaults to 'pytest'). Alternatively, require `build_lane_plan` to receive an already-resolved test_command (not rely on deriving it from global_test_command in the integration module). | 0cd011a6 |
| ARCH-002 | **medium** | datum/gate.py | 1066 | gate_plan verifies AC9.1 (INT lane `depends_on` matches union of Covers) by re-executing `derive_integration_lanes` and comparing the result. This creates a hidden dependency on the producer's entire grouping, ordering, and numbering algorithm. If a bug exists in grouping logic, both the producer (build_lane_plan) and validator (gate_plan) will compute it identically, making the defect invisible. Additionally, the two calls use potentially different test_command sources: build_lane_plan receives `global_test_command` from CLI/config, while gate_plan gets `config.get("test_command", "pytest")` with a different default. If test_command is None in config, gate_plan defaults to "pytest" but build_lane_plan defaults to empty string, causing them to derive different INT lanes and the validation to pass despite a mismatch. | Instead of re-deriving lanes in the gate, compute the expected `depends_on` directly from the invariant rows without invoking the full `derive_integration_lanes` function. Extract a helper function like `expected_covers_from_invariants(invariant_rows)` that returns only the union of Covers per group, then compare that set against each INT lane's `depends_on`. This isolates the validation concern from the production logic. Ensure both build_lane_plan and gate_plan read test_command from the same source (e.g., both from config with the same default). | dd21a28b |
| ARCH-003 | **info** | docs/epics/datum/integration-lanes/PROPERTIES.md | 1 | This epic's own `PROPERTIES.md` lacks the `## Integration Invariants` section it defines in AC1.4. Running `datum gate properties` on this branch will fail the gate (as intended by AC1.4), but the artifact does not comply with the spec it documents. | Add a `## Integration Invariants` table to `docs/epics/datum/integration-lanes/PROPERTIES.md` listing integration invariants for this epic's own tasks. This is not an architecture issue but a spec-compliance artifact gap. | 42844aab |
| CORR-001 | **high** | skills/src/shared/plan-steps.ts | 20 | R3: FAIL — the SPEC's intended flow is Properties derives Integration Invariants, then Plan's lane-plan reads PROPERTIES.md to schedule task-INT-<n> lanes (AC3.1). But the live `datum go` pipeline runs Plan before Properties (skills/src/datum-go.ts: `shouldRun('plan', 1)` at line ~341 then `shouldRun('properties', 2)` at line ~355), and `lanePlanCommand` (skills/src/shared/plan-steps.ts:20-21) — invoked only from datum-plan.ts's planBuildSteps, during the Plan phase — passes `--properties <epicDir>/PROPERTIES.md` at a point in the pipeline where that file does not exist yet (it is written later by datum-properties.ts, which never re-invokes `datum lane-plan`). `build_lane_plan`/`derive_integration_lanes` silently no-op when the properties file is absent (`if properties_path.is_file()`), so no error surfaces. Net effect: on a normal `datum go` run, zero task-INT lanes are ever scheduled, regardless of how many Integration Invariants Properties derives — the feature's core wiring is unreachable in the pipeline it targets. | Either reorder Properties before Plan, or add a lane-plan regeneration/merge step at the start (or end) of the Properties phase (after PROPERTIES.md is committed) that re-runs `datum lane-plan --properties PROPERTIES.md` to append INT lanes, then re-gates the plan. | b1255759 |
| CORR-002 | **high** | datum/gate.py | 1309 | R1: FAIL — AC1.4 requires `gate_properties` to fail on malformed rows including 'Covers referencing an unknown task id', alongside wrong column count and bad Source pattern. `gate_properties` (datum/gate.py:1278-1332) only calls `parse_integration_invariants` (which validates column count and Source pattern, per datum/integration_invariants.py:57-86) and never cross-checks `covers` against tasks.json — that check exists only in `gate_plan` via `unknown_covered_tasks` (AC3.4, gate.py:1043-1050). tests/test_gate_properties_integration.py's own module docstring explicitly scopes the new gate_properties checks to just the heading/shape and per-question coverage, omitting the unknown-task-id case entirely, confirming this is not covered at the Properties gate. | Either add an unknown-covered-task check to gate_properties (reading tasks.json if present) to satisfy AC1.4 literally, or note in SPEC.md that this sub-case is deliberately deferred to gate_plan (AC3.4) as the sole enforcement point. | f690c3cc |
| CORR-003 | **high** | docs/FLOW.md | 126 | R10: FAIL — AC10.1 requires docs/FLOW.md to gain a section describing integration lanes as RED-only (no GREEN in this slice), scheduled at merge frontiers, and with failure routing to covered tasks (not the INT lane). FLOW.md's integration-lanes text (line 126) covers merge-frontier scheduling and lane construction, but never mentions RED-only/GREEN behavior or failure routing — there are zero occurrences of 'RED-only', 'slice 2', 'covered task', or any integration-triage phrase anywhere in docs/FLOW.md. This exact gap was already raised in a prior review pass (docs/epics/datum/integration-lanes/REVIEW-REPORT.md, CORR-R10-001) and has not since been addressed. | Add a sentence to FLOW.md's integration-lanes section noting that in this slice an INT lane still runs the full RED/GREEN/skeptic/REFACTOR path (per SPEC.md §6) and that true RED-only execution plus failure-to-covered-task routing is deferred to slice 2. | f95bdecc |
| CORR-004 | **info** | docs/epics/datum/integration-lanes/SPEC.md | 33 | R2: UNVERIFIABLE — AC2.2 requires each question-sourced invariant's text to be 'a checkable, testable expectation (not a restatement of the question)'. This is a semantic-quality judgment about LLM-authored prose; gate_properties (datum/gate.py:1309-1332) only validates table shape, column count, and Source-pattern matching, never whether the invariant text restates the question. No static check or test in the diff could settle this for arbitrary generated PROPERTIES.md content. | If mechanical enforcement is desired, add a heuristic (e.g. reject near-duplicate text vs. the question); otherwise treat as reviewer-judgment-only and out of gate scope. | 8085af14 |
| CORR-005 | **low** | tests/test_worktree_manager.py | 1 | scope-creep: the diff against merge-base 9d1b24a touches 321 files / ~50k lines, far beyond SPEC.md's own classification metadata (estimated_files: 14, estimated_loc: 650, listing only gate.py, lane_plan.py, lane_spec_export.py, models/lane_plan_schema.py, skeleton_creator.py, the properties-derive prompt, docs/FLOW.md, SKILL.md). Large unrelated areas (tests/test_worktree_manager.py +1262, tests/test_lane_state_markers.py +719, vitest.config.ts, uv.lock, docs/research/*, docs/diagrams/*) are present in this diff, consistent with the branch having accumulated prior epics' commits. | No action required for this slice's authorship; if a tighter review diff is wanted, isolate the integration-lanes-specific commits before re-review. | e8004b0a |
