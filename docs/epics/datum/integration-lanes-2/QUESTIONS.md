## Refine — 2026-09-06

### Q1: [Behavior] What is the exact syntax of the `integration_failed:` error string — separator, ordering, and whether invariant ids are included alongside task ids?
> R2 requires the lane to fail with `error` starting `integration_failed:` and "listing the covered task ids from `depends_on` and the invariant ids from the acceptance criteria." R3 requires `classifyLaneError` to extract those task ids from the same string. Both the lane runner's error-string builder and the classifier's regex must agree on the exact format (e.g. `integration_failed: covered task-002, task-003; invariants INV-01, INV-03` vs. a bracketed-list or JSON-in-string variant), or the classifier's extraction will silently fail to find ids on a format change. I'm assuming a human-readable, comma-separated, semicolon-delimited two-part string — is that right, or should this be a more structured (e.g. JSON-suffix) format?

[Answer]: Human-readable, two parts, semicolon-delimited, comma-separated ids, exactly: `integration_failed: covered task-002, task-003; invariants INV-01, INV-03 (independent verify exit=1)`. Task ids are the lane's `depends_on` in order; invariant ids are the leading `ID:` tokens of the acceptance criteria in order. The classifier extracts task ids with one regex on `covered ([^;]+)` and nothing else parses the string. No JSON suffix.

### Q2: [Architecture] Does the independent test verify for an integration lane run as an additional step inside the existing `postRedSteps` batch call, or as a second, separate `runBatch` invocation?
> The ticket's Constraints say "No new phase, no new command runner call per lane beyond the existing batches" (plural). `postRedSteps` today bundles count-gate, placeholder-scan, and ownership-diff into one `datum-cli` batch call. R2's independent verify could be (a) folded into that same batch as one more step, or (b) issued as a second batch call, which would still be "beyond the existing [single] batch" in spirit even if the constraint text technically allows multiple named batches. I'm assuming (a) — extend `postRedSteps` itself to include the verify step for `expect_tests_pass` lanes — is that right, or is a second batch call acceptable?

[Answer]: (a). `postRedSteps` already takes an optional `verifyTestCmd` that appends a `test-verify` step to the same batch; for an `expect_tests_pass` lane the runner passes the lane's test command there. One batch call, no new runner call, and the verdict is read with the existing `testExitCode` on that step.

### Q3: [Architecture] Should `TriageClassification` gain a new structured field for covered task ids, or is embedding them as a substring of `reason` (extracted via regex from the `integration_failed:` error) sufficient?
> R3 says the classification must carry "the covered task ids into the classification so the halt message says which merged work broke the invariant." SPEC.md defaults to string-embedding in `reason` to minimize diff size (per AI-003 minimal-diffs), but this affects any downstream consumer of `TriageClassification` — notably `skills/src/datum-tdd-act-triage` which files GitHub issues from these classifications. I'm assuming string-embedding in `reason` is sufficient for the triage/issue-filing use case — is that right, or does the triage pipeline need a real `taskIds: string[]` field on the type?

[Answer]: String-embedding in `reason` is sufficient for this slice. The issue filer and the halt message both read `reason`; a structured field is slice 2b work if a consumer appears that needs it (no producer without a consumer).

### Q4: [Behavior] What counts as "the independent test verify did not run" (null exit → `green_verify_unavailable`) for an integration lane specifically — a timeout, a missing test command in the lane spec, or something else?
> R2 says "a null exit (verify did not run) is `green_verify_unavailable`, as for any lane; never a pass." GREEN's existing verify already has this exact tri-state behavior for task lanes, but the specific conditions that produce a null exit there (e.g. no test command configured, worktree missing, command-runner crash) need confirming as identical for the integration-lane case, since an integration lane's tests are being run against the merged epic branch rather than a fresh worktree. I'm assuming the same conditions and same code path as GREEN's verify apply unchanged — is that right, or does verifying against the merged branch introduce a new "unavailable" case (e.g. merge branch not yet built/checked out)?

[Answer]: Same conditions, same code path: `testExitCode` returns null when the batch is missing (runner refusal, empty reply, refused script) or the `test-verify` step is absent or printed no `TEST_EXIT=` line. No new case: an integration lane runs in its own worktree cut from the epic branch at intake, after every covered task has merged (its `depends_on` guarantees the order), so "merged branch" is simply the lane's worktree, exactly as for a task lane.

### Q5: [Behavior] Should the RED prompt for an `expect_tests_pass` lane surface the invariant ids as a distinct list, or embed them in prose alongside the "tests must already pass" instruction?
> R1 says "The RED prompt for such a lane says the tests must pass ... and names the invariant ids they cover." AC1.4 needs a concrete, testable shape (e.g. a `contains()` check against specific id strings) to assert against. I'm assuming the invariant ids appear as plain substrings in the prompt text (e.g. "This lane covers invariants: INV-01, INV-03") rather than a structured block — is that right, or is there an existing prompt-templating convention (e.g. a fenced `## Invariants` section) this should follow instead?

[Answer]: Plain substrings, one line, through a prompt slot the way every other lane fact reaches RED: `This lane covers invariants: INV-01, INV-03. The code under test is already merged: these tests must PASS on your first run; a failing test is a finding, report it, do not weaken it.` No fenced section; the test asserts `contains()` on the id strings and on "must PASS".

## Refine — 2026-09-06

### Q6: [Scope] How many tasks should this slice decompose into, and does one task need to cross the runner+prompt+classifier+export layers together to satisfy the `plan_not_sliced` vertical-slice constraint?
> The ticket's Constraints require "the first task is the thinnest end-to-end path: one integration lane runs RED-only and completes green," and explicitly forbids a task that "only touches one layer." SPEC.md's requirements (R1–R6) map fairly cleanly to layers (runner/prompt in R1-R2, classifier in R3, export/digest in R4, docs in R5, real-ride verification in R6), but the ticket does not say whether Plan should produce one big vertical task covering R1+R2+R4 (runner+prompt+export, since the runner needs `expect_tests_pass` from the digest to branch at all) plus a second task for R3 (classifier) plus a third for R5 (docs), or some other split. I'm assuming Plan (not this SPEC) makes the final task cut, but is there a minimum/maximum task count or layer-crossing rule beyond "not single-layer" that Plan should honor here — e.g. must R6's real-ride check be its own task, or is it verified incidentally by running the epic's own Act?

[Answer]: Plan makes the cut; the rule is only "no single-layer task". Expected shape: task 1 is the thin end-to-end path and crosses runner + lane-spec export + RED prompt (R1, R2, R4), because the runner cannot branch without `expect_tests_pass` in the spec and RED cannot be told to pass without the prompt line; task 2 crosses classifier + halt message + FLOW/SKILL docs (R3, R5). Two or three tasks; no maximum beyond that. R6 is not a task: it is verified by this epic's own Act running the integration lane Properties derives.

### Q7: [Integration] Should this slice's Refine/Plan phase re-verify (not just assume) that this epic's own Properties output already contains an `## Integration Invariants` table and that Plan schedules a `task-INT-<n>` lane, before Act starts — or is it acceptable to discover a missing integration lane only when Act completes (per R6, "that is a defect of this slice, not a pass")?
> R6 says a plan with no integration lane is a defect, but doesn't say when that should be caught. Catching it late (after Act runs) means the whole epic's Act phase could complete without ever exercising the RED-only path this slice exists to test, wasting the run. I'm assuming no new pre-Act gate is added (matching Out of Scope's "Refine pre-checking... is out of scope") and R6 is verified retrospectively by inspecting this epic's own Act log/lane-plan.json after the run — is that right, or should Plan itself hard-fail (not just warn) when zero integration lanes are produced for an epic whose own SPEC/Properties clearly describe integration-lane behavior?

[Answer]: Neither phase re-verifies by hand; the gates already do. `datum gate properties` fails without an Integration Invariants table and `datum gate plan` warns `no_integration_invariants` when no INT lane was scheduled, so a missing lane is visible at the plan gate after Properties, before Act. Keep it a warning in this slice; if it appears on this epic the operator stops the run there and it counts as the slice's first defect.

### Q8: [NFR] Is a real merged-branch fixture (actual git history with merged task commits) required for the R1/R2 vitest test, or is a fake-agent unit test against a mocked `postRedSteps`/`testExitCode` sufficient to satisfy the acceptance criteria's "drives the lane runner with a fake agent through an integration lane"?
> The Acceptance section says "A vitest test drives the lane runner with a fake agent through an integration lane and observes..." — this matches the existing `datum-tdd-act-lane.calls.test.ts` sandbox convention (compiled bundle + `new AsyncFunction` + fake `agent()`/command-runner responder), which does not need a real git worktree or merged branch, only mocked step outputs. I'm assuming the fake-agent/mocked-batch-response approach (no real worktree, no real merge) is sufficient and preferred, consistent with existing tests in that file — is that right, or does this slice also need an integration-style test that exercises a real worktree with actually-merged commits?

[Answer]: The fake-agent unit test is sufficient and is the established pattern (`skills/src/datum-tdd-act-lane.calls.test.ts` drives the whole lane with a fake `agent()` returning canned batch JSON). No git fixture: the batch reply supplies `TEST_EXIT=0` or `TEST_EXIT=1` for the `test-verify` step and the test asserts the outcome and the calls made. The real merged-branch case is R6, this epic's own run.
