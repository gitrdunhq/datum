# Review Report

**Findings:** 7 unique (2 high/critical)

## Findings

| ID | Severity | File | Line | Description | Suggestion | Key |
|---|---|---|---|---|---|---|
| ARCH-001 | **high** | datum/integration_invariants.py | 150 | The `invariants` field is being produced by the Properties phase as a new upstream field, changing the source of truth for invariant ids from runtime derivation (leading ID: tokens from acceptance_criteria) to a pre-computed field. This violates SPEC §6 which explicitly states: 'Any change to how Properties derives invariants or Plan schedules lanes (datum/integration_invariants.py, datum/lane_plan.py) beyond the two field additions in R4 — that logic is slice 1, already shipped.' R4 specifies only `expect_tests_pass` should be added to _LANE_FIELDS; adding `invariants` there as well crosses into Properties derivation logic that was out of scope. | Remove the `invariants` field from datum/integration_invariants.py (line 150) and datum/lane_plan_digest.py. Instead, have the runner extract invariant ids at runtime from the acceptance_criteria leading ID: tokens, as R1.2 specifies. This keeps the source of truth unified and avoids the cross-layer dependency between Python Properties and TypeScript runner. | 2a3ee4a4 |
| ARCH-002 | **high** | datum/gate.py | 1123 | A new `plan_not_sliced` warning check (31 lines) has been added to the gate that was not part of any planned task. SPEC §6 explicitly defers 'Turning the plan gate's `plan_not_sliced` warning into a halt — slice 2c.' Additionally, this violates §5's Minimal-diff NFR which enumerates permitted files: 'Changes confined to skills/src/datum-tdd-act-lane.ts, skills/src/shared/triage-classify.ts, skills/src/shared/types.ts, datum/lane_plan_digest.py, docs/FLOW.md, SKILL.md, plus their tests — no edits to generated skills/*.js bundles directly.' datum/gate.py is not in that list, and this layer-validation logic was explicitly deferred. | Remove the layer-validation check from datum/gate.py (lines 1123-1149). The plan_not_sliced warning logic is out of scope for slice 2a and should be added in slice 2c when it's planned as a halt condition. This keeps the diff scoped to the enumerated files per §5. | d41bdc38 |
| ARCH-003 | **medium** | skills/src/datum-tdd-act-lane.ts | 620 | The predicate for including `integrationNote` in the RED prompt requires both `isIntegration && lane.expect_tests_pass` (line 620), but the lane fast-path (line 785) triggers on `isIntegration` alone. SPEC §4 Failure Modes explicitly covers this drift case: 'Lane has kind: 'integration' but expect_tests_pass is missing/false in the spec (data drift between Plan and lane-plan digest) — Treat kind === 'integration' as the authoritative signal for the RED-only fast path... but do not silently fall back to full task-lane flow.' When this drift occurs, the runner executes the lane as 'tests must PASS' (per verify logic) but the RED agent receives the default prompt 'Your new tests MUST fail. Report tests_pass=false' because integrationNote is empty, creating a contract mismatch. | Fix the predicate for integrationNote to match the condition for triggering the fast path. Either: (a) Emit integrationNote when `isIntegration` is true (aligning with fast-path trigger), or (b) add an explicit log message that explains what the RED agent should expect when the drift occurs, documented in the RED prompt. Align the predicates so the agent's instructions match the verify contract. | 110c89a2 |
| ARCH-004 | **medium** | skills/src/shared/lane-steps.ts | 1124 | The `integrationVerifyCmd` function uses pattern matching (`/\bpytest\b|\bvitest\s+run\b/`) to detect framework-specific CLI conventions. For frameworks outside this list (Swift/Go, which red.md proves the repo supports), the function silently returns the unmodified test command, reintroducing the exact whole-suite failure it was added to fix (per comment at line 1115-1120: 'task-INT-5's nine tests passed and its verify still failed, because it ran the whole suite'). This is an abstraction leak that hard-codes test-framework conventions into the lane-steps module. | Refactor integrationVerifyCmd to accept a framework hint (from the lane or lane spec) so it can correctly handle Swift/Go test runners. Alternatively, document in SKILL.md and red.md which test frameworks are supported for integration-lane file-scoping, and make this a configuration requirement rather than a silent no-op for unsupported frameworks. | 840f4588 |
| CORR-INFO-1 | **info** | skills/src/datum-tdd-act-lane.ts | 617 | R1: PASS (with a noted deviation) — runLane detects lane.kind === 'integration' (line ~221), runs RED once, folds the independent verify into the single postRedSteps batch (no second runBatch, lines 779-786), and skips GREEN/skeptic/REFACTOR (the isIntegration block returns before reaching those calls). The RED prompt substring 'This lane covers invariants: <id>, <id>...' and 'must PASS' sentence are built at line 619-620 and asserted in skills/src/datum-tdd-act-lane.integration.test.ts (AC5). Deviation: R1.4/Assumption 5 specified invariant ids as 'the leading ID: tokens of the lane's acceptance criteria' parsed at runtime; the implementation instead adds a new Lane.invariants field populated upstream by datum/integration_invariants.py (ids travel pre-computed, never parsed from AC text). This is documented and justified in docs/FLOW.md's Closed ledger ('runLane may not read criteria text ... nothing else carried the ids') and is functionally equivalent, but it is a real substitution of the specified mechanism, not an implementation of the literal R1.4 wording. | No action needed — functionally sound and well-documented; consider updating SPEC.md's Assumption 5 text to match the shipped design for future readers. | f1de2029 |
| CORR-INFO-2 | **info** | skills/src/shared/lane-steps.ts | 1122 | R1.2/NFR: PASS with a scoping deviation — postRedSteps still receives verifyTestCmd as a single argument (one batch call, per NFR), but datum-tdd-act-lane.ts:785 passes integrationVerifyCmd(scopedTestCmd, testFiles) instead of the lane's bare test command, appending the lane's own test files to a pytest/vitest command so the independent verify is scoped to the lane rather than the whole suite. This is a post-hoc fix (commit aac16592, run 20260907-015322 noted in the code comment) for a real false-positive (an unrelated suite-wide red became integration_failed), not literally 'the lane's test command' as R1.2 states, but preserves the single-batch-call NFR and improves correctness of R2's verdict. | None required; consider noting the scoping behavior in SPEC.md R1.2 since it changes what 'the lane's test command' means in practice. | afd0d428 |
| CORR-003 | **low** | docs/FLOW.md | 307 | scope-creep: this PR bundles numerous infrastructure fixes unrelated to SPEC.md's R1-R6 (e.g. batch guard-row retry on batch_root_missing/batch_tool_missing, batch_timeout handling, empty-array-reply detection, count-gate JSON envelope on every exit, closeout gitnexus_reindex exit-code fix, skeleton-plan preflight projection, main-sync-steps hermetic fixture fix, plan_not_sliced layer-detection gate) discovered while dogfooding this epic's own Act run. SPEC.md's NFR 'Minimal diff' explicitly lists the files this change should be confined to (datum-tdd-act-lane.ts, triage-classify.ts, types.ts, lane_plan_digest.py, FLOW.md, SKILL.md plus tests) and this diff touches ~30 additional files/72 total. | None blocking — these are legitimate fixes surfaced by R6's real-ride requirement and are individually documented in the FLOW.md ledger, but they should be recognized as scope beyond the SPEC's stated minimal-diff boundary, not silently absorbed into this slice's review. | 90a7e07d |
