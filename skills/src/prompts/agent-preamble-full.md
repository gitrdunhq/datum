# datum — Full Agent Context

> Run `datum awake` to regenerate this from the live repo state.

This is the placeholder. Once `datum awake` runs, this file will contain:
- All coding rules with rationale
- Code examples showing correct patterns
- Test examples showing naming, fixtures, assertion conventions
- Error handling patterns from existing code
- Import conventions with examples
- Anti-patterns extracted from linter configs
