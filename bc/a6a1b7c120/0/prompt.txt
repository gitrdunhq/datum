please impllement #137 ensuring sorktree true isolation ensure sgents only work in their worktree.

---

hey use gitnexus to trace blast radius

---

use npx gitnexus mcp is down

---

fix those tests

---

fix those tests or the ckde

---

ok merge to main

---

reinstall

---

thefe is on the board an enhancement to make tdd a workflow

---

/workflow-orchestration-patterns

---

/nano-banana-pro-prompts-recommend-skill

---

/nano-banana-pro-prompts-recommend-skill

---

<task-notification>
<task-id>a5ce40bd5a42b6795</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/a5ce40bd5a42b6795.output</output-file>
<status>completed</status>
<summary>Agent "Opus review and enhance TDD workflow plan" completed</summary>
<result>Now I have a thorough understanding of the codebase. Let me compile the enhanced implementation plan.

---

## Enhanced Implementation Plan: Deterministic TDD Act Workflow Script (Issue #133)

### 1. Wave-Based vs. Per-Task Dependency Tracking

**Analysis of the problem:**

The `topological_order` array in `lane-plan.json` is a **linear ordering** produced by `topological_sort()` in `datum/lane_plan.py` (line 126-151). It is a standard DFS-based topological sort that returns a flat list like `["task-001", "task-003", "task-002"]`. This is NOT wave-grouped. Two independent tasks (both with zero deps) might appear at positions 0 and 2 with a dependent task at position 1.

**Why BFS wave grouping is correct but the draft's approach is incomplete:**

BFS-layered scheduling (Kahn's algorithm) groups tasks into "waves" where wave N contains tasks whose dependencies all appear in waves &lt; N. This correctly identifies maximum parallelism -- tasks in the same wave have no mutual dependencies and can run concurrently.

However, there is a subtlety the draft plan misses: `pipeline-dispatch.md` defines TWO kinds of dependency:
- **Signature dependency** (satisfied by stub commit) -- unblocks downstream RED
- **Behavior dependency** (satisfied by GREEN commit) -- unblocks downstream GREEN

This means tasks in different "waves" can actually overlap their RED phases if the dependency is only behavioral. Task C (wave 2, depends on A) can start RED as soon as A's stub commit lands, without waiting for A's GREEN to complete. The wave model is too conservative for RED dispatch but correct for GREEN dispatch.

**Recommendation: Hybrid approach -- wave parallelism with fine-grained GREEN gating.**

Build BFS waves from the dependency DAG for initial dispatch ordering. Within each wave, use `parallel()` for all tasks. Between waves, the barrier from `parallel()` ensures all prior wave tasks' RED+stub is committed before downstream wave tasks begin RED. For GREEN gating, implement per-task dependency checking inside `runLane()` that polls/awaits upstream GREEN commits before starting its own GREEN.

Why NOT `pipeline()`: The `pipeline()` primitive has NO barrier between stages (item A can be in stage 3 while B is in stage 1). This means if we put tasks into a pipeline, a task with dependencies could start before its dependency finishes. There is no hook to add a gate between pipeline stages for a specific item. This makes `pipeline()` unsuitable for the dependency DAG.

**The `buildWaves()` function should:**

```js
function buildWaves(lanePlan) {
  const lanes = lanePlan.lanes;
  const inDeg = {};
  const adj = {};
  for (const [id, lane] of Object.entries(lanes)) {
    inDeg[id] = (lane.depends_on || []).length;
    for (const dep of (lane.depends_on || [])) {
      (adj[dep] = adj[dep] || []).push(id);
    }
  }
  const waves = [];
  let queue = Object.keys(inDeg).filter(id =&gt; inDeg[id] === 0);
  while (queue.length &gt; 0) {
    waves.push([...queue]);
    const next = [];
    for (const id of queue) {
      for (const child of (adj[id] || [])) {
        inDeg[child]--;
        if (inDeg[child] === 0) next.push(child);
      }
    }
    queue = next;
  }
  return waves;
}
```

### 2. RED-GREEN Worktree Continuity

**Confirmed model: Pre-created persistent lane worktrees via `setup_pipeline_worktrees()`.**

The `worktree_manager.py` API at line 123-152 creates one worktree per lane under `.datum/worktrees/&lt;run_id&gt;/&lt;lane_id&gt;/`. Each lane gets its own branch `&lt;epic_branch&gt;/&lt;lane_id&gt;` starting from the epic branch tip. The worktree persists for the entire RED-GREEN-REFACTOR cycle.

**Critical: Do NOT use `isolation: 'worktree'` on agent calls.** Per the system facts, `isolation: 'worktree'` creates a FRESH worktree per agent call. Tests written by RED would not be present for GREEN. Instead, each agent must work in the pre-created worktree.

**How to pass the worktree path to agents:**

The worktree path should be injected into the agent prompt as a working-directory instruction AND passed as the `cwd` option (if available in the Workflow script API). The prompt must contain:

```
WORKING DIRECTORY: /absolute/path/to/.datum/worktrees/&lt;run_id&gt;/&lt;task_id&gt;
All file operations MUST use this directory as the repository root.
Run all commands (git, pytest, etc.) from this directory.
```

If the Workflow script's `agent()` function supports a `cwd:` option, use it. If not, the prompt instruction is the enforcement mechanism -- the agent must `cd` there first.

**The Setup phase must:**
1. Call `datum worktrees setup --run-id &lt;RUN_ID&gt; --epic-branch &lt;BRANCH&gt; --lane-ids task-001,task-002,...`
2. Capture the returned JSON mapping of `{lane_id: worktree_path}`
3. Store this mapping for use by `runLane()`

### 3. Verify-Stage: Agent vs. Direct Bash

**The Workflow script is JS-only -- no direct subprocess calls.** Verification must go through an `agent()` call.

**However, verification is a mechanical, deterministic check** -- run `datum verify-stage red|green &lt;worktree_path&gt;` and check the exit code. Using a full Sonnet-tier LLM for this is wasteful.

**Recommendation: Use a `fast` (Haiku) model agent with a minimal prompt.**

```js
const verifyResult = await agent(
  `Run this exact command and report the result:
  cd ${worktreePath} &amp;&amp; datum verify-stage red --test-command "${testCmd}"
  If the command succeeds (exit 0), respond with: {"verified": true}
  If it fails, respond with: {"verified": false, "error": "&lt;stderr output&gt;"}
  Do NOT interpret the error. Just report it.`,
  { model: modelConfig.fast, schema: { verified: 'boolean', error: 'string?' } }
);
```

This costs ~500 tokens per verification instead of ~5000 for a full Sonnet call.

**New CLI command needed: `datum verify-stage`** -- a thin wrapper around `tdd_driver.verify_red_stage()` and `tdd_driver.verify_green_baseline()`:

```python
@app.command(name="verify-stage")
def verify_stage_cmd(
    stage: str = typer.Argument(..., help="Stage to verify: 'red' or 'green'"),
    repo_path: str = typer.Option(".", "--repo", help="Repository path"),
    test_command: str = typer.Option("pytest -q", "--test-command", help="Test command"),
):
    """Verify TDD stage gate: RED tests must fail, GREEN tests must pass."""
```

### 4. File Ownership Sandbox Enforcement

**Current state:** The `lane_tools_runner.py` enforces file sandbox via `allowed_write_dirs` from `manifest.toml`. But this only applies to lane-tools scripts, not to the agent's direct file edits.

**For the Workflow script, enforcement must happen at the prompt level plus post-commit validation.**

**Prompt-level enforcement:**
Each RED/GREEN/REFACTOR agent prompt must include an explicit file restriction section:

```
### File Permissions
You may ONLY write to the following files:
- src/foo.py
- src/bar.py
- tests/test_foo.py  (RED only — GREEN and REFACTOR may not write to test files)

Writing to any file not in this list is a HARD STOP violation.
The commit queue will reject commits that touch files outside this set.
```

**Post-commit validation:**
After each stage commit, the workflow should verify the committed diff only touches files in `lane.files[]`. This is mechanical and can be done by the verify agent:

```js
// After stage commit, verify file ownership
const diffFiles = await agent(
  `cd ${worktreePath} &amp;&amp; git diff --name-only HEAD~1 HEAD`,
  { model: modelConfig.fast }
);
```

Compare diffFiles against `lane.files` + allowed test paths. If violation detected, reject and retry.

**For RED specifically:** The file set is the test files derived from `lane.files` (the skeleton paths from preflight). For GREEN: only implementation files (excluding test directory). For REFACTOR: both.

### 5. RED/GREEN/REFACTOR Agent Prompts

There is NO existing `datum act-red` CLI command. The agents are prompted directly. The brief is constructed per `references/brief-builder.md`.

**RED Agent Prompt Template:**

```js
function RED_PROMPT(lane, worktreePath, preflight, upstreamStubs, specSections, propertiesFiltered, gitnexusContext) {
  return `
## Role: RED Agent — Write Failing Tests

## Working Directory
${worktreePath}
ALL file operations and commands MUST run from this directory.

## Task: ${lane.id} — ${lane.title}

### Acceptance Criteria (your tests must prove at least one of these)
${lane.acceptance_criteria.map((ac, i) =&gt; `${i+1}. ${ac}`).join('\n')}

### Property to Prove
${propertiesFiltered}

### RED Note
${lane.red_note}

### Files You May Write To
${(preflight?.outputs || []).map(o =&gt; `- ${o.path}`).join('\n') || lane.files.filter(f =&gt; f.includes('test') || f.includes('Test')).map(f =&gt; `- ${f}`).join('\n')}

### Skeleton (pre-created — fill in assertion bodies, do NOT rename functions or move files)
${preflight ? JSON.stringify(preflight.outputs, null, 2) : 'No skeleton available — create test structure from scratch.'}

${lane.introduces_stubs ? `### Stub Requirement
Emit TWO commits in order:
1. Stub commit — declare public types/methods with fatalError/raise NotImplementedError bodies
2. Test commit — the failing test written against the stub
Commit message format: stub(${lane.id}): &lt;description&gt;` : '### Emit one commit: the failing test.'}

### Code Context (GitNexus)
${gitnexusContext || 'Not available.'}

${upstreamStubs ? `### Upstream Stubs (types you may reference)\n${upstreamStubs}` : ''}

### Done Condition
Run: cd ${worktreePath} &amp;&amp; ${testCommand}
Tests MUST FAIL. The failure message must reference the property under test.
If tests pass, you have green blindness — rewrite the test with a genuinely failing assertion.
Commit your work: git add . &amp;&amp; git commit -m "red(${lane.id}): &lt;description&gt;"
`;
}
```

**GREEN Agent Prompt Template:**

```js
function GREEN_PROMPT(lane, worktreePath, testSignal, specSections, propertiesFiltered, gitnexusContext) {
  return `
## Role: GREEN Agent — Make Tests Pass with Minimum Code

## Working Directory
${worktreePath}
ALL file operations and commands MUST run from this directory.

## Task: ${lane.id} — ${lane.title}

### Acceptance Criteria
${lane.acceptance_criteria.map((ac, i) =&gt; `${i+1}. ${ac}`).join('\n')}

### Property Being Proved
${propertiesFiltered}

### Files You May Write To (implementation files ONLY — NO test files)
${lane.files.filter(f =&gt; !f.includes('test') &amp;&amp; !f.includes('Test') &amp;&amp; !f.includes('spec')).map(f =&gt; `- ${f}`).join('\n')}

### What the Test Expects (redacted signal — you do NOT have access to test source)
${JSON.stringify(testSignal, null, 2)}

For compile errors: implement the missing type/method.
For assertion failures: implement the behavior described by the assertion message.

### Code Context (GitNexus)
${gitnexusContext || 'Not available.'}

### Done Condition
Run: cd ${worktreePath} &amp;&amp; ${testCommand}
ALL tests MUST PASS. Do not add new tests. Do not edit test files.
Commit: git add . &amp;&amp; git commit -m "green(${lane.id}): &lt;description&gt;"
`;
}
```

**REFACTOR Agent Prompt Template:**

```js
function REFACTOR_PROMPT(lane, worktreePath, implementationContent, testContent, testResults, specFull, propertiesFiltered, gitnexusImpact) {
  return `
## Role: REFACTOR Agent — Full Correctness + Clean Architecture

## Working Directory
${worktreePath}
ALL file operations and commands MUST run from this directory.

## Task: ${lane.id} — ${lane.title}

### AC Checklist (verify each before marking done)
${lane.acceptance_criteria.map((ac, i) =&gt; `- [ ] ${ac}`).join('\n')}

### Properties (all must hold after your changes)
${propertiesFiltered}

### Implementation (current HEAD)
${implementationContent}

### Test Source (you may read but NEVER weaken, remove, or skip tests)
${testContent}

### Test Results
${testResults}

### Impact Analysis (GitNexus)
${gitnexusImpact || 'Not available.'}

### What You May NOT Do
- Remove, rename, or disable a test (test_ratchet.py blocks this)
- Delete or weaken an assertion
- Add new tests (if missing AC found: log to brief_defects, signal orchestrator, STOP)

### Done Condition
ALL tests pass. Hooks pass. Linter clean. Formatter clean. Every AC checked off.
Commit: git add . &amp;&amp; git commit -m "refactor(${lane.id}): &lt;description&gt;"

Write proof of work to: .datum/runs/${runId}/proof-of-work/${lane.id}.md
`;
}
```

### 6. Skeleton Preflight

**Yes, skeleton preflight should run before RED for each lane.** Per `references/04-act-skeleton-preflight.md` and `references/04-act.md` (line 16-20), the protocol is:

```
datum skeleton --task-id &lt;id&gt; --language &lt;detected_lang&gt; --tasks .datum/lane-plan.json --output .datum/runs/&lt;RUN_ID&gt;/preflight-&lt;id&gt;.json
```

**This should be integrated INTO `runLane()`, not as a separate phase.** Each lane runs skeleton preflight at the start of its RED sub-phase, not globally before all lanes. Reason: for lanes with `introduces_stubs = true`, the skeleton must be written AFTER the stub commit so it can import declared types (per the preflight doc).

The skeleton creator (`datum/skeleton_creator.py`) writes test file scaffolding and returns a `preflight-result.json`. This is a mechanical operation that should use the `fast` tier.

### 7. Parallel Within Wave vs. Finer-Grained Deps

**Question 8 analysis in detail:**

Consider tasks A (wave 1, no deps), B (wave 1, no deps), C (wave 2, depends on A).

With wave-based `parallel()`:
- Wave 1: `parallel([runLane(A), runLane(B)])` -- both run fully (RED-GREEN-REFACTOR) before wave 2 starts
- Wave 2: `parallel([runLane(C)])` -- C starts RED only after A AND B are completely done

This is OVERLY CONSERVATIVE. C's RED can start as soon as A's stub commit lands. C's GREEN can start as soon as A's GREEN commits. B's completion is irrelevant to C.

**However, the wave model is the simplest correct approach and matches `parallel()`'s barrier semantics.** The performance cost is that some lanes wait unnecessarily. For a typical datum epic with 5-8 tasks, the wave model adds minimal wall-clock overhead because:
1. Most tasks are independent (wave 1 is large)
2. Dependent tasks are usually in a chain (wave 2, 3 are small)

**Finer-grained tracking would require:**
- A custom event loop that monitors each lane's state
- Per-lane dependency semaphores
- Polling or callback mechanism for "upstream committed"

This complexity is not warranted for v1. The wave model is correct (never dispatches too early), maximally parallel within waves, and uses the standard `parallel()` primitive.

**Recommendation: Use wave-based parallel for v1. Note the optimization opportunity for v2.**

### 8. Model Selection

Per `references/model-tiers.md`:

| Agent | Tier | Workflow `model:` |
|---|---|---|
| Topology reader | fast | `"haiku"` |
| Setup (worktrees) | fast | `"haiku"` |
| Skeleton preflight | fast | `"haiku"` |
| RED | standard | `"sonnet"` |
| RED verify | fast | `"haiku"` |
| GREEN | standard | `"sonnet"` |
| GREEN verify | fast | `"haiku"` |
| REFACTOR | standard | `"sonnet"` |
| REFACTOR verify | fast | `"haiku"` |
| Merge | fast | `"haiku"` |
| Cleanup | fast | `"haiku"` |

The workflow should read model config from `args` (passed at invocation time) or default to these tiers.

### 9. Closing Issue #138

After all tests pass and the PR is merged, the workflow's final phase should close issue #138 via `gh issue close 138`. This can be done by the merge agent or as a separate cleanup step.

---

## Complete Workflow Script Skeleton

```js
// .claude/workflows/datum-tdd-act.js
//
// Deterministic TDD Act phase orchestration.
// Replaces manual RED→GREEN→REFACTOR dispatch with a mechanical
// Workflow script that enforces gates, file ownership, and dependency order.
//
// Invocation:
//   /workflow datum-tdd-act --run-id &lt;ID&gt; --epic-branch &lt;BRANCH&gt; --lane-plan-path .datum/lane-plan.json

export const meta = {
  name: 'datum-tdd-act',
  description: 'Deterministic TDD Act phase: RED→GREEN→REFACTOR per lane with dependency gating',
  version: '1.0.0',
};

// ── Schema Definitions ──────────────────────────────────────────────────

const LanePlanSchema = {
  schema_version: 'string',
  total_lanes: 'number',
  topological_order: ['string'],
  file_ownership: 'object',
  lanes: 'object',
};

const StageVerifySchema = {
  verified: 'boolean',
  error: 'string?',
  exit_code: 'number?',
};

const FailureRecord = {
  task_id: 'string',
  stage: 'string',   // RED | GREEN | REFACTOR
  attempt: 'number',
  error: 'string',
  timestamp: 'string',
};

// ── Configuration ──────────────────────────────────────────────────────

const MODEL = {
  fast:     'haiku',
  standard: 'sonnet',
  reasoning: 'opus',
};

// ── Helpers ──────────────────────────────────────────────────────────────

function buildWaves(lanePlan) {
  const lanes = lanePlan.lanes;
  const ids = Object.keys(lanes);
  const inDeg = {};
  const adj = {};

  for (const id of ids) {
    const deps = lanes[id].depends_on || [];
    inDeg[id] = deps.length;
    for (const dep of deps) {
      (adj[dep] = adj[dep] || []).push(id);
    }
  }

  const waves = [];
  let queue = ids.filter(id =&gt; inDeg[id] === 0);

  while (queue.length &gt; 0) {
    waves.push([...queue]);
    const next = [];
    for (const id of queue) {
      for (const child of (adj[id] || [])) {
        inDeg[child]--;
        if (inDeg[child] === 0) next.push(child);
      }
    }
    queue = next;
  }

  return waves;
}

// ── Per-Lane TDD Execution ──────────────────────────────────────────────

async function runLane(taskId, lanePlan, worktreePaths, runId, testCommand, language) {
  const lane = lanePlan.lanes[taskId];
  const worktreePath = worktreePaths[taskId];
  const isStructural = lane.stage === 'structural';

  log(`[${taskId}] Starting lane: ${lane.title} (${isStructural ? 'structural' : 'behavioral'})`);

  if (isStructural) {
    // Structural tasks bypass RED and GREEN — REFACTOR only
    const refactorResult = await runRefactor(taskId, lane, worktreePath, runId, testCommand);
    if (!refactorResult) {
      appendFailure(runId, taskId, 'REFACTOR', 'Refactor failed for structural task');
      return null;
    }
    log(`[${taskId}] Structural lane complete`);
    return { taskId, status: 'completed' };
  }

  // ── Skeleton Preflight ──────────────────────────────────────────────
  const preflight = await agent(
    `Run this command and return the full JSON output:
cd ${worktreePath} &amp;&amp; datum skeleton --task-id ${taskId} --language ${language} --tasks .datum/lane-plan.json --output .datum/runs/${runId}/preflight-${taskId}.json
Then read and return the contents of .datum/runs/${runId}/preflight-${taskId}.json`,
    { model: MODEL.fast }
  );

  // ── RED ──────────────────────────────────────────────────────────────
  log(`[${taskId}] RED: writing failing tests`);

  const redResult = await agent(
    buildRedPrompt(taskId, lane, worktreePath, preflight, runId, testCommand),
    { model: MODEL.standard }
  );

  // Verify RED: tests must FAIL
  const redVerify = await agent(
    `cd ${worktreePath} &amp;&amp; datum verify-stage red --test-command "${testCommand}"
Report the result as JSON: {"verified": true/false, "error": "...if failed"}`,
    { model: MODEL.fast, schema: StageVerifySchema }
  );

  if (!redVerify.verified) {
    log(`[${taskId}] RED verification FAILED: ${redVerify.error}`);
    appendFailure(runId, taskId, 'RED', redVerify.error || 'Green blindness — tests passed at RED');
    return null;
  }

  log(`[${taskId}] RED verified — tests correctly failing`);

  // ── GREEN ─────────────────────────────────────────────────────────────
  log(`[${taskId}] GREEN: making tests pass`);

  // Get test signal for GREEN brief (redacted — no test source)
  const testSignal = await agent(
    `cd ${worktreePath} &amp;&amp; datum test-signal --framework pytest --input &lt;(cd ${worktreePath} &amp;&amp; ${testCommand} 2&gt;&amp;1 || true)
Return the JSON output only.`,
    { model: MODEL.fast }
  );

  const greenResult = await agent(
    buildGreenPrompt(taskId, lane, worktreePath, testSignal, runId, testCommand),
    { model: MODEL.standard }
  );

  // Verify GREEN: tests must PASS
  const greenVerify = await agent(
    `cd ${worktreePath} &amp;&amp; datum verify-stage green --test-command "${testCommand}"
Report the result as JSON: {"verified": true/false, "error": "...if failed"}`,
    { model: MODEL.fast, schema: StageVerifySchema }
  );

  if (!greenVerify.verified) {
    log(`[${taskId}] GREEN verification FAILED: ${greenVerify.error}`);
    appendFailure(runId, taskId, 'GREEN', greenVerify.error || 'Tests still failing after GREEN');
    return null;
  }

  log(`[${taskId}] GREEN verified — tests passing`);

  // ── REFACTOR ──────────────────────────────────────────────────────────
  const refactorResult = await runRefactor(taskId, lane, worktreePath, runId, testCommand);
  if (!refactorResult) {
    appendFailure(runId, taskId, 'REFACTOR', 'Refactor failed');
    return null;
  }

  log(`[${taskId}] Lane complete`);
  return { taskId, status: 'completed' };
}

async function runRefactor(taskId, lane, worktreePath, runId, testCommand) {
  log(`[${taskId}] REFACTOR: cleanup + full AC coverage`);

  const refactorResult = await agent(
    buildRefactorPrompt(taskId, lane, worktreePath, runId, testCommand),
    { model: MODEL.standard }
  );

  // Verify REFACTOR: tests must still PASS
  const refactorVerify = await agent(
    `cd ${worktreePath} &amp;&amp; datum verify-stage green --test-command "${testCommand}"
Report the result as JSON: {"verified": true/false, "error": "...if failed"}`,
    { model: MODEL.fast, schema: StageVerifySchema }
  );

  if (!refactorVerify.verified) {
    log(`[${taskId}] REFACTOR verification FAILED: ${refactorVerify.error}`);
    return null;
  }

  return refactorVerify;
}

function appendFailure(runId, taskId, stage, error) {
  // The agent will append to the JSONL file
  const record = JSON.stringify({
    task_id: taskId,
    stage,
    error,
    timestamp: new Date().toISOString(),
  });
  log(`FAILURE: ${record}`);
  // Actual file append happens in the cleanup agent
}

// ── Main Workflow ────────────────────────────────────────────────────────

export default async function run(args) {
  const runId = args.runId || new Date().toISOString().replace(/[:.]/g, '-').slice(0, 19);
  const epicBranch = args.epicBranch;
  const lanePlanPath = args.lanePlanPath || '.datum/lane-plan.json';
  const testCommand = args.testCommand || 'pytest -q';
  const language = args.language || 'python';

  // ── Phase: Topology ──────────────────────────────────────────────────
  phase('Topology');

  const lanePlan = await agent(
    `Read the file at ${lanePlanPath} and return its contents as a JSON object.`,
    { model: MODEL.fast, schema: LanePlanSchema }
  );

  const waves = buildWaves(lanePlan);
  log(`Topology: ${lanePlan.total_lanes} lanes in ${waves.length} waves`);
  for (let i = 0; i &lt; waves.length; i++) {
    log(`  Wave ${i}: [${waves[i].join(', ')}]`);
  }

  // ── Phase: Setup ──────────────────────────────────────────────────────
  phase('Setup');

  const laneIds = Object.keys(lanePlan.lanes);
  const setupResult = await agent(
    `Run this command and return the output:
datum worktrees setup --run-id ${runId} --epic-branch ${epicBranch} --lane-ids ${laneIds.join(',')}
Return the JSON mapping of lane_id to worktree_path.`,
    { model: MODEL.fast }
  );

  // Parse worktree paths from setup output
  const worktreePaths = setupResult; // agent returns parsed JSON

  // ── Phase: Act ────────────────────────────────────────────────────────
  phase('Act');

  const results = {};
  const failures = [];

  for (let waveIdx = 0; waveIdx &lt; waves.length; waveIdx++) {
    const wave = waves[waveIdx];
    log(`=== Wave ${waveIdx} starting: [${wave.join(', ')}] ===`);

    const waveResults = await parallel(
      wave.map(taskId =&gt; async () =&gt; {
        try {
          return await runLane(taskId, lanePlan, worktreePaths, runId, testCommand, language);
        } catch (err) {
          log(`[${taskId}] FATAL ERROR: ${err.message}`);
          appendFailure(runId, taskId, 'UNKNOWN', err.message);
          return null;
        }
      })
    );

    for (let i = 0; i &lt; wave.length; i++) {
      results[wave[i]] = waveResults[i];
      if (!waveResults[i]) {
        failures.push(wave[i]);
      }
    }

    log(`=== Wave ${waveIdx} complete: ${wave.length - failures.filter(f =&gt; wave.includes(f)).length}/${wave.length} succeeded ===`);
  }

  // ── Phase: Merge ──────────────────────────────────────────────────────
  phase('Merge');

  const completedLanes = Object.entries(results)
    .filter(([_, r]) =&gt; r?.status === 'completed')
    .map(([id]) =&gt; id);

  if (completedLanes.length === 0) {
    log('No lanes completed — skipping merge');
  } else {
    // Merge in topological order (only completed lanes)
    const mergeOrder = lanePlan.topological_order
      .map(item =&gt; typeof item === 'string' ? item : item.root)
      .filter(id =&gt; completedLanes.includes(id));

    await agent(
      `Run this command:
datum worktrees merge --epic-branch ${epicBranch} --lane-order ${mergeOrder.join(',')} --commit-message "act: merge ${completedLanes.length} lanes for ${epicBranch}"`,
      { model: MODEL.fast }
    );

    log(`Merged ${completedLanes.length} lanes in order: [${mergeOrder.join(', ')}]`);
  }

  // ── Phase: Cleanup ────────────────────────────────────────────────────
  phase('Cleanup');

  await agent(
    `Run this command:
datum worktrees cleanup --run-id ${runId} --epic-branch ${epicBranch}`,
    { model: MODEL.fast }
  );

  // Write failure records
  if (failures.length &gt; 0) {
    await agent(
      `Write the following failure records to .datum/runs/${runId}/act-failures.jsonl:
${failures.map(id =&gt; JSON.stringify({ task_id: id, stage: results[id]?.stage || 'UNKNOWN', error: results[id]?.error || 'Lane failed' })).join('\n')}`,
      { model: MODEL.fast }
    );
  }

  // ── Summary ──────────────────────────────────────────────────────────
  log(`\nAct phase complete:`);
  log(`  Total lanes: ${lanePlan.total_lanes}`);
  log(`  Completed: ${completedLanes.length}`);
  log(`  Failed: ${failures.length}`);
  if (failures.length &gt; 0) {
    log(`  Failed lanes: [${failures.join(', ')}]`);
  }

  return {
    runId,
    total: lanePlan.total_lanes,
    completed: completedLanes.length,
    failed: failures.length,
    failedLanes: failures,
  };
}
```

### New CLI Commands

**File: `datum/cli.py`** -- Add a `worktrees` subcommand group and a `verify-stage` command.

```python
# ── Worktree management ──────────────────────────────────────────────────

worktrees_app = typer.Typer(name="worktrees", help="Pipeline worktree lifecycle management.")
app.add_typer(worktrees_app)


@worktrees_app.command("setup")
def worktrees_setup(
    run_id: str = typer.Option(..., "--run-id", help="Unique pipeline run identifier"),
    epic_branch: str = typer.Option(..., "--epic-branch", help="Epic branch name"),
    lane_ids: str = typer.Option(..., "--lane-ids", help="Comma-separated lane IDs"),
):
    """Create one worktree per lane for parallel ACT execution.

    Creates .datum/worktrees/&lt;run_id&gt;/&lt;lane_id&gt;/ for each lane,
    each on its own sub-branch starting from the epic branch tip.
    Prints JSON mapping: {lane_id: worktree_path}.
    """
    from datum.worktree_manager import setup_pipeline_worktrees

    ids = [lid.strip() for lid in lane_ids.split(",")]
    mapping = setup_pipeline_worktrees(run_id, epic_branch, ids)
    # Convert Path values to strings for JSON serialization
    result = {k: str(v) for k, v in mapping.items()}
    console.print(json.dumps(result, indent=2))


@worktrees_app.command("merge")
def worktrees_merge(
    epic_branch: str = typer.Option(..., "--epic-branch", help="Epic branch name"),
    lane_order: str = typer.Option(..., "--lane-order", help="Comma-separated lane IDs in merge order"),
    commit_message: str = typer.Option(..., "--commit-message", help="Merge commit message"),
):
    """Squash-merge completed lane branches into the epic branch.

    Merges in the specified order (dependency order: depended-on first).
    All changes land in one commit on the epic branch.
    """
    from datum.worktree_manager import merge_lane_branches

    order = [lid.strip() for lid in lane_order.split(",")]
    sha = merge_lane_branches(epic_branch, order, commit_message)
    console.print(json.dumps({"sha": sha, "merged": order}))


@worktrees_app.command("cleanup")
def worktrees_cleanup(
    run_id: str = typer.Option(..., "--run-id", help="Run ID to clean up"),
    epic_branch: str = typer.Option(..., "--epic-branch", help="Epic branch name"),
):
    """Remove all lane worktrees for a given run.

    Discovers lanes by listing .datum/worktrees/&lt;run_id&gt;/ and
    removes each worktree and its sub-branch.
    """
    from datum.worktree_manager import cleanup_run_worktrees

    cleaned = cleanup_run_worktrees(run_id, epic_branch)
    console.print(json.dumps({"cleaned": cleaned}))


# ── TDD stage verification ────────────────────────────────────────────────

@app.command(name="verify-stage")
def verify_stage_cmd(
    stage: str = typer.Argument(..., help="Stage to verify: 'red', 'green', or 'baseline'"),
    repo_path: str = typer.Option(".", "--repo", help="Repository root path"),
    test_command: str = typer.Option("pytest -q", "--test-command", help="Test runner command"),
):
    """Verify TDD stage gate.

    red:      Tests must FAIL (green blindness check). Exit 0 if failing, exit 1 if passing.
    green:    Tests must PASS. Exit 0 if passing, exit 1 if failing.
    baseline: Tests must PASS before starting a new RED cycle. Exit 0 if passing, exit 1 if failing.
    """
    import shlex
    from datum.tdd_driver import (
        DirtyBaselineError,
        GreenBlindnessError,
        verify_green_baseline,
        verify_red_stage,
    )

    cmd = shlex.split(test_command)
    path = Path(repo_path).resolve()

    try:
        if stage == "red":
            verify_red_stage(path, test_command=cmd)
            console.print("[green]RED verified: tests are correctly failing.[/green]")
        elif stage in ("green", "baseline"):
            verify_green_baseline(path, test_command=cmd)
            console.print("[green]GREEN verified: tests are passing.[/green]")
        else:
            console.print(f"[red]Unknown stage: {stage}. Use 'red', 'green', or 'baseline'.[/red]")
            raise typer.Exit(1)
    except GreenBlindnessError as e:
        console.print(f"[red]RED verification FAILED: {e}[/red]")
        raise typer.Exit(1)
    except DirtyBaselineError as e:
        console.print(f"[red]Baseline verification FAILED: {e}[/red]")
        raise typer.Exit(1)
```

### Test List

**File: `tests/test_tdd_cli_commands.py`**

| Test | What it verifies |
|---|---|
| `test_worktrees_setup_creates_worktrees` | `datum worktrees setup` creates one worktree per lane under `.datum/worktrees/&lt;run_id&gt;/`, returns JSON mapping with valid paths. Uses a real git repo in `tmp_path`. |
| `test_worktrees_setup_fails_without_epic_branch` | `datum worktrees setup` exits non-zero when the epic branch does not exist. Verifies RuntimeError is surfaced. |
| `test_worktrees_setup_idempotent_error` | Calling setup twice with the same run_id raises a clear error (git worktree add fails for existing worktree). |
| `test_worktrees_merge_squashes_in_order` | `datum worktrees merge` applies squash-merges in the specified order. Creates 2 lane branches with different file changes, merges, verifies the epic branch has both changes in one commit. |
| `test_worktrees_merge_skips_nonexistent_lane` | `datum worktrees merge` raises RuntimeError when a lane branch does not exist (e.g., a failed lane that never committed). |
| `test_worktrees_cleanup_removes_all` | `datum worktrees cleanup` removes all worktrees and sub-branches for a run. After cleanup, `git worktree list` does not include the lane paths. |
| `test_verify_stage_red_passes_when_tests_fail` | `datum verify-stage red` exits 0 when the test suite fails. Uses a fixture repo with a deliberately failing test. |
| `test_verify_stage_red_fails_when_tests_pass` | `datum verify-stage red` exits 1 (GreenBlindnessError) when all tests pass. Detects green blindness. |
| `test_verify_stage_green_passes_when_tests_pass` | `datum verify-stage green` exits 0 when all tests pass. |
| `test_verify_stage_green_fails_when_tests_fail` | `datum verify-stage green` exits 1 (DirtyBaselineError) when tests fail. |
| `test_verify_stage_custom_test_command` | `datum verify-stage` respects the `--test-command` flag. Uses a custom command like `python -m pytest tests/specific_test.py`. |
| `test_verify_stage_unknown_stage_exits_1` | `datum verify-stage foobar` exits 1 with a clear error message. |

**File: `tests/test_workflow_wave_builder.py`** (unit tests for wave-building logic, if extracted to Python)

| Test | What it verifies |
|---|---|
| `test_buildwaves_independent_tasks` | All tasks with no deps go into wave 0. |
| `test_buildwaves_linear_chain` | A-&gt;B-&gt;C produces 3 waves of 1 task each. |
| `test_buildwaves_diamond` | A, B (both no deps), C (depends on A and B) produces wave 0=[A,B], wave 1=[C]. |
| `test_buildwaves_complex_dag` | Tests the real lane-plan.json structure: tasks 1,3,4,5,6 (no deps) in wave 0, task 2 (depends on 1) in wave 1, task 7 (depends on 1,3,4,6) in wave 1, task 8 (depends on 7) in wave 2. |

### Gaps and Concerns for the Implementer

1. **FleetView API uncertainty.** The `agent()`, `parallel()`, `phase()`, `log()` primitives are described in the system facts but there is no reference implementation or API documentation in the repo. The implementer should verify:
   - Whether `agent()` supports `cwd:` as an option (critical for worktree isolation)
   - Whether `agent()` supports `schema:` for structured output
   - Whether `parallel()` returns results in order or as a map
   - Whether there is a way to pass environment variables to agents

2. **Test signal generation for GREEN.** The GREEN agent needs a redacted test signal. The workflow uses `datum test-signal` but the exact invocation pattern (piping test output) needs to match the existing test_signal.py expectations. The test_signal module reads from a file or stdin -- verify the correct invocation.

3. **Commit queue integration.** The existing `pipeline-dispatch.md` describes a commit queue (`datum commit-queue`) for serializing commits. The worktree model (each lane has its own branch) eliminates the commit queue's primary purpose (preventing concurrent writes to the same branch). However, the merge phase still needs serialization. The implementer should decide: keep the commit queue for merge, or rely on `merge_lane_branches()` which already serializes merges.

4. **Retry logic is NOT in v1 scope.** The draft plan does not include the retry ladder (standard -&gt; reasoning -&gt; reasoning+verbose) from `references/model-tiers.md`. Each stage gets one attempt. Retry should be a v2 enhancement. The failure is recorded and the lane is skipped.

5. **Adversarial agent is NOT in v1 scope.** Per `references/04-act.md`, an ADVERSARIAL agent runs after REFACTOR. This is not in the draft plan and should be deferred to v2.

6. **Security and docs sidecars are NOT in v1 scope.** The Act phase doc mentions `security-sidecar` and `docs-scaffolder` running alongside the main loop. These are deferred.

7. **`topological_order` items are `TopologicalOrderItem` (RootModel), not plain strings.** When reading from `lane-plan.json`, the items are strings. But when using the Pydantic schema, they are wrapped in `TopologicalOrderItem.root`. The workflow reads raw JSON, so this is fine -- but the merge-order filtering line must handle this:
   ```js
   const mergeOrder = lanePlan.topological_order
     .map(item =&gt; typeof item === 'string' ? item : item.root)
   ```

8. **File ownership enforcement is prompt-only in v1.** There is no mechanical post-commit check in the workflow. The test_ratchet pre-commit hook catches some violations, but general file-ownership enforcement relies on the agent following instructions. The implementer should add a post-commit `git diff --name-only` check and fail the lane if non-owned files are touched.

9. **install.sh symlink.** The workflow file lives at `skills/datum-tdd-act.js` in the repo. For Claude Code to discover it, it needs to be symlinked to `~/.claude/workflows/datum-tdd-act.js`. The `install.sh` script's `register_tool` function creates symlinks to `~/.claude/skills/datum`, but workflows live in a different directory. A new registration step is needed in `install.sh`.

10. **No `datum act-red` / `datum act-green` CLI commands exist.** The agents are prompted directly with full briefs. There is no thin CLI wrapper for running a specific TDD stage. The workflow constructs the full brief in JS and passes it as the agent prompt. This is the correct approach -- adding CLI wrappers would create unnecessary indirection.

### Critical Files for Implementation

- `/Volumes/Extra/repos/gitrdunhq/datum/datum/cli.py`
- `/Volumes/Extra/repos/gitrdunhq/datum/datum/worktree_manager.py`
- `/Volumes/Extra/repos/gitrdunhq/datum/datum/tdd_driver.py`
- `/Volumes/Extra/repos/gitrdunhq/datum/datum/pipeline_scheduler.py`
- `/Volumes/Extra/repos/gitrdunhq/datum/references/pipeline-dispatch.md`</result>
<usage><subagent_tokens>114855</subagent_tokens><tool_uses>75</tool_uses><duration_ms>571831</duration_ms></usage>
</task-notification>

---

ok dogfood pick a feature off the board and use it

---

yes did we run the workflow as a claude workflow?

---

Push

---

so with workflows we can really enforce those JSON contracts we made right?

---

yeah lets wait and see what other friction points came up and then iterate an iterate

---

<task-notification>
<task-id>wrq2r5p6f</task-id>
<tool-use-id>toolu_01TeNHg5jgmdhgDpGd2pvc58</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/wrq2r5p6f.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Deterministic TDD Act: RED-&gt;GREEN-&gt;REFACTOR per lane with gate enforcement" completed</summary>
<result>{"total":2,"completed":2,"failed":0,"failedLanes":[],"completedLanes":["task-001","task-002"]}</result>
<usage><agent_count>20</agent_count><subagent_tokens>468326</subagent_tokens><tool_uses>126</tool_uses><duration_ms>902793</duration_ms></usage>
</task-notification>

---

#3 first and then #4

---

try it out

---

hey the workflow agents, do we control their entire context window?

---

how mich is the system prompt?

---

what are took schemas loading

---

<task-notification>
<task-id>w6u7s45m5</task-id>
<tool-use-id>toolu_01JvBwBEtRvdRzJkB3Pop2A3</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/w6u7s45m5.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Deterministic TDD Act: RED-&gt;GREEN-&gt;REFACTOR per lane with gate enforcement" completed</summary>
<result>{"total":8,"completed":2,"failed":6,"failedLanes":["task-001","task-004","task-005","task-006","task-002","task-008"],"completedLanes":["task-003","task-007"]}</result>
<usage><agent_count>51</agent_count><subagent_tokens>1911961</subagent_tokens><tool_uses>768</tool_uses><duration_ms>2757718</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>w72fznnf9</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/w72fznnf9.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Deterministic TDD Act: RED-&gt;GREEN-&gt;REFACTOR per lane with gate enforcement" completed</summary>
<result>{"total":2,"completed":0,"failed":2,"failedLanes":["task-001","task-002"],"completedLanes":[],"error":"Epic branch 'undefined' not found. Create it before setting up pipeline worktrees."}</result>
<usage><agent_count>2</agent_count><subagent_tokens>41030</subagent_tokens><tool_uses>4</tool_uses><duration_ms>14688</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>wgbbemmx0</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/wgbbemmx0.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Deterministic TDD Act: RED-&gt;GREEN-&gt;REFACTOR per lane with gate enforcement" completed</summary>
<result>{"total":2,"completed":0,"failed":2,"failedLanes":["task-001","task-002"],"completedLanes":[],"error":"Epic branch 'undefined' not found. Create it before setting up pipeline worktrees."}</result>
<usage><agent_count>2</agent_count><subagent_tokens>41036</subagent_tokens><tool_uses>4</tool_uses><duration_ms>13663</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>wrprnb0yg</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/wrprnb0yg.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Deterministic TDD Act: RED-&gt;GREEN-&gt;REFACTOR per lane with gate enforcement" completed</summary>
<result>{"runId":"run-default","total":2,"completed":0,"failed":2,"failedLanes":["task-001","task-002"],"completedLanes":[],"error":"git worktree add failed: cannot lock ref 'refs/heads/main/task-001': 'refs/heads/main' exists; cannot create 'refs/heads/main/task-001'. The worktree manager attempts to create sub-branches with pattern {epic_branch}/{lane_id}, but when epic_branch='main', Git cannot create branches under a leaf branch. Either: (1) use a different epic_branch that is namespace-ready, (2) modify the branch naming scheme to use separators other than '/' (e.g., 'main-task-001'), or (3) ensure the epic_branch is set up as a branch namespace before calling setup."}</result>
<usage><agent_count>2</agent_count><subagent_tokens>45706</subagent_tokens><tool_uses>6</tool_uses><duration_ms>27672</duration_ms></usage>
</task-notification>

---

does this help in anyway, send n agent to incorporate changes > ## Documentation Index
> Fetch the complete documentation index at: https://code.claude.com/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# Orchestrate subagents at scale with dynamic workflows

> Dynamic workflows orchestrate many subagents from a script Claude writes and you can rerun. Use them for codebase audits, large migrations, and cross-checked research.

{/* plan-availability: feature=workflows plans=pro,max,team,enterprise providers=all */}

<Note>
  Dynamic workflows require Claude Code v2.1.154 or later and are available on all paid plans, with Anthropic API access, and on Amazon Bedrock, Google Cloud Vertex AI, and Microsoft Foundry. On Pro, turn them on from the Dynamic workflows row in `/config`.
</Note>

A dynamic workflow is a JavaScript script that orchestrates [subagents](/en/sub-agents) at scale. Claude writes the script for the task you describe, and a runtime executes it in the background while your session stays responsive.

Reach for a workflow when a task needs more agents than one conversation can coordinate, or when you want the orchestration codified as a script you can read and rerun. Examples include a codebase-wide bug sweep, a 500-file migration, a research question that needs sources cross-checked against each other, and a hard plan worth drafting from several independent angles before you commit to one.

This page covers how to:

* Decide [when to use a workflow](#when-to-use-a-workflow) instead of subagents or skills
* [Run a bundled workflow](#run-a-bundled-workflow) with `/deep-research`
* [Have Claude write a workflow](#have-claude-write-a-workflow) for your task and save it
* Understand [how a workflow runs](#how-a-workflow-runs) and [manage runs](#manage-runs)

## When to use a workflow

[Subagents](/en/sub-agents), [skills](/en/skills), [agent teams](/en/agent-teams), and workflows can all run a multi-step task. The difference is who holds the plan:

|                                 | Subagents                      | Skills                       | Agent teams                            | Workflows                            |
| :------------------------------ | :----------------------------- | :--------------------------- | :------------------------------------- | :----------------------------------- |
| What it is                      | A worker Claude spawns         | Instructions Claude follows  | A lead agent supervising peer sessions | A script the runtime executes        |
| Who decides what runs next      | Claude, turn by turn           | Claude, following the prompt | The lead agent, turn by turn           | The script                           |
| Where intermediate results live | Claude's context window        | Claude's context window      | A shared task list                     | Script variables                     |
| What's repeatable               | The worker definition          | The instructions             | The team definition                    | The orchestration itself             |
| Scale                           | A few delegated tasks per turn | Same as subagents            | A handful of long-running peers        | Dozens to hundreds of agents per run |
| Interruption                    | Restarts the turn              | Restarts the turn            | Teammates keep running                 | Resumable in the same session        |

A workflow moves the plan into code. With subagents, skills, and agent teams, Claude is the orchestrator: it decides turn by turn what to spawn or assign next, and every result lands in a context window. A workflow script holds the loop, the branching, and the intermediate results itself, so Claude's context holds only the final answer.

Moving the plan into code also lets a workflow apply a repeatable quality pattern, not just run more agents: it can have independent agents adversarially review each other's findings before they're reported, or draft a plan from several angles and weigh them against each other, so you get a more trustworthy result than a single pass.

## Run a bundled workflow

The quickest way to see a workflow in action is to run `/deep-research`, the [built-in workflow](#bundled-workflows) Claude Code includes for investigating a question across many sources. You'll see agents work through a set of phases in the background while your session stays free, and get one report at the end instead of a turn-by-turn transcript.

<Steps>
  <Step title="Run the workflow">
    Run `/deep-research` with a question you want investigated. It fans out web searches across several angles, fetches and cross-checks the sources it finds, and synthesizes a cited report.

    ```text theme={null}
    /deep-research What changed in the Node.js permission model between v20 and v22?
    ```
  </Step>

  <Step title="Allow workflows">
    Claude Code asks whether to allow the workflow. Select **Yes** to continue. The exact prompt depends on your permission mode. See [Approve the plan before it runs](#approve-the-plan-before-it-runs) for the per-mode options.
  </Step>

  <Step title="Watch progress">
    The run starts in the background. Run `/workflows`, use the arrow keys to select the run, and press Enter to open its progress view:

    ```text theme={null}
    /workflows
    ```

    The view shows each phase with its agent count, token total, and elapsed time. Drill into any phase to see its agents and what each one found. See [Watch the run](#watch-the-run) for the full set of controls.

    You can also watch from the task panel below the input box: a one-line progress summary appears there while the run is going. Press the down arrow to focus it, then Enter to expand.
  </Step>

  <Step title="Read the report">
    When the run finishes, the report lands in your session. It cites the sources each claim came from, with claims that didn't survive cross-checking already filtered out.
  </Step>
</Steps>

To run a workflow for your own task, [have Claude write one](#have-claude-write-a-workflow), and once a run does what you wanted you can [save it](#save-the-workflow-for-reuse) as a command of your own.

### Bundled workflows

Claude Code includes `/deep-research` as a built-in workflow:

| Command                     | What it does                                                                                                                                                                                                                                                                                                      |
| :-------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/deep-research <question>` | Fans out web searches on a question across several angles, fetches and cross-checks the sources it finds, votes on each claim, and returns a cited report with claims that didn't survive cross-checking filtered out. Requires the [WebSearch tool](/en/tools-reference#websearch-tool-behavior) to be available |

[Workflows you save](#save-the-workflow-for-reuse) yourself become commands the same way and appear in `/` autocomplete alongside the bundled ones.

### Watch the run

Workflows run in the background, so the session stays responsive while agents work. Run `/workflows` at any time to list running and completed workflows, then select one to open its progress view.

```text theme={null}
/workflows
```

The progress view shows each phase with its agent counts, token totals, and elapsed time. The footer lists the key for each action:

| Key            | Action                                                                                              |
| :------------- | :-------------------------------------------------------------------------------------------------- |
| `↑` / `↓`      | Select a phase or agent                                                                             |
| `Enter` or `→` | Drill into the selected phase, then into an agent to read its prompt, recent tool calls, and result |
| `Esc`          | Back out one level                                                                                  |
| `j` / `k`      | Scroll within the agent detail when it overflows                                                    |
| `p`            | Pause or resume the run                                                                             |
| `x`            | Stop the selected agent, or stop the whole workflow when focus is on the run                        |
| `r`            | Restart the selected running agent                                                                  |
| `s`            | [Save](#save-the-workflow-for-reuse) the run's script as a command                                  |

## Have Claude write a workflow

You can have Claude write a workflow for your task in two ways:

* [Ask for a workflow](#ask-for-a-workflow-in-your-prompt) in your prompt, either in your own words or by including the keyword `ultracode`, and Claude writes one for the task.
* [Let Claude decide with ultracode](#let-claude-decide-with-ultracode): set `/effort ultracode` and Claude plans a workflow for every substantive task in the session.

You can also run a workflow command that already exists: a [bundled workflow](#bundled-workflows) like `/deep-research`, or one you've [saved](#save-the-workflow-for-reuse).

### Ask for a workflow in your prompt

To run a single task as a workflow without changing the session's effort level, include the keyword `ultracode` in your prompt. Asking in your own words, for example "use a workflow" or "run a workflow", also works: Claude treats a direct request as the same opt-in. Before v2.1.160 the literal trigger keyword was `workflow`; natural-language requests work in both versions.

```text theme={null}
ultracode: audit every API endpoint under src/routes/ for missing auth checks
```

Claude Code highlights the keyword in your input and Claude writes a workflow script for the task instead of working through it turn by turn. If you didn't mean to start a workflow, press `Option+W` on macOS or `Alt+W` on Windows and Linux to dismiss the highlight for this prompt, or press backspace while the cursor is right after the highlighted keyword. To stop the keyword from triggering at all, turn off Ultracode keyword trigger in `/config`.

If the run does what you wanted, you can [save it as a command](#save-the-workflow-for-reuse) afterward.

If you already have an orchestrator built another way, such as a folder of subagent prompts or a skill that fans work out, you can point Claude at it and ask for a workflow that does the same thing.

### Let Claude decide with ultracode

Ultracode is a Claude Code setting that combines `xhigh` [reasoning effort](/en/model-config#adjust-effort-level) with automatic workflow orchestration. With it on, Claude plans a workflow for each substantive task instead of waiting for you to ask.

```text theme={null}
/effort ultracode
```

With ultracode on, Claude decides when a task warrants a workflow. A single request can turn into several workflows in a row: one to understand the code, one to make the change, and one to verify it. This applies to every task in the session, so each request uses more tokens and takes longer than at lower effort levels.

Ultracode lasts for the current session and resets when you start a new one. Drop back with `/effort high` when you return to routine work. It's available on models that support `xhigh` [effort](/en/model-config#adjust-effort-level); on other models the `/effort` menu doesn't offer it.

### Approve the plan before it runs

In the CLI, the per-run prompt shows the planned phases and these options:

* **Yes, run it**: start the run
* **Yes, and don't ask again for `<name>` in `<path>`**: start, and skip this prompt for this workflow in this project from now on
* **View raw script**: read the script before deciding
* **No**: cancel

`Ctrl+G` opens the script in your editor. `Tab` lets you adjust the prompt before the run starts.

Whether you see this prompt depends on your [permission mode](/en/permission-modes):

| Permission mode                            | When you're prompted                                                                                                                                    |
| :----------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Default, accept edits                      | Every run, unless you've selected **Yes, and don't ask again** for that workflow in this project                                                        |
| Auto                                       | First launch only. Any **Yes** records consent in your user settings, and later launches start without prompting. Skipped entirely when ultracode is on |
| Bypass permissions, `claude -p`, Agent SDK | Never. The run starts immediately                                                                                                                       |

In the Desktop app, an approval card shows the workflow name, the phase list, and a token-usage caution, with **Once**, **Always**, and **Deny** actions. The progress view appears in the Background tasks side pane.

Your permission mode controls only the launch prompt above. The subagents the workflow spawns always run in `acceptEdits` mode and inherit your [tool allowlist](/en/settings#permission-settings), regardless of your session's mode. File edits are auto-approved.

Shell commands, web fetches, and MCP tools that aren't in your allowlist can still prompt you mid-run. To avoid this on a long run, add the commands the agents need to your allowlist before starting.

In `claude -p` and the Agent SDK there is no one to prompt, so tool calls follow your configured permission rules without interactive confirmation.

### Save the workflow for reuse

When Claude writes a workflow for a task you'll repeat, you can save that run's script as a command. A process like a review you run on every branch then runs the same orchestration each time.

Run `/workflows`, select the run you want to keep, and press `s`. In the save dialog, Tab toggles between the two save locations:

* `.claude/workflows/` in your project: shared with everyone who clones the repo
* `~/.claude/workflows/` in your home directory: available in every project, visible only to you

Press Enter to save. The workflow runs as `/<name>` in future sessions from either location.

If a project workflow and a personal workflow share a name, the project one runs.

### Pass input to a saved workflow

A saved workflow can accept input through the `args` parameter. The script reads it as a global named `args`. Use this to supply a research question, a list of target paths, or a configuration object at invocation time instead of editing the script for each run.

The following prompt runs a saved workflow with a list of issue numbers:

```text theme={null}
> Run /triage-issues on issues 1024, 1025, and 1030
```

Claude passes the list as structured data, so the script can call array and object methods on `args` directly without parsing it first. If `args` is omitted, the global is `undefined` inside the script.

## How a workflow runs

The workflow runtime executes the script in an isolated environment, separate from your conversation. Intermediate results stay in script variables instead of landing in Claude's context.

Every run writes its script to a file under your session's directory in `~/.claude/projects/`. Claude receives the path when the run starts, so you can ask for it. You can open that file to read the orchestration Claude wrote, diff it against a previous run's script, or edit it and ask Claude to relaunch from the edited version.

The runtime tracks each agent's result as the run progresses, which is what makes a run [resumable](#resume-after-a-pause) within the same session.

### Behavior and limits

The runtime applies the following constraints:

| Constraint                                                           | Why                                                                                                            |
| :------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------- |
| No mid-run user input                                                | Only agent permission prompts can pause a run. For sign-off between stages, run each stage as its own workflow |
| No direct filesystem or shell access from the workflow itself        | Agents read, write, and run commands. The script coordinates the agents                                        |
| Up to 16 concurrent agents, fewer on machines with limited CPU cores | Bounds local resource use                                                                                      |
| 1,000 agents total per run                                           | Prevents runaway loops                                                                                         |

## Manage runs

Once a run starts, you manage it from the `/workflows` view, or by expanding its progress line in the task panel below the input box.

### Resume after a pause

If you stop a run, you can resume it: agents that already completed return their cached results, and the rest run live. Resume a paused run from `/workflows` by selecting it and pressing `p`, or ask Claude to relaunch the workflow with the same script.

Resume works within the same Claude Code session. If you exit Claude Code while a workflow is running, the next session starts the workflow fresh.

### Cost

A workflow spawns many agents, so a single run can use meaningfully more tokens than working through the same task in conversation. Runs count toward your plan's usage and rate limits like any other session.

To gauge the spend before committing to a large task, run the workflow on a small slice first: one directory instead of the whole repo, or a narrow question instead of a broad one. The `/workflows` view shows each agent's token usage as the run progresses, and you can stop the run there at any time without losing completed work. The runtime's [agent caps](#behavior-and-limits) limit how many agents a single run can spawn, which bounds the cost of a runaway script.

Every agent in a workflow uses your session's model unless the script routes a stage to a different one. To control the model cost:

* Check `/model` before a large run if you usually switch to a smaller model for routine work
* Ask Claude to use a smaller model for stages that don't need the strongest one when you describe the task

### Turn workflows off

Workflows are available in the CLI, the Desktop app, the IDE extensions, [non-interactive mode](/en/headless) with `claude -p`, and the [Agent SDK](/en/agent-sdk/overview). The same disable settings apply on every surface.

To turn workflows off for yourself:

* Toggle Dynamic workflows off in `/config`. Persists across sessions.
* Set `"disableWorkflows": true` in `~/.claude/settings.json`. Persists across sessions.
* Set `CLAUDE_CODE_DISABLE_WORKFLOWS=1`. Read at startup, so it applies wherever you set it.

To turn workflows off for your whole organization, set `"disableWorkflows": true` in [managed settings](/en/server-managed-settings), or use the toggle on the [Claude Code admin settings](https://claude.ai/admin-settings/claude-code) page.

When workflows are disabled, the bundled workflow commands are unavailable, the `ultracode` keyword no longer triggers a run, and `ultracode` is removed from the `/effort` menu.

## Related resources

* [Run agents in parallel](/en/agents): compare subagents, agent view, agent teams, and workflows
* [Create custom subagents](/en/sub-agents): the worker primitive workflows orchestrate
* [Manage costs](/en/costs): how multi-agent runs count toward usage limits

---

<task-notification>
<task-id>a117cc36e4556198f</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/a117cc36e4556198f.output</output-file>
<status>completed</status>
<summary>Agent "Opus: plan tool-scoped agents for workflow" completed</summary>
<result>Good. Only one custom agent currently exists (`entire-search`). Now let me produce the comprehensive plan.

---

## Complete Analysis and Implementation Plan: Token-Efficient Agent Typing for datum-tdd-act

### Background and Problem

The workflow at `/Volumes/Extra/repos/gitrdunhq/datum/skills/datum-tdd-act.js` contains 12 `agent()` calls per lane (some running inside `runLane` which is called per task). Each agent call without a custom `agentType` spawns a default workflow subagent that inherits the FULL tool environment from the parent session. This includes:

- All 82+ internal tool schemas
- MCP server tool schemas for Gmail, Calendar, Drive, Notion, serena, headroom, context7
- CLAUDE.md / AGENTS.md rules
- Git status snapshot

From the documentation, a critical distinction exists:
- **Default/unnamed workflow agents**: inherit all tools from the parent session (the full ~76K token system prompt)
- **Custom-typed agents** (via `agentType:` referencing a `.claude/agents/*.md` file): receive "only this system prompt (plus basic environment details), not the full Claude Code system prompt" and only the tools listed in their `tools:` frontmatter

This means custom agent types are not just about filtering tools from the same prompt -- they replace the entire system prompt with a minimal one plus the agent's markdown body. This is where the real token savings come from.

### Answer 1: Agent Type Mapping for All 12 Roles

Based on the roles' actual needs:

| # | Role | Current Tools Needed | Recommended agentType | Tools | Model |
|---|------|---------------------|----------------------|-------|-------|
| 1 | Topology reader | Read (1 JSON file) | `datum-reader` (new) | `Read` | haiku |
| 2 | Setup | Bash (1 CLI command) | `datum-cli` (new) | `Bash` | haiku |
| 3 | Skeleton preflight | Bash (1 CLI command) | `datum-cli` (reuse) | `Bash` | haiku |
| 4 | RED agent | Read, Write, Edit, Bash | (default -- needs full toolbelt) | all | sonnet |
| 5 | Verify-red | Bash (1 CLI command) | `datum-cli` (reuse) | `Bash` | haiku |
| 6 | Test signal | Bash (1 test command) | `datum-cli` (reuse) | `Bash` | haiku |
| 7 | GREEN agent | Read, Write, Edit, Bash | (default -- needs full toolbelt) | all | sonnet |
| 8 | Verify-green | Bash (1 CLI command) | `datum-cli` (reuse) | `Bash` | haiku |
| 9 | REFACTOR agent | Read, Write, Edit, Bash | (default -- needs full toolbelt) | all | sonnet |
| 10 | Verify-refactor | Bash (1 CLI command) | `datum-cli` (reuse) | `Bash` | haiku |
| 11 | Merge | Bash (1 CLI command) | `datum-cli` (reuse) | `Bash` | haiku |
| 12 | Cleanup | Bash (1 CLI command) | `datum-cli` (reuse) | `Bash` | haiku |

### Answer 2: Custom Agent Types to Create

Agent type definitions live in `.claude/agents/*.md`. The project already has one: `.claude/agents/entire-search.md` which demonstrates the format (`tools: Bash`).

Two new custom agent types are needed:

**1. `datum-reader`** -- Read-only JSON reader. Needs only the Read tool.

Location: `/Volumes/Extra/repos/gitrdunhq/datum/.claude/agents/datum-reader.md`

Frontmatter:
```yaml
name: datum-reader
description: Read JSON files and return structured data. Used by datum workflows for file parsing.
tools: Read
model: haiku
maxTurns: 3
```

Body: A minimal prompt like "You read files and return their contents as structured JSON. Do not modify files. Do not run commands."

**2. `datum-cli`** -- Bash-only CLI executor. Runs one `datum` CLI command and returns JSON.

Location: `/Volumes/Extra/repos/gitrdunhq/datum/.claude/agents/datum-cli.md`

Frontmatter:
```yaml
name: datum-cli
description: Execute datum CLI commands and return structured JSON output. Used by datum workflows for verification, setup, merge, and cleanup.
tools: Bash
model: haiku
maxTurns: 3
```

Body: A minimal prompt like "You execute a single CLI command and return its output as structured JSON. Do not read or modify files directly. Do not use any tools other than Bash."

**Why not a third type for RED/GREEN/REFACTOR?** These agents need the full toolbelt (Read, Write, Edit, Bash, Grep, Glob) plus git operations. Creating a custom type for them would save the MCP tool schemas (Gmail/Calendar/Drive/Notion/serena/headroom/context7) but would lose CLAUDE.md and AGENTS.md rules. Since RED/GREEN/REFACTOR are the reasoning-heavy agents where code quality rules matter, they should keep the default type. However, you could create a `datum-coder` agent type with `tools: Read, Write, Edit, Bash, Grep, Glob` and include the critical coding-steering rules in its body. The tradeoff: more maintenance of the agent body vs. token savings from dropping MCP schemas.

**Decision**: For v1, create only `datum-reader` and `datum-cli`. Evaluate `datum-coder` for v2 after measuring actual savings.

### Answer 3: Expected Token Savings

**Current state**: Every agent call gets the full system prompt. From the problem statement: ~76K tokens of system prompt per agent.

**Per-lane agent distribution** (behavioral lane):
- 8 mechanical agents (topology, skeleton, verify-red, signal, verify-green, verify-refactor, merge, cleanup): all become `datum-cli` or `datum-reader`
- 3 reasoning agents (RED, GREEN, REFACTOR): keep default type
- Setup runs once per workflow, not per lane

**Custom agent system prompt size estimate**:
- `datum-cli`: ~200 tokens (frontmatter + minimal body) + ~2K tokens (Bash tool schema + environment details) = ~2.2K tokens
- `datum-reader`: ~200 tokens + ~1.5K tokens (Read tool schema + environment details) = ~1.7K tokens
- Default agent (RED/GREEN/REFACTOR): ~76K tokens (unchanged)

**Savings per behavioral lane**:
- 7 agents switch from 76K to ~2.2K each: 7 x (76K - 2.2K) = 7 x 73.8K = **516.6K tokens saved per lane**
- Plus topology reader: 76K - 1.7K = **74.3K tokens saved** (once per workflow)

**Savings for a 5-lane workflow**:
- Pre-optimization: (12 agents/lane x 5 lanes + 3 global agents) x 76K = **4.79M tokens** in system prompts
- Post-optimization: (3 reasoning agents/lane x 5 lanes) x 76K + (8 mechanical/lane x 5 + 3 global) x 2.2K = 1.14M + 94.6K = **1.23M tokens**
- **Net savings: ~3.56M tokens (74% reduction)**

This is a conservative estimate. The actual savings depend on how many tools the full prompt really contains. The key insight is that custom agent types get "only this system prompt, not the full Claude Code system prompt" per the official docs.

### Answer 4: Agent Merges to Reduce Total Count

Yes, several agents should be merged:

**Merge 1: verify-red + test-signal --&gt; single agent**

Currently these are two separate `agent()` calls at lines 198-215:
- `verify-red` runs `datum verify-stage red` and returns `{verified, error}`
- `signal` runs the test command and extracts `{exit_code, errors, assertion_messages}`

These can be a single Bash agent that runs both commands sequentially:
```
cd "${wt}" &amp;&amp; datum verify-stage red --test-command "${cfg.testCommand}" &amp;&amp; ${cfg.testCommand} 2&gt;&amp;1 || true
```

Return a combined schema:
```json
{
  "verified": true,
  "exit_code": 1,
  "errors": ["..."],
  "assertion_messages": ["..."]
}
```

**Savings**: 1 fewer agent per lane = 1 x (76K pre-optimization, or ~2.2K post-optimization) per lane. More importantly, eliminates one agent spawn/teardown cycle latency (~2-5 seconds per agent).

**Merge 2: merge + cleanup --&gt; single agent**

Lines 428-441. Both just run a single datum CLI command. Merge into one agent that runs both:
```
datum worktrees merge ... &amp;&amp; datum worktrees cleanup ...
```

**Savings**: 1 fewer agent globally. Minimal token savings but removes latency.

**After merges**: 10 agents per lane (down from 12), removing 2 per lane.

### Answer 5: Implementation Plan

**Phase 1: Create custom agent types (2 files)**

1. Create `/Volumes/Extra/repos/gitrdunhq/datum/.claude/agents/datum-reader.md`
2. Create `/Volumes/Extra/repos/gitrdunhq/datum/.claude/agents/datum-cli.md`

**Phase 2: Modify workflow script (`skills/datum-tdd-act.js`)**

Changes to `skills/datum-tdd-act.js`:

1. **Topology reader** (line 328-335): Add `agentType: 'datum-reader'`
   ```js
   const lanePlan = await agent(
     `Read the file ${lanePlanPath} and return its contents as JSON...`,
     { label: 'read-plan', phase: 'Topology', model: 'haiku', schema: LANE_PLAN_SCHEMA, agentType: 'datum-reader' }
   )
   ```

2. **Setup** (line 346-362): Add `agentType: 'datum-cli'`

3. **Skeleton preflight** (line 166-170): Add `agentType: 'datum-cli'`

4. **Merge verify-red + test-signal** (lines 198-215): Combine into one `agent()` call with `agentType: 'datum-cli'` and a merged schema:
   ```js
   const VERIFY_AND_SIGNAL_SCHEMA = {
     type: 'object',
     properties: {
       verified: { type: 'boolean' },
       stage: { type: 'string' },
       error: { type: 'string' },
       exit_code: { type: 'number' },
       errors: { type: 'array', items: { type: 'string' } },
       assertion_messages: { type: 'array', items: { type: 'string' } },
     },
     required: ['verified', 'exit_code'],
   }
   ```

5. **Verify-green** (line 244-247): Add `agentType: 'datum-cli'`

6. **Verify-refactor** (line 299-301): Add `agentType: 'datum-cli'`

7. **Merge merge+cleanup** (lines 410-441): Combine into one `agent()` call with `agentType: 'datum-cli'`

8. **RED, GREEN, REFACTOR** agents: Leave as-is (no `agentType`, keep default). They need the full toolbelt.

**Phase 3: Update `install.sh`**

No change needed. Agent types in `.claude/agents/` are discovered automatically by Claude Code at session start.

**Phase 4: Testing**

Test the workflow on a small lane-plan (1-2 lanes) to verify:
- `datum-reader` agent can still read lane-plan.json correctly
- `datum-cli` agents can still run CLI commands and return structured JSON
- The merged verify-red+signal agent returns the combined schema
- RED/GREEN/REFACTOR agents still have full tool access
- No regression in gate enforcement

**Sequencing**

The phases must execute in order:
1. Phase 1 first (agent types must exist before the workflow references them)
2. Phase 2 depends on Phase 1
3. Phase 3 is independent
4. Phase 4 depends on Phase 2

Phases 1 and 3 can be done in parallel.

### Risk Analysis

**Risk 1: Custom agent types may not load CLAUDE.md rules.** Per the docs: "Explore and Plan skip CLAUDE.md files" but "every other built-in and custom subagent loads both." Custom agents DO load CLAUDE.md. However, the `datum-cli` and `datum-reader` agents are so constrained (only Bash or Read) that CLAUDE.md rules about editing files, GitNexus impact analysis, etc. are irrelevant. The CLAUDE.md overhead is a small constant (~2-3K tokens).

**Risk 2: `agentType` parameter may not be supported by the Workflow `agent()` function.** Evidence that it IS supported: the `design-rfp-shaper` workflow at `/Users/samfakhreddine/.claude/projects/-Users-samfakhreddine--agents-skills-edified-rfp/c1638916-ad3a-4e4e-af4c-3efa9093a1df/workflows/scripts/design-rfp-shaper-wf_f8e71c57-3aa.js` uses `agentType: 'Explore'` successfully (line 76). The changelog entry for v2.1.140 confirms: "Improved Agent tool `subagent_type` matching to accept case- and separator-insensitive values." The Workflow `agent()` function spawns subagents internally and supports the same parameters.

**Risk 3: The `datum-cli` agent's Bash-only toolset may not support structured JSON output with `schema:`.** The `schema:` parameter is enforced at the tool-call layer, independent of which tools the agent has. Existing agents in the workflow already use `schema:` with haiku model for structured output. No risk.

### Critical Files for Implementation
- `/Volumes/Extra/repos/gitrdunhq/datum/skills/datum-tdd-act.js` -- the workflow script to modify (add `agentType:` to 8 agent calls, merge 2 pairs)
- `/Volumes/Extra/repos/gitrdunhq/datum/.claude/agents/datum-cli.md` -- new custom agent type (Bash-only, for all CLI/verify/signal/merge/cleanup agents)
- `/Volumes/Extra/repos/gitrdunhq/datum/.claude/agents/datum-reader.md` -- new custom agent type (Read-only, for topology reader)
- `/Volumes/Extra/repos/gitrdunhq/datum/.claude/agents/entire-search.md` -- existing agent type to reference as a pattern for the new definitions
- `/Volumes/Extra/repos/gitrdunhq/datum/assets/config.toml.default` -- reference for model tier configuration (already correct, no changes needed)</result>
<usage><subagent_tokens>124152</subagent_tokens><tool_uses>74</tool_uses><duration_ms>433385</duration_ms></usage>
</task-notification>

---

does this help in anyway send a subagent to incorproate the best / fixes > ## Documentation Index
> Fetch the complete documentation index at: https://code.claude.com/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# Orchestrate subagents at scale with dynamic workflows

> Dynamic workflows orchestrate many subagents from a script Claude writes and you can rerun. Use them for codebase audits, large migrations, and cross-checked research.

{/* plan-availability: feature=workflows plans=pro,max,team,enterprise providers=all */}

<Note>
  Dynamic workflows require Claude Code v2.1.154 or later and are available on all paid plans, with Anthropic API access, and on Amazon Bedrock, Google Cloud Vertex AI, and Microsoft Foundry. On Pro, turn them on from the Dynamic workflows row in `/config`.
</Note>

A dynamic workflow is a JavaScript script that orchestrates [subagents](/en/sub-agents) at scale. Claude writes the script for the task you describe, and a runtime executes it in the background while your session stays responsive.

Reach for a workflow when a task needs more agents than one conversation can coordinate, or when you want the orchestration codified as a script you can read and rerun. Examples include a codebase-wide bug sweep, a 500-file migration, a research question that needs sources cross-checked against each other, and a hard plan worth drafting from several independent angles before you commit to one.

This page covers how to:

* Decide [when to use a workflow](#when-to-use-a-workflow) instead of subagents or skills
* [Run a bundled workflow](#run-a-bundled-workflow) with `/deep-research`
* [Have Claude write a workflow](#have-claude-write-a-workflow) for your task and save it
* Understand [how a workflow runs](#how-a-workflow-runs) and [manage runs](#manage-runs)

## When to use a workflow

[Subagents](/en/sub-agents), [skills](/en/skills), [agent teams](/en/agent-teams), and workflows can all run a multi-step task. The difference is who holds the plan:

|                                 | Subagents                      | Skills                       | Agent teams                            | Workflows                            |
| :------------------------------ | :----------------------------- | :--------------------------- | :------------------------------------- | :----------------------------------- |
| What it is                      | A worker Claude spawns         | Instructions Claude follows  | A lead agent supervising peer sessions | A script the runtime executes        |
| Who decides what runs next      | Claude, turn by turn           | Claude, following the prompt | The lead agent, turn by turn           | The script                           |
| Where intermediate results live | Claude's context window        | Claude's context window      | A shared task list                     | Script variables                     |
| What's repeatable               | The worker definition          | The instructions             | The team definition                    | The orchestration itself             |
| Scale                           | A few delegated tasks per turn | Same as subagents            | A handful of long-running peers        | Dozens to hundreds of agents per run |
| Interruption                    | Restarts the turn              | Restarts the turn            | Teammates keep running                 | Resumable in the same session        |

A workflow moves the plan into code. With subagents, skills, and agent teams, Claude is the orchestrator: it decides turn by turn what to spawn or assign next, and every result lands in a context window. A workflow script holds the loop, the branching, and the intermediate results itself, so Claude's context holds only the final answer.

Moving the plan into code also lets a workflow apply a repeatable quality pattern, not just run more agents: it can have independent agents adversarially review each other's findings before they're reported, or draft a plan from several angles and weigh them against each other, so you get a more trustworthy result than a single pass.

## Run a bundled workflow

The quickest way to see a workflow in action is to run `/deep-research`, the [built-in workflow](#bundled-workflows) Claude Code includes for investigating a question across many sources. You'll see agents work through a set of phases in the background while your session stays free, and get one report at the end instead of a turn-by-turn transcript.

<Steps>
  <Step title="Run the workflow">
    Run `/deep-research` with a question you want investigated. It fans out web searches across several angles, fetches and cross-checks the sources it finds, and synthesizes a cited report.

    ```text theme={null}
    /deep-research What changed in the Node.js permission model between v20 and v22?
    ```
  </Step>

  <Step title="Allow workflows">
    Claude Code asks whether to allow the workflow. Select **Yes** to continue. The exact prompt depends on your permission mode. See [Approve the plan before it runs](#approve-the-plan-before-it-runs) for the per-mode options.
  </Step>

  <Step title="Watch progress">
    The run starts in the background. Run `/workflows`, use the arrow keys to select the run, and press Enter to open its progress view:

    ```text theme={null}
    /workflows
    ```

    The view shows each phase with its agent count, token total, and elapsed time. Drill into any phase to see its agents and what each one found. See [Watch the run](#watch-the-run) for the full set of controls.

    You can also watch from the task panel below the input box: a one-line progress summary appears there while the run is going. Press the down arrow to focus it, then Enter to expand.
  </Step>

  <Step title="Read the report">
    When the run finishes, the report lands in your session. It cites the sources each claim came from, with claims that didn't survive cross-checking already filtered out.
  </Step>
</Steps>

To run a workflow for your own task, [have Claude write one](#have-claude-write-a-workflow), and once a run does what you wanted you can [save it](#save-the-workflow-for-reuse) as a command of your own.

### Bundled workflows

Claude Code includes `/deep-research` as a built-in workflow:

| Command                     | What it does                                                                                                                                                                                                                                                                                                      |
| :-------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/deep-research <question>` | Fans out web searches on a question across several angles, fetches and cross-checks the sources it finds, votes on each claim, and returns a cited report with claims that didn't survive cross-checking filtered out. Requires the [WebSearch tool](/en/tools-reference#websearch-tool-behavior) to be available |

[Workflows you save](#save-the-workflow-for-reuse) yourself become commands the same way and appear in `/` autocomplete alongside the bundled ones.

### Watch the run

Workflows run in the background, so the session stays responsive while agents work. Run `/workflows` at any time to list running and completed workflows, then select one to open its progress view.

```text theme={null}
/workflows
```

The progress view shows each phase with its agent counts, token totals, and elapsed time. The footer lists the key for each action:

| Key            | Action                                                                                              |
| :------------- | :-------------------------------------------------------------------------------------------------- |
| `↑` / `↓`      | Select a phase or agent                                                                             |
| `Enter` or `→` | Drill into the selected phase, then into an agent to read its prompt, recent tool calls, and result |
| `Esc`          | Back out one level                                                                                  |
| `j` / `k`      | Scroll within the agent detail when it overflows                                                    |
| `p`            | Pause or resume the run                                                                             |
| `x`            | Stop the selected agent, or stop the whole workflow when focus is on the run                        |
| `r`            | Restart the selected running agent                                                                  |
| `s`            | [Save](#save-the-workflow-for-reuse) the run's script as a command                                  |

## Have Claude write a workflow

You can have Claude write a workflow for your task in two ways:

* [Ask for a workflow](#ask-for-a-workflow-in-your-prompt) in your prompt, either in your own words or by including the keyword `ultracode`, and Claude writes one for the task.
* [Let Claude decide with ultracode](#let-claude-decide-with-ultracode): set `/effort ultracode` and Claude plans a workflow for every substantive task in the session.

You can also run a workflow command that already exists: a [bundled workflow](#bundled-workflows) like `/deep-research`, or one you've [saved](#save-the-workflow-for-reuse).

### Ask for a workflow in your prompt

To run a single task as a workflow without changing the session's effort level, include the keyword `ultracode` in your prompt. Asking in your own words, for example "use a workflow" or "run a workflow", also works: Claude treats a direct request as the same opt-in. Before v2.1.160 the literal trigger keyword was `workflow`; natural-language requests work in both versions.

```text theme={null}
ultracode: audit every API endpoint under src/routes/ for missing auth checks
```

Claude Code highlights the keyword in your input and Claude writes a workflow script for the task instead of working through it turn by turn. If you didn't mean to start a workflow, press `Option+W` on macOS or `Alt+W` on Windows and Linux to dismiss the highlight for this prompt, or press backspace while the cursor is right after the highlighted keyword. To stop the keyword from triggering at all, turn off Ultracode keyword trigger in `/config`.

If the run does what you wanted, you can [save it as a command](#save-the-workflow-for-reuse) afterward.

If you already have an orchestrator built another way, such as a folder of subagent prompts or a skill that fans work out, you can point Claude at it and ask for a workflow that does the same thing.

### Let Claude decide with ultracode

Ultracode is a Claude Code setting that combines `xhigh` [reasoning effort](/en/model-config#adjust-effort-level) with automatic workflow orchestration. With it on, Claude plans a workflow for each substantive task instead of waiting for you to ask.

```text theme={null}
/effort ultracode
```

With ultracode on, Claude decides when a task warrants a workflow. A single request can turn into several workflows in a row: one to understand the code, one to make the change, and one to verify it. This applies to every task in the session, so each request uses more tokens and takes longer than at lower effort levels.

Ultracode lasts for the current session and resets when you start a new one. Drop back with `/effort high` when you return to routine work. It's available on models that support `xhigh` [effort](/en/model-config#adjust-effort-level); on other models the `/effort` menu doesn't offer it.

### Approve the plan before it runs

In the CLI, the per-run prompt shows the planned phases and these options:

* **Yes, run it**: start the run
* **Yes, and don't ask again for `<name>` in `<path>`**: start, and skip this prompt for this workflow in this project from now on
* **View raw script**: read the script before deciding
* **No**: cancel

`Ctrl+G` opens the script in your editor. `Tab` lets you adjust the prompt before the run starts.

Whether you see this prompt depends on your [permission mode](/en/permission-modes):

| Permission mode                            | When you're prompted                                                                                                                                    |
| :----------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Default, accept edits                      | Every run, unless you've selected **Yes, and don't ask again** for that workflow in this project                                                        |
| Auto                                       | First launch only. Any **Yes** records consent in your user settings, and later launches start without prompting. Skipped entirely when ultracode is on |
| Bypass permissions, `claude -p`, Agent SDK | Never. The run starts immediately                                                                                                                       |

In the Desktop app, an approval card shows the workflow name, the phase list, and a token-usage caution, with **Once**, **Always**, and **Deny** actions. The progress view appears in the Background tasks side pane.

Your permission mode controls only the launch prompt above. The subagents the workflow spawns always run in `acceptEdits` mode and inherit your [tool allowlist](/en/settings#permission-settings), regardless of your session's mode. File edits are auto-approved.

Shell commands, web fetches, and MCP tools that aren't in your allowlist can still prompt you mid-run. To avoid this on a long run, add the commands the agents need to your allowlist before starting.

In `claude -p` and the Agent SDK there is no one to prompt, so tool calls follow your configured permission rules without interactive confirmation.

### Save the workflow for reuse

When Claude writes a workflow for a task you'll repeat, you can save that run's script as a command. A process like a review you run on every branch then runs the same orchestration each time.

Run `/workflows`, select the run you want to keep, and press `s`. In the save dialog, Tab toggles between the two save locations:

* `.claude/workflows/` in your project: shared with everyone who clones the repo
* `~/.claude/workflows/` in your home directory: available in every project, visible only to you

Press Enter to save. The workflow runs as `/<name>` in future sessions from either location.

If a project workflow and a personal workflow share a name, the project one runs.

### Pass input to a saved workflow

A saved workflow can accept input through the `args` parameter. The script reads it as a global named `args`. Use this to supply a research question, a list of target paths, or a configuration object at invocation time instead of editing the script for each run.

The following prompt runs a saved workflow with a list of issue numbers:

```text theme={null}
> Run /triage-issues on issues 1024, 1025, and 1030
```

Claude passes the list as structured data, so the script can call array and object methods on `args` directly without parsing it first. If `args` is omitted, the global is `undefined` inside the script.

## How a workflow runs

The workflow runtime executes the script in an isolated environment, separate from your conversation. Intermediate results stay in script variables instead of landing in Claude's context.

Every run writes its script to a file under your session's directory in `~/.claude/projects/`. Claude receives the path when the run starts, so you can ask for it. You can open that file to read the orchestration Claude wrote, diff it against a previous run's script, or edit it and ask Claude to relaunch from the edited version.

The runtime tracks each agent's result as the run progresses, which is what makes a run [resumable](#resume-after-a-pause) within the same session.

### Behavior and limits

The runtime applies the following constraints:

| Constraint                                                           | Why                                                                                                            |
| :------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------- |
| No mid-run user input                                                | Only agent permission prompts can pause a run. For sign-off between stages, run each stage as its own workflow |
| No direct filesystem or shell access from the workflow itself        | Agents read, write, and run commands. The script coordinates the agents                                        |
| Up to 16 concurrent agents, fewer on machines with limited CPU cores | Bounds local resource use                                                                                      |
| 1,000 agents total per run                                           | Prevents runaway loops                                                                                         |

## Manage runs

Once a run starts, you manage it from the `/workflows` view, or by expanding its progress line in the task panel below the input box.

### Resume after a pause

If you stop a run, you can resume it: agents that already completed return their cached results, and the rest run live. Resume a paused run from `/workflows` by selecting it and pressing `p`, or ask Claude to relaunch the workflow with the same script.

Resume works within the same Claude Code session. If you exit Claude Code while a workflow is running, the next session starts the workflow fresh.

### Cost

A workflow spawns many agents, so a single run can use meaningfully more tokens than working through the same task in conversation. Runs count toward your plan's usage and rate limits like any other session.

To gauge the spend before committing to a large task, run the workflow on a small slice first: one directory instead of the whole repo, or a narrow question instead of a broad one. The `/workflows` view shows each agent's token usage as the run progresses, and you can stop the run there at any time without losing completed work. The runtime's [agent caps](#behavior-and-limits) limit how many agents a single run can spawn, which bounds the cost of a runaway script.

Every agent in a workflow uses your session's model unless the script routes a stage to a different one. To control the model cost:

* Check `/model` before a large run if you usually switch to a smaller model for routine work
* Ask Claude to use a smaller model for stages that don't need the strongest one when you describe the task

### Turn workflows off

Workflows are available in the CLI, the Desktop app, the IDE extensions, [non-interactive mode](/en/headless) with `claude -p`, and the [Agent SDK](/en/agent-sdk/overview). The same disable settings apply on every surface.

To turn workflows off for yourself:

* Toggle Dynamic workflows off in `/config`. Persists across sessions.
* Set `"disableWorkflows": true` in `~/.claude/settings.json`. Persists across sessions.
* Set `CLAUDE_CODE_DISABLE_WORKFLOWS=1`. Read at startup, so it applies wherever you set it.

To turn workflows off for your whole organization, set `"disableWorkflows": true` in [managed settings](/en/server-managed-settings), or use the toggle on the [Claude Code admin settings](https://claude.ai/admin-settings/claude-code) page.

When workflows are disabled, the bundled workflow commands are unavailable, the `ultracode` keyword no longer triggers a run, and `ultracode` is removed from the `/effort` menu.

## Related resources

* [Run agents in parallel](/en/agents): compare subagents, agent view, agent teams, and workflows
* [Create custom subagents](/en/sub-agents): the worker primitive workflows orchestrate
* [Manage costs](/en/costs): how multi-agent runs count toward usage limits

---

does this help > ## Documentation Index
> Fetch the complete documentation index at: https://code.claude.com/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# Run parallel sessions with worktrees

> Isolate parallel Claude Code sessions in separate git worktrees so changes don't collide. Covers the `--worktree` flag, subagent isolation, `.worktreeinclude`, cleanup, and non-git VCS hooks.

A [git worktree](https://git-scm.com/docs/git-worktree) is a separate working directory with its own files and branch, sharing the same repository history and remote as your main checkout. Running each Claude Code session in its own worktree means edits in one session never touch files in another, so you can have Claude building a feature in one terminal while fixing a bug in a second.

This page covers worktree isolation in the CLI. Everything below assumes a git repository. For other version control systems, see [Non-git version control](#non-git-version-control). The [desktop app](/en/desktop#work-in-parallel-with-sessions) creates a worktree for every new session automatically.

Worktrees are one of several ways to run Claude in parallel. They isolate file edits, while [subagents](/en/sub-agents) and [agent teams](/en/agent-teams) coordinate the work itself. See [Run agents in parallel](/en/agents) to compare the approaches, or skip ahead to [Isolate subagents with worktrees](#isolate-subagents-with-worktrees) to use worktrees and subagents together.

## Start Claude in a worktree

Pass `--worktree` or `-w` to create an isolated worktree and start Claude in it. By default, the worktree is created under `.claude/worktrees/<value>/` at your repository root, on a new branch named `worktree-<value>`:

```bash theme={null}
claude --worktree feature-auth
```

To put worktrees somewhere else, configure a [`WorktreeCreate` hook](#non-git-version-control). Run the command again with a different name in another terminal to start a second isolated session:

```bash theme={null}
claude --worktree bugfix-123
```

If you omit the name, Claude generates one such as `bright-running-fox`:

```bash theme={null}
claude --worktree
```

You can also ask Claude to "work in a worktree" during a session, and it will create one with the [`EnterWorktree`](/en/tools-reference) tool. Once in a worktree, Claude can switch directly to another one under `.claude/worktrees/` by calling `EnterWorktree` with the target path. The previous worktree stays on disk untouched.

Before using `--worktree` interactively in a directory for the first time, accept the workspace trust dialog by running `claude` once in that directory. If trust has not yet been accepted, `--worktree` exits with an error and prompts you to run `claude` in the directory first. Non-interactive runs with `-p` skip the [trust check](/en/security), so `claude -p --worktree` proceeds without it.

<Tip>
  Add `.claude/worktrees/` to your `.gitignore` so worktree contents don't appear as untracked files in your main checkout.
</Tip>

### Choose the base branch

Worktrees branch from your repository's default branch, `origin/HEAD`, so they start from a clean tree matching the remote. If no remote is configured or the fetch fails, the worktree falls back to your current local `HEAD`. To always branch from local `HEAD` instead, set `worktree.baseRef` to `"head"` in [settings](/en/settings#worktree-settings). Setting `baseRef` to `"head"` makes new worktrees carry your unpushed commits and feature-branch state, which is useful when isolating subagents that need to operate on in-progress work. The setting accepts only `"fresh"` or `"head"`, not arbitrary git refs:

```json theme={null}
{
  "worktree": {
    "baseRef": "head"
  }
}
```

To branch from a specific pull request, pass the PR number prefixed with `#`, or a full GitHub pull request URL. Claude Code fetches `pull/<number>/head` from `origin` and creates the worktree at `.claude/worktrees/pr-<number>`:

```bash theme={null}
claude --worktree "#1234"
```

For full control over how worktrees are created, configure a [`WorktreeCreate` hook](/en/hooks#worktreecreate), which replaces the default `git worktree` logic entirely.

## Copy gitignored files into worktrees

A worktree is a fresh checkout, so untracked files like `.env` or `.env.local` from your main repository are not present. To copy them automatically when Claude creates a worktree, add a `.worktreeinclude` file to your project root.

The file uses `.gitignore` syntax. Only files that match a pattern and are also gitignored are copied, so tracked files are never duplicated.

This `.worktreeinclude` copies two env files and a secrets config into each new worktree:

```text .worktreeinclude theme={null}
.env
.env.local
config/secrets.json
```

This applies to worktrees created with `--worktree`, [subagent worktrees](#isolate-subagents-with-worktrees), and parallel sessions in the [desktop app](/en/desktop#work-in-parallel-with-sessions).

## Isolate subagents with worktrees

Subagents can run in their own worktrees so parallel edits don't conflict. Ask Claude to "use worktrees for your agents", or set it permanently on a [custom subagent](/en/sub-agents#supported-frontmatter-fields) by adding `isolation: worktree` to the frontmatter. Each subagent gets a temporary worktree that is removed automatically when the subagent finishes without changes.

Subagent worktrees use the same [base branch](#choose-the-base-branch) as `--worktree`, so they branch from your repository's default branch unless `worktree.baseRef` is set to `"head"`.

## Clean up worktrees

When you exit a worktree session, cleanup depends on whether you made changes:

* **No uncommitted changes, no untracked files, and no new commits**: the worktree and its branch are removed automatically. If the session has a [name](/en/sessions#name-your-sessions), Claude prompts instead so you can keep the worktree for later
* **Uncommitted changes, untracked files, or new commits exist**: Claude prompts you to keep or remove the worktree. Keeping preserves the directory and branch so you can return later. Removing deletes the worktree directory and its branch, discarding any uncommitted changes, untracked files, and commits
* **Non-interactive runs**: worktrees created with `--worktree` alongside `-p` are not cleaned up automatically since there is no exit prompt. Remove them with `git worktree remove`

Worktrees that Claude created for subagents and [background sessions](/en/agent-view#how-file-edits-are-isolated) are removed automatically once they are older than your [`cleanupPeriodDays`](/en/settings#available-settings) setting, provided they have no uncommitted changes, no untracked files, and no unpushed commits. Worktrees you create with `--worktree` are never removed by this sweep.

While an agent is running, Claude runs `git worktree lock` on its worktree so that concurrent cleanup cannot remove it. The lock is released when the agent finishes. To clean up a worktree that the sweep keeps, run `git worktree remove`, adding `--force` if the worktree has uncommitted changes or untracked files.

## Manage worktrees manually

For full control over worktree location and branch configuration, create worktrees with Git directly. This is useful when you need to check out a specific existing branch or place the worktree outside the repository.

Create a worktree on a new branch:

```bash theme={null}
git worktree add ../project-feature-a -b feature-a
```

Create a worktree from an existing branch:

```bash theme={null}
git worktree add ../project-bugfix bugfix-123
```

Start Claude in the worktree:

```bash theme={null}
cd ../project-feature-a && claude
```

List your worktrees:

```bash theme={null}
git worktree list
```

Remove one when you're done with it:

```bash theme={null}
git worktree remove ../project-feature-a
```

See the [Git worktree documentation](https://git-scm.com/docs/git-worktree) for the full command reference. Remember to initialize your development environment in each new worktree: install dependencies, set up virtual environments, or run whatever your project's setup requires.

## Non-git version control

Worktree isolation uses git by default. For SVN, Perforce, Mercurial, or other systems, configure [`WorktreeCreate` and `WorktreeRemove` hooks](/en/hooks#worktreecreate) to provide custom creation and cleanup logic. Because the hook replaces the default git behavior, [`.worktreeinclude`](#copy-gitignored-files-into-worktrees) is not processed when you use `--worktree`. Copy any local configuration files inside your hook script instead.

This `WorktreeCreate` hook reads the worktree name from stdin, checks out a fresh SVN working copy, and prints the directory path so Claude Code can use it as the session's working directory:

```json theme={null}
{
  "hooks": {
    "WorktreeCreate": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'NAME=$(jq -r .name); DIR=\"$HOME/.claude/worktrees/$NAME\"; svn checkout https://svn.example.com/repo/trunk \"$DIR\" >&2 && echo \"$DIR\"'"
          }
        ]
      }
    ]
  }
}
```

Pair it with a `WorktreeRemove` hook to clean up when the session ends. See the [hooks reference](/en/hooks#worktreecreate) for the input schema and a removal example.

## See also

Worktrees handle file isolation. The related pages below cover delegating work into those isolated checkouts and switching between the sessions you create:

* [Subagents](/en/sub-agents): delegate work to isolated agents within a session
* [Agent teams](/en/agent-teams): coordinate multiple Claude sessions automatically
* [Manage sessions](/en/sessions): name, resume, and switch between conversations
* [Desktop parallel sessions](/en/desktop#work-in-parallel-with-sessions): worktree-backed sessions in the desktop app

---

<task-notification>
<task-id>wk5gw4bk1</task-id>
<tool-use-id>toolu_0132JWibfzEoBUUWE8QUEUiE</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/wk5gw4bk1.output</output-file>
<status>completed</status>
<summary>Dynamic workflow "Deterministic TDD Act: RED-&gt;GREEN-&gt;REFACTOR per lane with gate enforcement" completed</summary>
<result>{"runId":"run-default","total":2,"completed":0,"failed":2,"failedLanes":["task-001","task-002"],"completedLanes":[]}</result>
<usage><agent_count>13</agent_count><subagent_tokens>323538</subagent_tokens><tool_uses>128</tool_uses><duration_ms>658694</duration_ms></usage>
</task-notification>

---

> ## Documentation Index
> Fetch the complete documentation index at: https://code.claude.com/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# Automate actions with hooks

> Run shell commands automatically when Claude Code edits files, finishes tasks, or needs input. Format code, send notifications, validate commands, and enforce project rules.

Hooks are user-defined shell commands that execute at specific points in Claude Code's lifecycle. They provide deterministic control over Claude Code's behavior, ensuring certain actions always happen rather than relying on the LLM to choose to run them. Use hooks to enforce project rules, automate repetitive tasks, and integrate Claude Code with your existing tools.

For decisions that require judgment rather than deterministic rules, you can also use [prompt-based hooks](#prompt-based-hooks) or [agent-based hooks](#agent-based-hooks) that use a Claude model to evaluate conditions.

For other ways to extend Claude Code, see [skills](/en/skills) for giving Claude additional instructions and executable commands, [subagents](/en/sub-agents) for running tasks in isolated contexts, and [plugins](/en/plugins) for packaging extensions to share across projects.

<Tip>
  This guide covers common use cases and how to get started. For full event schemas, JSON input/output formats, and advanced features like async hooks and MCP tool hooks, see the [Hooks reference](/en/hooks).
</Tip>

## Set up your first hook

To create a hook, add a `hooks` block to a [settings file](#configure-hook-location). This walkthrough creates a desktop notification hook, so you get alerted whenever Claude is waiting for your input instead of watching the terminal.

<Steps>
  <Step title="Add the hook to your settings">
    Open `~/.claude/settings.json` and add a `Notification` hook. The example below uses `osascript` for macOS; see [Get notified when Claude needs input](#get-notified-when-claude-needs-input) for Linux and Windows commands.

    ```json theme={null}
    {
      "hooks": {
        "Notification": [
          {
            "matcher": "",
            "hooks": [
              {
                "type": "command",
                "command": "osascript -e 'display notification \"Claude Code needs your attention\" with title \"Claude Code\"'"
              }
            ]
          }
        ]
      }
    }
    ```

    If your settings file already has a `hooks` key, add `Notification` as a sibling of the existing event keys rather than replacing the whole object. Each event name is a key inside the single `hooks` object:

    ```json theme={null}
    {
      "hooks": {
        "PostToolUse": [
          {
            "matcher": "Edit|Write",
            "hooks": [{ "type": "command", "command": "jq -r '.tool_input.file_path' | xargs npx prettier --write" }]
          }
        ],
        "Notification": [
          {
            "matcher": "",
            "hooks": [{ "type": "command", "command": "osascript -e 'display notification \"Claude Code needs your attention\" with title \"Claude Code\"'" }]
          }
        ]
      }
    }
    ```

    You can also ask Claude to write the hook for you by describing what you want in the CLI.
  </Step>

  <Step title="Verify the configuration">
    Type `/hooks` to open the hooks browser. You'll see a list of all available hook events, with a count next to each event that has hooks configured. Select `Notification` to confirm your new hook appears in the list. Selecting the hook shows its details: the event, matcher, type, source file, and command.
  </Step>

  <Step title="Test the hook">
    Press `Esc` to return to the CLI. Ask Claude to do something that requires permission, then switch away from the terminal. You should receive a desktop notification.
  </Step>
</Steps>

<Tip>
  The `/hooks` menu is read-only. To add, modify, or remove hooks, edit your settings JSON directly or ask Claude to make the change.
</Tip>

## What you can automate

Hooks let you run code at key points in Claude Code's lifecycle: format files after edits, block commands before they execute, send notifications when Claude needs input, inject context at session start, and more. For the full list of hook events, see the [Hooks reference](/en/hooks#hook-lifecycle).

Each example includes a ready-to-use configuration block that you add to a [settings file](#configure-hook-location). The most common patterns:

* [Get notified when Claude needs input](#get-notified-when-claude-needs-input)
* [Auto-format code after edits](#auto-format-code-after-edits)
* [Block edits to protected files](#block-edits-to-protected-files)
* [Re-inject context after compaction](#re-inject-context-after-compaction)
* [Audit configuration changes](#audit-configuration-changes)
* [Reload environment when directory or files change](#reload-environment-when-directory-or-files-change)
* [Auto-approve specific permission prompts](#auto-approve-specific-permission-prompts)

For a production example of hooks that run a separate model review and feed findings back into the session, see [how the `security-guidance` plugin integrates with Claude Code](/en/security-guidance#how-the-plugin-integrates-with-claude-code).

### Get notified when Claude needs input

Get a desktop notification whenever Claude finishes working and needs your input, so you can switch to other tasks without checking the terminal.

This hook uses the `Notification` event, which fires when Claude is waiting for input or permission. Each tab below uses the platform's native notification command. Add this to `~/.claude/settings.json`:

<Tabs>
  <Tab title="macOS">
    ```json theme={null}
    {
      "hooks": {
        "Notification": [
          {
            "matcher": "",
            "hooks": [
              {
                "type": "command",
                "command": "osascript -e 'display notification \"Claude Code needs your attention\" with title \"Claude Code\"'"
              }
            ]
          }
        ]
      }
    }
    ```

    <Accordion title="If no notification appears">
      `osascript` routes notifications through the built-in Script Editor app. If Script Editor doesn't have notification permission, the command fails silently, and macOS won't prompt you to grant it. Run this in Terminal once to make Script Editor appear in your notification settings:

      ```bash theme={null}
      osascript -e 'display notification "test"'
      ```

      Nothing will appear yet. Open **System Settings > Notifications**, find **Script Editor** in the list, and turn on **Allow Notifications**. Run the command again to confirm the test notification appears.
    </Accordion>
  </Tab>

  <Tab title="Linux">
    ```json theme={null}
    {
      "hooks": {
        "Notification": [
          {
            "matcher": "",
            "hooks": [
              {
                "type": "command",
                "command": "notify-send 'Claude Code' 'Claude Code needs your attention'"
              }
            ]
          }
        ]
      }
    }
    ```
  </Tab>

  <Tab title="Windows (PowerShell)">
    ```json theme={null}
    {
      "hooks": {
        "Notification": [
          {
            "matcher": "",
            "hooks": [
              {
                "type": "command",
                "command": "powershell.exe -Command \"[System.Reflection.Assembly]::LoadWithPartialName('System.Windows.Forms'); [System.Windows.Forms.MessageBox]::Show('Claude Code needs your attention', 'Claude Code')\""
              }
            ]
          }
        ]
      }
    }
    ```
  </Tab>
</Tabs>

The empty `matcher` fires on all notification types. To fire only on specific events, set it to one of these values:

| Matcher                | Fires when                                             |
| :--------------------- | :----------------------------------------------------- |
| `permission_prompt`    | Claude needs you to approve a tool use                 |
| `idle_prompt`          | Claude is done and waiting for your next prompt        |
| `auth_success`         | Authentication completes                               |
| `elicitation_dialog`   | An MCP server opens an elicitation form                |
| `elicitation_complete` | An MCP elicitation form is submitted or dismissed      |
| `elicitation_response` | An MCP elicitation response is sent back to the server |

Type `/hooks` and select `Notification` to confirm the hook is registered. For the full event schema, see the [Notification reference](/en/hooks#notification).

### Auto-format code after edits

Automatically run [Prettier](https://prettier.io/) on every file Claude edits, so formatting stays consistent without manual intervention.

This hook uses the `PostToolUse` event with an `Edit|Write` matcher, so it runs only after file-editing tools. The command extracts the edited file path with [`jq`](https://jqlang.github.io/jq/) and passes it to Prettier. Add this to `.claude/settings.json` in your project root:

```json theme={null}
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | xargs npx prettier --write"
          }
        ]
      }
    ]
  }
}
```

<Note>
  The Bash examples on this page use `jq` for JSON parsing. Install it with `brew install jq` (macOS), `apt-get install jq` (Debian/Ubuntu), or see [`jq` downloads](https://jqlang.github.io/jq/download/).
</Note>

### Block edits to protected files

Prevent Claude from modifying sensitive files like `.env`, `package-lock.json`, or anything in `.git/`. Claude receives feedback explaining why the edit was blocked, so it can adjust its approach.

This example uses a separate script file that the hook calls. The script checks the target file path against a list of protected patterns and exits with code 2 to block the edit.

<Steps>
  <Step title="Create the hook script">
    Save this to `.claude/hooks/protect-files.sh`:

    ```bash theme={null}
    #!/bin/bash
    # protect-files.sh

    INPUT=$(cat)
    FILE_PATH=$(echo "$INPUT" | jq -r '.tool_input.file_path // empty')

    PROTECTED_PATTERNS=(".env" "package-lock.json" ".git/")

    for pattern in "${PROTECTED_PATTERNS[@]}"; do
      if [[ "$FILE_PATH" == *"$pattern"* ]]; then
        echo "Blocked: $FILE_PATH matches protected pattern '$pattern'" >&2
        exit 2
      fi
    done

    exit 0
    ```
  </Step>

  <Step title="Make the script executable (macOS/Linux)">
    Hook scripts must be executable for Claude Code to run them:

    ```bash theme={null}
    chmod +x .claude/hooks/protect-files.sh
    ```
  </Step>

  <Step title="Register the hook">
    Add a `PreToolUse` hook to `.claude/settings.json` that runs the script before any `Edit` or `Write` tool call:

    ```json theme={null}
    {
      "hooks": {
        "PreToolUse": [
          {
            "matcher": "Edit|Write",
            "hooks": [
              {
                "type": "command",
                "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/protect-files.sh"
              }
            ]
          }
        ]
      }
    }
    ```
  </Step>
</Steps>

### Re-inject context after compaction

When Claude's context window fills up, compaction summarizes the conversation to free space. This can lose important details. Use a `SessionStart` hook with a `compact` matcher to re-inject critical context after every compaction.

Any text your command writes to stdout is added to Claude's context. This example reminds Claude of project conventions and recent work. Add this to `.claude/settings.json` in your project root:

```json theme={null}
{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "compact",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Reminder: use Bun, not npm. Run bun test before committing. Current sprint: auth refactor.'"
          }
        ]
      }
    ]
  }
}
```

You can replace the `echo` with any command that produces dynamic output, like `git log --oneline -5` to show recent commits. For injecting context on every session start, consider using [CLAUDE.md](/en/memory) instead. For environment variables, see [`CLAUDE_ENV_FILE`](/en/hooks#persist-environment-variables) in the reference.

### Audit configuration changes

Track when settings or skills files change during a session. The `ConfigChange` event fires when an external process or editor modifies a configuration file, so you can log changes for compliance or block unauthorized modifications.

This example appends each change to an audit log. Add this to `~/.claude/settings.json`:

```json theme={null}
{
  "hooks": {
    "ConfigChange": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "jq -c '{timestamp: now | todate, source: .source, file: .file_path}' >> ~/claude-config-audit.log"
          }
        ]
      }
    ]
  }
}
```

The matcher filters by configuration type: `user_settings`, `project_settings`, `local_settings`, `policy_settings`, or `skills`. To block a change from taking effect, exit with code 2 or return `{"decision": "block"}`. See the [ConfigChange reference](/en/hooks#configchange) for the full input schema.

### Reload environment when directory or files change

Some projects set different environment variables depending on which directory you are in. Tools like [direnv](https://direnv.net/) do this automatically in your shell, but Claude's Bash tool does not pick up those changes on its own.

Pairing a `SessionStart` hook with a `CwdChanged` hook fixes this. `SessionStart` loads the variables for the directory you launch in, and `CwdChanged` reloads them each time Claude changes directory. Both write to `CLAUDE_ENV_FILE`, which Claude Code runs as a script preamble before each Bash command. Add this to `~/.claude/settings.json`:

```json theme={null}
{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "direnv export bash > \"$CLAUDE_ENV_FILE\""
          }
        ]
      }
    ],
    "CwdChanged": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "direnv export bash > \"$CLAUDE_ENV_FILE\""
          }
        ]
      }
    ]
  }
}
```

Run `direnv allow` once in each directory that has an `.envrc` so direnv is permitted to load it. If you use devbox or nix instead of direnv, the same pattern works with `devbox shellenv` or `devbox global shellenv` in place of `direnv export bash`.

To react to specific files instead of every directory change, use `FileChanged` with a `matcher` listing the filenames to watch, separated by `|`. To build the watch list, this value is split into literal filenames rather than evaluated as a regex. See [FileChanged](/en/hooks#filechanged) for how the same value also filters which hook groups run when a file changes. This example watches `.envrc` and `.env` in the working directory:

```json theme={null}
{
  "hooks": {
    "FileChanged": [
      {
        "matcher": ".envrc|.env",
        "hooks": [
          {
            "type": "command",
            "command": "direnv export bash > \"$CLAUDE_ENV_FILE\""
          }
        ]
      }
    ]
  }
}
```

See the [CwdChanged](/en/hooks#cwdchanged) and [FileChanged](/en/hooks#filechanged) reference entries for input schemas, `watchPaths` output, and `CLAUDE_ENV_FILE` details.

### Auto-approve specific permission prompts

Skip the approval dialog for tool calls you always allow. This example auto-approves `ExitPlanMode`, the tool Claude calls when it finishes presenting a plan and asks to proceed, so you aren't prompted every time a plan is ready.

Unlike the exit-code examples above, auto-approval requires your hook to write a JSON decision to stdout. A `PermissionRequest` hook fires when Claude Code is about to show a permission dialog, and returning `"behavior": "allow"` answers it on your behalf.

The matcher scopes the hook to `ExitPlanMode` only, so no other prompts are affected. Add this to `~/.claude/settings.json`:

```json theme={null}
{
  "hooks": {
    "PermissionRequest": [
      {
        "matcher": "ExitPlanMode",
        "hooks": [
          {
            "type": "command",
            "command": "echo '{\"hookSpecificOutput\": {\"hookEventName\": \"PermissionRequest\", \"decision\": {\"behavior\": \"allow\"}}}'"
          }
        ]
      }
    ]
  }
}
```

When the hook approves, Claude Code exits plan mode and restores whatever permission mode was active before you entered plan mode. The transcript shows "Allowed by PermissionRequest hook" where the dialog would have appeared. The hook path always keeps the current conversation: it cannot clear context and start a fresh implementation session the way the dialog can.

To set a specific permission mode instead, your hook's output can include an `updatedPermissions` array with a `setMode` entry. The `mode` value is any permission mode like `default`, `acceptEdits`, or `bypassPermissions`, and `destination: "session"` applies it for the current session only.

<Note>
  `bypassPermissions` only applies if the session was launched with bypass mode already available: `--dangerously-skip-permissions`, `--permission-mode bypassPermissions`, `--allow-dangerously-skip-permissions`, or `permissions.defaultMode: "bypassPermissions"` in settings, and not disabled by [`permissions.disableBypassPermissionsMode`](/en/permissions#managed-settings). It is never persisted as `defaultMode`.
</Note>

To switch the session to `acceptEdits`, your hook writes this JSON to stdout:

```json theme={null}
{
  "hookSpecificOutput": {
    "hookEventName": "PermissionRequest",
    "decision": {
      "behavior": "allow",
      "updatedPermissions": [
        { "type": "setMode", "mode": "acceptEdits", "destination": "session" }
      ]
    }
  }
}
```

Keep the matcher as narrow as possible. Matching on `.*` or leaving the matcher empty would auto-approve every permission prompt, including file writes and shell commands. See the [PermissionRequest reference](/en/hooks#permissionrequest-decision-control) for the full set of decision fields.

## How hooks work

Hook events fire at specific lifecycle points in Claude Code. When an event fires, all matching hooks run in parallel, and identical hook commands are automatically deduplicated. The table below shows each event and when it triggers:

| Event                 | When it fires                                                                                                                                          |
| :-------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `SessionStart`        | When a session begins or resumes                                                                                                                       |
| `Setup`               | When you start Claude Code with `--init-only`, or with `--init` or `--maintenance` in `-p` mode. For one-time preparation in CI or scripts             |
| `UserPromptSubmit`    | When you submit a prompt, before Claude processes it                                                                                                   |
| `UserPromptExpansion` | When a user-typed command expands into a prompt, before it reaches Claude. Can block the expansion                                                     |
| `PreToolUse`          | Before a tool call executes. Can block it                                                                                                              |
| `PermissionRequest`   | When a permission dialog appears                                                                                                                       |
| `PermissionDenied`    | When a tool call is denied by the auto mode classifier. Return `{retry: true}` to tell the model it may retry the denied tool call                     |
| `PostToolUse`         | After a tool call succeeds                                                                                                                             |
| `PostToolUseFailure`  | After a tool call fails                                                                                                                                |
| `PostToolBatch`       | After a full batch of parallel tool calls resolves, before the next model call                                                                         |
| `Notification`        | When Claude Code sends a notification                                                                                                                  |
| `MessageDisplay`      | While assistant message text is displayed                                                                                                              |
| `SubagentStart`       | When a subagent is spawned                                                                                                                             |
| `SubagentStop`        | When a subagent finishes                                                                                                                               |
| `TaskCreated`         | When a task is being created via `TaskCreate`                                                                                                          |
| `TaskCompleted`       | When a task is being marked as completed                                                                                                               |
| `Stop`                | When Claude finishes responding                                                                                                                        |
| `StopFailure`         | When the turn ends due to an API error. Output and exit code are ignored                                                                               |
| `TeammateIdle`        | When an [agent team](/en/agent-teams) teammate is about to go idle                                                                                     |
| `InstructionsLoaded`  | When a CLAUDE.md or `.claude/rules/*.md` file is loaded into context. Fires at session start and when files are lazily loaded during a session         |
| `ConfigChange`        | When a configuration file changes during a session                                                                                                     |
| `CwdChanged`          | When the working directory changes, for example when Claude executes a `cd` command. Useful for reactive environment management with tools like direnv |
| `FileChanged`         | When a watched file changes on disk. The `matcher` field specifies which filenames to watch                                                            |
| `WorktreeCreate`      | When a worktree is being created via `--worktree` or `isolation: "worktree"`. Replaces default git behavior                                            |
| `WorktreeRemove`      | When a worktree is being removed, either at session exit or when a subagent finishes                                                                   |
| `PreCompact`          | Before context compaction                                                                                                                              |
| `PostCompact`         | After context compaction completes                                                                                                                     |
| `Elicitation`         | When an MCP server requests user input during a tool call                                                                                              |
| `ElicitationResult`   | After a user responds to an MCP elicitation, before the response is sent back to the server                                                            |
| `SessionEnd`          | When a session terminates                                                                                                                              |

Each hook has a `type` that determines how it runs. Most hooks use `"type": "command"`, which runs a shell command. Four other types are available:

* `"type": "http"`: POST event data to a URL. See [HTTP hooks](#http-hooks).
* `"type": "mcp_tool"`: call a tool on an already-connected MCP server. See [MCP tool hooks](/en/hooks#mcp-tool-hook-fields).
* `"type": "prompt"`: single-turn LLM evaluation. See [Prompt-based hooks](#prompt-based-hooks).
* `"type": "agent"`: multi-turn verification with tool access. Agent hooks are experimental and may change. See [Agent-based hooks](#agent-based-hooks).

### Combine results from multiple hooks

When multiple hooks match the same event, every hook's command runs to completion before Claude Code merges the results. One hook returning `deny` does not stop sibling hooks from executing. Don't rely on one hook's `deny` to suppress side effects in another hook.

After all matching hooks finish, Claude Code combines their outputs. For `PreToolUse` permission decisions, the most restrictive answer wins, in the order `deny`, `defer`, `ask`, `allow`. Text from `additionalContext` is kept from every hook and passed to Claude together.

The example below registers two `PreToolUse` hooks on `Bash`. The first appends every command to a log file and exits 0. The second runs a script that exits 2 to deny when the command contains `rm -rf`:

```json theme={null}
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r .tool_input.command >> ~/.claude/bash.log"
          },
          {
            "type": "command",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/block-rm-rf.sh"
          }
        ]
      }
    ]
  }
}
```

When Claude tries to run `rm -rf /tmp/build`, both hooks execute in parallel. The logging hook writes the command to `~/.claude/bash.log` and exits 0, which reports no decision. The guardrail hook exits 2, which denies the tool call. The deny wins, so Claude Code blocks the command and shows Claude the guardrail's stderr. The log entry is still written because the logging hook already ran.

### Read input and return output

Hooks communicate with Claude Code through stdin, stdout, stderr, and exit codes. When an event fires, Claude Code passes event-specific data as JSON to your script's stdin. Your script reads that data, does its work, and tells Claude Code what to do next via the exit code.

#### Hook input

Every event includes common fields like `session_id` and `cwd`, but each event type adds different data. For example, when Claude runs a Bash command, a `PreToolUse` hook receives something like this on stdin:

```json theme={null}
{
  "session_id": "abc123",          // unique ID for this session
  "cwd": "/Users/sarah/myproject", // working directory when the event fired
  "hook_event_name": "PreToolUse", // which event triggered this hook
  "tool_name": "Bash",             // the tool Claude is about to use
  "tool_input": {                  // the arguments Claude passed to the tool
    "command": "npm test"          // for Bash, this is the shell command
  }
}
```

Your script can parse that JSON and act on any of those fields. `UserPromptSubmit` hooks get the `prompt` text instead, `SessionStart` hooks get the `source` (startup, resume, clear, compact), and so on. See [Common input fields](/en/hooks#common-input-fields) in the reference for shared fields, and each event's section for event-specific schemas.

#### Hook output

Your script tells Claude Code what to do next by writing to stdout or stderr and exiting with a specific code. For example, a `PreToolUse` hook that wants to block a command:

```bash theme={null}
#!/bin/bash
INPUT=$(cat)
COMMAND=$(echo "$INPUT" | jq -r '.tool_input.command')

if echo "$COMMAND" | grep -q "drop table"; then
  echo "Blocked: dropping tables is not allowed" >&2  # stderr becomes Claude's feedback
  exit 2                                               # exit 2 = block the action
fi

exit 0  # exit 0 = no decision; the normal permission flow applies
```

The exit code determines what happens next:

* **Exit 0**: the hook reports no objection and the action proceeds normally. For a `PreToolUse` hook this doesn't approve the tool call: the normal [permission flow](/en/permissions) still applies. For `UserPromptSubmit`, `UserPromptExpansion`, and `SessionStart` hooks, anything you write to stdout is added to Claude's context.
* **Exit 2**: the action is blocked. Write a reason to stderr, and Claude receives it as feedback so it can adjust. Some events cannot be blocked: for `SessionStart`, `Setup`, `Notification`, and others, exit 2 shows stderr to the user and execution continues. See [exit code 2 behavior per event](/en/hooks#exit-code-2-behavior-per-event) for the full list.
* **Any other exit code**: the action proceeds. The transcript shows a `<hook name> hook error` notice followed by the first line of stderr; the full stderr goes to the [debug log](/en/hooks#debug-hooks).

#### Structured JSON output

Exit codes only let you block or stay silent. For more control, exit 0 and print a JSON object to stdout instead.

<Note>
  Use exit 2 to block with a stderr message, or exit 0 with JSON for structured control. Don't mix them: Claude Code ignores JSON when you exit 2.
</Note>

For example, a `PreToolUse` hook can deny a tool call and tell Claude why, or escalate it to the user for approval:

```json theme={null}
{
  "hookSpecificOutput": {
    "hookEventName": "PreToolUse",
    "permissionDecision": "deny",
    "permissionDecisionReason": "Use rg instead of grep for better performance"
  }
}
```

With `"deny"`, Claude Code cancels the tool call and feeds `permissionDecisionReason` back to Claude. These `permissionDecision` values are specific to `PreToolUse`:

* `"allow"`: skip the interactive permission prompt. Deny and ask rules, including enterprise managed deny lists, still apply
* `"deny"`: cancel the tool call and send the reason to Claude
* `"ask"`: show the permission prompt to the user as normal

A fourth value, `"defer"`, is available in [non-interactive mode](/en/headless) with the `-p` flag. It exits the process with the tool call preserved so an Agent SDK wrapper can collect input and resume. See [Defer a tool call for later](/en/hooks#defer-a-tool-call-for-later) in the reference.

Returning `"allow"` skips the interactive prompt but does not override [permission rules](/en/permissions#manage-permissions). If a deny rule matches the tool call, the call is blocked even when your hook returns `"allow"`. If an ask rule matches, the user is still prompted. This means deny rules from any settings scope, including [managed settings](/en/settings#settings-files), always take precedence over hook approvals.

Other events use different decision patterns. For example, `PostToolUse` and `Stop` hooks use a top-level `decision: "block"` field, while `PermissionRequest` uses `hookSpecificOutput.decision.behavior`. See the [summary table](/en/hooks#decision-control) in the reference for a full breakdown by event.

For `UserPromptSubmit` hooks, use `additionalContext` instead to inject text into Claude's context. Prompt-based hooks (`type: "prompt"`) handle output differently: see [Prompt-based hooks](#prompt-based-hooks).

### Filter hooks with matchers

Without a matcher, a hook fires on every occurrence of its event. Matchers let you narrow that down. For example, if you want to run a formatter only after file edits (not after every tool call), add a matcher to your `PostToolUse` hook:

```json theme={null}
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          { "type": "command", "command": "prettier --write ..." }
        ]
      }
    ]
  }
}
```

The `"Edit|Write"` matcher fires only when Claude uses the `Edit` or `Write` tool, not when it uses `Bash`, `Read`, or any other tool. See [Matcher patterns](/en/hooks#matcher-patterns) for how plain names and regular expressions are evaluated.

<Note>
  Claude can also create or modify files by running shell commands through the `Bash` tool. If your hook must see every file change, such as for compliance scanning or audit logging, add a [`Stop`](/en/hooks#stop) hook that scans the working tree once per turn. For per-call coverage instead, also match `Bash` and have your script list modified and untracked files with `git status --porcelain`.
</Note>

Each event type matches on a specific field:

| Event                                                                                                                                                           | What the matcher filters                                              | Example matcher values                                                                                                                                                              |
| :-------------------------------------------------------------------------------------------------------------------------------------------------------------- | :-------------------------------------------------------------------- | :---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `PreToolUse`, `PostToolUse`, `PostToolUseFailure`, `PermissionRequest`, `PermissionDenied`                                                                      | tool name                                                             | `Bash`, `Edit\|Write`, `mcp__.*`                                                                                                                                                    |
| `SessionStart`                                                                                                                                                  | how the session started                                               | `startup`, `resume`, `clear`, `compact`                                                                                                                                             |
| `Setup`                                                                                                                                                         | which CLI flag triggered setup                                        | `init`, `maintenance`                                                                                                                                                               |
| `SessionEnd`                                                                                                                                                    | why the session ended                                                 | `clear`, `resume`, `logout`, `prompt_input_exit`, `bypass_permissions_disabled`, `other`                                                                                            |
| `Notification`                                                                                                                                                  | notification type                                                     | `permission_prompt`, `idle_prompt`, `auth_success`, `elicitation_dialog`, `elicitation_complete`, `elicitation_response`                                                            |
| `SubagentStart`                                                                                                                                                 | agent type                                                            | `general-purpose`, `Explore`, `Plan`, or custom agent names                                                                                                                         |
| `PreCompact`, `PostCompact`                                                                                                                                     | what triggered compaction                                             | `manual`, `auto`                                                                                                                                                                    |
| `SubagentStop`                                                                                                                                                  | agent type                                                            | same values as `SubagentStart`                                                                                                                                                      |
| `ConfigChange`                                                                                                                                                  | configuration source                                                  | `user_settings`, `project_settings`, `local_settings`, `policy_settings`, `skills`                                                                                                  |
| `StopFailure`                                                                                                                                                   | error type                                                            | `rate_limit`, `overloaded`, `authentication_failed`, `oauth_org_not_allowed`, `billing_error`, `invalid_request`, `model_not_found`, `server_error`, `max_output_tokens`, `unknown` |
| `InstructionsLoaded`                                                                                                                                            | load reason                                                           | `session_start`, `nested_traversal`, `path_glob_match`, `include`, `compact`                                                                                                        |
| `Elicitation`                                                                                                                                                   | MCP server name                                                       | your configured MCP server names                                                                                                                                                    |
| `ElicitationResult`                                                                                                                                             | MCP server name                                                       | same values as `Elicitation`                                                                                                                                                        |
| `FileChanged`                                                                                                                                                   | literal filenames to watch (see [FileChanged](/en/hooks#filechanged)) | `.envrc\|.env`                                                                                                                                                                      |
| `UserPromptExpansion`                                                                                                                                           | command name                                                          | your skill or command names                                                                                                                                                         |
| `UserPromptSubmit`, `PostToolBatch`, `Stop`, `TeammateIdle`, `TaskCreated`, `TaskCompleted`, `WorktreeCreate`, `WorktreeRemove`, `CwdChanged`, `MessageDisplay` | no matcher support                                                    | always fires on every occurrence                                                                                                                                                    |

A few more examples showing matchers on different event types:

<Tabs>
  <Tab title="Log every Bash command">
    Match only `Bash` tool calls and log each command to a file. The `PostToolUse` event fires after the command completes, so `tool_input.command` contains what ran. The hook receives the event data as JSON on stdin, and `jq -r '.tool_input.command'` extracts just the command string, which `>>` appends to the log file:

    ```json theme={null}
    {
      "hooks": {
        "PostToolUse": [
          {
            "matcher": "Bash",
            "hooks": [
              {
                "type": "command",
                "command": "jq -r '.tool_input.command' >> ~/.claude/command-log.txt"
              }
            ]
          }
        ]
      }
    }
    ```
  </Tab>

  <Tab title="Match MCP tools">
    MCP tools use a different naming convention than built-in tools: `mcp__<server>__<tool>`, where `<server>` is the MCP server name and `<tool>` is the tool it provides. For example, `mcp__github__search_repositories` or `mcp__filesystem__read_file`. Use a regex matcher to target all tools from a specific server, or match across servers with a pattern like `mcp__.*__write.*`. See [Match MCP tools](/en/hooks#match-mcp-tools) in the reference for the full list of examples.

    The command below extracts the tool name from the hook's JSON input with `jq` and writes it to stderr. Writing to stderr keeps stdout clean for JSON output and sends the message to the [debug log](/en/hooks#debug-hooks):

    ```json theme={null}
    {
      "hooks": {
        "PreToolUse": [
          {
            "matcher": "mcp__github__.*",
            "hooks": [
              {
                "type": "command",
                "command": "echo \"GitHub tool called: $(jq -r '.tool_name')\" >&2"
              }
            ]
          }
        ]
      }
    }
    ```
  </Tab>

  <Tab title="Clean up on session end">
    The `SessionEnd` event supports matchers on the reason the session ended. This hook only fires on `clear` (when you run `/clear`), not on normal exits:

    ```json theme={null}
    {
      "hooks": {
        "SessionEnd": [
          {
            "matcher": "clear",
            "hooks": [
              {
                "type": "command",
                "command": "rm -f /tmp/claude-scratch-*.txt"
              }
            ]
          }
        ]
      }
    }
    ```
  </Tab>
</Tabs>

For full matcher syntax, see the [Hooks reference](/en/hooks#configuration).

#### Filter by tool name and arguments with the `if` field

<Note>
  The `if` field requires Claude Code v2.1.85 or later. Earlier versions ignore it and run the hook on every matched call.
</Note>

The `if` field uses [permission rule syntax](/en/permissions) to filter hooks by tool name and arguments together, so the hook process only spawns when the tool call matches. This goes beyond `matcher`, which filters at the group level by tool name only.

For example, to run a hook only when Claude uses `git` commands rather than all Bash commands:

```json theme={null}
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git *)",
            "command": "\"$CLAUDE_PROJECT_DIR\"/.claude/hooks/check-git-policy.sh"
          }
        ]
      }
    ]
  }
}
```

Whether your hook command runs depends on the shape of your `if` pattern and the Bash command Claude is invoking:

| `if` pattern       | Bash command           | Hook runs? | Why                                                                                                 |
| :----------------- | :--------------------- | :--------- | :-------------------------------------------------------------------------------------------------- |
| `Bash(git *)`      | `git push`             | yes        | command name matches                                                                                |
| `Bash(git *)`      | `npm test && git push` | yes        | each subcommand is checked; `git push` matches                                                      |
| `Bash(git *)`      | `echo $(git log)`      | yes        | commands inside `$()` and backticks are checked; `git log` matches                                  |
| `Bash(git *)`      | `echo $(date)`         | no         | no subcommand matches `git *`                                                                       |
| `Bash(git push *)` | `echo $(date)`         | yes        | patterns that specify more than the command name run the hook anyway on `$()`, backticks, or `$VAR` |

The filter also fails open, running your hook regardless of pattern, when the Bash command cannot be parsed. Because the filter is best-effort, use the [permission system](/en/permissions) rather than a hook to enforce a hard allow or deny.

The `if` field accepts the same patterns as permission rules: `"Bash(git *)"`, `"Edit(*.ts)"`, and so on. To match multiple tool names, use separate handlers each with its own `if` value, or match at the `matcher` level where pipe alternation is supported.

`if` only works on tool events: `PreToolUse`, `PostToolUse`, `PostToolUseFailure`, `PermissionRequest`, and `PermissionDenied`. Adding it to any other event prevents the hook from running.

### Configure hook location

Where you add a hook determines its scope:

| Location                                                   | Scope                              | Shareable                                  |
| :--------------------------------------------------------- | :--------------------------------- | :----------------------------------------- |
| `~/.claude/settings.json`                                  | All your projects                  | No, local to your machine                  |
| `.claude/settings.json`                                    | Single project                     | Yes, can be committed to the repo          |
| `.claude/settings.local.json`                              | Single project                     | No, gitignored when Claude Code creates it |
| Managed policy settings                                    | Organization-wide                  | Yes, admin-controlled                      |
| [Plugin](/en/plugins) `hooks/hooks.json`                   | When plugin is enabled             | Yes, bundled with the plugin               |
| [Skill](/en/skills) or [agent](/en/sub-agents) frontmatter | While the skill or agent is active | Yes, defined in the component file         |

Run [`/hooks`](/en/hooks#the-%2Fhooks-menu) in Claude Code to browse all configured hooks grouped by event. To disable hooks, set `"disableAllHooks": true` in your settings file. Hooks configured in managed settings still run unless `disableAllHooks` is also set there.

If you edit settings files directly while Claude Code is running, the file watcher normally picks up hook changes automatically.

## Prompt-based hooks

For decisions that require judgment rather than deterministic rules, use `type: "prompt"` hooks. Instead of running a shell command, Claude Code sends your prompt and the hook's input data to a Claude model (Haiku by default) to make the decision. You can specify a different model with the `model` field if you need more capability.

The model's only job is to return a yes/no decision as JSON:

* `"ok": true`: the action proceeds
* `"ok": false`: what happens depends on the event:
  * `Stop` and `SubagentStop`: the `reason` is fed back to Claude so it keeps working
  * `PreToolUse`: the tool call is denied and the `reason` is returned to Claude as the tool error, so it can adjust and continue
  * `PostToolUse`, `PostToolBatch`, `UserPromptSubmit`, and `UserPromptExpansion`: the turn ends and the `reason` appears in the chat as a warning line

This example uses a `Stop` hook to ask the model whether all requested tasks are complete. If the model returns `"ok": false`, Claude keeps working and uses the `reason` as its next instruction:

```json theme={null}
{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "prompt",
            "prompt": "Check if all tasks are complete. If not, respond with {\"ok\": false, \"reason\": \"what remains to be done\"}."
          }
        ]
      }
    ]
  }
}
```

For full configuration options, see [Prompt-based hooks](/en/hooks#prompt-based-hooks) in the reference.

## Agent-based hooks

<Warning>
  Agent hooks are experimental. Behavior and configuration may change in future releases. For production workflows, prefer [command hooks](/en/hooks#command-hook-fields).
</Warning>

When verification requires inspecting files or running commands, use `type: "agent"` hooks. Unlike prompt hooks which make a single LLM call, agent hooks spawn a subagent that can read files, search code, and use other tools to verify conditions before returning a decision.

Agent hooks use the same `"ok"` / `"reason"` response format as prompt hooks, but with a longer default timeout of 60 seconds and up to 50 tool-use turns.

This example verifies that tests pass before allowing Claude to stop:

```json theme={null}
{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "agent",
            "prompt": "Verify that all unit tests pass. Run the test suite and check the results. $ARGUMENTS",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
```

Use prompt hooks when the hook input data alone is enough to make a decision. Use agent hooks when you need to verify something against the actual state of the codebase.

For full configuration options, see [Agent-based hooks](/en/hooks#agent-based-hooks) in the reference.

## HTTP hooks

Use `type: "http"` hooks to POST event data to an HTTP endpoint instead of running a shell command. The endpoint receives the same JSON that a command hook would receive on stdin, and returns results through the HTTP response body using the same JSON format.

HTTP hooks are useful when you want a web server, cloud function, or external service to handle hook logic: for example, a shared audit service that logs tool use events across a team.

This example posts every tool use to a local logging service:

```json theme={null}
{
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "http",
            "url": "http://localhost:8080/hooks/tool-use",
            "headers": {
              "Authorization": "Bearer $MY_TOKEN"
            },
            "allowedEnvVars": ["MY_TOKEN"]
          }
        ]
      }
    ]
  }
}
```

The endpoint should return a JSON response body using the same [output format](/en/hooks#json-output) as command hooks. To block a tool call, return a 2xx response with the appropriate `hookSpecificOutput` fields. HTTP status codes alone cannot block actions.

Header values support environment variable interpolation using `$VAR_NAME` or `${VAR_NAME}` syntax. Only variables listed in the `allowedEnvVars` array are resolved; all other `$VAR` references remain empty.

For full configuration options and response handling, see [HTTP hooks](/en/hooks#http-hook-fields) in the reference.

## Limitations and troubleshooting

### Limitations

* Command hooks communicate through stdout, stderr, and exit codes only. They cannot trigger `/` commands or tool calls. Text returned via `additionalContext` is injected as a system reminder that Claude reads as plain text. HTTP hooks communicate through the response body instead.
* Hook timeouts vary by type. Override per hook with the `timeout` field in seconds.
  * `command`, `http`, `mcp_tool`: 10 minutes. `UserPromptSubmit` lowers these to 30 seconds, and `MessageDisplay` lowers them to 10 seconds.
  * `prompt`: 30 seconds.
  * `agent`: 60 seconds.
* `PostToolUse` hooks cannot undo actions since the tool has already executed.
* `PermissionRequest` hooks do not fire in [non-interactive mode](/en/headless) (`-p`). Use `PreToolUse` hooks for automated permission decisions.
* `Stop` hooks fire whenever Claude finishes responding, not only at task completion. They do not fire on user interrupts. API errors fire [StopFailure](/en/hooks#stopfailure) instead.
* When multiple PreToolUse hooks return [`updatedInput`](/en/hooks#pretooluse) to rewrite a tool's arguments, the last one to finish wins. Since hooks run in parallel, the order is non-deterministic. Avoid having more than one hook modify the same tool's input.

### Hooks and permission modes

PreToolUse hooks fire before any permission-mode check. A hook that returns `permissionDecision: "deny"` blocks the tool even in `bypassPermissions` mode or with `--dangerously-skip-permissions`. This lets you enforce policy that users cannot bypass by changing their permission mode.

The reverse is not true: a hook returning `"allow"` does not bypass deny rules from settings. Hooks can tighten restrictions but not loosen them past what permission rules allow.

### Hook not firing

The hook is configured but never executes.

* Run `/hooks` and confirm the hook appears under the correct event
* Check that the matcher pattern matches the tool name exactly (matchers are case-sensitive)
* Verify you're triggering the right event type (e.g., `PreToolUse` fires before tool execution, `PostToolUse` fires after)
* If using `PermissionRequest` hooks in non-interactive mode (`-p`), switch to `PreToolUse` instead

### Hook error in output

You see a message like "PreToolUse hook error: ..." in the transcript.

* Your script exited with a non-zero code unexpectedly. Test it manually by piping sample JSON:
  ```bash theme={null}
  echo '{"tool_name":"Bash","tool_input":{"command":"ls"}}' | ./my-hook.sh
  echo $?  # Check the exit code
  ```
* If you see "command not found", use absolute paths or `${CLAUDE_PROJECT_DIR}` to reference scripts. To avoid shell quoting entirely, add `"args": []` to switch to [exec form](/en/hooks#exec-form-and-shell-form), which spawns the script directly without a shell
* If you see "jq: command not found", install `jq` or use Python/Node.js for JSON parsing
* If the script isn't running at all, make it executable: `chmod +x ./my-hook.sh`

### `/hooks` shows no hooks configured

You edited a settings file but the hooks don't appear in the menu.

* File edits are normally picked up automatically. If they haven't appeared after a few seconds, the file watcher may have missed the change: restart your session to force a reload.
* Verify your JSON is valid (trailing commas and comments are not allowed)
* Confirm the settings file is in the correct location: `.claude/settings.json` for project hooks, `~/.claude/settings.json` for global hooks

### Stop hook hits the block cap

Claude keeps working instead of stopping, then ends the turn with a warning that the Stop hook blocked too many consecutive times.

Claude Code overrides a Stop hook after it blocks 8 times in a row without progress. Your hook script needs to check whether it already triggered a continuation. Parse the `stop_hook_active` field from the JSON input and exit early if it's `true`:

```bash theme={null}
#!/bin/bash
INPUT=$(cat)
if [ "$(echo "$INPUT" | jq -r '.stop_hook_active')" = "true" ]; then
  exit 0  # Allow Claude to stop
fi
# ... rest of your hook logic
```

If your hook legitimately needs more than eight iterations to converge, raise the cap with [`CLAUDE_CODE_STOP_HOOK_BLOCK_CAP`](/en/env-vars).

### JSON validation failed

Claude Code shows a JSON parsing error even though your hook script outputs valid JSON.

When Claude Code runs a shell-form command hook (one without `args`), it spawns `sh -c` on macOS and Linux or Git Bash on Windows by default. This shell is non-interactive, but Git Bash and some configurations (such as `BASH_ENV` pointing at `~/.bashrc`) still source your profile. If that profile contains unconditional `echo` statements, the output gets prepended to your hook's JSON:

```text theme={null}
Shell ready on arm64
{"decision": "block", "reason": "Not allowed"}
```

Claude Code tries to parse this as JSON and fails. To fix this, wrap echo statements in your shell profile so they only run in interactive shells:

```bash theme={null}
# In ~/.zshrc or ~/.bashrc
if [[ $- == *i* ]]; then
  echo "Shell ready"
fi
```

The `$-` variable contains shell flags, and `i` means interactive. Hooks run in non-interactive shells, so the echo is skipped.

### Debug techniques

The transcript view, toggled with `Ctrl+O`, shows a one-line summary for each hook that fired: success is silent, blocking errors show stderr, and non-blocking errors show a `<hook name> hook error` notice followed by the first line of stderr.

For full execution details including which hooks matched, their exit codes, stdout, and stderr, read the debug log. Start Claude Code with `claude --debug-file /tmp/claude.log` to write to a known path, then `tail -f /tmp/claude.log` in another terminal. If you started without that flag, run `/debug` mid-session to enable logging and find the log path.

## Learn more

* [Hooks reference](/en/hooks): full event schemas, JSON output format, async hooks, and MCP tool hooks
* [Security considerations](/en/hooks#security-considerations): review before deploying hooks in shared or production environments
* [Bash command validator example](https://github.com/anthropics/claude-code/blob/main/examples/hooks/bash_command_validator_example.py): complete reference implementation

---

lets use more hooks

---

> ## Documentation Index
> Fetch the complete documentation index at: https://code.claude.com/docs/llms.txt
> Use this file to discover all available pages before exploring further.

# Create custom subagents

> Create and use specialized AI subagents in Claude Code for task-specific workflows and improved context management.

Subagents are specialized AI assistants that handle specific types of tasks. Use one when a side task would flood your main conversation with search results, logs, or file contents you won't reference again: the subagent does that work in its own context and returns only the summary. Define a custom subagent when you keep spawning the same kind of worker with the same instructions.

Each subagent runs in its own context window with a custom system prompt, specific tool access, and independent permissions. When Claude encounters a task that matches a subagent's description, it delegates to that subagent, which works independently and returns results. To see the context savings in practice, the [context window visualization](/en/context-window) walks through a session where a subagent handles research in its own separate window.

<Note>
  Subagents work within a single session. To run many independent sessions in parallel and monitor them from one place, see [background agents](/en/agent-view). For sessions that communicate with each other, see [agent teams](/en/agent-teams).
</Note>

Subagents help you:

* **Preserve context** by keeping exploration and implementation out of your main conversation
* **Enforce constraints** by limiting which tools a subagent can use
* **Reuse configurations** across projects with user-level subagents
* **Specialize behavior** with focused system prompts for specific domains
* **Control costs** by routing tasks to faster, cheaper models like Haiku

Claude uses each subagent's description to decide when to delegate tasks. When you create a subagent, write a clear description so Claude knows when to use it.

Claude Code includes several built-in subagents like **Explore**, **Plan**, and **general-purpose**. You can also create custom subagents to handle specific tasks. This page covers:

* [Built-in subagents](#built-in-subagents)
* [How to create your own](#quickstart-create-your-first-subagent)
* [Full configuration options](#configure-subagents)
* [Patterns for working with subagents](#work-with-subagents)
* [Forked subagents](#fork-the-current-conversation)
* [Example subagents](#example-subagents)

## Built-in subagents

Claude Code includes built-in subagents that Claude automatically uses when appropriate. Each inherits the parent conversation's permissions with additional tool restrictions.

Explore and Plan skip your CLAUDE.md files and the parent session's git status to keep research fast and inexpensive. Every other built-in and [custom subagent](#configure-subagents) loads both. For the full breakdown of what reaches a subagent, see [what loads at startup](#what-loads-at-startup).

<Tabs>
  <Tab title="Explore">
    A fast, read-only agent optimized for searching and analyzing codebases.

    * **Model**: Haiku (fast, low-latency)
    * **Tools**: Read-only tools (denied access to Write and Edit tools)
    * **Purpose**: File discovery, code search, codebase exploration

    Claude delegates to Explore when it needs to search or understand a codebase without making changes. This keeps exploration results out of your main conversation context.

    When invoking Explore, Claude specifies a thoroughness level: **quick** for targeted lookups, **medium** for balanced exploration, or **very thorough** for comprehensive analysis.
  </Tab>

  <Tab title="Plan">
    A research agent used during [plan mode](/en/permission-modes#analyze-before-you-edit-with-plan-mode) to gather context before presenting a plan.

    * **Model**: Inherits from main conversation
    * **Tools**: Read-only tools (denied access to Write and Edit tools)
    * **Purpose**: Codebase research for planning

    When you're in plan mode and Claude needs to understand your codebase, it delegates research to the Plan subagent so that exploration output stays in a separate context window while the main conversation remains read-only.
  </Tab>

  <Tab title="General-purpose">
    A capable agent for complex, multi-step tasks that require both exploration and action.

    * **Model**: Inherits from main conversation
    * **Tools**: All tools
    * **Purpose**: Complex research, multi-step operations, code modifications

    Claude delegates to general-purpose when the task requires both exploration and modification, complex reasoning to interpret results, or multiple dependent steps.
  </Tab>

  <Tab title="Other">
    Claude Code includes additional helper agents for specific tasks. These are typically invoked automatically, so you don't need to use them directly.

    | Agent             | Model  | When Claude uses it                                      |
    | :---------------- | :----- | :------------------------------------------------------- |
    | statusline-setup  | Sonnet | When you run `/statusline` to configure your status line |
    | claude-code-guide | Haiku  | When you ask questions about Claude Code features        |
  </Tab>
</Tabs>

Built-in subagents are always registered in interactive sessions. To block a specific built-in type, add it to `permissions.deny` as shown in [Disable specific subagents](#disable-specific-subagents). To prevent Claude from delegating to any subagent, deny the `Agent` tool itself with [`permissions.deny`](/en/permissions#tool-specific-permission-rules). In [non-interactive mode](/en/headless) and the [Agent SDK](/en/agent-sdk/overview), set [`CLAUDE_AGENT_SDK_DISABLE_BUILTIN_AGENTS=1`](/en/env-vars) to remove all built-in types and supply only your own.

Beyond these built-in subagents, you can create your own with custom prompts, tool restrictions, permission modes, hooks, and skills. The following sections show how to get started and customize subagents.

## Quickstart: create your first subagent

Subagents are defined in Markdown files with YAML frontmatter. You can [create them manually](#write-subagent-files) or use the `/agents` command.

This walkthrough guides you through creating a user-level subagent with the `/agents` command. The subagent reviews code and suggests improvements for the codebase.

<Steps>
  <Step title="Open the subagents interface">
    In Claude Code, run:

    ```text theme={null}
    /agents
    ```
  </Step>

  <Step title="Choose a location">
    Switch to the **Library** tab, select **Create new agent**, then choose **Personal**. This saves the subagent to `~/.claude/agents/` so it's available in all your projects.
  </Step>

  <Step title="Generate with Claude">
    Select **Generate with Claude**. When prompted, describe the subagent:

    ```text theme={null}
    A code improvement agent that scans files and suggests improvements
    for readability, performance, and best practices. It should explain
    each issue, show the current code, and provide an improved version.
    ```

    Claude generates the identifier, description, and system prompt for you.
  </Step>

  <Step title="Select tools">
    For a read-only reviewer, deselect everything except **Read-only tools**. If you keep all tools selected, the subagent inherits all tools available to the main conversation.
  </Step>

  <Step title="Select model">
    Choose which model the subagent uses. For this example agent, select **Sonnet**, which balances capability and speed for analyzing code patterns.
  </Step>

  <Step title="Choose a color">
    Pick a background color for the subagent. This helps you identify which subagent is running in the UI.
  </Step>

  <Step title="Configure memory">
    Select **User scope** to give the subagent a [persistent memory directory](#enable-persistent-memory) at `~/.claude/agent-memory/`. The subagent uses this to accumulate insights across conversations, such as codebase patterns and recurring issues. Select **None** if you don't want the subagent to persist learnings.
  </Step>

  <Step title="Save and try it out">
    Review the configuration summary. Press `s` or `Enter` to save, or press `e` to save and edit the file in your editor. The subagent is available immediately. Try it:

    ```text theme={null}
    Use the code-improver agent to suggest improvements in this project
    ```

    Claude delegates to your new subagent, which scans the codebase and returns improvement suggestions.
  </Step>
</Steps>

You now have a subagent you can use in any project on your machine to analyze codebases and suggest improvements.

You can also create subagents manually as Markdown files, define them via CLI flags, or distribute them through plugins. The following sections cover all configuration options.

## Configure subagents

### Use the /agents command

The `/agents` command opens a tabbed interface for managing subagents. The **Running** tab lists live and recently finished subagents and lets you open or stop them. The **Library** tab lets you:

* View all available subagents (built-in, user, project, and plugin)
* Create new subagents with guided setup or Claude generation
* Edit existing subagent configuration and tool access
* Delete custom subagents
* See which subagents are active when duplicates exist

This is the recommended way to create and manage subagents. For manual creation or automation, you can also add subagent files directly.

### Choose the subagent scope

Subagents are Markdown files with YAML frontmatter. Store them in different locations depending on scope. When multiple subagents share the same name, the higher-priority location wins.

| Location                     | Scope                   | Priority    | How to create                                 |
| :--------------------------- | :---------------------- | :---------- | :-------------------------------------------- |
| Managed settings             | Organization-wide       | 1 (highest) | Deployed via [managed settings](/en/settings) |
| `--agents` CLI flag          | Current session         | 2           | Pass JSON when launching Claude Code          |
| `.claude/agents/`            | Current project         | 3           | Interactive or manual                         |
| `~/.claude/agents/`          | All your projects       | 4           | Interactive or manual                         |
| Plugin's `agents/` directory | Where plugin is enabled | 5 (lowest)  | Installed with [plugins](/en/plugins)         |

**Project subagents** (`.claude/agents/`) are ideal for subagents specific to a codebase. Check them into version control so your team can use and improve them collaboratively.

Project subagents are discovered by walking up from the current working directory. Directories added with `--add-dir` [grant file access only](/en/permissions#additional-directories-grant-file-access-not-configuration) and are not scanned for subagents. To share subagents across projects, use `~/.claude/agents/` or a [plugin](/en/plugins).

**User subagents** (`~/.claude/agents/`) are personal subagents available in all your projects.

Claude Code scans `.claude/agents/` and `~/.claude/agents/` recursively, so you can organize definitions into subfolders such as `agents/review/` or `agents/research/`. The subdirectory path does not affect how a subagent is identified or invoked, because identity comes only from the `name` frontmatter field. Keep `name` values unique across the whole tree: if two files within one scope declare the same name, Claude Code keeps one and discards the other without warning.

Plugin `agents/` directories are also scanned recursively. Unlike project and user scopes, a subfolder inside a plugin's `agents/` directory becomes part of the [scoped identifier](#invoke-subagents-explicitly): a file at `agents/review/security.md` in plugin `my-plugin` registers as `my-plugin:review:security`.

**CLI-defined subagents** are passed as JSON when launching Claude Code. They exist only for that session and aren't saved to disk, making them useful for quick testing or automation scripts. You can define multiple subagents in a single `--agents` call:

<Tabs>
  <Tab title="macOS, Linux, WSL">
    ```bash theme={null}
    claude --agents '{
      "code-reviewer": {
        "description": "Expert code reviewer. Use proactively after code changes.",
        "prompt": "You are a senior code reviewer. Focus on code quality, security, and best practices.",
        "tools": ["Read", "Grep", "Glob", "Bash"],
        "model": "sonnet"
      },
      "debugger": {
        "description": "Debugging specialist for errors and test failures.",
        "prompt": "You are an expert debugger. Analyze errors, identify root causes, and provide fixes."
      }
    }'
    ```
  </Tab>

  <Tab title="Windows PowerShell">
    ```powershell theme={null}
    claude --agents @'
    {
      "code-reviewer": {
        "description": "Expert code reviewer. Use proactively after code changes.",
        "prompt": "You are a senior code reviewer. Focus on code quality, security, and best practices.",
        "tools": ["Read", "Grep", "Glob", "Bash"],
        "model": "sonnet"
      },
      "debugger": {
        "description": "Debugging specialist for errors and test failures.",
        "prompt": "You are an expert debugger. Analyze errors, identify root causes, and provide fixes."
      }
    }
    '@
    ```
  </Tab>
</Tabs>

The `--agents` flag accepts JSON with the same [frontmatter](#supported-frontmatter-fields) fields as file-based subagents: `description`, `prompt`, `tools`, `disallowedTools`, `model`, `permissionMode`, `mcpServers`, `hooks`, `maxTurns`, `skills`, `initialPrompt`, `memory`, `effort`, `background`, `isolation`, and `color`. Use `prompt` for the system prompt, equivalent to the markdown body in file-based subagents.

**Managed subagents** are deployed by organization administrators. Place markdown files in `.claude/agents/` inside the [managed settings directory](/en/settings#settings-files), using the same frontmatter format as project and user subagents. Managed definitions take precedence over project and user subagents with the same name.

**Plugin subagents** come from [plugins](/en/plugins) you've installed. They appear in `/agents` alongside your custom subagents. See the [plugin components reference](/en/plugins-reference#agents) for details on creating plugin subagents.

<Note>
  For security reasons, plugin subagents do not support the `hooks`, `mcpServers`, or `permissionMode` frontmatter fields. These fields are ignored when loading agents from a plugin. If you need them, copy the agent file into `.claude/agents/` or `~/.claude/agents/`. You can also add rules to [`permissions.allow`](/en/settings#permission-settings) in `settings.json` or `settings.local.json`, but these rules apply to the entire session, not just the plugin subagent.
</Note>

Subagent definitions from any of these scopes are also available to [agent teams](/en/agent-teams#use-subagent-definitions-for-teammates): when spawning a teammate, you can reference a subagent type and the teammate uses its `tools` and `model`, with the definition's body appended to the teammate's system prompt as additional instructions. See [agent teams](/en/agent-teams#use-subagent-definitions-for-teammates) for which frontmatter fields apply on that path.

### Write subagent files

Subagent files use YAML frontmatter for configuration, followed by the system prompt in Markdown:

<Note>
  Subagents are loaded at session start. If you add or edit a subagent file directly on disk, restart your session to load it. Subagents created through the `/agents` interface take effect immediately without a restart.
</Note>

```markdown theme={null}
---
name: code-reviewer
description: Reviews code for quality and best practices
tools: Read, Glob, Grep
model: sonnet
---

You are a code reviewer. When invoked, analyze the code and provide
specific, actionable feedback on quality, security, and best practices.
```

The frontmatter defines the subagent's metadata and configuration. The body becomes the system prompt that guides the subagent's behavior. Subagents receive only this system prompt (plus basic environment details like working directory), not the full Claude Code system prompt.

A subagent starts in the main conversation's current working directory. Within a subagent, `cd` commands do not persist between Bash or PowerShell tool calls and do not affect the main conversation's working directory. To give the subagent an isolated copy of the repository instead, set [`isolation: worktree`](#supported-frontmatter-fields).

#### Supported frontmatter fields

The following fields can be used in the YAML frontmatter. Only `name` and `description` are required.

| Field             | Required | Description                                                                                                                                                                                                                                                                                                                              |
| :---------------- | :------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `name`            | Yes      | Unique identifier using lowercase letters and hyphens. [Hooks](/en/hooks#subagentstart) receive this value as `agent_type`. The filename does not have to match                                                                                                                                                                          |
| `description`     | Yes      | When Claude should delegate to this subagent                                                                                                                                                                                                                                                                                             |
| `tools`           | No       | [Tools](#available-tools) the subagent can use. Inherits all tools if omitted. To preload Skills into context, use the `skills` field rather than listing `Skill` here                                                                                                                                                                   |
| `disallowedTools` | No       | Tools to deny, removed from inherited or specified list                                                                                                                                                                                                                                                                                  |
| `model`           | No       | [Model](#choose-a-model) to use: `sonnet`, `opus`, `haiku`, `fable`, a full model ID (for example, `claude-opus-4-8`), or `inherit`. Defaults to `inherit`                                                                                                                                                                               |
| `permissionMode`  | No       | [Permission mode](#permission-modes): `default`, `acceptEdits`, `auto`, `dontAsk`, `bypassPermissions`, or `plan`. Ignored for [plugin subagents](#choose-the-subagent-scope)                                                                                                                                                            |
| `maxTurns`        | No       | Maximum number of agentic turns before the subagent stops                                                                                                                                                                                                                                                                                |
| `skills`          | No       | [Skills](/en/skills) to preload into the subagent's context at startup. The full skill content is injected, not just the description. Subagents can still invoke unlisted project, user, and plugin skills through the Skill tool                                                                                                        |
| `mcpServers`      | No       | [MCP servers](/en/mcp) available to this subagent. Each entry is either a server name referencing an already-configured server (e.g., `"slack"`) or an inline definition with the server name as key and a full [MCP server config](/en/mcp#installing-mcp-servers) as value. Ignored for [plugin subagents](#choose-the-subagent-scope) |
| `hooks`           | No       | [Lifecycle hooks](#define-hooks-for-subagents) scoped to this subagent. Ignored for [plugin subagents](#choose-the-subagent-scope)                                                                                                                                                                                                       |
| `memory`          | No       | [Persistent memory scope](#enable-persistent-memory): `user`, `project`, or `local`. Enables cross-session learning                                                                                                                                                                                                                      |
| `background`      | No       | Set to `true` to always run this subagent as a [background task](#run-subagents-in-foreground-or-background). Default: `false`                                                                                                                                                                                                           |
| `effort`          | No       | Effort level when this subagent is active. Overrides the session effort level. Default: inherits from session. Options: `low`, `medium`, `high`, `xhigh`, `max`; available levels depend on the model                                                                                                                                    |
| `isolation`       | No       | Set to `worktree` to run the subagent in a temporary [git worktree](/en/worktrees), giving it an isolated copy of the repository branched by default from your [default branch](/en/worktrees#choose-the-base-branch) rather than the parent session's `HEAD`. The worktree is automatically cleaned up if the subagent makes no changes |
| `color`           | No       | Display color for the subagent in the task list and transcript. Accepts `red`, `blue`, `green`, `yellow`, `purple`, `orange`, `pink`, or `cyan`                                                                                                                                                                                          |
| `initialPrompt`   | No       | Auto-submitted as the first user turn when this agent runs as the main session agent (via `--agent` or the `agent` setting). [Commands](/en/commands) and [skills](/en/skills) are processed. Prepended to any user-provided prompt                                                                                                      |

### Choose a model

The `model` field controls which [AI model](/en/model-config) the subagent uses:

* **Model alias**: Use one of the available aliases: `sonnet`, `opus`, `haiku`, or `fable`
* **Full model ID**: Use a full model ID such as `claude-opus-4-8` or `claude-sonnet-4-6`. Accepts the same values as the `--model` flag
* **inherit**: Use the same model as the main conversation
* **Omitted**: If not specified, defaults to `inherit` (uses the same model as the main conversation)

When Claude invokes a subagent, it can also pass a `model` parameter for that specific invocation. Claude Code resolves the subagent's model in this order:

1. The [`CLAUDE_CODE_SUBAGENT_MODEL`](/en/model-config#environment-variables) environment variable, if set
2. The per-invocation `model` parameter
3. The subagent definition's `model` frontmatter
4. The main conversation's model

### Control subagent capabilities

You can control what subagents can do through tool access, permission modes, and conditional rules.

#### Available tools

Subagents inherit the [internal tools](/en/tools-reference) and MCP tools available in the main conversation by default. The following tools depend on the main conversation's UI or session state and are not available to subagents, even when listed in the `tools` field:

* `AskUserQuestion`
* `EnterPlanMode`
* `ExitPlanMode`, unless the subagent's [`permissionMode`](#permission-modes) is `plan`
* `ScheduleWakeup`
* `WaitForMcpServers`

To restrict tools, use either the `tools` field (allowlist) or the `disallowedTools` field (denylist). This example uses `tools` to exclusively allow Read, Grep, Glob, and Bash. The subagent can't edit files, write files, or use any MCP tools:

```yaml theme={null}
---
name: safe-researcher
description: Research agent with restricted capabilities
tools: Read, Grep, Glob, Bash
---
```

This example uses `disallowedTools` to inherit every tool from the main conversation except Write and Edit. The subagent keeps Bash, MCP tools, and everything else:

```yaml theme={null}
---
name: no-writes
description: Inherits every tool except file writes
disallowedTools: Write, Edit
---
```

If both are set, `disallowedTools` is applied first, then `tools` is resolved against the remaining pool. A tool listed in both is removed.

#### Restrict which subagents can be spawned

When an agent runs as the main thread with `claude --agent`, it can spawn subagents using the Agent tool. To restrict which subagent types it can spawn, use `Agent(agent_type)` syntax in the `tools` field.

<Note>In version 2.1.63, the Task tool was renamed to Agent. Existing `Task(...)` references in settings and agent definitions still work as aliases.</Note>

```yaml theme={null}
---
name: coordinator
description: Coordinates work across specialized agents
tools: Agent(worker, researcher), Read, Bash
---
```

This is an allowlist: only the `worker` and `researcher` subagents can be spawned. If the agent tries to spawn any other type, the request fails and the agent sees only the allowed types in its prompt. To block specific agents while allowing all others, use [`permissions.deny`](#disable-specific-subagents) instead.

To allow spawning any subagent without restrictions, use `Agent` without parentheses:

```yaml theme={null}
tools: Agent, Read, Bash
```

If `Agent` is omitted from the `tools` list entirely, the agent cannot spawn any subagents.

The `Agent(agent_type)` allowlist syntax applies only to an agent running as the main thread with `claude --agent`. In a subagent definition, listing `Agent` in `tools` lets that subagent [spawn nested subagents](#spawn-nested-subagents), but any type list inside the parentheses is ignored.

#### Scope MCP servers to a subagent

Use the `mcpServers` field to give a subagent access to [MCP](/en/mcp) servers that aren't available in the main conversation. Inline servers defined here are connected when the subagent starts and disconnected when it finishes. String references share the parent session's connection.

<Note>
  The `mcpServers` field applies in both contexts where an agent file can run:

  * As a subagent, spawned through the Agent tool or an @-mention
  * As the main session, launched with [`--agent`](#invoke-subagents-explicitly) or the `agent` setting

  When the agent is the main session, inline server definitions connect at startup alongside servers from [`.mcp.json`](/en/mcp) and settings files.
</Note>

Each entry in the list is either an inline server definition or a string referencing an MCP server already configured in your session:

```yaml theme={null}
---
name: browser-tester
description: Tests features in a real browser using Playwright
mcpServers:
  # Inline definition: scoped to this subagent only
  - playwright:
      type: stdio
      command: npx
      args: ["-y", "@playwright/mcp@latest"]
  # Reference by name: reuses an already-configured server
  - github
---

Use the Playwright tools to navigate, screenshot, and interact with pages.
```

Inline definitions use the same schema as `.mcp.json` server entries (`stdio`, `http`, `sse`, `ws`), keyed by the server name.

To keep an MCP server out of the main conversation entirely and avoid its tool descriptions consuming context there, define it inline here rather than in `.mcp.json`. The subagent gets the tools; the parent conversation does not.

As of v2.1.153, the MCP restrictions that apply to the main session also cover servers declared in subagent frontmatter:

* [`--strict-mcp-config`](/en/cli-reference) and [`--bare`](/en/cli-reference)
* [Enterprise managed MCP configuration](/en/managed-mcp)
* [`allowedMcpServers` and `deniedMcpServers` policies](/en/managed-mcp#policy-based-control-with-allowlists-and-denylists)

When one of these blocks a server, Claude Code skips it and shows a warning naming the blocked servers.

Managed-settings restrictions apply to every subagent regardless of how it is defined. `--strict-mcp-config` does not filter servers you pass inline via `--agents` or the SDK `agents` option, since those are explicit caller input.

#### Permission modes

The `permissionMode` field controls how the subagent handles permission prompts. Subagents inherit the permission context from the main conversation and can override the mode, except when the parent mode takes precedence as described below.

| Mode                | Behavior                                                                                                                                    |
| :------------------ | :------------------------------------------------------------------------------------------------------------------------------------------ |
| `default`           | Standard permission checking with prompts                                                                                                   |
| `acceptEdits`       | Auto-accept file edits and common filesystem commands for paths in the working directory or `additionalDirectories`                         |
| `auto`              | [Auto mode](/en/permission-modes#eliminate-prompts-with-auto-mode): a background classifier reviews commands and protected-directory writes |
| `dontAsk`           | Auto-deny permission prompts (explicitly allowed tools still work)                                                                          |
| `bypassPermissions` | Skip permission prompts                                                                                                                     |
| `plan`              | Plan mode (read-only exploration)                                                                                                           |

<Warning>
  Use `bypassPermissions` with caution. It skips permission prompts, allowing the subagent to execute operations without approval, including writes to `.git`, `.config/git`, `.claude`, `.vscode`, `.idea`, `.husky`, `.cargo`, `.devcontainer`, `.yarn`, and `.mvn`. Explicit [`ask` rules](/en/permissions#manage-permissions) and root and home directory removals such as `rm -rf /` still prompt. See [permission modes](/en/permission-modes#skip-all-checks-with-bypasspermissions-mode) for details.
</Warning>

If the parent uses `bypassPermissions` or `acceptEdits`, this takes precedence and cannot be overridden. If the parent uses [auto mode](/en/permission-modes#eliminate-prompts-with-auto-mode), the subagent inherits auto mode and any `permissionMode` in its frontmatter is ignored: the classifier evaluates the subagent's tool calls with the same block and allow rules as the parent session.

#### Preload skills into subagents

Use the `skills` field to inject skill content into a subagent's context at startup. This gives the subagent domain knowledge without requiring it to discover and load skills during execution.

```yaml theme={null}
---
name: api-developer
description: Implement API endpoints following team conventions
skills:
  - api-conventions
  - error-handling-patterns
---

Implement API endpoints. Follow the conventions and patterns from the preloaded skills.
```

The full content of each listed skill is injected into the subagent's context at startup. This field controls which skills are preloaded, not which skills the subagent can access: without it, the subagent can still discover and invoke project, user, and plugin skills through the Skill tool during execution. To prevent a subagent from invoking skills entirely, omit `Skill` from the [`tools`](#available-tools) list or add it to `disallowedTools`.

You cannot preload skills that set [`disable-model-invocation: true`](/en/skills#control-who-invokes-a-skill), since preloading draws from the same set of skills Claude can invoke. If a listed skill is missing or disabled, Claude Code skips it and logs a warning to the debug log.

<Note>
  This is the inverse of [running a skill in a subagent](/en/skills#run-skills-in-a-subagent). With `skills` in a subagent, the subagent controls the system prompt and loads skill content. With `context: fork` in a skill, the skill content is injected into the agent you specify. Both use the same underlying system.
</Note>

#### Enable persistent memory

The `memory` field gives the subagent a persistent directory that survives across conversations. The subagent uses this directory to build up knowledge over time, such as codebase patterns, debugging insights, and architectural decisions.

```yaml theme={null}
---
name: code-reviewer
description: Reviews code for quality and best practices
memory: user
---

You are a code reviewer. As you review code, update your agent memory with
patterns, conventions, and recurring issues you discover.
```

Choose a scope based on how broadly the memory should apply:

| Scope     | Location                                      | Use when                                                                                    |
| :-------- | :-------------------------------------------- | :------------------------------------------------------------------------------------------ |
| `user`    | `~/.claude/agent-memory/<name-of-agent>/`     | the subagent should remember learnings across all projects                                  |
| `project` | `.claude/agent-memory/<name-of-agent>/`       | the subagent's knowledge is project-specific and shareable via version control              |
| `local`   | `.claude/agent-memory-local/<name-of-agent>/` | the subagent's knowledge is project-specific but should not be checked into version control |

When memory is enabled:

* The subagent's system prompt includes instructions for reading and writing to the memory directory.
* The subagent's system prompt also includes the first 200 lines or 25KB of `MEMORY.md` in the memory directory, whichever comes first, with instructions to curate `MEMORY.md` if it exceeds that limit.
* Read, Write, and Edit tools are automatically enabled so the subagent can manage its memory files.

##### Persistent memory tips

* `project` is the recommended default scope. It makes subagent knowledge shareable via version control. Use `user` when the subagent's knowledge is broadly applicable across projects, or `local` when the knowledge should not be checked into version control.
* Ask the subagent to consult its memory before starting work: "Review this PR, and check your memory for patterns you've seen before."
* Ask the subagent to update its memory after completing a task: "Now that you're done, save what you learned to your memory." Over time, this builds a knowledge base that makes the subagent more effective.
* Include memory instructions directly in the subagent's markdown file so it proactively maintains its own knowledge base:

  ```markdown theme={null}
  Update your agent memory as you discover codepaths, patterns, library
  locations, and key architectural decisions. This builds up institutional
  knowledge across conversations. Write concise notes about what you found
  and where.
  ```

#### Conditional rules with hooks

For more dynamic control over tool usage, use `PreToolUse` hooks to validate operations before they execute. This is useful when you need to allow some operations of a tool while blocking others.

This example creates a subagent that only allows read-only database queries. The `PreToolUse` hook runs the script specified in `command` before each Bash command executes:

```yaml theme={null}
---
name: db-reader
description: Execute read-only database queries
tools: Bash
hooks:
  PreToolUse:
    - matcher: "Bash"
      hooks:
        - type: command
          command: "./scripts/validate-readonly-query.sh"
---
```

Claude Code [passes hook input as JSON](/en/hooks#pretooluse-input) via stdin to hook commands. The validation script reads this JSON, extracts the Bash command, and [exits with code 2](/en/hooks#exit-code-2-behavior-per-event) to block write operations:

```bash theme={null}
#!/bin/bash
# ./scripts/validate-readonly-query.sh

INPUT=$(cat)
COMMAND=$(echo "$INPUT" | jq -r '.tool_input.command // empty')

# Block SQL write operations (case-insensitive)
if echo "$COMMAND" | grep -iE '\b(INSERT|UPDATE|DELETE|DROP|CREATE|ALTER|TRUNCATE)\b' > /dev/null; then
  echo "Blocked: Only SELECT queries are allowed" >&2
  exit 2
fi

exit 0
```

See [Hook input](/en/hooks#pretooluse-input) for the complete input schema and [exit codes](/en/hooks#exit-code-output) for how exit codes affect behavior. On Windows, write hook scripts in PowerShell and add `shell: powershell` to the hook entry as shown in [running hooks in PowerShell](/en/hooks#windows-powershell-tool).

#### Disable specific subagents

You can prevent Claude from using specific subagents by adding them to the `deny` array in your [settings](/en/settings#permission-settings). Use the format `Agent(subagent-name)` where `subagent-name` matches the subagent's name field.

```json theme={null}
{
  "permissions": {
    "deny": ["Agent(Explore)", "Agent(my-custom-agent)"]
  }
}
```

This works for both built-in and custom subagents. You can also use the `--disallowedTools` CLI flag:

```bash theme={null}
claude --disallowedTools "Agent(Explore)"
```

See [Permissions documentation](/en/permissions#tool-specific-permission-rules) for more details on permission rules.

### Define hooks for subagents

Subagents can define [hooks](/en/hooks) that run during the subagent's lifecycle. There are two ways to configure hooks:

1. **In the subagent's frontmatter**: Define hooks that run only while that subagent is active
2. **In `settings.json`**: Define hooks that run in the main session when subagents start or stop

#### Hooks in subagent frontmatter

Define hooks directly in the subagent's markdown file. These hooks only run while that specific subagent is active and are cleaned up when it finishes.

<Note>
  Frontmatter hooks fire when the agent is spawned as a subagent through the Agent tool or an @-mention, and when the agent runs as the main session via [`--agent`](#invoke-subagents-explicitly) or the `agent` setting. In the main-session case they run alongside any hooks defined in [`settings.json`](/en/hooks).
</Note>

All [hook events](/en/hooks#hook-events) are supported. The most common events for subagents are:

| Event         | Matcher input | When it fires                                                       |
| :------------ | :------------ | :------------------------------------------------------------------ |
| `PreToolUse`  | Tool name     | Before the subagent uses a tool                                     |
| `PostToolUse` | Tool name     | After the subagent uses a tool                                      |
| `Stop`        | (none)        | When the subagent finishes (converted to `SubagentStop` at runtime) |

This example validates Bash commands with the `PreToolUse` hook and runs a linter after file edits with `PostToolUse`:

```yaml theme={null}
---
name: code-reviewer
description: Review code changes with automatic linting
hooks:
  PreToolUse:
    - matcher: "Bash"
      hooks:
        - type: command
          command: "./scripts/validate-command.sh $TOOL_INPUT"
  PostToolUse:
    - matcher: "Edit|Write"
      hooks:
        - type: command
          command: "./scripts/run-linter.sh"
---
```

When the agent is invoked as a subagent, `Stop` hooks in frontmatter are automatically converted to `SubagentStop` events.

#### Project-level hooks for subagent events

Configure hooks in `settings.json` that respond to subagent lifecycle events in the main session.

| Event           | Matcher input   | When it fires                    |
| :-------------- | :-------------- | :------------------------------- |
| `SubagentStart` | Agent type name | When a subagent begins execution |
| `SubagentStop`  | Agent type name | When a subagent completes        |

Both events support matchers to target specific agent types by name. This example runs a setup script only when the `db-agent` subagent starts, and a cleanup script when any subagent stops:

```json theme={null}
{
  "hooks": {
    "SubagentStart": [
      {
        "matcher": "db-agent",
        "hooks": [
          { "type": "command", "command": "./scripts/setup-db-connection.sh" }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          { "type": "command", "command": "./scripts/cleanup-db-connection.sh" }
        ]
      }
    ]
  }
}
```

See [Hooks](/en/hooks) for the complete hook configuration format.

## Work with subagents

### Understand automatic delegation

Claude automatically delegates tasks based on the task description in your request, the `description` field in subagent configurations, and current context. To encourage proactive delegation, include phrases like "use proactively" in your subagent's description field.

### Invoke subagents explicitly

When automatic delegation isn't enough, you can request a subagent yourself. Three patterns escalate from a one-off suggestion to a session-wide default:

* **Natural language**: name the subagent in your prompt; Claude decides whether to delegate
* **@-mention**: guarantees the subagent runs for one task
* **Session-wide**: the whole session uses that subagent's system prompt, tool restrictions, and model via the `--agent` flag or the `agent` setting

For natural language, there's no special syntax. Name the subagent and Claude typically delegates:

```text theme={null}
Use the test-runner subagent to fix failing tests
Have the code-reviewer subagent look at my recent changes
```

**@-mention the subagent.** Type `@` and pick the subagent from the typeahead, the same way you @-mention files. This ensures that specific subagent runs rather than leaving the choice to Claude:

```text theme={null}
@"code-reviewer (agent)" look at the auth changes
```

Your full message still goes to Claude, which writes the subagent's task prompt based on what you asked. The @-mention controls which subagent Claude invokes, not what prompt it receives.

Subagents provided by an enabled [plugin](/en/plugins) appear in the typeahead under their scoped name, such as `my-plugin:code-reviewer` or `my-plugin:review:security` when the plugin [organizes agents into subfolders](#choose-the-subagent-scope). Named background subagents currently running in the session also appear in the typeahead, showing their status next to the name. You can also type the mention manually without using the picker: `@agent-<name>` for local subagents, or `@agent-` followed by the scoped name for plugin subagents, for example `@agent-my-plugin:code-reviewer`.

**Run the whole session as a subagent.** Pass [`--agent <name>`](/en/cli-reference) to start a session where the main thread itself takes on that subagent's system prompt, tool restrictions, and model:

```bash theme={null}
claude --agent code-reviewer
```

The subagent's system prompt replaces the default Claude Code system prompt entirely, the same way [`--system-prompt`](/en/cli-reference) does. `CLAUDE.md` files and project memory still load through the normal message flow. The agent name appears as `@<name>` in the startup header so you can confirm it's active.

This works with built-in and custom subagents, and the choice persists when you resume the session.

For a plugin-provided subagent, you can pass just the agent name and Claude Code will find it:

```bash theme={null}
claude --agent security-reviewer
```

If multiple plugins provide agents with the same name, pass the scoped name to disambiguate:

```bash theme={null}
claude --agent my-plugin:security-reviewer
```

If the plugin places the agent in a subfolder of its `agents/` directory, include the subfolder in the scoped name, for example `claude --agent my-plugin:review:security`.

To make it the default for every session in a project, set `agent` in `.claude/settings.json`:

```json theme={null}
{
  "agent": "code-reviewer"
}
```

The CLI flag overrides the setting if both are present.

### Run subagents in foreground or background

Subagents can run in the foreground (blocking) or background (concurrent):

* **Foreground subagents** block the main conversation until complete. Permission prompts are passed through to you as they come up.
* **Background subagents** run concurrently while you continue working. They run with the permissions already granted in the session and auto-deny any tool call that would otherwise prompt. If a background subagent needs to ask clarifying questions, that tool call fails but the subagent continues.

If a background subagent fails due to missing permissions, you can start a new foreground subagent with the same task to retry with interactive prompts.

Claude decides whether to run subagents in the foreground or background based on the task. You can also:

* Ask Claude to "run this in the background"
* Press **Ctrl+B** to background a running task

To disable all background task functionality, set the `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS` environment variable to `1`. See [Environment variables](/en/env-vars).

When [`CLAUDE_CODE_FORK_SUBAGENT`](#fork-the-current-conversation) is set to `1`, every subagent spawn runs in the background regardless of the `background` field. Forks still surface permission prompts in your terminal as they occur; named subagents auto-deny anything that would prompt, as described above.

### Common patterns

#### Isolate high-volume operations

One of the most effective uses for subagents is isolating operations that produce large amounts of output. Running tests, fetching documentation, or processing log files can consume significant context. By delegating these to a subagent, the verbose output stays in the subagent's context while only the relevant summary returns to your main conversation.

```text theme={null}
Use a subagent to run the test suite and report only the failing tests with their error messages
```

#### Run parallel research

For independent investigations, spawn multiple subagents to work simultaneously:

```text theme={null}
Research the authentication, database, and API modules in parallel using separate subagents
```

Each subagent explores its area independently, then Claude synthesizes the findings. This works best when the research paths don't depend on each other.

<Warning>
  When subagents complete, their results return to your main conversation. Running many subagents that each return detailed results can consume significant context.
</Warning>

For tasks that need sustained parallelism or exceed your context window, [agent teams](/en/agent-teams) give each worker its own independent context.

#### Chain subagents

For multi-step workflows, ask Claude to use subagents in sequence. Each subagent completes its task and returns results to Claude, which then passes relevant context to the next subagent.

```text theme={null}
Use the code-reviewer subagent to find performance issues, then use the optimizer subagent to fix them
```

### Choose between subagents and main conversation

Use the **main conversation** when:

* The task needs frequent back-and-forth or iterative refinement
* Multiple phases share significant context (planning → implementation → testing)
* You're making a quick, targeted change
* Latency matters. Subagents start fresh and may need time to gather context

Use **subagents** when:

* The task produces verbose output you don't need in your main context
* You want to enforce specific tool restrictions or permissions
* The work is self-contained and can return a summary

Consider [Skills](/en/skills) instead when you want reusable prompts or workflows that run in the main conversation context rather than isolated subagent context.

For a quick question about something already in your conversation, use [`/btw`](/en/interactive-mode#side-questions-with-%2Fbtw) instead of a subagent. It sees your full context but has no tool access, and the answer is discarded rather than added to history.

### Spawn nested subagents

{/* min-version: 2.1.172 */}As of Claude Code v2.1.172, a subagent can spawn its own subagents. Use this when a delegated task itself splits into parallel subtasks, such as a reviewer subagent that dispatches a verifier per finding, so the intermediate output never reaches your main conversation. Only the top-level subagent's summary returns to you.

A nested subagent is configured the same way as a top-level one and resolves from the same [scopes](#choose-the-subagent-scope). The subagent panel below the prompt input shows the full tree: each row displays a `(+N)` count of descendants, and opening a row shows that subagent's direct children with a path back to `main`. The Running tab in [`/agents`](#use-the-%2Fagents-command) lists running subagents as a flat list.

Depth is counted as the number of subagent levels below the main conversation, regardless of whether each level runs in the [foreground or background](#run-subagents-in-foreground-or-background):

* **Foreground subagents**: can spawn at any depth. Each level blocks its parent until it returns, so the chain is self-limiting: the main conversation waits on the entire chain.
* **Background subagents**: a background subagent at depth five does not receive the Agent tool and cannot spawn further. The limit is fixed and not configurable, and exists to prevent runaway concurrent trees.

To prevent a specific subagent from spawning others, omit `Agent` from its [`tools`](#available-tools) list or add it to `disallowedTools`.

A [fork](#fork-the-current-conversation) still cannot spawn another fork. It can spawn other subagent types, and those count toward the depth limit.

### Manage subagent context

#### What loads at startup

Each subagent starts with a fresh, isolated context window. It does not see your conversation history, the skills you've already invoked, or the files Claude has already read. Claude composes a delegation message that summarizes the task, and the subagent works from there. The exception is a [fork](#fork-the-current-conversation), which inherits the parent conversation instead of starting fresh.

A non-fork subagent's initial context contains:

* **System prompt**: the agent's own prompt plus environment details that Claude Code appends, not the full Claude Code system prompt. Custom subagents define theirs in the [markdown body](#write-subagent-files) or `prompt` field. Built-in agents have predefined prompts.
* **Task message**: the delegation prompt Claude writes when it hands off the work.
* **CLAUDE.md and memory**: every level of the [memory hierarchy](/en/memory#how-claude-md-files-load) the main conversation loads, including `~/.claude/CLAUDE.md`, project rules, `CLAUDE.local.md`, and managed policy files. The built-in Explore and Plan agents skip this.
* **Git status**: a snapshot taken at the start of the parent session. Absent when the working directory isn't a Git repository or when [`includeGitInstructions`](/en/settings#available-settings) is `false`. Explore and Plan skip it regardless.
* **Preloaded skills**: full content of any skill named in the agent's [`skills` field](#preload-skills-into-subagents). Built-in agents don't preload skills.

Explore and Plan are the only subagents that omit CLAUDE.md and git status. There is no frontmatter field or per-agent setting to change which agents skip them.

The main conversation reads Explore and Plan results with full CLAUDE.md context, so most rules don't need to reach the subagent itself. If a rule must, such as "ignore the `vendor/` directory," restate it in the prompt you give Claude when delegating.

#### Resume subagents

Each subagent invocation creates a new instance with fresh context. To continue an existing subagent's work instead of starting over, ask Claude to resume it.

Resumed subagents retain their full conversation history, including all previous tool calls, results, and reasoning. The subagent picks up exactly where it stopped rather than starting fresh.

When a subagent completes, Claude receives its agent ID. The built-in Explore and Plan agents are one-shot and return no agent ID, so they can't be resumed; use `general-purpose` or a custom subagent when you need to continue the work. Claude uses the `SendMessage` tool with the agent's ID as the `to` field to resume it. The `SendMessage` tool is only available when [agent teams](/en/agent-teams) are enabled via `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`.

To resume a subagent, ask Claude to continue the previous work:

```text theme={null}
Use the code-reviewer subagent to review the authentication module
[Agent completes]

Continue that code review and now analyze the authorization logic
[Claude resumes the subagent with full context from previous conversation]
```

If a stopped subagent receives a `SendMessage`, it auto-resumes in the background without requiring a new `Agent` invocation.

You can also ask Claude for the agent ID if you want to reference it explicitly, or find IDs in the transcript files at `~/.claude/projects/{project}/{sessionId}/subagents/`. Each transcript is stored as `agent-{agentId}.jsonl`.

Subagent transcripts persist independently of the main conversation:

* **Main conversation compaction**: When the main conversation compacts, subagent transcripts are unaffected. They're stored in separate files.
* **Session persistence**: Subagent transcripts persist within their session. You can [resume a subagent](#resume-subagents) after restarting Claude Code by resuming the same session.
* **Automatic cleanup**: Transcripts are cleaned up based on the `cleanupPeriodDays` setting (default: 30 days).

#### Auto-compaction

Subagents support automatic compaction using the same logic as the main conversation. Compaction triggers under the same conditions, and `CLAUDE_AUTOCOMPACT_PCT_OVERRIDE` applies to subagents as well. See [environment variables](/en/env-vars) for when the override takes effect.

Compaction events are logged in subagent transcript files:

```json theme={null}
{
  "type": "system",
  "subtype": "compact_boundary",
  "compactMetadata": {
    "trigger": "auto",
    "preTokens": 167189
  }
}
```

The `preTokens` value shows how many tokens were used before compaction occurred.

## Fork the current conversation

<Note>
  Forked subagents require Claude Code v2.1.117 or later. {/* min-version: 2.1.161 */}From v2.1.161 the `/fork` command is enabled by default; on earlier versions it requires setting the [`CLAUDE_CODE_FORK_SUBAGENT`](/en/env-vars) environment variable to `1`. Making forks the model's *default* spawn behavior is experimental and may change in future releases. This default may also be enabled in interactive sessions as part of a staged rollout.
</Note>

A fork is a subagent that inherits the entire conversation so far instead of starting fresh. This drops the input isolation that subagents otherwise provide: a fork sees the same system prompt, tools, model, and message history as the main session, so you can hand it a side task without re-explaining the situation. The fork's own tool calls still stay out of your conversation and only its final result comes back, so your main context window stays clean. Use a fork when a named subagent would need too much background to be useful, or when you want to try several approaches in parallel from the same starting point.

To control fork mode regardless of the staged rollout, set [`CLAUDE_CODE_FORK_SUBAGENT`](/en/env-vars) to `1` to enable it explicitly or to `0` to disable it. The variable is honored in interactive mode and via the SDK or `claude -p`.

Enabling fork mode changes Claude Code in two ways:

* Claude spawns a fork whenever it would otherwise use the [general-purpose](#built-in-subagents) subagent. Named subagents such as Explore still spawn as before.
* Every subagent spawn runs in the [background](#run-subagents-in-foreground-or-background), whether it is a fork or a named subagent. Set `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS` to `1` to keep spawns synchronous.

You can start a fork yourself with `/fork` followed by a directive, with or without the variable set. Claude Code names the fork from the first words of the directive. The following example forks the conversation to draft test cases while you continue with the implementation in the main session:

```text theme={null}
/fork draft unit tests for the parser changes so far
```

The fork appears in a panel below your prompt and runs in the background while you keep working. When it finishes, its result arrives as a message in your main conversation. The next section covers the panel controls for watching and steering forks while they run.

### Observe and steer running forks

Running forks appear in a panel below the prompt input, with one row for the main session and one for each fork. Use these keys to interact with the panel:

| Key       | Action                                                             |
| :-------- | :----------------------------------------------------------------- |
| `↑` / `↓` | Move between rows                                                  |
| `Enter`   | Open the selected fork's transcript and send it follow-up messages |
| `x`       | Dismiss a finished fork or stop a running one                      |
| `Esc`     | Return focus to the prompt input                                   |

### How forks differ from named subagents

A fork inherits everything the main session has at the moment it spawns. A named subagent starts from its own definition.

|                         | Fork                             | Named subagent                                                                           |
| :---------------------- | :------------------------------- | :--------------------------------------------------------------------------------------- |
| Context                 | Full conversation history        | Fresh context with the prompt you pass                                                   |
| System prompt and tools | Same as main session             | From the subagent's [definition file](#write-subagent-files)                             |
| Model                   | Same as main session             | From the subagent's `model` field                                                        |
| Permissions             | Prompts surface in your terminal | [Auto-denied](#run-subagents-in-foreground-or-background) when running in the background |
| Prompt cache            | Shared with main session         | Separate cache                                                                           |

Because a fork's system prompt and tool definitions are identical to the parent, its first request reuses the parent's [prompt cache](/en/prompt-caching#subagents-and-the-cache). This makes forking cheaper than spawning a fresh subagent for tasks that need the same context.

When Claude spawns a fork through the Agent tool, it can pass `isolation: "worktree"` so the fork's file edits are written to a separate git worktree instead of your checkout.

### Limitations

Setting `CLAUDE_CODE_FORK_SUBAGENT=1` enables fork mode in interactive sessions, [non-interactive mode](/en/headless), and the Agent SDK; setting it to `0` disables fork mode everywhere, including any server-side rollout. A fork cannot spawn further forks.

## Example subagents

These examples demonstrate effective patterns for building subagents. Use them as starting points, or generate a customized version with Claude.

<Tip>
  **Best practices:**

  * **Design focused subagents:** each subagent should excel at one specific task
  * **Write detailed descriptions:** Claude uses the description to decide when to delegate
  * **Limit tool access:** grant only necessary permissions for security and focus
  * **Check into version control:** share project subagents with your team
</Tip>

### Code reviewer

A read-only subagent that reviews code without modifying it. This example shows how to design a focused subagent with limited tool access (no Edit or Write) and a detailed prompt that specifies exactly what to look for and how to format output.

```markdown theme={null}
---
name: code-reviewer
description: Expert code review specialist. Proactively reviews code for quality, security, and maintainability. Use immediately after writing or modifying code.
tools: Read, Grep, Glob, Bash
model: inherit
---

You are a senior code reviewer ensuring high standards of code quality and security.

When invoked:
1. Run git diff to see recent changes
2. Focus on modified files
3. Begin review immediately

Review checklist:
- Code is clear and readable
- Functions and variables are well-named
- No duplicated code
- Proper error handling
- No exposed secrets or API keys
- Input validation implemented
- Good test coverage
- Performance considerations addressed

Provide feedback organized by priority:
- Critical issues (must fix)
- Warnings (should fix)
- Suggestions (consider improving)

Include specific examples of how to fix issues.
```

### Debugger

A subagent that can both analyze and fix issues. Unlike the code reviewer, this one includes Edit because fixing bugs requires modifying code. The prompt provides a clear workflow from diagnosis to verification.

```markdown theme={null}
---
name: debugger
description: Debugging specialist for errors, test failures, and unexpected behavior. Use proactively when encountering any issues.
tools: Read, Edit, Bash, Grep, Glob
---

You are an expert debugger specializing in root cause analysis.

When invoked:
1. Capture error message and stack trace
2. Identify reproduction steps
3. Isolate the failure location
4. Implement minimal fix
5. Verify solution works

Debugging process:
- Analyze error messages and logs
- Check recent code changes
- Form and test hypotheses
- Add strategic debug logging
- Inspect variable states

For each issue, provide:
- Root cause explanation
- Evidence supporting the diagnosis
- Specific code fix
- Testing approach
- Prevention recommendations

Focus on fixing the underlying issue, not the symptoms.
```

### Data scientist

A domain-specific subagent for data analysis work. This example shows how to create subagents for specialized workflows outside of typical coding tasks. It explicitly sets `model: sonnet` for more capable analysis.

```markdown theme={null}
---
name: data-scientist
description: Data analysis expert for SQL queries, BigQuery operations, and data insights. Use proactively for data analysis tasks and queries.
tools: Bash, Read, Write
model: sonnet
---

You are a data scientist specializing in SQL and BigQuery analysis.

When invoked:
1. Understand the data analysis requirement
2. Write efficient SQL queries
3. Use BigQuery command line tools (bq) when appropriate
4. Analyze and summarize results
5. Present findings clearly

Key practices:
- Write optimized SQL queries with proper filters
- Use appropriate aggregations and joins
- Include comments explaining complex logic
- Format results for readability
- Provide data-driven recommendations

For each analysis:
- Explain the query approach
- Document any assumptions
- Highlight key findings
- Suggest next steps based on data

Always ensure queries are efficient and cost-effective.
```

### Database query validator

A subagent that allows Bash access but validates commands to permit only read-only SQL queries. This example shows how to use `PreToolUse` hooks for conditional validation when you need finer control than the `tools` field provides.

```markdown theme={null}
---
name: db-reader
description: Execute read-only database queries. Use when analyzing data or generating reports.
tools: Bash
hooks:
  PreToolUse:
    - matcher: "Bash"
      hooks:
        - type: command
          command: "./scripts/validate-readonly-query.sh"
---

You are a database analyst with read-only access. Execute SELECT queries to answer questions about the data.

When asked to analyze data:
1. Identify which tables contain the relevant data
2. Write efficient SELECT queries with appropriate filters
3. Present results clearly with context

You cannot modify data. If asked to INSERT, UPDATE, DELETE, or modify schema, explain that you only have read access.
```

Claude Code [passes hook input as JSON](/en/hooks#pretooluse-input) via stdin to hook commands. The validation script reads this JSON, extracts the command being executed, and checks it against a list of SQL write operations. If a write operation is detected, the script [exits with code 2](/en/hooks#exit-code-2-behavior-per-event) to block execution and returns an error message to Claude via stderr.

Create the validation script anywhere in your project. The path must match the `command` field in your hook configuration:

```bash theme={null}
#!/bin/bash
# Blocks SQL write operations, allows SELECT queries

# Read JSON input from stdin
INPUT=$(cat)

# Extract the command field from tool_input using jq
COMMAND=$(echo "$INPUT" | jq -r '.tool_input.command // empty')

if [ -z "$COMMAND" ]; then
  exit 0
fi

# Block write operations (case-insensitive)
if echo "$COMMAND" | grep -iE '\b(INSERT|UPDATE|DELETE|DROP|CREATE|ALTER|TRUNCATE|REPLACE|MERGE)\b' > /dev/null; then
  echo "Blocked: Write operations not allowed. Use SELECT queries only." >&2
  exit 2
fi

exit 0
```

On macOS and Linux, make the script executable:

```bash theme={null}
chmod +x ./scripts/validate-readonly-query.sh
```

On Windows, write the validation script in PowerShell and add `shell: powershell` to the hook entry. See [running hooks in PowerShell](/en/hooks#windows-powershell-tool).

The hook receives JSON via stdin with the Bash command in `tool_input.command`. Exit code 2 blocks the operation and feeds the error message back to Claude. See [Hooks](/en/hooks#exit-code-output) for details on exit codes and [Hook input](/en/hooks#pretooluse-input) for the complete input schema.

## Next steps

Now that you understand subagents, explore these related features:

* [Distribute subagents with plugins](/en/plugins) to share subagents across teams or projects
* [Run Claude Code programmatically](/en/headless) with the Agent SDK for CI/CD and automation
* [Use MCP servers](/en/mcp) to give subagents access to external tools and data

---

dogfood it up!

---

can we centralize in .agents? and check it in?

---

0yeah push and run

---

<task-notification>
<task-id>a86da6d9c4f8e36a6</task-id>
<tool-use-id>toolu_013ipWoCwYsFCWi3xcQSKe7E</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/a86da6d9c4f8e36a6.output</output-file>
<status>completed</status>
<summary>Agent "Fetch Aider coder source files" completed</summary>
<result>Here is the complete architectural analysis of Aider's coder system.

---

## 1. `aider/coders/__init__.py` -- Module Registry

Full contents:

```python
from .architect_coder import ArchitectCoder
from .ask_coder import AskCoder
from .base_coder import Coder
from .context_coder import ContextCoder
from .editblock_coder import EditBlockCoder
from .editblock_fenced_coder import EditBlockFencedCoder
from .editor_diff_fenced_coder import EditorDiffFencedCoder
from .editor_editblock_coder import EditorEditBlockCoder
from .editor_whole_coder import EditorWholeFileCoder
from .help_coder import HelpCoder
from .patch_coder import PatchCoder
from .udiff_coder import UnifiedDiffCoder
from .udiff_simple import UnifiedDiffSimpleCoder
from .wholefile_coder import WholeFileCoder

__all__ = [
    HelpCoder, AskCoder, Coder, EditBlockCoder, EditBlockFencedCoder,
    WholeFileCoder, PatchCoder, UnifiedDiffCoder, UnifiedDiffSimpleCoder,
    ArchitectCoder, EditorEditBlockCoder, EditorWholeFileCoder,
    EditorDiffFencedCoder, ContextCoder,
]
```

**14 coder classes** are registered. The `__all__` list is used by `Coder.create()` as a dispatch table -- it iterates through these classes matching on `edit_format`.

---

## 2. `aider/coders/architect_coder.py` -- Architect Mode (FULL SOURCE)

```python
from .architect_prompts import ArchitectPrompts
from .ask_coder import AskCoder
from .base_coder import Coder

class ArchitectCoder(AskCoder):
    edit_format = "architect"
    gpt_prompts = ArchitectPrompts()
    auto_accept_architect = False

    def reply_completed(self):
        content = self.partial_response_content
        if not content or not content.strip():
            return

        if not self.auto_accept_architect and not self.io.confirm_ask("Edit the files?"):
            return

        kwargs = dict()
        editor_model = self.main_model.editor_model or self.main_model
        kwargs["main_model"] = editor_model
        kwargs["edit_format"] = self.main_model.editor_edit_format
        kwargs["suggest_shell_commands"] = False
        kwargs["map_tokens"] = 0
        kwargs["total_cost"] = self.total_cost
        kwargs["cache_prompts"] = False
        kwargs["num_cache_warming_pings"] = 0
        kwargs["summarize_from_coder"] = False

        new_kwargs = dict(io=self.io, from_coder=self)
        new_kwargs.update(kwargs)

        editor_coder = Coder.create(**new_kwargs)
        editor_coder.cur_messages = []
        editor_coder.done_messages = []

        if self.verbose:
            editor_coder.show_announcements()

        editor_coder.run(with_message=content, preproc=False)

        self.move_back_cur_messages("I made those changes to the files.")
        self.total_cost = editor_coder.total_cost
        self.aider_commit_hashes = editor_coder.aider_commit_hashes
```

### Key architectural findings -- Architect Mode:

1. **Inherits from `AskCoder`**, not `Coder` directly. `AskCoder` itself inherits from `Coder` and has `edit_format = "ask"` with no `get_edits`/`apply_edits` overrides. This means the architect LLM **cannot edit files itself** -- it only produces prose/instructions.

2. **Two-model dispatch pattern**: The architect uses `self.main_model` (the "architect" model) for the initial planning call. Then in `reply_completed()`, it creates a **separate `Coder` instance** using `self.main_model.editor_model` (or falls back to `self.main_model`) with a different `edit_format` (`self.main_model.editor_edit_format`).

3. **The editor coder is ephemeral**: It gets a fresh `cur_messages = []` and `done_messages = []`. The architect's entire response is passed as `with_message=content` to `editor_coder.run()`. The editor has no history context -- it only sees the architect's instructions plus the file contents from `from_coder`.

4. **No tool/function calling**: The architect has no `functions` attribute set. It relies purely on prose output.

5. **Feature restrictions on the editor**: Shell commands are disabled (`suggest_shell_commands=False`), repo map is disabled (`map_tokens=0`), cache is disabled, and summarization from the parent coder is disabled.

6. **Cost propagation**: After the editor runs, cost and commit hashes are propagated back to the architect coder.

7. **Human gate**: If `auto_accept_architect` is False (default on the class, but True by default in `__init__`), the user is prompted "Edit the files?" before dispatching to the editor.

---

## 3. `aider/coders/base_coder.py` -- The Core (2,485 lines)

### Class Hierarchy

```
Coder (base_coder.py)
├── AskCoder (ask_coder.py)          -- edit_format="ask", read-only analysis
│   └── ArchitectCoder               -- edit_format="architect", dispatches to editor
├── HelpCoder (help_coder.py)        -- edit_format="help", no-op edits
├── ContextCoder (context_coder.py)  -- edit_format="context", file selection
├── EditBlockCoder                   -- edit_format="diff", SEARCH/REPLACE blocks
│   ├── EditBlockFencedCoder         -- edit_format="diff-fenced"
│   │   └── EditorDiffFencedCoder    -- edit_format="editor-diff-fenced"
│   └── EditorEditBlockCoder         -- edit_format="editor-diff"
├── WholeFileCoder                   -- edit_format="whole", full file replacement
│   └── EditorWholeFileCoder         -- edit_format="editor-whole"
├── PatchCoder                       -- edit_format="patch"
├── UnifiedDiffCoder                 -- edit_format="udiff"
└── UnifiedDiffSimpleCoder           -- edit_format="udiff-simple"
```

The `Editor*` coders are thin subclasses that only change `edit_format` and `gpt_prompts` -- they use the same edit-application logic as their parents but have prompts focused on "you are an editor, apply these changes" rather than "you are a coder, solve this problem."

### `Coder.create()` -- Factory Method (line 125)

```python
@classmethod
def create(self, main_model=None, edit_format=None, io=None,
           from_coder=None, summarize_from_coder=True, **kwargs):
```

This is the **sole dispatch point**. It:
1. Resolves the model (from `from_coder` or creates default)
2. Resolves the `edit_format` (from arg, from `from_coder`, or from model default)
3. If `from_coder` is provided, **transfers state**: `abs_fnames`, `abs_read_only_fnames`, `done_messages`, `cur_messages`, `aider_commit_hashes`, `commands`, `total_cost`, `file_watcher`, etc.
4. If the edit format changed and `summarize_from_coder=True`, it **summarizes the chat history** to avoid confusing the new LLM with old-format assistant messages.
5. Iterates `coders.__all__` matching `coder.edit_format == edit_format`.
6. Raises `UnknownEditFormat` if no match.

### `__init__()` -- Constructor (line 299, ~240 lines)

Key parameters:
```python
def __init__(self, main_model, io, repo=None, fnames=None,
             read_only_fnames=None, show_diffs=False,
             auto_commits=True, dirty_commits=True, dry_run=False,
             map_tokens=1024, verbose=False, stream=True, use_git=True,
             cur_messages=None, done_messages=None,
             restore_chat_history=False, auto_lint=True, auto_test=False,
             lint_cmds=None, test_cmd=None, aider_commit_hashes=None,
             commands=None, summarizer=None, total_cost=0.0,
             cache_prompts=False, num_cache_warming_pings=0,
             suggest_shell_commands=True, chat_language=None,
             detect_urls=True, file_watcher=None,
             auto_copy_context=False, auto_accept_architect=True):
```

Notable setup:
- **`self.functions = None`** at the class level -- function calling is opt-in per subclass.
- If `self.functions` is set, the constructor validates each function against JSON Schema (`Draft7Validator`).
- Sets up `RepoMap` for code context, `ChatSummary` for history compression, `Linter` for auto-lint.

### Execution Flow: `run()` -&gt; `run_one()` -&gt; `send_message()` -&gt; `send()`

**`run(with_message=None, preproc=True)`** (line 876):
- If `with_message` is provided: runs once and returns `self.partial_response_content`
- Otherwise: enters an interactive loop calling `get_input()` + `run_one()` repeatedly

**`run_one(user_message, preproc)`** (line 924):
- Calls `init_before_message()` to reset per-message state
- Optionally preprocesses input (commands like `/add`, `/drop`, URL detection)
- Calls `send_message(message)` in a **reflection loop** -- if `self.reflected_message` is set (e.g., from lint errors, file mentions, or edit failures), it sends the reflection as a follow-up, up to `max_reflections=3`

**`send_message(inp)`** (line 1419):
- Appends user message to `cur_messages`
- Calls `format_messages()` to assemble the full prompt (system + examples + done + repo map + readonly files + chat files + current messages + reminder)
- Calls `self.send(messages, functions=self.functions)` in a retry loop with exponential backoff
- Handles `ContextWindowExceededError`, `FinishReasonLength` (with assistant prefill for infinite output), and keyboard interrupts
- After the LLM responds:
  - Calls `self.reply_completed()` -- this is the **hook point** where subclasses (like `ArchitectCoder`) can intercept
  - If `reply_completed()` returns truthy, processing stops (used by `ContextCoder`)
  - Otherwise, calls `self.apply_updates()` which runs `get_edits()` -&gt; `apply_edits_dry_run()` -&gt; `prepare_to_edit()` -&gt; `apply_edits()`
  - After edits: auto-commit, auto-lint (with reflection on errors), shell command execution, auto-test (with reflection on errors)

**`send(messages, model=None, functions=None)`** (line 1783):
- Calls `model.send_completion(messages, functions, stream, temperature)`
- Handles streaming vs non-streaming output
- Captures `partial_response_content` and `partial_response_function_call`
- Function calls come through `completion.choices[0].message.tool_calls[0].function`

### `reply_completed()` -- The Hook Point (line 1625)

```python
def reply_completed(self):
    pass
```

Base implementation is a **no-op**. Overridden by:
- **`ArchitectCoder`**: Creates an editor sub-coder and dispatches to it
- **`ContextCoder`**: Extracts file mentions from the response, updates `abs_fnames`, sets `reflected_message` for another round

### Tool/Function Calling

Aider has **infrastructure** for JSON Schema function calling but **does not use it in any shipped coder**:
- `self.functions = None` at the class level (line 96)
- The constructor validates functions if set (lines 533-542)
- `send()` passes `functions` to `model.send_completion()` (line 1799)
- Response function calls are captured in `self.partial_response_function_call` and stored in `cur_messages` with `role="assistant", content=None, function_call=...`
- `parse_partial_args()` (line 2338) attempts to parse partial JSON from function call arguments with progressively lenient JSON parsing
- The commented-out `SingleWholeFileFunctionCoder` in `__init__.py` suggests function calling was experimented with but abandoned

**Bottom line: Aider does NOT use LLM tool/function calling.** All edit formats are prompt-engineered text patterns (SEARCH/REPLACE blocks, whole-file replacement, unified diffs, patches) parsed from the LLM's text response.

### Subclass Contract

To create a new edit format, subclass `Coder` and override:
- `edit_format` -- string identifier (used for dispatch)
- `gpt_prompts` -- instance of a `CoderPrompts` subclass (system prompt, examples, reminders)
- `get_edits(mode="update")` -- parse edits from `self.partial_response_content`
- `apply_edits(edits)` -- write edits to disk
- Optionally: `apply_edits_dry_run(edits)`, `reply_completed()`, `render_incremental_response()`

---

## 4. `aider/coders/editblock_coder.py` -- The Default Edit Format (diff)

**Class**: `EditBlockCoder(Coder)`, `edit_format = "diff"`

Key methods:
- **`get_edits()`**: Parses SEARCH/REPLACE blocks from `self.partial_response_content` using `find_original_update_blocks()`. Also extracts shell commands from fenced code blocks (bash/sh/shell/etc.).
- **`apply_edits(edits)`**: For each `(path, original, updated)` tuple:
  - Reads the file, calls `do_replace()` which tries `replace_most_similar_chunk()`
  - If the edit fails on the target file, tries all other files in chat (fallback)
  - On failure: generates a detailed error message with "Did you mean..." suggestions and raises `ValueError` (which becomes `self.reflected_message`)

The `find_original_update_blocks()` function (line ~330) is a custom parser using regex patterns for `&lt;&lt;&lt;&lt;&lt;&lt;&lt; SEARCH`, `=======`, `&gt;&gt;&gt;&gt;&gt;&gt;&gt; REPLACE` markers. It handles:
- Multi-block edits in a single response
- Filename detection from preceding lines (with fuzzy matching)
- Shell command extraction from fenced code blocks
- `...` ellipsis handling for partial matches

The `replace_most_similar_chunk()` function implements a multi-strategy match:
1. Perfect string match
2. Whitespace-flexible match (handles LLM whitespace errors)
3. `...` dotdotdot expansion
4. (Dead code) Fuzzy edit-distance matching at 0.8 threshold

---

## 5. Other Coders

### `AskCoder(Coder)` -- `edit_format = "ask"`
Minimal: inherits everything from `Coder`, overrides nothing except prompts. Since `get_edits()` returns `[]` by default, it never edits files. Pure Q&amp;A mode.

### `HelpCoder(Coder)` -- `edit_format = "help"`
Explicitly returns empty edits:
```python
def get_edits(self, mode="update"): return []
def apply_edits(self, edits): pass
```

### `WholeFileCoder(Coder)` -- `edit_format = "whole"`
Parses fenced code blocks, matches them to filenames, writes entire file contents. Has filename-matching logic with priority system (`block` &gt; `saw` &gt; `chat` sources).

### `ContextCoder(Coder)` -- `edit_format = "context"`
Used to ask the LLM which files need editing. Overrides `reply_completed()` to extract file mentions and update `abs_fnames`. Uses reflection to iterate until file set stabilizes. Disables `check_for_file_mentions()` (no-op).

### `PatchCoder(Coder)` -- `edit_format = "patch"`
Uses a structured patch format with `*** Add File`, `*** Delete File`, `*** Update File` actions and `@@ ... @@` chunk headers.

### Editor Coders (`EditorEditBlockCoder`, `EditorWholeFileCoder`, `EditorDiffFencedCoder`)
Thin wrappers that only swap `gpt_prompts` to editor-focused instructions. Same edit logic as parents. These are the coders instantiated by `ArchitectCoder.reply_completed()`.

---

## 6. Key Architectural Patterns

### No Subagent/Multi-Turn Tool Use
Aider does **not** use LLM tool calling, function calling, or multi-turn agent loops. The LLM produces a single text response, which is parsed for edits by the coder subclass. The only "multi-turn" behavior is the **reflection loop** (max 3 iterations) triggered by lint errors, test failures, or malformed edits.

### Architect = Two Sequential LLM Calls
The architect pattern is **not** a subagent system. It is two sequential, independent LLM calls:
1. Architect model produces prose instructions
2. Editor model receives those instructions as a user message and produces edits

The editor has no access to the original user request, no chat history, and no repo map. It only sees the architect's output and the file contents.

### No Hooks/Middleware System
There is no plugin, hook, or middleware system in the coder layer. The only extension point is subclassing `Coder` and overriding `reply_completed()`, `get_edits()`, and `apply_edits()`. The `Commands` class handles slash commands separately.

### Message Assembly
Messages are assembled in a strict order via `ChatChunks`:
1. System prompt
2. Example messages
3. Read-only file contents
4. Repo map
5. Done messages (previous conversation, possibly summarized)
6. Chat file contents (editable files)
7. Current messages (current turn)
8. Reminder (system prompt recap)

### State Transfer Between Coders
`Coder.create(from_coder=...)` is the mechanism for switching edit formats mid-session. It transfers files, messages, costs, commands, and file watcher state. If the edit format changes, it optionally summarizes the history to prevent format confusion.

---

## Summary Table

| Coder | `edit_format` | Parent | Can Edit Files? | Key Mechanism |
|---|---|---|---|---|
| `Coder` | (base) | -- | Yes (via subclass) | Factory + execution loop |
| `AskCoder` | `"ask"` | `Coder` | No | Prose-only responses |
| `ArchitectCoder` | `"architect"` | `AskCoder` | No (dispatches to editor) | `reply_completed()` creates editor coder |
| `HelpCoder` | `"help"` | `Coder` | No | No-op edits |
| `ContextCoder` | `"context"` | `Coder` | No (selects files) | `reply_completed()` updates file set |
| `EditBlockCoder` | `"diff"` | `Coder` | Yes | SEARCH/REPLACE blocks |
| `EditBlockFencedCoder` | `"diff-fenced"` | `EditBlockCoder` | Yes | Fenced SEARCH/REPLACE |
| `WholeFileCoder` | `"whole"` | `Coder` | Yes | Full file replacement |
| `PatchCoder` | `"patch"` | `Coder` | Yes | Structured patch format |
| `UnifiedDiffCoder` | `"udiff"` | `Coder` | Yes | Unified diff format |
| `EditorEditBlockCoder` | `"editor-diff"` | `EditBlockCoder` | Yes | Editor-focused prompts |
| `EditorWholeFileCoder` | `"editor-whole"` | `WholeFileCoder` | Yes | Editor-focused prompts |
| `EditorDiffFencedCoder` | `"editor-diff-fenced"` | `EditBlockFencedCoder` | Yes | Editor-focused prompts |</result>
<usage><subagent_tokens>59935</subagent_tokens><tool_uses>31</tool_uses><duration_ms>318982</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a3317cd4d315b0aa0</task-id>
<tool-use-id>toolu_01CMHHQQUMTQ5vWMy3gx4LAz</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/a3317cd4d315b0aa0.output</output-file>
<status>completed</status>
<summary>Agent "Search academic papers on adversarial LLM code review" completed</summary>
<result>Here is the comprehensive research report:

---

## Multi-Agent Debate &amp; Adversarial LLM Code Review: Research Landscape

### TIER 1: Directly Relevant -- Adversarial/Debate for Code

**1. SWE-Debate: Competitive Multi-Agent Debate for Software Issue Resolution**
- Authors: YerbaPage et al. | Year: 2025 | Venue: ICSE 2026
- Link: https://arxiv.org/abs/2507.23348
- Architecture: Creates multiple fault propagation traces via dependency graph traversal, then runs a **3-round competitive debate** among specialized agents, each embodying a distinct reasoning perspective along the trace. Output feeds into an MCTS-based patch generator.
- Adversarial mechanism: Agents argue for competing localization hypotheses; structured competition forces convergence on the strongest fix plan.
- Results: New SOTA on SWE-bench among open-source frameworks. The debate structure specifically overcomes single-agent "local solution" traps.
- Integration: Outputs a consolidated fix plan consumed by a code modification agent.

**2. DebateCoder: Collective Intelligence via Test-Case-Driven LLM Debate**
- Authors: Chen et al. | Year: 2025 | Venue: ACL 2025
- Link: https://aclanthology.org/2025.acl-long.589/
- Architecture: Two+ LLMs generate competing code solutions. Each generates **adversarial test cases** to challenge the other's code. Execution results drive contrastive analysis. No third-party moderator.
- Adversarial mechanism: Models craft tests specifically designed to break the opponent's code. Convergence is determined by test pass rates, not LLM judgment.
- Results: Diversity-based ensemble achieves up to 95% of theoretical performance potential. Effective even in 2-model setups.
- Key insight: Using test execution as the debate medium eliminates moderator hallucination.

**3. Code-A1: Adversarial Evolving of Code LLM and Test LLM via RL**
- Authors: ZJU-REAL | Year: March 2026
- Link: https://arxiv.org/abs/2603.15611
- Architecture: Two separate models with **opposing RL objectives** -- Code LLM is rewarded for passing tests, Test LLM is rewarded for exposing defects. White-box test generation (Test LLM can inspect candidate code).
- Adversarial mechanism: True adversarial training loop. Includes a "Mistake Book" for experience replay and composite reward balancing test validity with adversarial difficulty.
- Results: Matches or exceeds models trained on human-annotated tests (Qwen2.5-Coder). Eliminates self-collusion risk inherent in self-play.
- Key insight: Decoupling generator and adversary into separate models with opposing objectives is critical.

**4. CodeCoR: Self-Reflective Multi-Agent Framework for Code Generation**
- Authors: Pan, Zhang, Liu (Chongqing University) | Year: January 2025
- Link: https://arxiv.org/abs/2501.07811
- Architecture: Four agents -- prompt generator, coder, test case generator, repair advisor. Each generates multiple outputs and **prunes low-quality ones**. Failing code goes to repair agent. Best code (by test pass count) is returned.
- Adversarial mechanism: Self-reflective -- agents evaluate each other's effectiveness. Quality pruning at each stage.
- Results: Average Pass@1 of 77.8% across HumanEval, HumanEval-ET, MBPP, MBPP-ET. Outperforms CodeCoT and MapCoder.

**5. AgentCoder: Multi-Agent Code Generation with Iterative Testing**
- Authors: Huang et al. | Year: December 2023
- Link: https://arxiv.org/abs/2312.13010
- Architecture: Three agents -- **programmer, test designer, test executor**. Programmer generates code, test designer creates tests, executor runs and feeds back results for iterative refinement.
- Adversarial mechanism: The test designer acts as adversary by generating edge-case tests. Iterative loop continues until convergence.
- Results: 77.4% and 89.1% Pass@1 on HumanEval-ET and MBPP-ET with GPT-3.5 (vs. 69.5% and 63.0% SOTA baselines). Tested across 14 LLMs and 16 optimization baselines.

**6. RedCoder: Automated Multi-Turn Red Teaming for Code LLMs**
- Authors: Mo et al. (Amazon) | Year: July 2025
- Link: https://arxiv.org/abs/2507.22063
- Architecture: Red-teaming agent fine-tuned on multi-agent gaming prototype conversations. Uses **RAG-enhanced strategy retrieval** at deployment to steer multi-turn conversations toward vulnerability-inducing code.
- Adversarial mechanism: Pure adversarial -- the agent's sole purpose is to elicit CWE vulnerabilities (path traversal, SQL injection, etc.) from Code LLMs through multi-turn dialogue.
- Results: Vulnerability rates of 39-65% across all tested models, significantly outperforming single-turn and multi-turn baselines.

---

### TIER 2: Multi-Agent SE Frameworks with Review Components

**7. ChatDev: Communicative Agents for Software Development**
- Authors: Qian et al. (OpenBMB) | Year: July 2023 | Venue: ACL 2024
- Link: https://arxiv.org/abs/2307.07924
- Architecture: Chat chain divides SDLC into phases. Agents with roles (programmer, reviewer, tester) communicate via multi-turn dialogue. "Communicative dehallucination" guides what and how agents communicate.
- Review mechanism: Reviewer agent critiques code produced by programmer agent. Not formally adversarial -- cooperative with role specialization.
- Key finding: Natural language communication is beneficial for design; programming language communication helps debugging.

**8. MetaGPT: Meta Programming for Multi-Agent Collaborative Framework**
- Authors: Hong et al. | Year: August 2023 | Venue: ICLR 2024
- Link: https://arxiv.org/abs/2308.00352
- Architecture: Assembly-line paradigm with **Standardized Operating Procedures (SOPs)** encoded into prompt sequences. Agents verify intermediate results against SOP standards.
- Review mechanism: Verification against structured outputs at each stage rather than adversarial debate. Reduces cascading hallucination.
- Key finding: SOP-based verification of intermediate artifacts is more effective than free-form review.

**9. MASAI: Modular Architecture for Software-engineering AI Agents**
- Authors: Arora et al. | Year: June 2024
- Link: https://arxiv.org/abs/2406.11638
- Architecture: Modular sub-agents with **well-defined objectives and tuned strategies**. Sub-agents gather info from different repo sources, avoiding long trajectories that inflate cost/context.
- Review mechanism: Implicit via modular decomposition -- each sub-agent validates its narrow scope.
- Results: 28.33% resolution rate on SWE-bench Lite (highest at time of publication).

**10. CodeAgent: Autonomous Communicative Agents for Code Review**
- Authors: Tang et al. | Year: February 2024 | Venue: EMNLP 2024
- Link: https://arxiv.org/abs/2402.02172
- Architecture: Multi-agent LLM system with a **supervisory QA-Checker agent** ensuring all agents' contributions address the initial review question.
- Review tasks: (1) code-commit message inconsistency, (2) vulnerability detection, (3) code style adherence, (4) code revision suggestion.
- Results: New SOTA in code review automation. Supervisory agent prevents agent drift.

---

### TIER 3: Self-Critique, Oversight, and Foundational Work

**11. RefineCoder: Iterative Improving via Adaptive Critique Refinement**
- Link: https://arxiv.org/abs/2502.09183
- Architecture: Self-play with **LLM-as-Judge** (composite scoring) and **LLM-as-Critic** (selective critique of low-quality self-generated code). No teacher model distillation.
- Results: Comparable or superior performance to baselines using less training data.

**12. ColMAD: Collaborative Multi-Agent Debate for Error Detection**
- Link: https://arxiv.org/abs/2510.20963
- Architecture: Reframes debate as **non-zero-sum game** (vs. prior zero-sum). Agents critique supportively to complement missing points.
- Key finding: Zero-sum debate causes "debate hacking" (misleading the judge). Collaborative framing yields up to 4% improvement in error detection vs. single-agent methods.

**13. Confirmation Bias in LLM-Assisted Security Code Review**
- Link: https://arxiv.org/pdf/2603.18740
- Key finding: Framing a change as "bug-free" reduces vulnerability detection by **16-93%**. Adversarial framing succeeds in 35-88% of cases. Directly relevant to why adversarial review matters.

**14. LLM Code Reviewers Are Harder to Fool Than You Think**
- Link: https://arxiv.org/pdf/2602.16741
- Key finding: Adversarial comments embedded in code are not the real threat. The actual blind spots are **race conditions, timing attacks, and vulnerability patterns** that models inherently struggle with.

**15. From Industry Claims to Empirical Reality (MSR 2026)**
- Link: https://arxiv.org/abs/2604.03196
- Key finding: Industry claims that CRAs handle 80% of PRs are not empirically supported. Low signal-to-noise ratios in CRA comments correlate with PR abandonment. Analyzed 3,109 PRs from AIDev.

**16. LLM-Based Multi-Agent Systems for Software Engineering: Literature Review**
- Authors: He, Treude, Lo | Year: April 2024, revised July 2025 | Venue: TOSEM 2025
- Link: https://arxiv.org/abs/2404.04834
- Comprehensive survey mapping LMA applications across the full SDLC. Good reference for taxonomy of agent architectures.

---

### KEY SYNTHESIS: Does Adversarial Review Actually Improve Code Quality?

**Yes, with caveats:**

1. **Test-grounded debate works best.** DebateCoder and AgentCoder show that using test execution as the arbitration medium (not LLM judgment) eliminates moderator hallucination and produces measurable Pass@1 improvements of 8-26 percentage points over baselines.

2. **Opposing RL objectives beat self-play.** Code-A1 demonstrates that separating generator and adversary into models with opposing rewards prevents self-collusion and matches human-annotated test quality.

3. **Competitive debate improves localization.** SWE-Debate's 3-round competitive format specifically overcomes the "local optimum" trap where single agents fixate on one hypothesis.

4. **Zero-sum debate has failure modes.** ColMAD found that pure adversarial (zero-sum) debate causes "debate hacking" where agents mislead judges. Collaborative-adversarial framing is more effective.

5. **Real-world deployment is still immature.** The MSR 2026 empirical study found that current CRA signal-to-noise ratios cause PR abandonment. The gap between benchmark performance and production utility remains large.

6. **Common architecture pattern:** Generator + Adversarial Reviewer + Arbiter/Judge, with test execution as ground truth. The strongest systems avoid using an LLM as the final arbiter and instead use deterministic signals (test pass rates, static analysis).</result>
<usage><subagent_tokens>50689</subagent_tokens><tool_uses>25</tool_uses><duration_ms>149358</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a33f91f497d914477</task-id>
<tool-use-id>toolu_01W9uVeQn9c2gYbk1QGrg8tF</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/a33f91f497d914477.output</output-file>
<status>completed</status>
<summary>Agent "Research CodeRabbit review architecture" completed</summary>
<result>Now I have a comprehensive picture. Here's the full report:

---

## CodeRabbit Architecture: Implementation Details

### 1. Open Source Status

CodeRabbit's core product is **proprietary/closed-source**. Their GitHub org ([github.com/coderabbitai](https://github.com/coderabbitai)) has ~29 repos, but they're all supporting utilities:
- **ast-grep-essentials** / **ast-grep-langs** -- AST pattern matching rules (their static analysis backbone)
- **openai-chat-tokens** -- token counting fork
- **git-worktree-runner** -- worktree management for their sandboxed environments
- **bitbucket** -- TypeScript API client

No open-source review engine, no agent framework, no prompt libraries. The interesting stuff stays closed.

### 2. Multi-Pass Pipeline Architecture

CodeRabbit runs a **four-stage agent pipeline**, not a single-model pass:

**Stage 1 -- Router Agent (Triage)**
- A small, fast model classifies each diff hunk into: `COSMETIC`, `DOCS`, `LOGIC`, `SECURITY`
- This is the cost gate: 70% of changes take the fast path (static analysis + rules only), 30% go to the expensive reasoning model
- Result: ~70% cost reduction through intelligent routing

**Stage 2 -- Context Builder (GraphRAG)**
- Parses ASTs to extract symbols, imports, function calls
- Queries a code dependency graph (they call it "Codegraph")
- Ranks callers by proximity: same file &gt; same module &gt; tests &gt; distant modules
- Returns top-N relevant code contexts
- Maintains a semantic code index via **LanceDB** (vector embeddings of functions, classes, modules, tests, prior PRs)
- Also integrates PR descriptions, Jira/Linear tickets, linter outputs, SAST tool results
- Target: roughly 1:1 ratio of code to context in the final prompt

**Stage 3 -- Reasoning Agent (Deep Review)**
- Large language model receives enriched context (~2K tokens of context per review unit)
- Generates review comments with chain-of-thought reasoning
- Only invoked for `LOGIC` and `SECURITY` classifications (the 30%)
- Uses a custom diff format "closer to how humans understand changes" rather than standard unified diff

**Stage 4 -- Aggregation Agent (Filtering)**
- Deduplicates findings across comments
- Scores severity: Critical (5) down to Nitpick (1)
- Routes inline for critical/high, batches medium, collapses low-priority into collapsible sections
- Runs a verification pass checking suggested comments against code, team guidelines, and repo config

### 3. Model Strategy: Three-Tier Claude Cascade

Confirmed from their blog on the planning layer:

| Tier | Model | Role |
|------|-------|------|
| Strategic | **Claude Opus** | Primary orchestrator, strategic decisions, exploration strategies |
| Tactical | **Claude Sonnet** | Targeted tasks, well-defined substantive work |
| Granular | **Claude Haiku** | Function extraction, context distillation, cheap operations |

They also support **NVIDIA Nemotron 3 Ultra** for self-hosted deployments. Historical references mention GPT-4 and GPT-3.5-turbo (likely from their early architecture before they moved to Claude as primary).

### 4. Severity Classification -- Four Tiers

| Level | Scope | Routing |
|-------|-------|---------|
| **Critical** | Security vulns, data exposure, auth flaws, injection | Inline comment, blocks merge |
| **High** | Bug-prone patterns, missing error handling, resource leaks, race conditions | Inline comment |
| **Medium** | Code duplication, complexity, missing tests, doc gaps | Summary table |
| **Low** | Style, naming, minor optimizations, organization | Collapsible section |

Each finding contains: severity level, issue type/category, description with context, and optionally `codegenInstructions` (indicating the issue is auto-fixable).

### 5. False Positive Handling

Three mechanisms:

1. **Feedback Memory Loop**: Stores `positive_patterns` (user-approved comments) and `negative_patterns` (rejected findings) in a vector database. Queries this DB before generating new comments. Automatically suppresses patterns users previously flagged as unhelpful.

2. **Static Analysis AI Layer**: AI validates static analysis findings through a reasoning layer before surfacing them. Rather than showing raw linter output, the model evaluates whether each finding is contextually relevant.

3. **Verification Scripts**: The system generates shell/Python scripts to confirm assumptions before posting comments -- "comments come with receipts." These run in sandboxed environments (Jailkit + Linux cgroups on Cloud Run).

### 6. Infrastructure &amp; Orchestration

**Runtime**: Google Cloud Run (2nd gen execution environment, microVM)
- 8 vCPUs, 32 GB RAM per instance
- Concurrency: 8 requests/instance
- Timeout: 3600s (1 hour) per review
- Scales to 200+ instances at peak (~10 req/s)

**Orchestration**: Fan-out/fan-in architecture, strongly resembling **Temporal** (they reference it as the exemplar). Workflow is durable and resumable -- state is saved after each activity, and if the server crashes it resumes from the last checkpoint.

**Pipeline flow**:
1. GitHub/GitLab webhook hits a lightweight Cloud Run service
2. Forwarded to Google Cloud Tasks queue (burst handling)
3. Execution service clones repo into in-memory volume mount
4. Runs 20-30+ static analyzers
5. LLM generates shell scripts for deeper analysis (ast-grep, grep, Python)
6. Scripts execute in Jailkit sandbox
7. Parallel file reviews with concurrency controls
8. Severity assessment and conditional routing
9. Comment posting or rejection

**Vector Store**: LanceDB -- "tens of thousands of tables," sub-second P99 latency, handles 50K+ daily PRs. Stores embeddings of code, PRs, tickets, team instructions, and user preferences.

### 7. Prompt Patterns

Key techniques identified:

- **Custom diff format** rather than standard unified diff -- designed to match how humans mentally model changes
- **Few-shot examples** embedded in prompts for desired output formatting
- **1:1 code-to-context ratio** -- as much context as code in every prompt
- **Cohort-based ordering** -- semantically related code blocks are clustered and ordered by logical dependency (schema changes before business logic before UI before tests)
- **Level-appropriate depth** -- review depth scales with file complexity
- **Subtask isolation** -- context for sub-tasks is kept separate so it doesn't pollute the main review thread
- **Final context pass** -- results from earlier agents pass through a final selection/compression step before reaching the reasoning model

### 8. Multi-Repo / Cross-Repo Analysis

This is their differentiator vs. RAG-based tools. Instead of pre-indexing into vector stores and doing similarity search:

- An autonomous agent explores linked repositories **in real-time** (clones at HEAD)
- Teams declare related repos in config with architectural context
- The agent iterates search strategies -- if initial symbol search fails, it tries alternative names, import paths, dependency declarations
- Evidence-based: reports only findings with precise file paths and line numbers
- Can trace cross-repo API breakage (e.g., a changed function signature in repo A that breaks callers in repo B)

### 9. What They DON'T Disclose

- Exact prompt templates
- Confidence thresholds for the triage router
- Embedding model used with LanceDB
- Token budgets per stage
- How the verification script generation prompt is structured
- Whether the aggregation agent is a separate model call or post-processing code
- Their Temporal-like workflow engine (custom or off-the-shelf)

---

**Bottom line**: CodeRabbit is a four-stage pipeline (route -&gt; context-build -&gt; reason -&gt; aggregate) with a Claude three-tier model cascade, a LanceDB vector store for memory/context, 20-30 static analyzers feeding into the prompt, sandboxed script execution for verification, and a durable fan-out/fan-in orchestrator on Cloud Run. It's not a single-model wrapper -- it's a full agent system with deterministic pre-processing doing most of the heavy lifting and expensive models reserved for the 30% of changes that actually need deep reasoning.

Sources:
- [Agentic Code Review vs RAG](https://www.coderabbit.ai/blog/agentic-code-review-vs-rag-multi-repo-analysis)
- [Agentic Code Validation](https://www.coderabbit.ai/blog/how-coderabbits-agentic-code-validation-helps-with-code-reviews)
- [Pipeline AI vs Agentic AI](https://www.coderabbit.ai/blog/pipeline-ai-vs-agentic-ai-for-code-reviews-let-the-model-reason-within-reason)
- [Explainable AI Review](https://www.coderabbit.ai/blog/coderabbit-review-reads-a-pr-how-author-would-explain-it)
- [Accurate Reviews on Massive Codebases](https://www.coderabbit.ai/blog/how-coderabbit-delivers-accurate-ai-code-reviews-on-massive-codebases)
- [Intelligence Layer Architecture](https://learnwithparam.com/blog/architecting-coderabbit-ai-agent-intelligence-layer)
- [Orchestration Brain Architecture](https://learnwithparam.com/blog/architecting-coderabbit-ai-agent-orchestration-brain)
- [Context Engineering](https://www.coderabbit.ai/blog/the-art-and-science-of-context-engineering)
- [Google Cloud Run Architecture](https://cloud.google.com/blog/products/ai-machine-learning/how-coderabbit-built-its-ai-code-review-agent-with-google-cloud-run)
- [LanceDB Case Study](https://www.lancedb.com/blog/case-study-coderabbit)
- [Deep Dive](https://www.coderabbit.ai/blog/coderabbit-deep-dive)
- [Planning Layer on Claude](https://www.coderabbit.ai/blog/how-coderabbit-built-a-planning-layer-on-claude)
- [IDE Review Tool](https://www.coderabbit.ai/blog/how-we-built-our-ai-code-review-tool-for-ides)
- [Static Analysis + AI](https://www.coderabbit.ai/blog/boosting-static-analysis-accuracy-with-ai)
- [DeepWiki: Review Results Structure](https://deepwiki.com/coderabbitai/claude-plugin/4.2-understanding-review-results)
- [CodeRabbit GitHub Org](https://github.com/coderabbitai)
- [Anthropic Webinar](https://www.anthropic.com/webinars/how-coderabbit-orchestrates-agents-to-strengthen-ai-generated-code)</result>
<usage><subagent_tokens>45052</subagent_tokens><tool_uses>24</tool_uses><duration_ms>166081</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a3f1417c0747032a1</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/a3f1417c0747032a1.output</output-file>
<status>completed</status>
<summary>Agent "Search adversarial code review agents" completed</summary>
<result>I now have comprehensive data on all the key repos. Here's the full report:

---

## Adversarial &amp; Multi-Agent Code Review: Concrete Implementations

### Tier 1: Strongest Adversarial Implementations

**1. ng/adversarial-review** -- https://github.com/ng/adversarial-review
- **Architecture**: Two-pass adversarial model with Optimizer (finds issues) and Skeptic (challenges findings with external tool evidence). Cost-gating escalates: scores &lt;=0 skip review, 1-4 use Sonnet-only (2 agents), &gt;=5 trigger dual-model (4 agents).
- **Adversarial framing**: Skeptic has explicit anti-rationalization guards listing failure modes ("verification avoidance", "being seduced by the first 80%"). Evidence-gated verdicts require `Command run` output -- "No evidence means the verdict is a guess." Deception detection lens checks whether function names/comments match actual behavior.
- **Output**: Severity tiers (Critical/Major/Minor/Nit/Pre-existing). Markdown artifacts in `.reviews/&lt;branch&gt;/`. Haiku scoring pass filters findings (&gt;=50 confidence retained). A separate "signal gate" enforces an 8-point checklist proving each finding is actionable and PR-introduced.
- **Multi-model**: Sonnet + Opus deliberately exploited for different blind spots. Consensus across both gates auto-fixes. When models disagree, disputes go to the human.
- **Integration**: Claude Code plugin + GitHub Action.
- **"Guilty until proven" pattern**: YES. Every finding must survive external tool verification. Cites Huang et al. 2023 showing LLMs cannot reliably self-correct through reasoning alone.

**2. th3vib3coder/vibe-science** -- https://github.com/th3vib3coder/vibe-science
- **Architecture**: OTAE-Tree (Observe-Think-Act-Evaluate) with 7 agent roles. R2-Deep role explicitly "assumes every claim is wrong; no congratulations." R3-Judge reviews R2's reviews (meta-review), not claims directly.
- **Adversarial framing**: Falsification-first. Seeded Fault Injection (SFI) introduces known faults before review to test vigilance. Blind-First Pass (BFP) -- R2 reviews claims without seeing researcher justification first. Sessions cannot end until all claims are reviewed/killed/disputed.
- **Output**: Structured claim ledger with claim ID, evidence references (DOI/PMID verification), confounder status (raw/conditioned/matched), confidence 0-1, R2 verdict (ACCEPT/DISPUTE/KILL).
- **Multi-model**: Single model (Claude) but 7 specialized roles with separation of powers. R2 produces verdicts; orchestrator writes to ledger.
- **Integration**: Claude Code plugin with 7 lifecycle hooks, SQLite persistence (16 tables).
- **"Guilty until proven" pattern**: YES, the strongest example. Claims default to WRONG. Three-stage validation: Raw claim -&gt; Conditioned claim -&gt; Matched claim. Sign change = ARTIFACT.

**3. wan-huiyan/agent-review-panel** -- https://github.com/wan-huiyan/agent-review-panel
- **Architecture**: 4-6 AI reviewers with assigned divergent stances (Correctness Hawk, Security Auditor, Devil's Advocate, etc.), then a supreme judge (Opus instance). 14-phase pipeline.
- **Adversarial framing**: Blind final scoring (reviewers commit verdicts before seeing peers' finals). Sycophancy detection flags when &gt;50% of position changes lack new evidence. Anti-rhetoric assessment surfaces changes driven by eloquence rather than evidence. Unanimous agreement triggers explicit warning: "unanimous agreement is the most dangerous failure mode."
- **Output**: Three files per run: `review_panel_report.md` (executive summary, P0-P3 action items), `review_panel_process.md` (director's cut), `review_panel_report.html` (interactive dashboard with 10-section issue cards). Phase 14.5 verification gate re-verifies the judge's new findings against ground truth.
- **Multi-model**: All Claude instances but with calibrated skepticism (20-60% range per persona) and different reasoning strategies (systematic enumeration, adversarial simulation, backward reasoning).
- **Integration**: Claude Code skill.
- **"Guilty until proven" pattern**: Partial. Anti-groupthink is the main mechanism rather than default-guilty.

---

### Tier 2: Multi-Agent with Cross-Examination

**4. alecnielsen/adversarial-review** -- https://github.com/alecnielsen/adversarial-review
- **Architecture**: 4-phase iterative debate loop -- Independent reviews (Claude + Codex in parallel) -&gt; Cross-review (each critiques the other) -&gt; Meta-review (agents respond to critiques) -&gt; Synthesis (Claude consolidates). Repeats until consensus or max iterations. Circuit breaker at 5+ iterations without consensus.
- **Output**: Structured status blocks with `ISSUES_FOUND`, `CRITICAL_COUNT`, `CONFIDENCE`, `EXIT_SIGNAL`. Artifacts per iteration.
- **Multi-model**: YES -- Claude + GPT Codex explicitly chosen for different blind spots.
- **Integration**: Shell script orchestration (100% Shell). Requires `claude` and `codex` CLI tools.
- **Language**: Shell (100%).

**5. robertoecf/adversarial-review** -- https://github.com/robertoecf/adversarial-review
- **Architecture**: Review triad (adversarial plan review, coding adversarial review, prompt optimization). Cross-platform: detects host (Claude Code vs Codex) and routes heavy critique to the *other* agent. Anti-recursion guard (`ADVERSARIAL_REVIEW_DEPTH`) prevents re-delegation loops. Falls back through Gemini variants if both unavailable.
- **Output**: Findings tagged `[cross-validated]` (both found it), `[external-only]` (partner found, host missed), `[host-only]` (host found, partner missed). Severity P0-P3.
- **Multi-model**: YES -- core principle is "the partner reviews, never the host."
- **Integration**: Claude Code plugin + Codex plugin (same source).
- **Language**: Shell (100%).

**6. gaurav-yadav/adversarial-ai-review** -- https://github.com/gaurav-yadav/adversarial-ai-review
- **Architecture**: Reviews run in git worktrees -- agents grep, trace call chains, verify authorization paths across the entire codebase, not just the diff. Agent definitions in `.claude/agents/` with a shared adversarial preamble. Separate service reviewers paired with matched dev validators.
- **Output**: Dual verdicts per finding: `IMPLEMENTATION_CORRECTNESS x SOLUTION_FIT` (VALID/INVALID/AMBIGUOUS). Every finding requires specific code path, specific failure condition, specific evidence. Generic observations ("consider adding error handling") rejected.
- **Adversarial framing**: "Separate agents with opposed objectives force genuine cross-examination." Agents in `.claude/agents/` directory include: `adversarial-preamble.md`, `review-orchestrator.md`, `review-{service}.md`, `dev-{service}.md`, plus cross-cutting specialists (security, architecture, impact, observability, solution-fit).

---

### Tier 3: Structured Multi-Agent (Less Adversarial)

**7. spencermarx/open-code-review** -- https://github.com/spencermarx/open-code-review
- **Architecture**: 28 reviewer personas (generalists, specialists, famous engineers like Martin Fowler and Sandi Metz). Structured discourse with 4 interaction modes: AGREE, CHALLENGE, CONNECT, SURFACE. Configurable team via `.ocr/config.yaml` or per-review override.
- **Output**: Session directories with individual reviews, discourse transcripts, and synthesized finals. Requirements Verification table mapping requirements to implementation status.
- **Integration**: Dashboard (browser-based), AI assistant commands (`/ocr-review`), CLI.
- **Adversarial**: Collaborative rather than adversarial. CHALLENGE mode exists but the philosophy is supportive.

**8. calimero-network/ai-code-reviewer** -- https://github.com/calimero-network/ai-code-reviewer
- **Architecture**: 2-5+ parallel agents with specialized focus (Security on Sonnet 4.6, Performance on Sonnet 4.6, Patterns, Logic, Style on Haiku 4.5). Findings weighted by consensus: `[3/3 agents]` notation.
- **Output**: CRITICAL/WARNING/SUGGESTION tiers with agreement ratios. Quality score (e.g. 87%).
- **Integration**: GitHub PR reviews via webhooks with inline comments + CLI.
- **Configuration**: YAML agent definitions with custom prompt appending per agent.

**9. Agent-Field/pr-af** -- https://github.com/Agent-Field/pr-af
- **Architecture**: 7-phase adaptive pipeline. Dynamically generates review dimensions per PR through semantic, mechanical, and systemic lenses. Uses AST extraction for evidence grounding. Falsifiability gates "actively attempt to invalidate its own findings."
- **Output**: JSON findings with severity, file, line, evidence fields. Inline GitHub comments.
- **Adversarial**: Self-adversarial via falsifiability gates, not multi-agent debate.

---

### Tier 4: Devil's Advocate / Self-Critique

**10. brandonsimpson/devils-advocate** -- https://github.com/brandonsimpson/devils-advocate
- **Architecture**: Independence-gated self-critique. When critiquing work Claude wrote in the same conversation, dispatches an independent subagent that never sees the author's reasoning. 20 criteria across 7 dimensions (code) or 22 criteria across 10 dimensions (plans).
- **Output**: Binary pass/fail per criterion with `file:line` evidence and fix suggestions. "15/20 PASS -- 5 criteria need fixing."
- **Pattern**: "If 5+ instances in your codebase do something one way, that's the established pattern -- violations fail even if undocumented."
- **Integration**: Claude Code plugin marketplace.

**11. richiethomas/claude-devils-advocate** -- https://github.com/richiethomas/claude-devils-advocate
- **Architecture**: Multi-round Author/Reviewer simulation. Reviewer must raise a substantive concern every round. Author must push back before conceding. Both must provide code snippets, not just descriptions. Priority ordering: correctness &gt; error handling &gt; performance &gt; security &gt; maintainability &gt; testing gaps.
- **Output**: Round-by-round exchanges, summary table, priority-ranked action items, deferred topics.
- **Integration**: Copy `devils-advocate.md` to `~/.claude/commands/`. Invoke via `/devils-advocate 6`.
- **Research basis**: Du et al. 2023 and Kim et al. 2024 on multiagent LLM debate.

**12. aaddrick/contrarian** -- https://github.com/aaddrick/contrarian
- **Architecture**: Devil's advocate analyst agent. Stress-tests proposals (not code) via pre-mortem analysis. Steel-man arguments. Calibration scales intensity to stakes.
- **Integration**: Copy `.claude/agents/contrarian.md` to project or global agents directory.
- **Scope**: Strategy/architecture review, not line-level code review.

---

### Tier 5: Production-Scale (Not Adversarial But Notable)

**13. alibaba/open-code-review** -- https://github.com/alibaba/open-code-review
- **Architecture**: Hybrid deterministic pipelines + LLM agent. Built-in fine-tuned ruleset for NPE, thread-safety, XSS, SQL injection. Template-engine rule matching (not prompt-driven) for reliability. External positioning and reflection modules for line-level accuracy.
- **Multi-model**: OpenAI + Anthropic compatible.
- **Integration**: CLI, GitHub Action, GitLab CI. Apache-2.0 license.
- **Note**: Battle-tested at Alibaba scale. Not adversarial but the most production-hardened.

---

### Key Patterns Across Implementations

1. **Model diversity as defense**: ng/adversarial-review, alecnielsen/adversarial-review, and robertoecf/adversarial-review all use different models (Sonnet+Opus, Claude+Codex) specifically because different training distributions create different blind spots.

2. **Evidence-gated verdicts**: The strongest implementations (ng/adversarial-review, vibe-science) require external tool evidence (test runs, grep, linter output) rather than trusting LLM reasoning. This is backed by Huang et al. 2023 showing LLMs cannot reliably self-correct.

3. **Anti-groupthink mechanisms**: agent-review-panel's sycophancy detection, blind scoring, and "unanimous agreement is the most dangerous failure mode" warning are the most sophisticated.

4. **Falsification-first**: vibe-science's Seeded Fault Injection (inject known faults to test reviewer vigilance) and Blind-First Pass (review without seeing justification) are the most rigorous "guilty until proven innocent" patterns.

5. **Cross-validation tagging**: robertoecf's `[cross-validated]`/`[external-only]`/`[host-only]` tags provide clear provenance for multi-model findings.</result>
<usage><subagent_tokens>49204</subagent_tokens><tool_uses>23</tool_uses><duration_ms>209028</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a06c2b2e3abc2de22</task-id>
<tool-use-id>toolu_01VU6EtxLkBbhjsk61cDsMmm</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/a06c2b2e3abc2de22.output</output-file>
<status>completed</status>
<summary>Agent "Search adversarial code review patterns" completed</summary>
<result>I now have comprehensive coverage across all five search vectors. Let me compile the findings.

Here is the compiled research on how AI coding tools implement adversarial review subagents.

---

## 1. Architecture: How Is the Reviewer Isolated?

**Session isolation (universal pattern)**
- The single most consistent architectural pattern: the reviewer agent operates in a completely separate session/context from the generator. It never sees the original task prompt, reasoning chain, or conversation history that produced the code. It receives only the diff or final output.
- Source: [MindStudio multi-agent code review guide](https://www.mindstudio.ai/blog/automated-code-review-multiple-ai-agents)

**Subprocess sandboxing (dementev-dev/adversarial-review)**
- The reviewer (Codex) runs via `codex exec` in a subprocess with a 10-minute timeout. Sandbox modes include `read-only`, `workspace-write`, `danger-full-access`, or `inherit`. Tracked-file mutation detection triggers a hard stop. Pre/post `git status --porcelain` + `sha256sum` snapshots detect unauthorized mutations.
- Reviewer operates under a `&lt;reviewer_permissions&gt;` prompt contract: read + verify only, no edits.
- Source: [dementev-dev/adversarial-review](https://github.com/dementev-dev/adversarial-review)

**Cross-model isolation (4-agent MCP team)**
- The architect agent has no cognitive harness and no HTTP tool -- it cannot author concerns. It can only delegate, classify, and integrate. Each specialist agent is locked to a different cognitive scaffold. Models span Anthropic, Google, Alibaba, and Zhipu to reduce correlated failure modes.
- Source: [DEV Community -- 4-agent adversarial review MCP](https://dev.to/frank_brsrk/i-open-sourced-a-4-agent-adversarial-code-review-team-any-coding-agent-can-call-it-as-an-mcp-36oe)

**Parallel subagent isolation (agent-review-panel)**
- 4-6 Claude instances launch in parallel, each assigned a distinct persona. They evaluate independently before seeing peers' output. Blind final scoring -- reviewers commit verdicts independently without knowing others' results.
- Source: [wan-huiyan/agent-review-panel](https://github.com/wan-huiyan/agent-review-panel)

**Tool restrictions (ASDLC pattern)**
- The Critic agent is explicitly prohibited from modifying code. Its constitution states: "You are skeptical. Your job is to reject code that violates the Spec, even if it 'works.' Favor false positives over false negatives." Advanced implementations use model routing to assign different model classes by capability.
- Source: [ASDLC adversarial code review pattern](https://asdlc.io/patterns/adversarial-code-review/)

---

## 2. Prompting: What System Prompt Patterns Make It Adversarial?

**"Guilty until proven innocent" framing (alirezarezvani/claude-skills)**
- "Assume every variable could be null/undefined until proven otherwise. Assume every external call will fail."
- Each of three personas (Saboteur, New Hire, Security Auditor) MUST find at least one issue -- no "LGTM" escapes.
- "If I deleted this change entirely, what would break?" -- if the answer is "nothing," the change might be unnecessary.
- Read bottom-up; state function contracts before reading bodies.
- Source: [alirezarezvani/claude-skills adversarial-reviewer](https://github.com/alirezarezvani/claude-skills/blob/main/engineering-team/skills/adversarial-reviewer/SKILL.md)

**Defaults to skepticism (dementev-dev)**
- Role framing: "adversarial reviewer, defaults to skepticism"
- Operating stance: "Break confidence, not validate"
- Each finding must answer: what can fail, why it is vulnerable, the impact, and a recommendation.
- Scope exclusions: no style, naming, or speculative comments. Only production-risk findings.
- Source: [dementev-dev/adversarial-review](https://github.com/dementev-dev/adversarial-review)

**Constitution-driven rejection bias (ASDLC)**
- "You are skeptical. Your job is to reject code that violates the Spec, even if it 'works.' Favor false positives over false negatives."
- This is the strongest articulation of asymmetric review: the cost of missing a real bug is framed as higher than the cost of a false alarm.
- Source: [ASDLC adversarial code review](https://asdlc.io/patterns/adversarial-code-review/)

**Adversarial loop prompting (courtroom model)**
- Generator = defense lawyer; Critic = prosecutor; Human = judge.
- Falsifiability requirement: "If you claim there's a problem, you need to prove it." Critics must write tests for their assertions.
- Source: [nhoj.substack.com -- Adversarial Loop Prompting](https://nhoj.substack.com/p/adversarial-loop-prompting)

**Actor-Critic paranoid persona**
- Critic persona: "Assume the code is vulnerable until proven otherwise."
- Systematically evaluates across 8 dimensions: security, architecture, performance, testing, error handling, documentation, accessibility, code quality.
- Source: [Understanding Data -- Actor-Critic Adversarial Coding](https://understandingdata.com/posts/actor-critic-adversarial-coding/)

**Anti-sycophancy mechanisms (agent-review-panel)**
- Sycophancy detection: flags when &gt;50% of position changes lack new evidence.
- Anti-rhetoric assessment: distinguishes evidence-based shifts from eloquence-driven ones.
- Correlated-bias warning when all reviewers converge (called "the most dangerous failure mode").
- Calibrated skepticism levels (20-60%) per persona.
- Source: [wan-huiyan/agent-review-panel](https://github.com/wan-huiyan/agent-review-panel)

---

## 3. Output Structure: Severity Levels, Confidence Scores, Structured JSON

**Three-tier severity with verdict (alirezarezvani/claude-skills)**
- CRITICAL: causes data loss, breach, or outage; blocks merge
- WARNING: edge-case bugs or maintainability debt; fix recommended
- NOTE: style or minor improvement; author discretion
- Verdict: BLOCK (&gt;=1 critical), CONCERNS (0 criticals, &gt;=2 warnings), CLEAN (notes only)
- Promotion rule: issues caught by 2+ personas escalate one severity tier.

**Four-tier with epistemic labels (agent-review-panel)**
- P0: ship-blocker (correctness, data integrity, security)
- P1: should fix soon (real defect but not release-blocking)
- P2: nice to have (stylistic or future-rot)
- P3: nit (optional polish)
- Epistemic tags: `[VERIFIED]`, `[CONSENSUS]`, `[DISPUTED]`, `[UNVERIFIED]`, `[WEB-VERIFIED]`, `[WEB-CONTRADICTED]`, `[LIVE-VERIFIED]`, `[JUDGE-HALLUCINATED]`
- Three output files: executive markdown, verbatim process log, interactive HTML dashboard.

**Status block format (alecnielsen/adversarial-review)**
- `ISSUES_FOUND: 3 / CRITICAL_COUNT: 1 / HIGH_COUNT: 1 / MEDIUM_COUNT: 1 / LOW_COUNT: 0 / CONFIDENCE: HIGH`
- Enables automated parsing. Framework analyzes exit signals and confidence ratings.

**OpenAI Codex structured output schema**
- Findings array with: `title` (string, max 80 chars), `body`, `confidence_score` (0-1), `priority` (integer 0-3), `code_location` (absolute_file_path + line_range).
- Overall assessment: `overall_correctness` enum ("patch is correct" / "patch is incorrect"), `overall_explanation`, `overall_confidence_score` (0-1).
- Source: [OpenAI Codex SDK code review cookbook](https://developers.openai.com/cookbook/examples/codex/build_code_review_with_codex_sdk)

**Actor-Critic output**
- Verdict: APPROVED or NEEDS_CHANGES.
- Issues categorized by severity: Critical (must fix), Warning (should fix), Minor (nice to fix).
- Each issue: file:line location, problem explanation, concrete code fix example.

---

## 4. Integration: Merge Blocking, Issue Filing, Inline Comments

**CI-triggered PR blocking (MindStudio pattern)**
- Review output posts as PR comments. Blocking criteria: "block on anything the validator flags as 'critical' (security issues, data corruption risk, broken interfaces)."
- Source: [MindStudio](https://www.mindstudio.ai/blog/automated-code-review-multiple-ai-agents)

**Iterative fix loop (dementev-dev)**
- Five-round maximum. After review, the lead agent applies fixes. Rounds 2-5 resume via `codex exec resume &lt;session-id&gt;` (maintains context). Terminal states: APPROVED, max rounds reached, NOT VERIFIED, or ABORTED.
- CI integration listed as roadmap item; currently CLI-only.

**ASDLC verdict gating**
- PASS: code advances to human acceptance gate.
- FAIL: violations returned to builder as blocking task; code cannot proceed to the next phase.

**Qodo Merge (production tool)**
- Runs automatically on PR open across GitHub, GitLab, Bitbucket, Azure DevOps. Multi-agent architecture with specialized agents for bugs, code quality, security, and test coverage gaps. Posts structured findings as PR comments.
- Source: [Qodo blog](https://www.qodo.ai/blog/the-next-generation-of-ai-code-review-from-isolated-to-system-intelligence/)

**4-phase debate loop (alecnielsen/adversarial-review)**
- Phase 1: independent review by both models. Phase 2: cross-review (each critiques the other). Phase 3: meta-review (responses to critiques). Phase 4: synthesis (final decision). Repeats until both report no issues or iteration limit is reached.

**AgentDesk (one API call)**
- Dual independent reviewers. Consensus required: both must pass. Scored 0-100 with structured feedback. Proprietary internals; exposed as simple REST API.
- Source: [DEV Community -- adversarial review in one API call](https://dev.to/rih0z/why-ai-agent-outputs-need-adversarial-review-and-how-to-add-it-in-one-api-call-1l92)

---

## 5. Model Choice: Different Model or Temperature Than Primary Agent

**Cross-provider diversity (strongest pattern)**
- alecnielsen/adversarial-review: Claude + GPT Codex operating in parallel (different providers = different failure modes).
- 4-agent MCP team: models span Anthropic, Google, Alibaba, Zhipu explicitly "to reduce correlated failure modes."
- MindStudio: "Using different underlying models for builder and validator adds another layer of independence. Claude and Codex have different training approaches and tend to make different kinds of mistakes."
- ASDLC: Builder = high-throughput model; Critic = high-reasoning model. Different model classes assigned by role capability.

**Same model, separate sessions still works**
- MindStudio notes that "separate sessions alone improve results, even with identical models" because context isolation prevents the reviewer from inheriting the generator's reasoning chain.

**Model-specific configuration (dementev-dev)**
- Reviewer default: `gpt-5.5`. Reasoning effort configurable: `low`, `medium`, `high`, `xhigh`. Override syntax: `/adversarial-review model:gpt-5.4`.
- Temperature not specified in documentation.

**agent-review-panel**
- Every subagent launch explicitly passes `model: "opus"` to eliminate reasoning variance across reviewers.

**Actor-Critic cost profile**
- Approximately 5 LLM calls per feature cycle, $0.50-$1.50 total. 2-5 minutes per feature vs. 30-60 seconds single-pass. 50%+ fewer human review cycles.

---

## 6. Critical Research Finding: Confirmation Bias Vulnerability

A June 2026 paper on confirmation bias in LLM code review found that adversarial framing in PR metadata can catastrophically degrade vulnerability detection:

- GPT-4o-mini: 97.2% detection dropped to 3.6% under "bug-free" framing (93.5pp drop)
- Claude 3.5 Haiku: 68.4% to 8.5% (59.9pp drop)
- DeepSeek V3: 96.8% to 53.8% (42.9pp drop)

**Defense**: Metadata redaction (removing PR descriptions) recovered 68.75% of missed detections. Explicit instructions to "ignore metadata and analyze only code" achieved 94% recovery. This directly supports the architectural pattern of giving reviewers only the diff, not the original task description.

Source: [arxiv.org/html/2603.18740v1](https://arxiv.org/html/2603.18740v1)

---

## 7. Constitutional Spec-Driven Development (Tangent but Relevant)

A methodology that embeds security constraints as a machine-readable "constitution" with CWE references, RFC 2119 enforcement levels (MUST/SHOULD/MAY), and compliance traceability matrices. Context-managed prompting limits the constitution to 3-5 relevant principles per request (96% compliance vs. 78% with full constitution). Achieved 73% reduction in security vulnerabilities vs. unconstrained generation.

Source: [arxiv.org/html/2602.02584v1](https://arxiv.org/html/2602.02584v1)

---

## Key Takeaways

1. **Session isolation is non-negotiable.** Every serious implementation separates generator and reviewer into distinct sessions/contexts. The reviewer must never see the original task prompt or reasoning chain.

2. **Cross-provider model diversity is the gold standard** for reducing correlated failures. Same-model-different-session is a weaker but still effective fallback.

3. **"Must find at least one issue" is the critical prompt pattern** that prevents rubber-stamping. Combined with "assume vulnerable until proven otherwise" and "favor false positives over false negatives."

4. **Structured output with verdicts is universal.** Every implementation uses some form of BLOCK/PASS gating with severity tiers. The OpenAI schema (confidence 0-1, priority 0-3, file+line location) is the most machine-parseable.

5. **3-5 round iterative loops** are the sweet spot -- diminishing returns beyond 5 rounds.

6. **Metadata stripping is a security requirement**, not a nice-to-have. The confirmation bias research proves that PR descriptions can suppress vulnerability detection by 90+ percentage points.</result>
<usage><subagent_tokens>53853</subagent_tokens><tool_uses>31</tool_uses><duration_ms>274565</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a449d956fbf1aee12</task-id>
<tool-use-id>REDACTED</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/a449d956fbf1aee12.output</output-file>
<status>completed</status>
<summary>Agent "Search multi-agent review patterns" completed</summary>
<result>I now have comprehensive data across all the major implementations. Here is the full report.

---

## Multi-Agent Code Review: Open Source Implementations Report

### 1. Open Code Review (Spencer Marx) -- The Most Complete Multi-Agent Architecture

**URL**: [github.com/spencermarx/open-code-review](https://github.com/spencermarx/open-code-review)
**NPM**: `@open-code-review/cli`
**Stack**: TypeScript (87%), Node &gt;= 22.5, SQLite for state

**8-Phase Pipeline:**
1. Context Discovery -- load config and project standards
2. Change Analysis -- parse git diffs
3. Tech Lead Assessment -- summarize changes, select reviewer team
4. Parallel Reviews -- independent reviewer examination
5. Aggregation -- merge redundant findings
6. Discourse -- reviewers debate findings
7. Synthesis -- produce prioritized final review
8. Presentation -- display or post results

**28 Reviewer Personas in 4 Tiers:**
- Generalists: Principal, Quality
- Specialists: Security, Testing, Performance
- Famous Engineers: Martin Fowler, Kent Beck, Sandi Metz, Rich Hickey, Anders Hejlsberg, John Ousterhout, Kamil Mysliwiec, Tanner Linsley, Vladimir Khorikov
- Custom: user-defined persistent or ephemeral reviewers

**Discourse/Debate Phase -- 4 Interaction Modes:**
- AGREE -- validate with supporting evidence
- CHALLENGE -- question assumptions
- CONNECT -- relate findings across reviewers
- SURFACE -- raise new concerns discovered during debate

**Disagreement Resolution**: Synthesis phase deduplicates and prioritizes. Requirements verification table shows met/gaps/ambiguous. Dashboard supports manual triage (needs review, in progress, changes made, acknowledged, dismissed).

**Different Models Per Reviewer:**
```yaml
models:
  aliases:
    workhorse: claude-sonnet-4-6
    big-brain: claude-opus-4-7
  default: claude-sonnet-4-6
default_team:
  principal: { count: 1, model: big-brain }
  quality: 2
```

**Output**: Sessions persist to `.ocr/sessions/{date}-{branch}/` with individual reviewer outputs, discourse transcripts, and synthesized final review. Two posting modes: raw Team Review or Human Review Translation (AI-rewritten in natural voice following Google's code review guidelines).

**Key Architectural Insight**: "An uncontested opinion is worth less than a contested one." Reviewers are defined via markdown files with explicit focus areas, not code-based role definitions.

---

### 2. AI Code Reviewer (Calimero Network) -- Consensus-Based Scoring

**URL**: [github.com/calimero-network/ai-code-reviewer](https://github.com/calimero-network/ai-code-reviewer)

**Orchestration**: 2-5+ agents run in parallel via Anthropic Messages API.

**Specialized Agents:**
- Security Agent (Sonnet 4.6): security, architecture
- Performance Agent (Sonnet 4.6): performance, logic
- Patterns Agent (Sonnet 4.6): pattern analysis
- Logic Agent (Sonnet 4.6): logic verification
- Style Agent (Haiku 4.5): style, readability (tool use disabled for cost)

**Disagreement Resolution -- Consensus-Based Scoring:**
1. Cluster similar findings across agents
2. Compute consensus scores weighted by agreement count
3. Rank by severity x agreement strength
4. Findings backed by more agents get higher weight

**Output**: CRITICAL/WARNING/SUGGESTION with agent agreement counts (e.g., "[3/3 agents]"). Overall quality percentage derived from agreement strength.

**Notable**: Uses different model tiers -- reasoning-heavy agents on Opus with extended thinking, broader agents on Sonnet. Style agent on Haiku with tool use disabled. Repo exploration via tool use (read_file/glob/grep) backed by GitHub Contents API -- no cloning required. Incremental reviews with convergence detection that stops reviewing when findings stabilize.

---

### 3. Alibaba Open Code Review -- Hybrid Deterministic + Agent Pipeline

**URL**: [github.com/alibaba/open-code-review](https://github.com/alibaba/open-code-review)
**Stack**: Go (72.5%), TypeScript (18.9%)

**Two-Layer Architecture:**

*Deterministic Layer* (no LLM):
- Precise file selection ensuring no changes are missed
- Smart file bundling -- groups related files for isolated sub-agent review
- Fine-grained rule matching via template engine
- Built-in ruleset in `system_rules.json` covering NPE, thread-safety, XSS, SQL injection

*Agent Layer*:
- Scenario-tuned prompts optimized from production call traces
- Distilled toolset (read full files, search codebase, inspect other changed files)
- External positioning module for line-level accuracy
- Comment-reflection module for content accuracy

**Rule Priority Chain**: CLI flags &gt; project config &gt; global config &gt; system defaults. Rules use glob patterns with file inclusion/exclusion.

**Output**: text (human) or json (CI/CD). This is a single-agent system but notable for its production-hardened deterministic scaffolding around the LLM.

---

### 4. Parallel Subagents Pattern (Pydantic AI)

**Source**: [Medium article by Kacper Wlodarczyk](https://medium.com/@kacperwlodarczyk/build-an-ai-pr-reviewer-with-3-parallel-subagents-in-python-security-style-performance-in-one-285a55d4c6c8)

**3 Parallel Subagents:**
- Security Reviewer: SQL injection, XSS, hardcoded secrets, unsafe deserialization, command injection
- Style Reviewer: naming violations, duplication, complexity, missing type hints, dead code
- Performance Reviewer: N+1 queries, unnecessary allocations, blocking I/O in async, missing indexes

**Orchestration**: Parent agent delegates to all three in parallel via `create_deep_agent(..., subagents=[...])`. Each subagent is a `SubAgentConfig` with name, description, and domain-specific system prompt instructions.

**Output Schema** (Pydantic model `ReviewFinding`):
- `file`: source location
- `line`: numeric position
- `severity`: critical/warning/info
- `category`: security/style/performance
- `description`: explanation
- `suggestion`: remediation

**Aggregation**: Parent agent merges, deduplicates, retains highest severity for identical issues. Built on Claude Sonnet 4.5 + pydantic-deepagents.

---

### 5. MindStudio Builder-Validator Pattern

**Source**: [mindstudio.ai/blog](https://www.mindstudio.ai/blog/automated-code-review-multiple-ai-agents)

**Core Principle**: "Split code generation and code review into two completely independent agent sessions."

**Two-Agent Base:**
- Builder Agent -- generates code, outputs clean diffs only (no reasoning)
- Validator Agent -- receives diff + original requirements (NOT builder reasoning), reviews independently

**Three-Agent Extension:**
- Architect agent -- structural and design concerns
- Security agent -- vulnerabilities and input validation
- QA agent -- testability and edge case coverage

**Key Patterns:**
- Session isolation: unique session IDs per agent to prevent context contamination
- Clean handoff: "the validator must not see the builder's reasoning. Only the output."
- Different models for builder vs validator (e.g., Claude for building, Codex for validation)
- Tiered model routing: Haiku/GPT-4o-mini for triage, Opus/o3 for complex diffs

**Structured Rubric for Validators**: Correctness against requirements, edge case handling, security issues, performance bottlenecks, error handling, breaking changes.

---

### 6. CodeAgent (Academic -- arxiv 2402.02172)

**URL**: [github.com/Code4Agent/codeagent](https://github.com/Code4Agent/codeagent)
**Paper**: [arxiv.org/abs/2402.02172](https://arxiv.org/abs/2402.02172)

Multi-agent LLM system with a supervisory QA-Checker agent that ensures all agents' contributions address the initial review question. Evaluated on four tasks: detecting inconsistencies between code changes and commit messages, identifying vulnerability introductions, validating code style adherence, and suggesting code revisions. Implementation available but README is sparse -- real details are in the paper PDF and Zenodo archives.

---

### 7. Microsoft BlueCodeAgent -- Red/Blue Adversarial

**Source**: [microsoft.com/research/blog/bluecodeagent](https://www.microsoft.com/en-us/research/blog/bluecodeagent-a-blue-teaming-agent-enabled-by-automated-red-teaming-for-codegen-ai/)

**Red Team Agent** (3 strategies):
- Policy-based generation: collect security/ethical policies, prompt uncensored model to generate violating instances
- Seed-based optimization: iteratively refine adversarial prompts using jailbreak tools until high attack success
- Vulnerability generation: synthesize diverse vulnerable + secure code examples

**Blue Team Agent** (2 mechanisms):
- Principled-Level Defense: distill red-team data into actionable constitutions (explicit rules from attack data)
- Nuanced-Level Analysis: dynamic sandbox execution in Docker to verify whether flagged vulnerabilities actually manifest

**Pipeline**: Red-team generates risky cases -&gt; distilled into safety constitutions -&gt; guide blue agent detection -&gt; sandbox validates predictions. Covers biased instructions, malicious instructions, and vulnerable code output. F1 scores approach 1.0 for bias/malicious instruction detection, 12.7% average F1 improvement across four datasets.

---

### 8. DebateLLM (InstaDeep) -- Debate Protocol Framework

**URL**: [github.com/instadeepai/DebateLLM](https://github.com/instadeepai/DebateLLM)

**6 Debate Protocols Implemented:**
1. Society of Minds -- agents collaborate through discussion
2. Medprompt -- specialized Q&amp;A approach
3. Multi-Persona -- multiple character perspectives debate
4. Ensemble Refinement -- aggregate and refine multiple outputs
5. ChatEval -- conversational evaluation
6. Solo Performance Prompting -- individual agent optimization

**Key Finding**: "No single debate or prompting strategy consistently outperforms others." Agreement intensity modulation (adjusting consensus thresholds) yields ~15% gains for Multi-Persona, ~5% for Society of Minds. Uses Hydra configs for experiment management. Not code-review-specific but directly applicable as debate infrastructure.

---

### 9. Prompt Libraries

**baz-scm/awesome-reviewers**: [github.com/baz-scm/awesome-reviewers](https://github.com/baz-scm/awesome-reviewers) -- reusable prompts distilled from real code review feedback in leading OSS repos. Skills in `_reviewers/` directory covering quality, security, readability, testing, infrastructure.

**jwadow/agentic-prompts**: [github.com/jwadow/agentic-prompts](https://github.com/jwadow/agentic-prompts) -- Prompt-as-Code architecture with specialized personas: Principal Engineer (architecture), Lead Implementer (implementation), Gardener (refactoring/debt), Mr. Robot (security audits), Observer (performance/observability), Annihilator (ruthless complexity elimination).

**bobmatnyc/ai-code-review**: [github.com/bobmatnyc/ai-code-review](https://github.com/bobmatnyc/ai-code-review) -- 7 review types (quick-fixes, architectural, security, performance, unused-code, best-practices, evaluation), 95%+ token reduction via TreeSitter semantic chunking, severity (High/Medium/Low) + confidence (0.0-1.0) scoring, supports Gemini/Claude/OpenAI/OpenRouter.

---

### Cross-Cutting Patterns Summary

| Pattern | Who Uses It | How It Works |
|---|---|---|
| Parallel independent review | OCR, Calimero, Pydantic AI | Each agent reviews independently in parallel, findings merged after |
| Structured discourse/debate | OCR (Spencer Marx) | Reviewers examine each other's findings with AGREE/CHALLENGE/CONNECT/SURFACE |
| Consensus scoring | Calimero | Findings weighted by agent agreement count, reduces false positives |
| Builder-Validator separation | MindStudio | Generator produces code, separate agent reviews without seeing reasoning |
| Red/Blue adversarial | Microsoft BlueCodeAgent | Red team generates attacks, blue team builds defenses from attack data |
| Deterministic + Agent hybrid | Alibaba OCR | Rule-based layer handles critical correctness, agent handles dynamic analysis |
| Different models per agent | OCR, Calimero, MindStudio | Heavy reasoning on Opus, broad review on Sonnet, style on Haiku |
| Structured output schemas | Pydantic AI, Calimero, bobmatnyc | Pydantic/JSON schema with severity, confidence, file, line, suggestion |</result>
<usage><subagent_tokens>50877</subagent_tokens><tool_uses>31</tool_uses><duration_ms>236575</duration_ms></usage>
</task-notification>

---

<task-notification>
<task-id>a52bf360ff4d2e3c6</task-id>
<tool-use-id>toolu_0168PPcMpSLvAL2kYvPXSRwk</tool-use-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/a52bf360ff4d2e3c6.output</output-file>
<status>completed</status>
<summary>Agent "Research PR-Agent by CodiumAI/Qodo" completed</summary>
<result>I now have comprehensive data. Let me compile the research report.

---

Here is the full architecture breakdown of PR-Agent by Qodo (formerly CodiumAI).

## Repository

The canonical open-source repo is **[qodo-ai/pr-agent](https://github.com/qodo-ai/pr-agent)** (11.6k stars, Apache-2.0). An earlier fork at `Codium-ai/pr-agent` is frozen. A community fork at `The-PR-Agent/pr-agent` also exists.

---

## 1. Directory Structure

```
pr_agent/
  settings/                          # All prompts + config as TOML
    configuration.toml               # Master config (model, thresholds, flags)
    pr_reviewer_prompts.toml         # /review system+user prompts
    pr_description_prompts.toml      # /describe prompts
    pr_evaluate_prompt_response.toml # Meta-evaluator (A/B scoring)
    code_suggestions/
      pr_code_suggestions_prompts.toml          # /improve prompts
      pr_code_suggestions_prompts_not_decoupled.toml
      pr_code_suggestions_reflect_prompts.toml  # Self-reflection scoring
    custom_labels.toml
    language_extensions.toml
    ignore.toml
  tools/                             # One Python class per command
    pr_reviewer.py                   # /review
    pr_code_suggestions.py           # /improve
    pr_description.py                # /describe
    pr_add_docs.py                   # /add_docs
    pr_questions.py                  # /ask
    pr_line_questions.py             # /ask on specific lines
    pr_update_changelog.py           # /update_changelog
    pr_generate_labels.py            # /generate_labels
    pr_similar_issue.py
    ticket_pr_compliance_check.py
  algo/
    ai_handlers/
      litellm_ai_handler.py          # LiteLLM-based multi-provider router
      base_ai_handler.py             # Abstract interface
    pr_processing.py                 # Diff splitting, token budgeting
    utils.py                         # ModelType enum, load_yaml, get_model()
    token_handler.py                 # Token counting (tiktoken + Anthropic API)
```

---

## 2. Prompt Architecture

All prompts live in **TOML files** under `pr_agent/settings/`. Each has a `[system]` and `[user]` section containing Jinja2 templates.

### Reviewer Persona (`pr_reviewer_prompts.toml`)

The system prompt opens with:

&gt; "You are PR-Reviewer, a language model designed to review a Git Pull Request (PR). Your task is to provide constructive and concise feedback for the PR."

Key framing instructions (verbatim excerpts):

- **Bug detection threshold**: "For clear bugs and security issues, be thorough. Do not skip a genuine problem just because the trigger scenario is narrow."
- **Low-severity gating**: "For lower-severity concerns, be certain before flagging. If you cannot confidently explain why something is a problem with a concrete scenario, do not flag it."
- **High-impact uncertainty rule**: "When confidence is limited but the potential impact is high (e.g., data loss, security), report it with an explicit note on what remains uncertain. Otherwise, prefer not reporting over guessing."
- **Actionability requirement**: Each issue must be "discrete and actionable, not a vague concern about the codebase in general."
- **Scope guard**: Reviews only `+` lines (new code). No speculation about code outside the diff.
- **Tone**: "Direct, matter-of-fact, helpful -- avoiding accusatory language and excessive praise."

### Code Suggestion Persona (`pr_code_suggestions_prompts.toml`)

Defines the agent as "an AI specializing in Pull Request (PR) code analysis and suggestions." Two modes:

**Standard mode labels**: security, possible bug, possible issue, performance, enhancement, best practice, maintainability, typo

**Focus-only-on-problems mode labels**: security, critical bug, general

### Exclusion List (what NOT to suggest)

This is critical design -- they explicitly suppress low-value noise:
- Docstrings, type hints, or comments
- Removing unused imports
- More specific exception types
- Changing package versions
- Adding missing imports
- Declaring undefined variables
- Removing unused variables
- Duplicating existing functionality

---

## 3. Output Format / Schema

### Review Output (`key_issues_to_review`)

Each issue is a YAML object with:
- `relevant_file` -- full file path
- `issue_header` -- one or two word title
- `issue_content` -- short description with trigger scenario (no line numbers in text)
- `start_line` / `end_line` -- line number references

Additional review fields:
- `estimated_effort_to_review_[1-5]` -- 1-5 scale
- `score` -- 0-100 quality rating
- `relevant_tests` -- yes/no
- `security_concerns` -- vulnerability assessment
- `can_be_split` -- list of independent sub-PR candidates
- `ticket_compliance_check` -- validates PR against related tickets

Empty issue list is explicitly acceptable: "An empty list is acceptable if no clear issues are found."

### Code Suggestion Output (`CodeSuggestion`)

Each suggestion:
- `relevant_file`, `language`
- `existing_code` -- snippet from the new hunk
- `suggestion_content` -- actionable improvement (prose, no code)
- `improved_code` -- refined replacement
- `one_sentence_summary` -- max 6 words
- `label` -- one of the category labels above

Output format is **YAML** (not JSON), parsed by `load_yaml()` which has **nine progressively aggressive recovery strategies** for malformed YAML.

---

## 4. Multi-Model Architecture

### ModelType Enum

```python
class ModelType(str, Enum):
    REGULAR = "regular"
    WEAK = "weak"
    REASONING = "reasoning"
```

### Model Selection (`get_model()` in `algo/utils.py`)

```python
def get_model(model_type: str = "model_weak") -&gt; str:
    if model_type == "model_weak" and get_settings().get("config.model_weak"):
        return get_settings().config.model_weak
    elif model_type == "model_reasoning" and get_settings().get("config.model_reasoning"):
        return get_settings().config.model_reasoning
    return get_settings().config.model  # fallback to primary
```

### Default Configuration (`configuration.toml`)

- **Primary model**: `gpt-5.5-2026-04-23`
- **Fallback models**: `["gpt-5.4-mini"]`
- **Reasoning model**: `gpt-5.4-mini` (optional, commented out)
- **Weak model**: `gpt-5.4-nano` (optional, commented out)
- **Max tokens**: 32,000
- **Temperature**: 0.2
- **Reasoning effort**: "medium"

### Two-Model Suggestion Pipeline

This is the most interesting architectural pattern. `pr_code_suggestions.py` uses **two distinct model passes**:

1. **Generation pass** (primary model): Generates code suggestions via the main prompt
2. **Self-reflection pass** (`model_reasoning`): Re-evaluates suggestions independently using `pr_code_suggestions_reflect_prompts.toml`

The self-reflection scores each suggestion 0-10:
- **8-10**: Critical bugs, security vulnerabilities, major functional issues
- **3-7**: Style, readability, maintainability
- **0**: Docstrings, type hints, comments, import changes, when existing equals improved

Filtering logic:
```python
score_threshold = max(1, int(get_settings().pr_code_suggestions.suggestions_score_threshold))
if score &gt;= score_threshold:
    data["code_suggestions"].append(prediction)
```

Score-to-category mapping: High (&gt;=9), Medium (&gt;=7), Low (&lt;7).

If reflection fails, default score of 7 is assigned (permissive fallback).

### Meta-Evaluation (`pr_evaluate_prompt_response.toml`)

A/B comparison evaluator that scores two competing responses on a 1-10 scale, producing:
- `which_response_was_better` (0, 1, or 2)
- `score_response1`, `score_response2`
- `why` -- specific justification

Explicitly anti-length-bias: "Don't necessarily rank higher a response that is longer."

---

## 5. Multi-Provider Support (LiteLLM Handler)

The `litellm_ai_handler.py` routes through LiteLLM, supporting:

- OpenAI/Azure, Anthropic, Cohere, Groq, SambaNova, Replicate, XAI, HuggingFace, Ollama, VertexAI, Google AI Studio, DeepSeek, Mistral, OpenRouter, AWS Bedrock

Model-specific handling:
- **Reasoning models (GPT-5)**: Temperature removed, reasoning effort via enum
- **Claude extended thinking**: `budget_tokens` + `max_tokens`, temperature forced to 1
- **User-message-only models**: System+user merged into single message
- **Streaming-required models**: Auto-detected per model

Retry: `@retry` decorator with `MODEL_RETRIES = 2` on `openai.APIError`. AWS Bedrock has additional IMDS-to-static-credential fallback.

---

## 6. Token Budget Management

Two-threshold system:
- **Soft threshold**: 1,500 tokens reserved for output buffer
- **Hard threshold**: 1,000 tokens reserved for safety margin

Large PR handling pipeline:
1. **Single-pass attempt**: Full diff with context lines (max 10 before/after)
2. **Compression fallback**: Remove context, prune less important files
3. **Multi-call mode**: `get_pr_multi_diffs()` splits remaining files across up to `max_calls=5` sequential API calls
4. Files sorted by token count descending (priority inclusion for largest files)

Token counting: tiktoken for OpenAI, actual Anthropic API for Claude (9MB content limit), estimation factor for others.

---

## 7. Integration Methods

- **GitHub Action**: Docker container via `Dockerfile.github_action_dockerhub`
- **GitHub App**: Webhook-based, always-on
- **CLI**: Direct invocation
- **Docker**: Self-hosted
- **Platforms**: GitHub, GitLab, Bitbucket, Azure DevOps, Gitea

The GitHub Action definition is minimal -- just `name`, `description`, `branding`, and Docker image reference.

---

## 8. Adversarial / "Assume Bugs Exist" Framing

PR-Agent does **not** use explicit adversarial framing. The approach is more calibrated:

- High-severity issues get thorough treatment regardless of trigger probability
- Low-severity issues require high confidence before flagging
- The self-reflection pass acts as an adversarial filter, actively looking to downgrade/eliminate weak suggestions
- The exclusion list aggressively suppresses noise categories

This is a **confidence-gated** approach rather than a "guilty until proven innocent" one. The explicit instruction "prefer not reporting over guessing" for low-impact items is the opposite of adversarial.

---

## 9. Qodo Merge 2.0 (Commercial Multi-Agent Product)

Released February 2026, built on top of PR-Agent's engine but with a fundamentally different architecture:

**Agent count**: 12+ specialized agents running in parallel, each with dedicated context (not competing for attention in a single prompt).

**Agent specializations**: Backend bugs, UI issues, runtime failures, rule violations, security risks, performance regressions, accessibility gaps, and more.

**Judge agent**: A separate aggregation agent that:
- Resolves conflicts between agents
- Removes duplicates
- Filters low-signal results
- Only surfaces issues meeting high confidence + relevance thresholds

**Context engine**: Incorporates full PR history, repository context, and organizational standards (via "Qodo's Rule System").

**Benchmarks** (Qodo Code Review Benchmark 1.0):
- 580 defects across 100 real production PRs
- **F1: 60.1%** (highest among 8 tested tools)
- **Recall: 56.7%** (highest)
- 9% margin over next-best

**What's NOT disclosed**: Whether different LLMs power different agents, inter-agent communication format, exact prompt structures, or the judge agent's scoring mechanism. These are proprietary to the commercial product.

---

## Key Architectural Patterns Worth Stealing

1. **TOML prompt files with Jinja2 templating** -- Clean separation of prompt content from orchestration code
2. **Two-model generate-then-reflect pipeline** -- Primary model generates, reasoning model scores and filters
3. **Explicit exclusion lists** -- Suppress known noise categories at the prompt level rather than post-filtering
4. **Confidence-gated severity** -- High-impact gets thorough treatment; low-impact requires certainty
5. **Nine-level YAML recovery** -- Progressively aggressive parsing for malformed LLM output
6. **Score-to-category mapping** -- Numeric scores (0-10) mapped to High/Medium/Low for display
7. **Multi-diff chunking** -- Large PRs split across multiple API calls with token-budget-aware file prioritization</result>
<usage><subagent_tokens>50779</subagent_tokens><tool_uses>36</tool_uses><duration_ms>302068</duration_ms></usage>
</task-notification>

---

what do you mean IS something

---

so all this information we collected, we can make our our own workflow more robust right?

---

al,so i can already see MASSIVE token savings

---

no steal ALL THE GOOD IDEAS

---

<task-notification>
<task-id>a28b044e03ce6c5c5</task-id>
<output-file>/private/tmp/claude-501/-Volumes-Extra-repos-gitrdunhq-datum/498c339f-c317-4cee-9251-a540fa9de6b3/tasks/a28b044e03ce6c5c5.output</output-file>
<status>completed</status>
<summary>Agent "Research Sweep AI and Aider review" completed</summary>
<result>All six research agents are complete. Here is the consolidated report.

---

## Code Review Tool Architecture Research

### 1. Sweep AI

**Repo**: https://github.com/sweepai/sweep (7.7k stars, fully open source, Python). Team has since pivoted to a JetBrains plugin, but the GitHub App codebase remains intact and readable.

**Integration**: GitHub App via FastAPI webhook server. Self-hostable via Docker Compose. Requires Anthropic, OpenAI, and Cohere API keys.

**Prompting Strategy -- 8 Distinct Personas**:
- "careful and smart tech lead" -- initial code analysis
- "busy tech manager" -- severity filtering (Stage 3)
- "tech lead checking rules" -- custom SWEEP.md rule validation
- "tech lead asking/answering questions" -- self-Q&amp;A edge case detection (Stage 2)
- "expert programmer with keen eye" / "proficient programmer" -- duplicate function detection (Stage 4)
- "talented software engineer" -- PR summarization
- "helpful detail-oriented engineer" -- issue severity sorting (Stage 5)

**The Majority Vote Architecture (most distinctive feature)**: The entire review pipeline runs **5 times in parallel** with different random seeds. All generated issues from all 5 runs are embedded into 512-dim vectors. **DBSCAN clustering** (eps=0.375, min_samples=2) groups semantically similar issues. Only clusters appearing in **4+ of 5 runs** become confirmed issues; clusters at exactly 3 become "potential issues"; everything else is discarded. The representative issue per cluster is selected by highest cosine similarity to other cluster members.

**5-Stage Pipeline**:
1. Initial code analysis (per-file, structured XML output)
2. Edge case detection via self-Q&amp;A (model generates questions phrased so "Yes" = problem, then independently answers them)
3. Critical review with 3-question severity assessment per issue, incorporating existing PR comment threads
4. Function deduplication via embedding-based search against the full codebase
5. Severity sorting, capped at 10 final issues

**Output Format**: Structured XML tags (not JSON). `CodeReviewIssue` has only 3 fields: `file_name`, `issue_description`, `line_number`. No formal severity enum -- severity is implicit in the pipeline survival (confirmed vs. potential). Posted as a single PR comment with markdown and file/diff URL links.

**Multi-Model**: Claude 3.5 Sonnet (hardcoded primary for all review calls), with fallback to GPT-4o. Claude 3 Opus for issue-to-PR generation. Haiku for lightweight tasks. Temperature 0.1 globally.

**Custom Rules**: `SWEEP.md` files at any directory level, cascading from subdirectory to root.

---

### 2. Aider

**Repo**: https://github.com/Aider-AI/aider (46k+ stars, Apache 2.0, Python)

**No Dedicated Review Mode**. Aider has no `/review` command, no reviewer persona, no adversarial framing. Issue #723 requested it in June 2024 -- never implemented. Its quality feedback loop is entirely lint-and-test driven.

**Lint Architecture** (`/aider/linter.py`) -- Three Layers:
1. **Tree-sitter basic lint**: Walks AST looking for `node.type == "ERROR"` or `node.is_missing`. Language-agnostic.
2. **Python compile check**: `compile(code, fname, "exec")`, catches SyntaxError.
3. **Flake8 fatal subset**: `--select=E9,F821,F823,F831,F406,F407,F701,F702,F704,F706` -- only fatal errors, not style.
4. **External linter**: Any user-configured command via `--lint-cmd`.

**The Novel Error Presentation Format**: Uses `TreeContext` from `grep_ast` (Aider-maintained). Error lines marked with `█` in the left gutter, non-error lines get `│`, parent scopes (enclosing functions/classes) automatically included via AST walking, gaps collapsed to `...⋮...`, with 3 lines of padding above/below each error. This gives the LLM the error embedded within its containing function scope rather than just a bare `file:line: message`.

**Reflection Loop** (`/aider/coders/base_coder.py`): `reflected_message` mechanism -- after edits, lint runs automatically. If errors found, error text becomes the next user message. Max 3 reflections (hardcoded, shared counter between lint and test). Each reflection is a full LLM round-trip. Auto-commit happens between cycles.

**Prompting**: Zero review framing. System prompt: `"Act as an expert software developer."` Lint error preamble: `"# Fix any errors below, if possible."` Quality is entirely delegated to the external linter configuration.

**Multi-Model (Architect Mode)**: Two-model pipeline for code generation (not review): Architect model (e.g., o1-preview) provides natural-language change description, Editor model (e.g., DeepSeek) converts to SEARCH/REPLACE blocks. Not a review pipeline.

**Key Takeaway for Datum**: The `TreeContext` error presentation format (error-in-scope with `█` markers) is genuinely worth studying. The reflection loop is clean. But there's nothing here for code review -- Aider's philosophy is that LLMs generate code and external tools validate it.

---

### 3. Sourcery.ai

**Product**: https://www.sourcery.ai/ | **Docs**: https://docs.sourcery.ai/
**Repo**: https://github.com/sourcery-ai/sourcery (MIT, 1.8k stars -- but this is just the CLI client wrapper. The review engine is proprietary SaaS.)

**Integration**: GitHub App, GitLab, VS Code, JetBrains, Cursor, GitHub Action (`sourcery-ai/action@v1`), pre-commit hook. Uses LSP for IDE integration.

**7-Step Review Pipeline**:
1. **Split into atomic chunks** -- per-function/per-class fragments. They explicitly avoid sending full-file context because "increasing the size of the context has diminishing returns and seems to actually harm accuracy."
2. **Deterministic heuristic pre-filter** -- eliminates import-only, small, non-code changes. Saves ~5000 tokens per review.
3. **Context expansion** -- surviving diff hunks expanded to full function bodies (before/after).
4. **LLM analysis with chain-of-thought** -- first LLM pass generates reasoning + initial self-filtering.
5. **LLM structured response** -- second pass formats analysis into developer-facing feedback.
6. **Validator LLM** -- separate LLM instance evaluates each comment against 4 critical checks: (a) valid to the code, (b) actionable, (c) specific to this code, (d) valuable to the author. Before validation: ~40% usefulness. After: ~60%.
7. **Post-processing** -- deduplication, formatting, assembly.

**Prompting Strategy -- Three Published Techniques**:
1. **Panel-of-Experts** (Tree-of-Thought variant): Three named experts (Alice, Bob, Charles) each write one step of thinking, share with the group, challenge each other. If any realizes they're wrong, they leave. Consensus via deliberation. Reduced error rate from ~40% to ~20%. Cost: 2x tokens.
2. **Classification over negative prompting**: Negative prompting ("don't do X") backfires. Instead, ask the LLM to self-classify output with boolean metadata (e.g., `has_existing_docstring: true/false`), then filter programmatically.
3. **Multi-pass over single-pass**: Sequential small-context LLM calls with deterministic filtering between steps.

**Multi-Model**: Sends code to both OpenAI and Anthropic models. Multiple specialized reviewer personas run in parallel (general quality, security, complexity, documentation, testing, custom).

**Severity**: No formal Critical/High/Medium/Low on individual comments. Rule types: `refactoring`, `suggestion`, `comment`. Code quality metrics: Quality Score (0-100%), Cognitive Complexity, Method Length, Working Memory (max simultaneous variables, based on cognitive science 5-9 item limit).

**Output**: PR summary with categorized bullets, reviewer's guide with file-level changes table, auto-generated Mermaid diagrams, inline diff comments with thumbs up/down reactions.

**Testing LLM Output**: Calls real LLMs in tests (no mocking), asserts on comment type classification not exact wording, 95% CI pass threshold accepting 5% flakiness.

**Key Limitation**: Primarily file-scoped analysis -- no cross-file dependency analysis.

---

### 4. Claude Code Adversarial Review Skills/Plugins

**Seven distinct open-source implementations found:**

| Implementation | Repo | Architecture | Multi-Model |
|---|---|---|---|
| **dementev-dev/adversarial-review** | [GitHub](https://github.com/dementev-dev/adversarial-review) | Claude writes code, Codex reviews, max 5 rounds | Yes: Claude + OpenAI Codex (gpt-5.5) |
| **ng/adversarial-review** | [GitHub](https://github.com/ng/adversarial-review) | Optimizer/Skeptic dual-agent with cost gating | Yes: Sonnet + Opus + Haiku confidence scoring |
| **alirezarezvani/claude-skills** | [GitHub](https://github.com/alirezarezvani/claude-skills) | Three hostile personas (Saboteur, New Hire, Security Auditor) | No, single session |
| **alecnielsen/adversarial-review** | [GitHub](https://github.com/alecnielsen/adversarial-review) | 4-phase: Independent Reviews, Cross-Review, Meta-Review, Synthesis | Yes: Claude + GPT cross-critique |
| **wan-huiyan/agent-review-panel** | [GitHub](https://github.com/wan-huiyan/agent-review-panel) | 16-phase multi-agent panel, Supreme Judge, post-judge hallucination gate | Multiple personas, all Claude |
| **pauhu/claude-codex-review** | [GitHub](https://github.com/pauhu/claude-codex-review) | Five perspectives (Security, Correctness, Compliance, Performance, Maintainability) | Optional Codex CLI |
| **awesome-skills/code-review-skill** | [GitHub](https://github.com/awesome-skills/code-review-skill) | 4-phase progressive disclosure | No |

**Most Architecturally Interesting -- ng/adversarial-review**:
- **Cost-gated escalation**: Weighted scoring (auth/crypto +3, API changes +2, &gt;50 files +3). Score 0: skip. Score 1-4: 2 Sonnet agents. Score 5+: 4 agents (Sonnet + Opus).
- **Skeptic Anti-Rationalization Protocol**: Two explicit failure patterns documented (RUBBER-STAMPING, LAZY DISAGREEMENT). Every verdict requires an `Evidence` field with actual command output. Confidence scored 0-100 with tiers (0=guess, 50=reasoning only, 75=validated with grep/blame/tests, 100=mechanically confirmed).
- **8-point Signal Gate**: Every finding must pass: (1) meaningfully impacts quality, (2) discrete and actionable, (3) doesn't demand rigor absent from rest of codebase, (4) introduced in this PR, (5) author would fix if aware, (6) doesn't rely on unstated assumptions, (7) can provably identify affected code path, (8) clearly not intentional.
- **Haiku Independence Pass**: After synthesis, parallel Haiku agents score each confirmed finding 0-100 without seeing the adversarial debate context. Catches groupthink.

**Severity Systems Vary**:
- Three-tier (alirezarezvani): CRITICAL, WARNING, NOTE. Cross-persona promotion rule.
- Five-tier (ng): Critical, Major, Minor, Nit, Pre-existing. "Critical requires universal breakage -- if it requires specific scenarios, it is Major at most."
- Four-tier with epistemic labels (wan-huiyan): P0-P3 plus `[VERIFIED]`, `[CONSENSUS]`, `[SINGLE-SOURCE]`, `[DISPUTED]`, `[JUDGE-HALLUCINATED]`.

**Anti-Groupthink Mechanisms**: Blind assessment before debate (wan-huiyan), sycophancy detection when &gt;50% position changes lack new evidence, post-judge hallucination gate, three-layer mutation detection preventing reviewer from modifying code it reviews (dementev-dev).

---

### 5. Greptile

**Product**: https://www.greptile.com | **Docs**: https://www.greptile.com/docs
YC W24, $25M Series A (Benchmark). Core engine fully closed source.

**Integration**: GitHub App, GitLab (group access token + webhook), MCP Server (v2), REST API, VS Code extension, CLI.

**Codebase Understanding (Most Distinctive Feature)**:
1. **AST Parsing**: Tree-sitter + SCIP indexers (forked from Sourcegraph for TypeScript, Python, Go, Java)
2. **Recursive Docstring Generation**: Rather than embedding raw code, they generate natural-language docstrings for each AST node, then embed those. HyDE-like approach achieving ~81.5% cosine similarity to semantic queries vs ~72.8% for raw code (12% improvement).
3. **Graph Construction**: Nodes (files, functions, classes, variables, external calls) + edges (calls, imports, dependencies). Processed in dependency order, leaves first.
4. **Storage**: PostgreSQL with pgvector + Redis caching.
5. **Retrieval**: Vector similarity + keyword search + agentic multi-hop search (agent evaluates relevance and follows source references).

**Agent Architecture**: Built on **Claude Agent SDK**. Primary model: **Claude Opus 4.5**. Multi-model router dispatches each task to the best tier:
- Smart/reasoning tier (Opus 4.5, Sonnet, GPT-4o): Code analysis, bug detection
- Fast tier (GPT-4o-mini, Haiku): Classification, summarization
- Embedding tier: Indexing

Described as a "swarm of agents" running in parallel. Deterministic hooks guarantee every PR file gets examined regardless of agent investigation path.

**Quality Control -- Vector Embedding Feedback Loop (not prompt engineering)**:
- Every review comment gets an embedding vector, stored per-team
- Developer reactions (upvote/downvote/address/ignore) train the system
- If a new comment has high cosine similarity with 3+ unique downvoted comments, it gets blocked
- "Most nit comments cluster into a relatively small number of semantic categories" -- once a team downvotes enough similar comments, analogous future comments are automatically filtered
- Result: 55%+ address rate within two weeks, 256% better upvote/downvote ratio (v3)
- Critical findings (security, memory leaks, infinite loops, null pointers) are NEVER suppressed

**Output Format**:
- PR summary with confidence score (0-5 scale)
- Inline comments with severity badges: P0 (Critical), P1 (High), P2 (Medium)
- Comment type classification: Logic, Syntax, Style
- Most comments include applicable code suggestions formatted as diffs
- Auto-generated Mermaid diagrams (sequence, ER, class, flow)

---

### 6. Ellipsis.dev

**Product**: https://www.ellipsis.dev | **Docs**: https://docs.ellipsis.dev
YC W24, $2M seed. 67k+ repo installations, 80M+ daily tokens.

**Integration**: GitHub App only (2-click install). All compute in Ellipsis's cloud (AWS private VPC). No BYOK for models.

**Architecture -- Two Phases**:

**Phase 1: Parallel Comment Generation** -- Multiple specialized generators run in parallel, each targeting different issue types (logical errors, anti-patterns, security, spelling, documentation drift, custom rule violations, code duplication). Different generators can use **different models** (GPT-4o and Claude 3.5 Sonnet mixed in the same review). Each comment must include Evidence (code snippet links) for downstream validation.

**Phase 2: Multi-Stage Filtering Pipeline** -- 5 sequential filters:
1. Deduplication Filter
2. Logical Correctness Filter (validates against attached Evidence)
3. Hallucination Filter
4. Confidence Filter (customer-configurable threshold, e.g., 0.85)
5. Customer Feedback Filter (embedding search over past thumbs-up/thumbs-down reactions)

**Prompting Strategy**:
- **Decomposition over monolithic prompts**: Dozens of smaller specialized agents, each independently benchmarkable.
- **Prompts live in code**: Explicitly rejected prompt management platforms. Version-controlled alongside code for reproducible evals.
- **No frameworks**: Explicitly rejected LangChain. "Agents are just a while-loop; the wrong abstraction is far worse than no abstraction."
- **Evidence-attached pattern**: Primary anti-hallucination mechanism.

**Multi-Model**: Claude 3.5 Sonnet (primary), GPT-4o (secondary), o1 for harder problems (agent trajectory auditing). Smaller models for subtasks. No fine-tuning -- uses embedding similarity on feedback instead.

**Output**: Inline GitHub PR review comments. Confidence score per comment. No formal severity classification (Critical/High/Medium/Low) -- confidence scores serve as primary signal. Quiet mode suppresses "LGTM" reviews.

**Infrastructure**: Hookdeck (webhook ingestion), FastAPI, Hatchet (workflow engine, forked), Turbopuffer (vector store), tree-sitter (AST chunking), Lsproxy via Modal (language server for go-to-definition/find-references), custom DynamoDB LLM cache.

**Key Insight**: "Because our workflows are asynchronous, latency is not nearly as big of a concern as accuracy." This enables multiple agent passes and sophisticated filtering without real-time pressure.

---

### Cross-Cutting Patterns

| Pattern | Who Uses It |
|---|---|
| **DBSCAN/embedding-based dedup** | Sweep (majority vote), Greptile (feedback filtering), Ellipsis (feedback filtering) |
| **Generate-then-filter &gt; prompt engineering** | Sourcery (4-check validator), Ellipsis (5-stage filter), Sweep (5-run majority vote) |
| **Multi-model (different models for different tasks)** | All except Aider. Greptile and Ellipsis are most explicit about routing. |
| **Evidence-attached comments** | Ellipsis (mandatory evidence links), ng/adversarial-review (Evidence field requirement) |
| **Team-specific feedback loops** | Greptile (per-team vector DB), Ellipsis (customer feedback filter) |
| **Tree-sitter for AST parsing** | Aider (lint), Sweep (context), Sourcery (static analysis), Ellipsis (chunking), Greptile (SCIP forks) |
| **No fine-tuning** | Ellipsis (explicit), Greptile (embedding feedback instead), Sourcery (classification metadata instead) |
| **Asynchronous pipeline (accuracy &gt; latency)** | Ellipsis (explicit design principle), Sweep (5 parallel runs) |
| **Self-Q&amp;A / self-interrogation** | Sweep (Stage 2 edge case detection) |
| **Cost-gated escalation** | ng/adversarial-review (weighted scoring determines agent count) |</result>
<usage><subagent_tokens>57209</subagent_tokens><tool_uses>6</tool_uses><duration_ms>1126719</duration_ms></usage>
</task-notification>

---

biw expensive would sweep be

---

lets adapt!

---

send an ipis agent to find code duplication efficiences and way to compute and atore valuee once if they are hsed mutiple times in anrun

---

cant we use xgrammar or similar to force shape with workflows

---

search for that code bug on gh issues